# Ambition TODO

Centralized work queue for multi-hour autonomous agent sessions.

This file is intentionally **not** a changelog. It is the place to keep unfinished work that is useful enough for an agent to pick up without re-discovering it from git history, scratch notes, or old overlay readmes.

## Operating rules

- **Sandbox-first.** The sandbox is the vertical slice: gameplay components should be assembled in test-arranged rooms before they are story-arranged into the final hub / first zone / boss sequence.
- **Outstanding work lives here.** `FEATURES.md` is the compact capability inventory for landed behavior; this file is the centralized queue for unfinished behavior, docs, and validation work.
- **Verify before claiming done.** Re-grep code and docs before closing an item. Many old TODO entries turned out to be shipped or superseded.
- **Move, do not duplicate forever.** When a task lands, either remove it from this file or move the durable lesson to `docs/history/progression-log.md`, `dev/journals/`, or `docs/archive/`.
- **Prefer agent-sized tasks.** Each accepted item should be concrete enough for a 1-4 hour autonomous session with clear files, tests, or validation commands.
- **Sprites + SFX ship with the entity.** When a task adds a new actor or prop that needs a visual, creating its sprite — and any necessary SFX — is *implicitly part of that task*. Don't call an entity done on a placeholder / generic-fallback / invisible visual without filing a tracked sprite follow-up (section B / the sprite list).

Useful companion docs:

- Current state: `docs/current/state.md`
- Current next moves: `docs/current/next.md`
- Planning sequence: `docs/planning/path-forward.md`
- Tech debt: `docs/planning/tech-debt-log.md`
- Capability inventory: `FEATURES.md`
- Mechanics status: `docs/mechanics/expressibility-checklist.md`

## Persistent autonomous-loop instruction

When you wake up here, pick the next task from this list and work on it without asking permission. Honor the long-running discipline (`[[never-stop-during-long-run]]` in memory): never stop until the time limit, even if the headline task feels finished — there's always more on this file. When you do close a task, leave this instruction in place so the next agent finds it.


## Jon's Bug List

- [ ] When gravity is sideways you can't wall jump on a ceiling (which should be considered walls when gravity is towards the side).

- [ ] Gravity pointing towards the right side of the screen, horizontal motion is incorrectly flipped.

- [ ] Portal gun can cause OOB in the portal bridge room with ease.

- [ ] Cube arrow key navigation is still wrong. 

- [x] ~~Portal floor-bounce STILL slowly bleeds momentum over MANY cycles~~ FIXED 2026-06-10 (`6d526f6c`): root cause was the carve sweep reading a one-frame-stale `SimDt` (+ pre-gravity velocity), so a frame hitch at re-entry left the floor solid and grounded the body. Replaced with a dt-independent approach-box carve; pinned by `floor_portal_bounce_conserves_momentum_per_transit_under_variable_dt`.

## Jon's Polish List

- [x] Fireballs and most projectiles need to be transitable through portals. _Done 2026-06-14 (autonomous): pure `crate::projectile::try_projectile_portal_transit` (mirrors `portal_teleport_ground_items`) maps an in-flight shot's pos+velocity through the portal pair (speed-preserving rotation) when it enters a portal face; both the player and enemy projectile systems call it before their shared world-collision step and skip collision on transit. 5 pure unit tests; replay bit-identical._

- [x] Fireballs will still explode as if they hit a wall when they should transit through portals without exploding. _Done 2026-06-14 — same change: transit happens BEFORE `resolve_world_collision`, so a shot threading the aperture never reaches the wall-hit/explode branch. **Handoff (feel):** the trigger is an AABB-overlap with the portal frame (matching the ground-item convention); a very fast shot could in principle tunnel the thin frame in one step — if that shows up in play, upgrade to a swept plane-cross test (`front_distance` sign change). Sized-projectile partial clipping through the aperture is not modelled (the shot teleports whole)._

- [x] I want to increase the terminal velocity, or there is some effect that maxes out your out velocity when you travel through a portal. We need better momentum preservation and buildup. _Diagnosed + fixed 2026-06-14 (autonomous): there WAS an effect maxing out your velocity — the fall-speed clamp (`cap_fall_speed` in engine_core integration; `vy.min(max_fall_speed)` in platformer_runtime kinematic) actively DECELERATED any body above terminal velocity, so a portal fling (transit preserves the speed) got clipped back to terminal on the very next tick. Fix: terminal velocity is now an **equilibrium** gravity accelerates UP TO — not a brake — so over-cap momentum (a portal exit) is preserved and persists. Implemented as `effective_cap = max(cap, pre-gravity fall speed)`: a normal fall stays below the cap → byte-identical (replay-safe); gliding keeps its hard clamp (a parachute is a deliberate brake); water keeps its drag. 2 new kinematic tests. **Handoff (feel):** the cap is no longer raised globally — momentum just isn't thrown away. If you also want a higher BASE terminal velocity, bump `MAX_FALL_SPEED` (engine_core `movement/tuning.rs`, currently 1900); and a longer fall between repositioned portals now builds and keeps speed, so an in-game feel pass on the fling distances is the natural next step._

- [x] ~~When a laser sword times out or hits a wall it should explode~~ DONE (autonomous run): a timed-out or wall-killed lasersword now emits a `VfxMessage::Explosion` (`ExplosionKind::ClassicBurst`, the same rendered explosion the exploding-mite death uses) at its position via `lasersword_detonation` in `enemy_projectile::systems`; other shots keep their plain `Impact` spark. VFX-only (replay-neutral), keyed on the `lasersword:` owner prefix. Pinned by `lasersword_detonates_on_death_other_shots_do_not`. _Handoff: pick the explosion sprite/`ExplosionKind` you prefer in-game (Classic/Burst/Shockwave/Smoke/Starburst all exist); area-damage on detonation is a deliberate non-goal here (the ask was the visual)._

- [ ] We need a feature where we can "tie a knot", where the player emits a trail, that might drape down onto the collision, and perhaps be pulled taught, and it needs to work through portals.


## Jon's List

- [~] I want portals to have the effect where some percent of the entity going into it is visible on the other side instead of being a: you touch it you teleport. This can be a story element later because we have effectively topologically glued two parts of the world together in an interesting non Euclidean way. The 2007 portal 2D game does this well (although it doesn't do the feet in feet out well, and we do at least do that). We need proper portal physics. — _**Core landed 2026-06-05 (autonomous run):** portals are now an **aperture, not a trigger**. New pure module `crates/ambition_sandbox/src/portal_pieces.rs` is the **Core invariant** — `compute_body_pieces` decomposes any body straddling a portal plane into a `here` slice + a `through` slice mapped to the exit chart (the portal map: `map_point` / `map_aabb` / velocity rotation all share one rotation); `subtract_aabb` carves the host surface; `straddles` / `front_distance` drive the transit. The **host floor/wall becomes non-solid inside the opening** (`publish_portal_carves` → `FeatureEcsWorldOverlay.portal_carves` → `world_with_sandbox_solids` carves it, so EVERY collision query — player, enemies, items — agrees), and the carve only happens when the **full pair** exists (a lone floor portal can't open a bottomless hole). The instant teleport is replaced by a **transit state machine** (`PortalTransit` + `portal_transit_system`): the body physically sinks into the carved opening, the authoritative body **transfers when the centroid crosses** the plane (carrying momentum through the pair rotation, floored exit speed), and transit **clears once the trailing edge is out** (a proper latch, not just a cooldown). The player renders as **two clipped pieces** mid-crossing (`sync_portal_body_pieces` — entry slice + exit slice, the visible "feet in, feet out"). Also added `raycast_through_portals` (recursive, bounded-depth portal-aware ray for LoS/beams/grapple/aim). **27 new tests** (11 piece-geometry, transit machine, recursion), full lib suite + `portal_bridge_reachability` green. _**Iteration 2 (2026-06-05, Jon feedback):** (1) **carve is now transient** — a portal's host surface is carved ONLY while a body is actively transiting it (`publish_portal_carves` gates on `PortalTransit.straddling`), so the permanent walk-in wall pocket (wiggle/ledge-grab into a solid wall) is gone. (2) **ALL actors unified** onto the aperture model — `portal_transit_actors` (enemies/NPCs + bosses) replaces the instant `portal_teleport_actors`, sharing the new pure `transit_step` core with the player so every body sinks-then-transfers identically. (3) **Somersault + reorientation ported** — the centroid transfer applies `portal_transit_roll` to the body's `ActorRoll` (then `update_actor_roll` eases to gravity-upright), for the player too, matching the NPC behavior Jon confirmed correct (feet-in → reorient). (4) **Partial-render hide fixed** — the real sprite is hidden only once a slice has actually crossed (via the tested `compute_body_pieces`), killing the "full body + pieces double-up / wrong-side-clipped / back-of-head-out" artifact. +3 tests (transient carve, ECS hide+2-piece, actor transit drives).** **Remaining polish (detailed checklist below):** texture-accurate piece clipping (still silhouette quads — atlas frame + feet anchor make pixel cropping fiddly; needed to fully judge orientation), partial-render for non-player actors (they transit physically but still show their whole sprite sinking/popping), portalized hurt/hitboxes + hit-dedup, ray wiring, sized-projectile transit, attached-object chart, AI perception, placement validity, chart-aware VFX, moving platforms._

- [x] Going through a portal causes debug traces to be dumped because the debug
  traces are "catching the teleport". Maybe fixing the above issue fixes this
to? Probably not actually. — _Done 2026-06-05: it WAS related. The `IntentionalTeleport` resource + `expected_teleport` plumbing already existed but the teleport system never set the flag, so the trace position-delta detector auto-dumped on every jump. The new `portal_transit_system` sets `IntentionalTeleport(true)` on the centroid transfer (the one frame the authoritative body snaps), and it runs in `PlayerSimulation ⊂ CoreSimulation`, before `SandboxSet::Trace` reads it — so the snap is recognized as intentional and not dumped. Covered by `transit_is_gradual_centroid_crossing_flags_the_teleport_then_clears`._

- [x] The real 3d cube OOT pause menu is on the table. We need to make sure that we can revert to a simpler bevy-ui design if we don't like it, or it is too expensive, but its probably worth testing a real integration pass with it to see how it feels. Changes to the submodules/ambition_inventory_ui are in scope for this item if needed. Ideally we want a very clean seam between the game and this UI frontend so we can swap it out if we need to. There is a mock demo that tries to exercise this pattern: submodules/ambition_inventory_ui/crates/ambition_mock_demo

- [x] The actor sprits face the wrong way horizontally when they are flipped. E.g. I move the left, but the sprite direction faces to the right. However, if I attack the attack hitbox does go in the right direction. So this is just a sign or simple angle issue I think. However, there a lot of weird movement inconsistencies when gravity is inverted. Jump is much shorter when upside down.  **_Done (2026-06-04) — sprite-flip (up + sideways) + jump:_** (1) sprite-flip — `gravity_aware_flip_x` now inverts the facing flip under UP _and_ RIGHT gravity. (You flagged the sideways case was still broken after the up fix — now fixed: the 90° roll points the rolled sprite `(g.y, -g.x)` opposite the down=right move-axis, so RIGHT inverts and LEFT keeps the normal flip; physics.rs `#33`, with right+left test cases.) **To eyeball the sideways direction: a new `dir=left` GravityZone is in the gravity_lab (`glab_wall_walk_left`, against the left wall) next to the existing `dir=right`.** (2) short jump — now gravity-VECTOR-relative: `set_jump_velocity` launches `-jump_speed * gravity_dir` (opposite gravity, full magnitude), replacing the Y-only `vel.y = -speed * gravity_sign`, so height is symmetric under inversion. _The flip is a derived visual fix — please confirm both side zones look right._

- [x] The gravity zones in appear to be hard coded instead of placed via ldtk. There is one in the ninja dojo, which I don't think was intentional. But even in the gravity lab there is no ldtk entity indicating that a gravity zone would spawn there. The layout of levels and physical features in rooms must correspond to some authoring in ldtk, it can never be hard coded. — _Done (verified 2026-06-04): gravity zones are LDtk `GravityZone` entities (3 in gravity_lab, + wall_run/ceiling_cross this run); no hard-coded spawns remain (the debug `spawn_debug_gravity_zone_once` was retired), so the unintentional ninja-dojo zone is gone._

- [x] The portal gun still seems to spawn in `central_hub_main` but there is no ldtk entity I see for its spawn zone. — _Done (verified 2026-06-04): a `PortalGunSpawn` LDtk entity authors it (in `central_hub_basement`); the debug-spawn tables were retired, so nothing is hard-coded._

- [x] Portals should be able to adhere to one way platforms. — _Done (verified 2026-06-04): `raycast_solids` includes one-way platforms for portal shots (portal.rs, `#39`); covered by a unit test (blink/dive still pass through)._

- [x] An earlier TODO says portals despawn when you leave a room, but that does not happen. They reappear if I leave and come back. — _Done (verified 2026-06-04): portals are now `RoomScopedEntity`, so a room transition despawns them (portal.rs, `#41`) — they no longer reappear when you return._

- [x] bug: If you throw and attack and your hitbox is entirely inside the mockingbird hurtbox, it does not register. — _Done — investigated + guarded (2026-06-04): containment registers. Every hit path (boss/actor/projectile, via `ecs_hit_event_hits_boss` / `apply_boss_hit`) routes through `strict_intersects`, which detects a hitbox fully inside a hurtbox — added `strict_intersects_accepts_full_containment` to prove + guard it. The mockingbird's damageable region is its per-frame visible body (`damageable_volumes` → per-animation hurtbox / alpha bbox), so a hit inside it lands. If a miss persists it's the hit landing in the transparent quad OUTSIDE the alpha bbox (intended), not a containment bug._

- [x] If I have two portals on the right wall and I enter from the left moving right, I exit the other portal moving left (correct), but upside down (incorrect). This is another spatial angle / sign problem. We need to make sure portal physics are correct (up to the gameplay feel consideration we make where the character orients itself after leaving the portal). — _Done (verified 2026-06-04): `portal_teleport_system` no longer applies the velocity-turn to the sprite roll; `update_actor_roll` holds gravity-upright, so two same-wall portals just turn you around, not upside down (portal.rs, `#47`)._

- [x] There are a significant numbers of out of bound errors, in the movement. Defer this for a non-autonomous run.  **_Detection legwork done (2026-06-04, still deferred for your interactive fix):_** a fuzz oracle (`tests/collision_invariant_oracle.rs`) swept 63 rooms — 0 embed / clip-through / teleport, 11 open-boundary walk-offs (level-authoring, not physics). See `dev/reviews/collision-oracle-findings-2026-06-04.md`.

- [x] The fireball item doesn't have a sprite, and it shoots a sword, not a fireball, so maybe we need a fireball sprite. The explosion is nice though. — _Done (verified 2026-06-04): the fireball has a bespoke prop + in-flight shot sprite (`gauntlet_fireball`, `FIREBALL_ID`, `explode_half`); it no longer fires the gun-sword bolt._

- [ ] Not sure what the grapple hook really does.  **_Clarified + bug found (2026-06-04):_** it now draws a rope line so it reads as a reel-in (grapple.rs `#53`), BUT it's under-powered — the 620 burst is killed by drag in ~45px, so it can't reach a wall beyond ~45px. Filed `dev/reviews/grapple-pull-underpowered-2026-06-04.md` for a feel/balance pass.

- [ ] Ledge grab does not work correctly under different gravity, _**Scoped 2026-06-04:** the ledge-grab module (`engine_core/ledge_grab.rs`, 2438 lines) is pervasively Y-down — head = `pos.y - half.y`, ledge = `block.top()`, "space directly above", "Sim convention: +Y is down" throughout detect/grab/climb. Making it gravity-aware is a vector-gravity generalization across the whole module (like the one-way `#55` fix but far larger), not a focused change — best for a dedicated session._

- [x] Switch entities need a sprite with a distinct "on" and "off" state. _**Scoped 2026-06-04:** the on/off sprites already exist (`switch_armed.png` = glowing red button = on, `switch_disabled.png` = off, in the renderer's `generated/entities`). The gap is wiring: the runtime draws a colored block — `actors.rs:126` sets `sprite.color` from `FeatureView::switch_on`, and `entity_sprite_for_kind(Switch)` is deliberately `None`. To finish: give the Switch a texture instead of a color (set `sprite.image` to an armed/disabled handle by `switch_on`; load both into `GameAssets` via two `EntitySprite` variants; ensure `sprites/entities/switch_*.png` ship via regen; update the `entity_sprite_for_kind(Switch).is_none()` test)._

- [x] One way platforms should still work when gravity flips. You should be able to fall and stand on them. (on the bottom of them this time, so the one wayness of them depends on how gravity is pushing on them). — _Done (verified 2026-06-04): one-ways are solid from the gravity-up side and passable from the other (lands on the BOTTOM face under flipped gravity; collision.rs, `#55`); covered by `one_way_platform_works_under_flipped_gravity`._

## ⭐ Elevated for the next long run — architectural unblocks (2026-06-03)

The 2026-06-03 long run built out the sandbox **loop** (24-item OoT inventory, merchant economy, 4 wired abilities + cooldowns + arrival shockwaves, boss→ability drops, localized gravity / gravity_lab / flip switch / sliding columns / gravity grenade, shield-block, full loot: coins/hearts/weapons/abilities + magnet) — see `FEATURES.md` rows dated 2026-06-03. It then hit a wall: **the next tier of player-facing features are each gated by exactly one architectural change.** These are the priority for the next long run. Each is a real player feature; the blocker for each was **verified during the run**. Full task entries are further down — this is the elevated index + the specific gate to clear first.

1. **Player wields boss attacks** — ability-sharing ("let the player learn a boss attack"; "every upgrade a theorem"). _**First slice DONE 2026-06-03**: the player wields a boss-style ground-slam AOE (`crate::shockwave`) via an actor-generic `ShockwaveSlam` Special + faction-aware `Hitbox` path — see the **Effect-primitive ability/attack vocabulary** item in section A for the full writeup + what remains._ _Blocker (verified):_ the six `spawn_*_from_special_messages` systems in `content/features/ecs/brain_effects.rs` are **boss-keyed** — each reads the `ActorActionMessage::Special` stream but then iterates a `Query<… bosses, With<FeatureSimEntity> …>` and spawns the attack from the **boss's** position, so a player-sourced `Special` message is silently dropped (no boss matches). Unblock = make them **actor-generic** (spawn from the message owner's position). That is the **`Effect-primitive ability/attack vocabulary`** item below; the smaller shippable first slice is **`Move boss special-attack tunings into boss_profiles.ron`** (derive `Deserialize` on `SpecialActionSpec`).

2. **Possession** (Down+Interact to take over an actor) — _**Control slice DONE 2026-06-03**: `crate::possession` possesses + drives a non-boss actor from player input via its own `ActorControlFrame` (no boss `Brain::Player` needed — bosses are excluded). See the **Possession** item in section A for the full writeup + handoff (hold-timer / camera / vacate-visual / boss case)._ _Blocker:_ the universal-brain shadow exists (`Brain`+`ActionSet`+`ActorControl` on every actor) but the **EFFECTS-consumer flip that makes it the live control path is still pending** (`[[project_controllable_unification]]`), and bosses still `unreachable!()` on `Brain::Player` (`content/features/ecs/bosses.rs:558`). Unblock = flip that control seam so a human can drive any actor's `ActionSet`.

3. **Sideways / arbitrary-direction gravity for the player** — the flagship gravity payoff (wall-walking), and the last open part of Jon's gravity ask. _Blocker (verified):_ the player integrator applies gravity to **`vel.y` only** via a scalar `gravity_sign` (`engine_core/movement/integration.rs:74,86`), and jump / fall / ground-detection all assume a Y-down world. Non-player actors already resolve a gravity **vector** (`physics::gravity_dir_at`), so the work is to make the **player's** movement model gravity-direction-relative (vector gravity + relative jump/ground). Big, but it turns this run's localized-gravity system into true wall-walking.

4. **A real platforming gauntlet / connected gridvania content** — see the **`Avoid doors… gridvania`** and **`Basement / first-zone Gridvania authoring`** items. _**UNBLOCKED — the tooling already exists (drift found 2026-06-03 long run).**_ `ambition_ldtk_tools` has a wired **`intgrid` subcommand** (`paint`/`erase`/`query`/`summarize`): `intgrid paint <ldtk> --rect … --value N` sets every IntGrid cell overlapping a px/size rect (1=Solid, 2=OneWayPlatform, …) — exactly the "lay out IntGrid platform geometry" the blocker said was missing (`area_authoring.py::paint_intgrid_rect`, `cli.py::cmd_intgrid`). So the gate is cleared; what remains is **authoring** the rooms (`area create` a level → `intgrid paint` its platforms → `entity add` PlayerStart/doors → `door`/EdgeExit wiring → `validate`+`roundtrip`), which is content work best done **interactively** (platform reachability/feel wants playtesting; compute jump arcs from `DEFAULT_TUNING` for a first pass + hand off the feel-tune). _**A first room — `gradient_ascent` — is now authored via this exact flow (`9f0e732f`): a RON spec → `area create` (Solid entities lowered to 362 IntGrid cells) → reciprocal hub door into `central_hub_complex`; headless loads it (47 rooms), roundtrip clean. So the end-to-end pipeline is proven; the remaining gridvania work is more rooms + a feel pass. See `tools/ambition_ldtk_tools/specs/gradient_ascent_area.ron` for the template.**_ _Old blocker text (now stale):_ `ambition_ldtk_tools area create` only authors a fill mode — it cannot lay out IntGrid platform geometry; the unblock is a new subcommand that authors IntGrid shapes.

## ⭐ Elevated refactors (high priority) — make the codebase easier to work with

From the 2026-06-03 long run's firsthand friction; full write-up + evidence in `dev/reviews/refactor-proposals-2026-06-03.md`. Each was hit while building and verified against current code; none needs a backwards-compat shim (`[[feedback_pre_release_no_compat]]`) — each is a single clean commit. **Suggested order: 4 → 1 → 2 → 3** (quick, compiler-checked) **→ 5 → 6** (one file per commit).

- [~] **Refactor 6: split the god-modules to shrink the incremental rebuild unit** `[V3/D3]` — `[[feedback_compile_time]]` (sandbox build ~10 min); the biggest files force a full-file recompile on any edit. _**Major progress (autonomous run):** 11 of the listed god-modules split — `brain_effects.rs` (2256→518, dissolved into content during the de-naming work); `ledge_grab.rs` (2438→935+1503 tests); `boss_pattern.rs` (2142→1328+814); `state_machine.rs` (1676→782+894); `action_set.rs` (1404→777+627); `attack_geometry.rs` (1588→783+805); `damage.rs` (1443→1225, drops + predicates into `damage_drops.rs`/`damage_predicates.rs`); `features/ecs/actors.rs` (1386→1112+274); `items/pickup.rs` (1354→1026+328); `assets/game_assets.rs` (1299→1003+296); `boss_encounter/sprites.rs` (1248→1007+241). All via the dir-conversion pattern (production in `mod.rs`, the `mod tests` in `tests.rs`; module paths unchanged) except damage.rs (sibling production-concern files). Every one replay bit-identical. **Watch-out learned:** the test mod is NOT always at EOF (boss_pattern's `BossEncounterPhase` enum lives after it) — verify before sweeping `cfg(test)..EOF` into tests. **Remaining:** `portal.rs` (now in the portal crate / restructured — not at the listed path); the production-concern splits inside the big files that are still ~1000+ lines of production (e.g. actors.rs, game_assets.rs) for the genuine production-rebuild win; small-test-mod files (`dev_tools`, `menu/ir/settings`) are low-value._ Split by concern behind `mod` dirs with re-exports (pure code-move, public paths preserved). One file per commit. _**2026-06-15 long run — 7 more splits + the big structural cuts:** `combat/components.rs` (1185→features/actors/spawn), `features/enemies.rs` (1302→+integration tick), `features/ecs/actors/mod.rs` (1111→conversion/update), `assets/game_assets/mod.rs` (1002→entity_sprite/resolvers), `dev/dev_tools/mod.rs` (1064→profiles/developer_tools/editable — the 'low-value' note was wrong, it split cleanly), `features/ecs/bosses/mod.rs` (719→sync/tick), `features/ecs/damage/mod.rs` (768→actor_hit/boss_hit). All replay bit-identical, 0 warnings. This run ALSO did the bigger cuts the splits were a prelude to: extracted the whole presentation layer to a new `ambition_render` crate + completed the sim/render seam (8 render-in-sim modules moved up); extracted `ambition_interaction` foundation crate; moved the boss-sprite registry + the music-cue catalog to data/content. Remaining big files are a prototype (`falling_sand.rs`) or have awkward non-contiguous impls (`settings/model`) — low ROI._

- [ ] **Refactor 8: debug-spawn placement belongs in LDtk, not game code** `[V4/D3]` — the 12 `spawn_debug_*_once` systems + the debug gravity column spawn the held items / props near the player on the first frame, so they appear in whatever room the player starts in (effectively every room). **Game code must not dictate placement — LDtk owns spatial** (`[[feedback_ldtk_authoring_philosophy]]`). Move these to LDtk-authored placement (pickup / prop entities in the rooms that should have them; the held-item debug pickups, the gravity column, the gravity switch, the shrine), and gate-or-delete the code debug-spawns (a single dev-only flag at most). Pairs with Refactor 4 (consolidating the spawn_debug systems), but the real fix is the placement source.

## ⭐ Sprites + SFX for entities added this run (2026-06-03)

Per the "Sprites + SFX ship with the entity" operating rule, these are the new props/actors the 2026-06-03 run added that currently render with **no prop sprite** (`item_pickup::item_sprite` only maps axe/javelin/gun_sword; the rest draw no held visual) or via the **generic boss fallback**. Tracked so none is forgotten. (Puppy-slug-gun prop has a sprite item in section B; the heal shrine sprite landed 2026-06-04.)

- [ ] **Held-item prop sprites for the new held items** `[V2/D2]` — generate prop sprites (in-hand + ground-item visual) like the axe/javelin/portal-gun props (section-B convention) and wire each into `ItemArt` + `item_pickup::item_sprite`, for: **Bomb**, **Blink**, **Grapple**, **Fireball**, **Mark/Recall**, **Gravity grenade**. SFX are reused (PLAYER_BLINK / PLAYER_DASH / WORLD_ROCK_HIT) so no new SFX for these. Keep `regen_sprites.sh` green on a fresh clone (`[[feedback_regen_invariant]]`).

- [ ] **Fireball projectile sprite + bespoke SFX** `[V2/D2]` — the Fireball shot reuses the gun-sword laser-bolt visual and `weapon.lasersword.fire` SFX; give it a fire-looking in-flight projectile sprite + a bespoke whoosh/ignite SFX so it reads as a fireball, not a laser (`item_pickup::HeldProjectile` + its visual; the explosion VFX already exists).

- [ ] **Mark/Recall beacon sprite** `[V2/D2]` — the dropped mark is VFX-only (a burst on set + on recall); add a persistent beacon/glyph sprite at the `mark_recall::PlayerMark` position so the player can see where they'll recall to.

- [~] **Boss sprite-wiring + roar SFX: FSM + T-Rex** `[V4/D3]` — _Sprite-wiring DONE 2026-06-03 (long run, with Refactor 7's boss half): both now render their own published sheets (`flying_spaghetti_monster_boss_spritesheet.png` / `trex_enemy_spritesheet.png`) via `FLYING_SPAGHETTI_MONSTER_SHEET` / `TREX_BOSS_SHEET` + GameAssets fields + `upgrade_boss_sprites` branches — no longer the generic fallback. Render scale / anchor (`collision_scale`, `feet_anchor_y`) are first-pass and want an in-game feel-tune. **Remaining:** the bespoke **roar/scream telegraph SFX** (still the generic placeholder "cry"); the FSM's left/right authored-facing (set `authored_faces_left` if it faces away like the mockingbird did)._ ⭐ **Jon-flagged visible bug**: both render via the **generic boss fallback** even though their sheets **already exist + are published** — `flying_spaghetti_monster_boss_spritesheet.png` and (for the T-Rex) `trex_enemy_spritesheet.png`. They aren't wired into the boss resolver: `presentation/rendering/actors.rs` (~660) maps `behavior.id`→`boss_key` through a hardcoded `else if boss_key == "..."` chain (gnu_ton/mockingbird/smirking_behemoth) and FSM (`flying_spaghetti_monster_boss`) + T-Rex (`trex_boss`) fall through to `assets.boss` (generic). Wire each: a `BossSheetSpec` matching the sheet's row layout + a `GameAssets` field loaded via `load_named_boss_sprite_via_catalog` + an `all_boss_sprite_filenames` entry + the resolver branch (like `smirking_behemoth`). Plus a bespoke roar/scream telegraph SFX (the generic placeholder "cry" is in the phase-transition feedback). **Refactor 7 (key by id, not display_name) is the structural fix** that would make this resolve automatically. Full context in the **Flying Spaghetti Monster boss fight** + **T-rex boss fight** items in section C.

- [~] Integrate the OOT menu. Keep the seam easy to cut in case we don't want this. But we need a variant of it that is more touch friendly. I do want the grid of possible items you can have. In fact I want the exact same number of items that the OOT menu has slots for. That will be the finite number of pickup items for THIS game. — _Shipped 2026-06-03 (autonomous long run): the OoT item subscreen is integrated as a native Bevy-UI **6×4 grid** (`crate::oot_menu`, feature `oot_inventory`, default-on in `desktop_dev`), opened by the Inventory action (keyboard preset + gamepad RightThumb). **24 = OoT's item-subscreen slot count** (per `submodules/ambition_inventory_ui/DESIGN-OOT-DEMO.md`) is now the game's **finite pickup-item set**: `crate::items::Item` (always-on core) enumerates all 24 in grid order with metadata + an `OwnedItems` resource. The grid shows **every** possible item (owned bright / un-acquired dim / equipped green / cursor gold), OoT-style. Confirm **equips** a weapon (shared `item_pickup` HeldItem/ActionSet seam — axe/gun-sword/javelin) or **uses** a health/mana cell. **Touch-friendly**: 88×66 slots + `MenuTapMode` select-then-tap pointer nav. **Easy-cut seam**: `plugins.rs` swaps the Inventory-button input system by feature (OoT on → grid; off → legacy 3-tab adventure menu); legacy spawn/sync gated off so no dormant panel. Visibility reuses the shared `InventoryUiState.visible` flag so the pause menu's existing suppression keeps working untouched. Pickups + `<<give_item>>` + the Yarn state mirror all read/write the 24-item catalog; grabbing the portal gun / axe / gun-sword grants+equips its slot. 38 tests (catalog, grid nav, effects, 6 end-to-end ECS equip/use/nav). **Remaining (handoff):**_
  - _**3D OoT "cube" renderer** — the literal N64 spinning-cube visual lives in `submodules/ambition_inventory_ui` (vendored `bevy_lunex`, its own cargo workspace). Deferred deliberately: taking that dep risks a nested-workspace tangle + a large compile-time blowup (`bevy_lunex`/`cosmic-text`/`bevy_rich_text3d`) against `[[feedback_compile_time]]`. The data model (`OwnedItems` + `OotMenuState`) is renderer-independent on purpose, so the cube can consume it later without touching the catalog/input/effects. See that submodule's `DESIGN-OOT-DEMO.md`._
  - _**Item icons** — slots use text labels as placeholders (sprites are a separate task per the section-B sprite convention); generate 24 item-icon sprites and swap them into the slots + the C-button affordances._
  - _**Map/Quests pages + C-button quick-assign** — the OoT subscreen is Items-only today; add page-turn (L/R) for Map/Quests parity and the OoT C-button item assignment once a HUD quick-bar + gameplay trigger exist (maps awkwardly to the current one-held-item-at-a-time model — revisit when multiple held items land)._
  - _**Persistence** — ✅ _Done 2026-06-03 (long run): `crate::inventory_persist` persists `OwnedItems` **and** the `PlayerWallet` across save/load. `SandboxSaveData` gained `items: Vec<PersistedItem>` (keyed by stable `dialog_id`, not grid index) + `wallet: i32` + an `inventory_saved` flag (so a genuinely-saved-but-empty bag is distinguished from a fresh save's starter). `restore_inventory_from_save` applies the saved set once the player exists (a fresh save keeps the starter); `persist_inventory_to_save` mirrors live changes back only when they differ (so autosave's change-detection stays honest). 4 tests (load-over-starter, fresh-keeps-starter, id round-trip + unique-clamp). **Remaining:** equipped-weapon state isn't persisted yet (re-equip from the grid on load).__
  - _**Reflect real capability** — ✅ _largely done later this run (2026-06-03):_ four abilities are now real, equippable from the grid via the held-item seam — **Blink** (`blink.rs`), **Grapple** (`grapple.rs`), **Fireball** (AOE shot), **Mark/Recall** (`mark_recall.rs`), all with `held_item_id`s so `from_held_item_id` round-trips and `decide()` treats a "wired" ability (one with a `HeldItemSpec`) as equip/unequip. Granted three ways: the **merchant** sells them, **bosses drop them** (FSM→Blink, T-Rex→Grapple, GNU-Ton→Fireball, Clockwork→Mark/Recall via `PickupKind::Ability`), and the debug ground pickups grant `OwnedItems`. Blink+Grapple share an `AbilityCooldown`. **Remaining:** Fly / BubbleShield / MorphBall (movement-feel-deep) + portal-gun-drop-clears-equipped._
  - _**Collapse the dual store** — legacy `PlayerInventory` (3-kind bag) still exists for the off-feature build; once the OoT menu is the only inventory, make `PlayerInventory` a one-way view of `OwnedItems` (or delete it) so there's a single source of truth._
  - _**Run-wide economy/combat loop (2026-06-03 long run)** — the catalog now sits in a full loop: earn coins (enemies + breakables drop `Currency`; hearts too) + learn abilities (boss drops) → spend at the merchant (with an `AlreadyOwned` guard so unique re-buys don't waste coins) → equip from the grid → use (4 abilities + shield-block defense + gravity grenade). A loot magnet pulls drops in. See `FEATURES.md` rows dated 2026-06-03._
  - _**SYSTEM menu — deferred settings + localization (2026-06-06)** — the cube's System face was broadened onto a `SystemMenuModel` IR (`crates/ambition_sandbox/src/persistence/settings/system_menu.rs`): a flat top level **Radio · Video · Audio · Controls · Gameplay · Language · Developer · Reset Sandbox** (Developer + Reset are DEV-BUILD gated on `cfg!(feature = "dev_tools")`; Reset fires immediately, no confirm). Two things are deliberately left for a later pass:_
    - _**The pure-`[new]` settings still need `UserSettings` fields + real effects before they can be surfaced** — **VSync**, **Brightness**, **Rebind Keys**, **Gamepad Layout**, **Screen Shake**, **Tutorials**, and the whole **Accessibility** category (omitted entirely so there's no empty bucket). Each needs a new field on the matching `VideoSettings`/`ControlSettings`/`GameplaySettings` struct + a system that actually applies it, THEN a row in `settings_menu_model` + an entry in `system_menu::curated_options`. Don't add dead rows before the effect exists._
    - _**Localization / i18n is its own foundational pass** — the `Language ▸` entry is a **stub** today (lists English·Español·Français·Deutsch·日本語; only English selectable, the rest visibly disabled; selecting English is a confirm, others beep). Real language switching needs a `Locale` resource + a string catalog + migrating every UI label from a literal to a key (bevy_fluent or a light home-grown catalog). Once that lands, the Language screen's `LocaleId` rows drive `Locale` and the disabled flag drops. The menu slot already exists so the i18n work has a surface to land on._
    - _**Cube interaction — Esc CLOSE fixed, mouse-CLICK activation still BROKEN (2026-06-06)**. **Esc (Bug 1) fixed:** the bevy-UI pause-menu systems (`pause_menu_navigate` + pointer/drag) ran every `Paused` frame even under the Cube backend and re-set `inventory.visible = true`, so Esc closed-then-reopened; each is now gated by `.run_if(pause_menu::pause_menu_ui_active)` in `app/plugins.rs` (regression test `esc_close_stays_closed_with_pause_menu_in_schedule`). **Mouse-click (Bug 2) NOT fixed — only instrumented:** clicking a cube control (Items grid / System rows / nav arrows) doesn't dispatch its action, though keyboard select does. Leading root cause: `republish_cube_pages` → `rebuild_cube_faces` despawns/respawns ALL control entities on every cursor change, so a click's press and release land on different entity ids and Bevy drops `Pointer<Click>` (keyboard reads the cursor directly, so it's immune). **Fix:** update highlight/detail-text in place on cursor-only changes instead of a full face rebuild (the in-place update deferred during the earlier responsiveness fix). When fixing, REMOVE the temporary `[cube-pick TEMP]` / `[cube-move TEMP]` / `[cube-click TEMP]` `info!` logs in `submodules/ambition_inventory_ui/src/cube.rs` + `oot_cube_app.rs`. **Open decision:** dev-gating shape — (a) compile-gate only the cube's Developer/Reset entries vs (b) feature-gate the whole dev surface out of release builds._

- [ ] Improve shark without a rider attack patterns so it feels maybe more wild than the pirate who was controlling it, it should be able to move both vertically and horizontally. It needs a charge attack and it should explode if it hits a wall with too much force. 

- [~] Develop an NPC to flex / force the player / boss unification code (do this with Alice). _**Mechanism landed 2026-06-03 (`1a28c603`): possession now drives NPCs** through the human control path (`update_ecs_npcs` honors `Possessed`, driving the body from the player's `ActorControlFrame` like the enemy path) — the literal "drive any actor's ActionSet via the human path" flex, now true for enemies AND NPCs. Also fixed a latent crawl bug (possession movement fed a `[-1,1]` direction straight to the integration → ~1 px/s; now scaled by `POSSESSED_MOVE_SPEED`). **Remaining (the "with Alice" part):** NPCs are peaceful (no attack/interact verbs), so possessing one only flexes movement — give Alice a richer `ActionSet` (a unique interact/special verb) so driving her through the human path exercises a verb a human never drove. That's the real oversight-flush the item wants._

- [~] Goblins need to have an action set as rich as the player (flexing or giving a use case to force the player / enemy unification). _**Done 2026-06-03 (long run, `90b1791b`): the unification flex — the Smash brain now picks its verb by range, exactly as scoped.** `brain::smash::maybe_substitute_ranged` (between `choose_action` and `apply_difficulty`, so a ranged shot inherits the same accuracy-jitter/commit-roll as a melee swing) substitutes a ranged shot for a *closing* action when the actor has a ranged verb, is at mid-range (inside aggro, outside melee reach), is approaching/holding (not retreating), isn't mid-swing, and a brain-side cadence (`SmashState.ranged_cooldown_remaining` + `RANGED_COOLDOWN_S` 1.1s — the projectile pool doesn't rate-limit enemy fire, so the cadence lives in brain state) is ready. Goblins (`MediumStriker`) got `ranged: Some(Rock(speed 360, dmg 1))`; the RON→`CombatKit`→`ActionSet` wiring already existed, so this is the first archetype to run `Smash` + a ranged verb together (the `enemy_archetypes.ron` note always allowed it). So a goblin now pokes with a thrown rock at range, then closes for the swing. 3 brain tests (fires-at-mid-range / cadence-gates-back-to-back / melee-precedence-in-reach) + a `brain_builders` lock test for the goblin's ranged Rock through the full RON→ActionSet chain; headless 900 ticks clean. **Handoff (the interactive half):** no dedicated goblin throw animation yet (the rock spawns from the goblin in its walk/idle pose — `[[feedback_no_drop_shadows]]`-style sprite task in section B); and the balance/feel of ranged goblins (cadence, rock speed/damage, the kite-vs-rush mix) wants an in-game pass._ _Original scope below._ _Scoped 2026-06-02 (autonomous):_ goblins are the `MediumStriker` archetype (`enemy_archetypes.ron`), which today carries only `melee: Swipe` + `move_style: Walk` and runs the `Smash` brain. The `ActionSet` container can already hold `ranged` + `special`, so the real gap is the **brain's action-selection vocabulary**: `Smash` only ever commits melee, so adding ranged/dash/special to the goblin's set wouldn't be used until the brain learns to choose among verbs by range/situation the way the player can. Concrete next step: extend `Smash` (or add a richer melee+ranged brain template) that fires `ranged` at mid-range and closes for `melee`, then unit-test the range-based choice — that's the unification flex. Pairs with "Goblins need more sprites" (animations to read the new verbs) and is a balance/feel change best validated in-game, so it wants an interactive session rather than a blind autonomous one.

- [ ] Goblins need more sprites for more animations to go with the different actions so they are full smash-capable characters.

- [ ] The player need more sprites for more animations to go with the different actions. Defer this in autonomous mode.

- [ ] When the player is holding the axe, their attack should be replaced with an axe swing. 

- [x] Portals and held items don't reset on sandbox reset. They should. — _Done 2026-06-03 (`3c055ed7`): `clear_transient_on_sandbox_reset` (chained before `process_sandbox_reset_request`) despawns placed portals + in-flight shots + the portal-gun pickup + ground items + summoned puppy-slug allies, and strips the player's `HeldItem`/`PortalGun`/stashed set (restoring the base `ActionSet`). +1 test._

- [x] Portals despawn when no portal gun is present in the room (held or as a pickup) — the "gun destroyed" case (`portal::despawn_orphaned_portals`); leaving the room still clears via the room reset. _Done 2026-06-03 (`55d3f19`)._

- [ ] puppy slugs don't travel with platforms when the are on them. We might need some better physics here. Or fix it and mark it as a place where using the real physics engine might help. 

- [ ] I'm thinking the 2d portal system probably should be its own bevy plugin. It seems like it could be fairly general. 

- [x] We don't need the blue / orange indicator above the players head with the new gun sprites because the gun itself is the visual mode indicator. 

- [~] Boss phase transitions "screams" / "animations" — _Feel layer done 2026-06-03 (`c63ccdfe`): `boss_phase_transition_feedback` kicks the camera shake + plays a placeholder soft "cry" SFX on a dramatic phase change (Transition/Phase2/Enrage/Stagger), for every boss. **Remaining:** the per-boss "scream" SPRITE animation + a bespoke quiet cry SFX (reuses shake + WORLD_ROCK_HIT for now)._ 

- [ ] Boss transitions are not noticable and too short. They should spawn a visual sprite to show the "scream lines" emitting from them. Better sprites will polish this later.

- [x] When are ledge grabbing, if you get hit you should fall off the ledge. — _Done 2026-06-03 (`abbc204e`): `PlayerLedgeState::knock_off_on_hit` clears the grab + arms a re-grab lockout on a Knockback hit; +2 tests._

- [ ] When you ledge grab on a moving platform if it moves into a wall, that should cause you to get hit and fall off. Currently you incorrectly just get pushed through the wall.

- [~] Implement the TODOS for the kernel NPC dialog tree — _Progress 2026-06-02 (autonomous):_ the dialogue↔inventory **read+grant** bridge now works. `inventory_has("HealthPotion")` reads the live `PlayerInventory` via a new `inventory_counts` slice on `YarnStateMirror` (refreshed each frame before the save early-return, loose item-id normalization). `<<give_item "kind" count>>` is no longer a logged stub — `cmd_give_item` resolves the kind through `ItemKind::from_dialog_id` and adds to the live `PlayerInventory`. Both have unit tests (`yarn_bindings::tests`) and the `hub_guide__test_inv` kernel node reports real state. **Remaining kernel-dialog stubs:** (a) ✅ _Done 2026-06-03:_ `merchant_seed` is now a real buy/sell shop — the economy decision landed (a coin/credits `PlayerWallet` + the 24-item `OwnedItems` catalog), and `<<buy_item>>`/`<<sell_item>>` commands + `can_afford`/`wallet_balance` functions do the affordability check + spend + grant (see the Merchant item below); (b) `spawn_chest` Yarn command is still a logged stub (chest spawns are room/encounter-spec driven, not dialogue-driven).

- [ ] Silksong levels of input buffering.

- [ ] Ledge grabbing on a moving platform leaves you stationary in air, but it should have you move with the platform or be knocked off if it would push you through a wall through something with heavy collision. 

- [ ] Increase the resolution of gnuton so the sprite is not drawn pixelated. 

- [ ] **Avoid doors + teleports in the vertical slice; build a gridvania world.** `[V5/D5]` — ⭐ **ELEVATED for the next long run** (see the unblocks index at the top): _**tooling gate cleared (drift found 2026-06-03):**_ `ambition_ldtk_tools intgrid paint` already lays out IntGrid platform geometry (`--rect … --value 1=Solid/2=OneWayPlatform`), so this is now **authoring** work, not tooling work. Use `area create` + `intgrid paint` + `entity`/`door` wiring; reachability/feel wants an interactive playtest pass. - Current intro chains 10+ rooms via Door LoadingZones, which makes the slice feel like a hub with a million side-rooms. Convert to EdgeExits (stitched corridors) and connected geometry so the player traverses continuously. The new mid-air-doors lint already flags the doors that need to go.

- [ ] **Better door sprites + variable door sizes** `[V3/D3]` — current door visuals look the same regardless of width/height. Procedurally generate frame variants (small / standard / tall / wide / double) keyed off `LoadingZone.size`, with the door art aligned to the size. Could probably reuse the lasersword DESIGN/OUTPUT split pattern.

- [ ] **Wall-clipping bugs in the intro** `[V4/D4]` — Jon's noted ongoing bad-clipping-through-walls errors during intro playthroughs. Probably a mix of (a) sub-pixel collision drift at high speeds, (b) thin-wall (16-px) corner cases, (c) the trace-replay-driven lock-wall debt already in the active blockers list. Cross-link with the existing wall-cling teleport entry in `docs/planning/tech-debt-log.md`.

- [~] **Contextual button label follow-ups** — `player::affordances` module + touch HUD overlay shipped 2026-05-23. _(a) done 2026-06-02 (autonomous):_ `update_button_glyph_from_active_input` now resolves the player's selected preset from `settings.controls.keyboard_preset_index` via the new bounds-safe `KeyboardPreset::by_index` (out-of-range → Arrows+ZXC, unit-tested) instead of hardcoding the default, so HUD glyphs follow a rebind. Outstanding: (b) wire gamepad-kind detection once Bevy 0.18's gamepad API is verified (`GamepadKind` still reads `Generic`); (c) add a regression test once gameplay subsystems start consuming the same resolvers so HUD/sim can't drift. See `docs/systems/control-affordances.md`.


- [ ] **Decompose `HazardRuntime` into ECS cluster components** `[V2/D3]` (2026-06-02) - Last `*Feature { runtime: *Runtime }` shape after the actor→ECS migration completed (Enemy/Npc/Boss all done, commit `77ff72fd`). `HazardFeature { hazard: HazardRuntime, spawn }` at [`content/features/hazards.rs:4`](crates/ambition_sandbox/src/mechanics/combat/hazard_runtime.rs#L4) wraps a small payload (id/name/pos/size/volume/motion/mode). NOT a bridge layer (no mirror-shadowing) and hazards aren't actors (no Brain/ActorControl), so low-priority pure-consistency work — but finishes the "every Feature is cluster-shaped" story. Follow the boss pattern: `HazardKinematics`/`HazardConfig` components + `HazardRef`/`HazardMut` views + scratch; repoint the ~handful of hazard systems. Good fit for a future long run.


- [~] **Renderer-time "no shadow below foot" assertion** `[V2/D1→D3]` — pair with the no-drop-shadow rule. _Empirically scoped 2026-06-02 (autonomous) — the D1 estimate was wrong; the simple version false-positives and the robust version is D3+._ Probed all 107 baked `*_spritesheet.png` against their RON `feet_pixel.y`:
  - **"No opaque pixels below the foot anchor" is false for 58/107 sheets** — `feet_pixel.y` is a *semantic contact anchor*, not the lowest opaque row. Real content sits below it on legitimate sprites (the trex's tail/feet ~17 rows below `feet_y=300`; the `boss` sheet ~34 rows; most characters' rounded boots extend a few px past the anchor). A bottom-of-bbox-equals-anchor test would fail nearly the whole roster.
  - **The narrower "transparent gap → detached band below" signature also false-positives** — 16/107 sheets show a ≥2px transparent row-gap followed by a content band, but the bands are 6–165 rows tall (held staves, tucked-leg jump frames, hovering robots), i.e. legitimate disjoint body parts, not thin cast shadows.
  - **Net:** a robust detector must require *thin* (≤~6 rows) AND *translucent* (alpha in a soft-shadow window) AND *horizontally wide/centered* bands, and those thresholds can't be tuned without eyeballing candidates — unsafe to land blind as a hard CI gate (a wrong threshold red-lights CI on good art). Recommend doing this generator-side instead, where the shadow pass is a known, isolable step (diff alpha before/after the shadow compositing), or ship it first as a *non-failing* diagnostic report an artist reviews. Probe scripts: `/tmp/probe_shadows.py`, `/tmp/probe_gap.py` (regenerable; not committed — they're throwaway analysis).


  Follow-ups deferred from this refactor:
  - **`boss_encounters/<id>.ron` per-boss phase schedules** `[V3/D3]` — `BossPattern` brain preset references encounter ids but the schedules are still hardcoded in Rust. Move to RON files under `crates/ambition_sandbox/assets/data/boss_encounters/`. _Status 2026-05-24:_ numeric-fields half landed for all 3 authored bosses (`gnu_ton`, `mockingbird`, `clockwork_warden`); each ships an `<id>.ron` that overrides the hardcoded `BossEncounterSpec` constructor via `default_boss_profiles`. Remaining: move per-phase brain schedules once `BossPattern` has hooks for them.
  - **`SheetRegistry`-driven sprite specs** `[V3/D4]` — currently the per-character `CharacterSheetSpec` consts come from hardcoded Rust statics in `presentation/character_sprites/sheets.rs` paired with a `sheet_for_character_id` match. Drive the spec from the on-disk manifest at startup so adding a new character is a single catalog-row + manifest pair, no Rust patch.
  - **Per-instance brain override semantics** `[V2/D2]` — `NpcSpawn.brain_override` / `brain_overrides` fields. Planned in the original RFC but no concrete use case has landed yet; defer until a story room demands it.

## Status legend

- `[ ]` not started
- `[~]` scaffolded / partially shipped but not feature-complete
- `[x]` done recently; keep only briefly when it prevents immediate re-adds
- **`[V?/D?]`** value / difficulty, both 1-5. V: 1=marginal, 5=critical. D: 1=<=30min, 2=1-3hr, 3=half day, 4=multi-day, 5=week+ or risky.

## S - Active blockers and high-signal defects

- [~] Enemy hurt boxes still don't visualize correctly. I can throw a fireball at gnuton and he will get hit before the fire ball collides with the visible box, so something is up with collision or the box isn't drawing. — _Diagnosed + fixed 2026-06-03 (long run): the boss hurtbox (cyan) and the damage path use the **identical** `damageable_volumes(ctx)` (same context, same animation frame), so they cannot drift — that box is correct. The real culprit was the player's **held-item Fireball shot**: it renders as a thin lasersword bolt but hits via a wider 24×18 contact box and detonates a 112×112 splash, and **none of that geometry was drawn** ("the box isn't drawing"), so a hit read as landing before the bolt visibly touched gnuton. Fix: gave `HeldProjectile` a single-source `contact_aabb()` / `splash_aabb()` (the collision system now uses `contact_aabb()`), and the debug overlay draws both (`draw_held_projectiles`, light-blue contact + faint-orange splash). The drawn box can no longer differ from the box that hits. +1 test. **Remaining (deferred, lower-signal):** feel-tune whether the 24×18 contact / 56-half splash is too generous for the fireball's visible size, and overlay dynamically-spawned enemy hurtboxes (encounter waves) — but the visualization gap that drove this report is closed._

- [ ] **Goblin encounter music transition still sounds like a section swap** `[V4/D3]` - The player hears the intro fade out / wave1 enter instead of one continuous musical idea. Existing scratch context was promoted into `docs/recipes/generated-music-workflow.md`; use `debug-notes-music.md` only as historical detail if still present.
  - Diagnose first: compare generated and installed OGGs, run `audit_cue_balance.py`, inspect logs for `gain_start=target`, then decide whether the fix is score arrangement, mastering, stem balance, or runtime fade policy.
  - Related follow-ups live under the audio section below: level report, live gain HUD, equal-power crossfade, and per-stem mastering.

## A - Sandbox expressiveness and mechanics

### Movement, traversal, and collision

- [~] **Ledge grab + climb-up polish / engine contract** `[V4/D3]` - `ambition_engine::ledge_grab` exists, but the mechanic still needs polish, animation coverage, and corner/one-way validation. Add diagonal-corner probe tests and confirm the sandbox driver uses the engine primitive rather than duplicating probe logic. Instead of 

- [~] **Gravity direction as a first-class concept (gravity effects / gravity rooms)** `[V4/D4]` - _Largely done 2026-06-03 (`1178fe0`,`155dc6d`,`1c71463`):_ `portal::GravityField { dir }` (default down) is **wired into the movement integrator** via `MovementTuning.gravity_sign` — the **player** falls along it (gravity + fast-fall + all jump/pogo impulses oppose it, and ground detection treats a ceiling contact as ground under flipped gravity), and **thrown / ground items** fall along it too. A **`GravityFlipSwitch`** (a tall pressure-plate column in the sandbox) flips it up↔down; gravity resets on room reset. The orient-to-gravity roll is **unified across all actors** (`ActorRoll` + `update_actor_roll`): player, enemies, and NPCs somersault through portals and self-right to the current gravity. 5 tests. _Extended 2026-06-03 (`cbae2f5`): a shared **`physics` module** now owns `GravityField` (moved out of `portal.rs`) with `gravity_accel`/`vertical_sign` + an `apply_world_forces` seam — the ONE place to add the next global force. **Enemies** (`KinematicTuning.gravity_sign` through `step_kinematic` + jumps, via `update_ecs_actors`), **projectiles** (`ProjectileBody::tick(dt, gravity_sign)` on both player + enemy systems), and **items** all fall along `GravityField` now, so a flip pulls everything up. +2 tests (enemy rises onto a ceiling; physics helpers)._ _Gravity zones landed 2026-06-03 (`7c2b9d9`): `physics::GravityZone { aabb, dir }` + `BaseGravity` (ambient) + `resolve_active_gravity` — while the player is inside a zone the world gravity points that way and everything reorients (via `update_actor_roll`); the flip switch now sets the **ambient** so zones override it. A debug gravity-up zone spawns in the sandbox (violet overlay). +2 tests._ _NPCs now fall along gravity too (`2ea0730`), so player + enemies + NPCs + items + projectiles all obey `GravityField`._ _**Localized in space 2026-06-03 (`71451b64`) — Jon: gravity can't be one global resource or NPCs/enemies in a column don't feel it.** Every non-player body now resolves gravity from its **own position**: `physics::GravityZones` snapshot + `gravity_dir_at(pos)`/`local_gravity_sign(pos)`, bundled into a `GravityCtx` SystemParam (sign_at/dir_at). Enemies, NPCs, ground items, player + enemy projectiles, and the orient-to-gravity roll each resolve by their own position; the player stays localized via `resolve_active_gravity`→`GravityField`, and the flip switch still flips `BaseGravity` (ambient) so out-of-zone bodies — and the whole room — flip together. +4 tests (two bodies at different positions feel different gravity)._ _**Dedicated gravity room 2026-06-03 (`f5207c5d`):** the gravity COLUMNS moved out of the main sandbox into a new `gravity_lab` LDtk room (authored via `ambition_ldtk_tools`, reached by a hub door), with a demo puppy slug that falls up + crawls the ceiling; `spawn_debug_gravity_zone_once` gates on `active_spec().id == "gravity_lab"`. The one global flip switch stays in the start room._ **Remaining:** (1) **LDtk-authored** `GravityZone` placement (still debug-spawned in `gravity_lab`; a real LDtk GravityZone entity + loader would let authors paint zones) + non-AABB shapes; (2) bosses run zero-gravity and boss visuals don't render `ActorRoll` yet; (3) arbitrary (non-vertical) gravity still needs the one-way-platform / glide sign work; (4) wire other ad-hoc fallers (falling chest, anvil) to gravity if a flip should affect them.

- [ ] **Stitched / loading-zone-free room transitions** `[V4/D4]` - Prefer Gridvania-style side-scrolling exits and large connected spaces over door-heavy room hops where practical. Current LDtk concepts mention stitched boundaries; make one robust prototype where camera, collision, and transition safety work across adjacent rooms without a door load.
  - Decide per case whether the better authoring answer is stitched rooms or one large LDtk level.

- [~] **Simple falling-sand sim room** `[V2/D4]` - Prototype landed in `crates/ambition_sandbox/src/falling_sand.rs` (feature `falling_sand`, on by default for `desktop_dev`). Spouts emit sand / water / oil / mixed, LDtk room walls are mirrored as `bevy_falling_sand` wall particles, dense tiles are projected as one-way `ae::Block`s so the player collides with piles, and a 1Hz `fs-diag` log streams counts + Y range + per-component coverage. Root cause of "particles never moved" was a missing `ChunkLoader` entity — v0.7.0's `update_chunk_loading` returns early when none exists, so no chunk entities ever get spawned and the movement-by-chunks query returns nothing (fixed bc1d5d5). Pile-up still reads "a bit weird" — leaving in-progress for polish. **Follow-ups (in rough priority):** (a) the `fs-diag` `band_grid_y` calculation picks the topmost LDtk block via `min(block.min.y)` and labels it the floor — should pick the largest `block.min.y` (visible floor) instead; (b) some material seems to pile near the room ceiling — investigate whether the spout/ceiling wall geometry leaves stranded pockets, possibly skip seeding walls on overhead blocks; (c) the projected one-way `Block` flicker remains (sort-by-count fix helped but particles flowing through dense tiles still oscillate the cap admission); (d) consider switching emit to `try_multiple` over a tall column so spouts choke gracefully on backed-up streams; (e) eventually port to v0.8 once it stabilizes (`Particle` becomes a marker, API simpler). Also: fire-from-fireballs and traceable-stream interactions aren't wired yet — that was the original scope.

### Combat, abilities, and interactions

- [~] **Effect-primitive ability/attack vocabulary** `[V5/D4]` — ⭐ **ELEVATED for the next long run** (see the unblocks index at the top): this is the gate for *player-wields-boss-attacks* — the `spawn_*_from_special_messages` systems are boss-keyed (iterate a bosses query), so making them actor-generic + data-authored is the unlock. _**First slice LANDED 2026-06-03 (long run): the player wields a boss attack.**_ The crux turned out to be **faction**, not the boss-query: the projectile pools are faction-segregated (`update_enemy_projectiles` only ever damages the player), but the `Hitbox` primitive already carries `source: ActorFaction`. So the slice rode the hitbox path: (1) made `apply_hitbox_damage`'s **Player-faction branch** (was a no-op) emit an attacker-side Volume `HitEvent` that `apply_feature_hit_events` resolves against enemies/bosses — the player end of the same primitive a Boss-faction hitbox uses on the player; (2) added an **actor-generic** `SpecialActionSpec::ShockwaveSlam` + `spawn_shockwave_from_special_messages` consumer that spawns a World-anchored `Hitbox` at the **emitter's** position with the **emitter's** faction (works for player OR boss/enemy — proven by tests for both); (3) wired a player **"shockwave gauntlet"** held item (`crate::shockwave`) whose Attack emits the Special, so the player fires a boss-style ground-slam AOE that damages enemies, not themselves. _**Ranged slice ALSO landed (same run): the projectile-faction unification + a wielded ranged attack.**_ The projectile pools were faction-segregated (`update_enemy_projectiles` only ever damaged the player); now `EnemyProjectileState::spawn_with_faction` stamps a body's `ProjectileFaction` and `update_enemy_projectiles` **routes by it** — a `Player`-faction shot damages the enemies/bosses it overlaps (a `PlayerProjectile` Volume hit via the shared `ecs_hit_event_hits_actor/boss` helpers) and expires on contact, while `Enemy` shots keep the byte-identical player-damage path. A player **"volley gauntlet"** (`crate::volley`) fires a fan of player-faction bolts through that shared pool. So both wielded boss-attack types — melee-AOE (hitbox) + ranged (projectile) — now work, on the substrates a boss already uses. +12 tests total (shockwave/hitbox/volley/routing); headless clean. **Remaining (the full vocabulary):** migrate the six existing **boss-keyed** special consumers onto the actor-generic shape (so the player can wield the *authored* boss attacks — apple rain, eye beam, …, not just the two new generic ones); decompose into composable `Vec<Primitive>`; `Deserialize` for RON authoring. Original scope below. - The single biggest expressability unlock. Today adding one boss special touches 6–7 hardcoded Rust sites: a `BossAttackProfile` variant (`brain/boss_pattern.rs:251`) → `is_special()` arm (`:307`) → `SpecialActionSpec` variant (`brain/action_set.rs:266`) → `volumes_for_profile` arm (`boss_attack_geometry.rs:678`) → `boss_special_for_profile` arm + a block of `const` tunings (`bosses.rs:350`, consts `:41–117`) → a dedicated `spawn_*_from_special_messages` system (`brain_effects.rs`) → schedule registration (`app/combat_schedule.rs`). And `SpecialActionSpec` is **not** `Deserialize` (verified), so the tunings live as Rust consts, absent from `boss_profiles.ron` — even re-tuning an existing special needs a recompile. Decompose attacks into a small composable, data-authored primitive set (`SpawnProjectileBarrage`, `AnchoredHazard`, `SpawnMinions`, `ToggleHazardAxes`, …); make `SpecialActionSpec` = `{ effects: Vec<Primitive> }` and `Deserialize`; replace the six `spawn_*_from_special_messages` systems with one generic `apply_special_effects` consumer; move tunings into `boss_profiles.ron`. Then migrate the player's Hadouken/Fireball specials (separate `ProjectileKind` path at `projectile/spec.rs:21` + `player/affordances/resolvers.rs`) onto the same vocabulary so player and actors share one attack system — unlocks ability-sharing ("let the player learn a boss attack", aligns with the "every upgrade a theorem" north star). Also collapse `RangedActionSpec`'s 4 visual-only variants (`Rock/Arrow/Pistol/Bolt`, `action_set.rs:167`, all just `speed`+`damage` at the spawn site) into `{ speed, damage, visual: ProjectileVisualId }` and delete the parallel `*Preset` mirror enums + the `character_catalog/resolver.rs` translation layer. Subsumes the "dedup the 6 brain_effects spawn systems" cleanup.

- [ ] **Bubble shield dodge / roll extension** `[V3/D3]` - Bubble + Down should dodge; Bubble + direction should roll. Define resource cost, invulnerability frames, collision safety, and whether roll is a locomotion state or gameplay effect.

- [~] **Player mana: fireball cost + fast sandbox regen** `[V3/D2]` - _Partial 2026-06-03 (`c47e39a`): `PlayerMana` now regenerates over time (`regen_player_mana`) and is shown on the HUD bar._ **Remaining (the decided unification):** make fireballs spend `PlayerMana` and retire the separate `ProjectileSpawner` meter — today the HUD mana bar regenerates but the fireball still drains its own meter, so the bar doesn't visibly move when you fire. Most of this already exists — the player carries a `PlayerMana` component (`player/bundles.rs:91`) backed by the generic `ResourceMeter` (stamina/mana/ammo/charge, `engine_core/player_state.rs:404`), and projectile firing already gates on a resource via `ProjectileSpawner { meter }.try_spend(kind.cost())` (`projectile/spawn.rs:47`, with the `out_of_resource_blocks_fire` test). What's missing/unverified is the wiring. **Decided (Jon): unify onto `PlayerMana`** — fireballs spend the player's mana; retire the separate `ProjectileSpawner` meter. First-pass sandbox tuning: a high regen rate so mana refills quickly (sandbox feel, not a balance pass) — out of mana briefly blocks fireballs, then you're topped up. Surface the value in the player HUD (item above). Keep `ResourceMeter`'s cost/regen as the tunables so a real-game balance pass later is just a number change.

- [~] **Pick-up / throw held items (Smash-style)** `[V4/D3]` - _Vertical slice landed 2026-06-02 (autonomous run, commits `440f4e3`..`f9c0ce7`): `crates/ambition_sandbox/src/item_pickup.rs`._ `GroundItem` + `Attack`-to-pick-up (empty-handed, overlapping) stashes the player's `ActionSet`, overlays the item's verbs (`apply_to_action_set`), and attaches `HeldItem`; `Shield+Attack` throws (restoring the stashed set). **Axe** (keep-on-use melee) and **javelin** (`is_pure_throwable` → throw-on-use: a plain Attack throws it) are both wired; thrown items **arc under gravity** (`ground_item_physics`) and settle on contact; ground-item + held-in-hand visuals. 5 tests; one-held-item-at-a-time as decided. _Update 2026-06-03: held items now **replace** the attack rather than just gating it — the **axe** re-tunes the swing timing+damage (`AttackSpec::with_held_melee`, `68e2285`); a **gun-sword** held item fires the pirates' lasersword bolt with full aim and the real spinning-sword sprite (`734bd7d`/`9b9b0be`); the **portal gun** routes through the same stash/replace path so it no longer swings while held (`734bd7d`); the held-item prop sprites (axe / javelin / gun-sword) are wired and the gun-sword/portal gun rotate toward the aim direction. Debug axe + gun-sword spawn in the room._ **Remaining:** multiple held items via inventory cycling, authored placement (only debug items spawn today), consumable/heal items, unifying with the inventory-equip path. Original build plan below. The substrate already exists: `HeldItem { spec: HeldItemSpec }` (`content/features/ecs/held_items.rs:14`) is the durable "what is this actor holding?" component, and `HeldItemSpec { id, melee, ranged }` + `apply_to_action_set` (`brain/action_set.rs:70/85`) already overlays the item's verbs onto the holder's `ActionSet` (the player carries an `ActionSet`). So a held item already *grants its abilities* — what's missing is the player acquiring and discarding one off the ground. Build:
  1. **Ground-item entity** — a droppable item feature (sprite + pickup AABB + a `HeldItemSpec`), mirroring the existing collectible pattern (`PickupFeature` / `collect_ecs_pickups` / `Collected` in `content/features/ecs/pickups.rs`), except instead of consuming into the counted bag it stays pickup-able.
  2. **Pickup** — a player with empty hands overlapping a ground item presses `Attack` (`input/actions.rs:28`) → attach `HeldItem` to the player + re-derive its `ActionSet` (`apply_to_action_set`) + show the held sprite. Contextual-`Attack` rule: empty-handed + overlapping a pickup ⇒ pick up; otherwise `Attack` ⇒ normal swing. Respect the single-press interaction discipline (see `[[feedback_interact_binding]]`) so it never double-fires.
  3. **Throw** — **Decided (Jon): `Shield + Attack`** (Smash grab-throw), directional: detaches the `HeldItem` and spawns a thrown entity (gravity arc) that travels and lands as a ground item again (or shatters). Reuse the projectile spawn pattern (`projectile/spawn.rs`, `ProjectileSpawner`). NB single-use items like the **javelin** throw on plain `Attack` — that's their `ThrowOnUse` *use* behavior firing (a consequence of using the item), not the generic Shield+Attack throw.
  - **Test items (deliberately varied use-behaviors):** wire the existing held-item prop sprites as `HeldItemSpec`s. (1) **Axe** (`axe` / `pirate_heavy_axe`, anims `axe_swing` / `axe_cleave`) — grants a persistent **melee** verb you *keep*: pick up, swing repeatedly, throw to discard. (2) **Javelin** (`throwing_javelin` / `javelin`) — *using* it **throws it**, so the use action itself drops it and leaves you empty-handed (classic Smash thrown-item feel). The two differ on purpose, to force the clean design below.
  - **Design surface (the real goal):** axe-vs-javelin shows `HeldItemSpec` needs a small **use-behavior** vocabulary — e.g. `KeepOnUse` (axe melee swing), `ThrowOnUse` (javelin: the attack throws it and self-drops), `ConsumeOnUse` (future) — each composing a verb (melee / ranged / thrown-arc) with what happens to the held item afterward. Get this right and the next Smash-like item is a data edit, not new Rust. Shares the thrown-arc / projectile primitives with the **Effect-primitive ability/attack vocabulary** item above.
  - **Decided (Jon): one held item at a time initially**, then generalize to carrying multiple once the inventory is hooked up — the inventory becomes the swap/cycle mechanism for several held items. Coexists with the inventory-equip path below — both attach/detach the same `HeldItem`, so pickup/throw and equip/unequip are two front-ends to one substrate. (Keep both; decide which *feels* better during real-game polish.)

- [~] **Grid inventory + equip / unequip** `[V4/D4]` - _Phase 1 largely shipped 2026-06-03 by the **OoT menu integration** above: the OoT 6×4 grid (`crate::oot_menu`) is the equip/unequip grid UI, backed by `crate::items::OwnedItems` (24-slot model + `equipped`), with cursor/pointer/touch nav and equip⇒grants-abilities via the shared `item_pickup` HeldItem seam (axe/gun-sword). Remaining here = the **Phase 2 OoT 3D cube** front-end (`submodules/ambition_inventory_ui` + `bevy_lunex`), persistence, and collapsing the legacy `PlayerInventory` dual store — all tracked under the OoT-menu item's handoff bullets._ Original scope below. — Today `crate::inventory` is a **counted consumable bag** (`PlayerInventory` = `[u32;3]` over `ItemKind` HealthPotion/SpareBattery/DataChip, `inventory/model.rs`) behind a **tabbed Zelda/phone-style 2D overlay** (`InventoryTab` Items/Map/Quests, `InventoryUiState`, `inventory/ui.rs` on `bevy_material_ui`). It has no grid and no "equipped" concept. Want a grid layout to equip/remove items.
  - **Phase 1 (simplified, on the existing 2D stack):** (a) an equippable-item model — items as instances/specs that can be equipped, including weapon/tool items carrying a `HeldItemSpec`; add an `EquipmentGrid` (slots) + equip slots alongside `PlayerInventory`. (b) a grid UI with cursor move + equip/unequip, reusing `inventory/input.rs` + `inventory/pointer.rs` nav (keyboard/gamepad/pointer/touch). (c) **equip ⇒ grants abilities**: equipping a weapon/tool item attaches the same `HeldItem` to the player and re-derives the `ActionSet`; unequip detaches it — the bridge to the held-item feature above.
  - **Test item: the portal gun** — equip/unequip the portal gun to toggle its ability on the player. Depends on the **Build the portal gun sprite** (section B) and **Portal system** (this section) items landing first — the portal gun isn't built yet (no `portal_gun` symbol/asset). Until then validate the equip flow with the **axe** or **javelin**.
  - **Phase 2 (later, optional):** hook up the OOT-style 3D interface in `submodules/ambition_inventory_ui` (Bevy 0.18 + vendored `bevy_lunex`, a rotating "inside-the-cube" page volume; reusable crate ~420 LOC `lib.rs` + `ambition_demo`/`oot_pause_demo`). Version-compatible (0.18). Integration = depend on/vendor the crate, bridge its UI model to `PlayerInventory`/equipment state, and add `bevy_lunex` as a new UI dep. Do the simple 2D grid first; promote to the OOT cube as a polish option. (Promotes the deferred "N64/OOT-style spinning-cube inventory" idea in the drop-zone.)

- [~] **Portal system (portal gun mechanic)** `[V5/D5]` - _Vertical slice landed 2026-06-02 (autonomous run, commits `44095de`..`82898b2`): `crates/ambition_sandbox/src/portal.rs`._ Fire/toggle/teleport works end-to-end and is headless-tested: `Attack` raycasts (`raycast_solids`, slab method) and places the active-color portal on the nearest solid face; `Interact` toggles blue↔orange; overlapping the pair teleports the player out the linked portal carrying momentum via `portal_transform_velocity` (rotates velocity through the pair rotation). Plus: colored portal visuals, clear-on-room-reset, an `F7` dev off-switch, the gun granted **inactive** and **picked up in a room** to activate (`PortalGunPickup`), and **thrown items travel through portals** too (`portal_teleport_ground_items`). _Update 2026-06-03 (commits `55d3f19`,`260b2ba`,`734bd7d`,`bb9c507`,`16a50b8`, 11 tests): the shot now **travels** as a visible projectile before opening the portal; **non-player actors** teleport too, **size-gated** so only bodies that fit the opening pass (bosses repositioned, enemies carry momentum); portals are **thin oriented doorways** with a consistent face size and the player **warps right at the face**; the **portal-gun sprite** (blue/orange, mode-reflecting, aim-rotated) is wired for both the held gun and the ground pickup; equipping the gun **replaces the attack** (no more swing-while-holding) and **dropping** it works (arm-timer fix); portals **despawn when the gun is gone**; and portal transit gives the player an **aerial roll** (somersault to reorient). Debug-overlay now draws the portal capture box._ _**Proper portal physics landed 2026-06-05 (autonomous run): `crates/ambition_sandbox/src/portal_pieces.rs` + transit machine in `portal.rs`.** Portals are now an **aperture, not a trigger** — see the top-of-file "some percent of the entity is visible on the other side" item for the full writeup. Pure Core-invariant module (`compute_body_pieces`, portal map, `subtract_aabb` carve, `straddles`/`front_distance`); host surface carved non-solid in the opening for ALL collision (`publish_portal_carves` → overlay → `world_with_sandbox_solids`, pair-gated so a lone portal can't open a void); instant teleport replaced by the `PortalTransit` centroid-crossing state machine (momentum carried, latch-clears on trailing-edge-out); player drawn as two clipped pieces mid-crossing; `raycast_through_portals` recursive bounded ray; debug-trace auto-dump fixed via `IntentionalTeleport(true)` on transfer. 27 new tests; full lib + reachability green._ **Remaining portal-physics polish (the design checklist, by priority):**
  - _**Iteration 3 done 2026-06-05** (commits fa2d73f4, 60ccacc7, b2259fad): wall↔wall no longer somersaults (`somersault_roll` gravity-aware); partial render draws the sprite TWICE + boxes the INVISIBLE slice; **1→N portal pairs** (5 complementary color pairs, all systems pair-aware); and an **LDtk-authored `Portal` entity + `portal_lab` test room** (4 stations A–D, reached by a door on a basement one-way platform — authored via `def register-entity` + `area create specs/portal_lab_area.ron`, never hand-edited)._
  - [ ] **Ledge-grab / wall-movement THROUGH portals (with abilities enabled)** — today (2026-06-05) wall abilities (ledge-grab, cling, wall-jump, wall-climb) are DISABLED mid-transit (`suppress_ledge_grab_during_transit`, gated on `SuppressWallAbilitiesInPortal`, default on) because the carve's split edges read as grabbable ledges, so you'd grab "into" a wall portal and pop back out the entry instead of crossing. Jon: grabbing/climbing INTO or OUT of a portal "might be desirable — we just need to make sure it works right." So the real goal is to make the transit cooperate with the scripted ledge-grab/getup motion (which sets position directly, bypassing the velocity-based centroid crossing) so a grab on a portal-aperture edge carries you THROUGH to the partner. Flip the toggle off to experiment. Decide the desired behavior (grab-edge-pulls-you-through vs grab disabled at portal apertures only vs …) then wire it.
  - [ ] **Structural: per-frame wholesale ability reset clobbers transient mods** `[V3/D3]` — `sync_live_ability_edits_clusters` does `abilities.abilities = desired` EVERY frame (resetting `PlayerAbilities` to the editable loadout). Any system that wants to *transiently* modify an ability (portal wall-suppression, a debuff, a status effect) must therefore re-apply it every frame; a set-once/restore pattern is silently clobbered after one frame (this was the "portal disable didn't work" bug, 2026-06-05). More elegant: a **base abilities + transient modifiers** layer (base loadout, then a stack of per-frame modifiers applied after the reset), so transient suppressions compose cleanly instead of fighting the reset. Until then, transient ability mods MUST re-apply each frame and run after the reset.
  - [ ] **Texture-accurate piece clipping** — the partial-transit exit copy is the full sprite + an opaque mask box today (`sync_portal_body_pieces`); clip the actual character atlas frame instead so the mask isn't a visible box. Fiddly because the player sprite is an atlas frame with a custom feet anchor — map the body slice to a cropped `Sprite.rect` and account for the anchor/render-vs-collision scale. Do the same for non-player actors (they still instant-teleport).
  - [ ] **Portalize hurt/hitboxes + dedup by logical owner** — route damage/contact/pogo/pickup/sensor volumes through `compute_body_pieces` so a body half-through a portal is hittable on its visible piece(s) and NOT on its clipped-away part; a hit overlapping both pieces of one owner counts once (store logical target + which piece); impact FX at the visible contact piece. Touches `content/features/ecs/damage.rs` (`ecs_hit_event_hits_actor`, `apply_feature_hit_events`) and the player hurtbox path. The pure primitive is done; this is the wiring.
  - [ ] **Wire `raycast_through_portals` into gameplay rays** — grapple (`grapple.rs` uses `raycast_solids`), aim assist, line-of-sight, beams. The recursive primitive + test exist; swap call sites and pass the placed portals.
  - [ ] **Sized-projectile gradual transit + per-projectile policies** — portal projectiles/thrown items currently instant-teleport (`portal_teleport_ground_items`); give sized projectiles the same AABB straddle/transit as bodies, and a per-projectile policy enum (pass-through / die / bounce / stick / ignore). Point-like projectiles can stay simple (transform on center crossing).
  - [ ] **Attached-object chart consistency** — held items / weapons / riders / weak points should use their owner's portal chart (a sword swing from a half-through actor originates from the visible hand piece, not the authoritative center).
  - [ ] **AI perception of portal pieces** — simple→advanced: target authoritative centers (today) → perceive/attack visible exit-side pieces → pathfinding treats linked portals as graph edges.
  - [ ] **Placement-validity + crushing rules** — portal must fit the host surface, aperture must not overlap invalid geometry, two portals must not overlap unstably; define the forced-into-blocked-geometry rule (block movement first; avoid telefrag unless intentional). Today placement is "nearest solid face" with no validity gate.
  - [ ] **Chart-aware VFX + camera polish** — SFX already fire at the entry/exit portal positions; add entry/exit ripples, a textured portal sprite (vs the rim+core bars), and optional wipe/dissolve transition. Camera should support, not define, the effect.
  - [ ] **Moving-platform / dynamic-geometry portals** — transform the portal frame over time and apply moving-frame velocity correction (advanced; static-world portals are the supported case today).
  - **Earlier remaining:** surface-validity placement rules (folded into the checklist above), textured portal / in-flight-shot VFX (above), feel tuning, the held-item-equip front-end via the grid inventory. Original design context below. The flagship ability: fire a portal pair and travel through it (Portal-style). Model it as a portal-gun `HeldItemSpec` that spawns/links portal entities, plus a portal-pair system that teleports the player between linked portals. **Decided (Jon, merged from the old "Portal Gun" low-hanging note):** classic **blue/orange two-color** pair; **carries momentum** through the portals; it's a **held item you pick up in a room**; **`Attack` fires/places portals** and **`Interact` toggles blue↔orange** (the context-sensitive-Interact override — Interact becomes "toggle mode" when there's no genuine interaction nearby, see the Context-sensitive Interact item above). Remaining open design calls: portal placement rules (any surface vs valid-surface-only), what *else* carries through (projectiles / thrown javelins or player-only), interaction with room/LDtk world bounds, and keeping it **deterministic for the headless sim**. Uses the portal gun sprite (section B) as its held visual and is the headline equip test for the grid inventory above. Big, multi-session feature — scope a vertical slice (one room, one portal pair, player-only teleport) before generalizing. The long-discussed flagship "build a real feature" target (see `[[feedback_long_run_build_big]]`).

- [ ] **Port pirates off the dedicated "sniper" path onto held-gun-sword unification** `[V3/D3]` - **Flexed on the player first (2026-06-03):** the player can now pick up a `gun_sword` `HeldItem` and `Attack` fires its `RangedActionSpec::Bolt` laser bolt instead of swinging (`item_pickup::fire_held_ranged_system` / `held_projectile_step`); picking it up *replaces* the player's melee with the item's ranged verb. The pirate end still has bespoke routing: `PirateOnShark` carries `held_item: Some("gun_sword")` but its ranged fire is special-cased in `content/features/ecs/brain_effects.rs::spawn_enemy_projectiles_from_brain_actions` (the `uses_gun_sword` / `lasersword:` owner-prefix branch) and the brain leans on a Skirmisher/Sniper template. Generalize so the brain "just knows it's holding a gun-sword and uses it" the same way the player does: drive enemy ranged fire purely off `ActionSet.ranged` + the held item (one shared spawn path for player and pirate laser bolts), and drop any dedicated sniper-mode branch (`brain/state_machine.rs::tick_sniper` / `SniperCfg`) that exists only to make the gun-sword fire. Keep the lasersword muzzle/visual routing. The player slice is the reference implementation; this closes the unification.

- [~] **Possession (down + hold Interact to take over an actor)** `[V4/D3]` — _**Substantially LANDED 2026-06-03 (long run): `crate::possession`.** **Hold Down+Interact ~2s** (real-time, `raw_dt`) near a non-boss actor to possess it; press again to release. The actor's **own** `ActorControlFrame` is filled by `tick_player_brain_from_control` (the exact translation the player brain uses) instead of its `Brain`, in `update_ecs_actors`, so it moves + attacks through its own update path — the honest "drive any actor's control path" the `ActorControlFrame` doc literally anticipates ("a possessed goblin"). Pieces: a `Possessed { control, original_faction }` component (control synced each frame by `sync_possession_input`, so the already-15-param `update_ecs_actors` only gains a nested query field); the player's own control phase gated OFF (`not_possessing` run-condition) so input drives one body; `release_possession_if_target_lost` clears it if the actor dies; **the camera follows the possessed body**; and — the tactical payoff — **the actor flips to `ActorFaction::Player` while possessed**, so other enemies target it and its **melee + ranged attacks redirect at its former allies** (player-faction Hitbox via the `apply_hitbox_damage` player-branch + player-faction projectiles via the faction-aware pool — both built earlier this run), restored on release. **Bounded to non-boss actors** (excluded from the candidate set — bosses still `unreachable!()` on `Brain::Player`), exactly sidestepping the cited blocker. 5 tests + headless clean. **Remaining (handoff):** the **Vacate** body-disappears visual (the player sprite still shows); a possessed actor's **contact-body** damage is now gated OFF (it no longer hurts you on contact — melee + ranged already redirect at its former allies), but doesn't yet *redirect* contact damage to enemies; driving NPCs (only the enemy `clusters` branch is wired — NPCs tick through `update_ecs_npcs`/`tick_via_brain`); and the boss case once `Brain::Player` is handled. Original scope below._ ⭐ **ELEVATED for the next long run** (see the unblocks index at the top): gated on flipping the universal-brain shadow to the live control path (`[[project_controllable_unification]]`) + the boss `unreachable!()` on `Brain::Player`. - Holding `Down + Interact` for ~2s near any actor possesses it: swap that actor's `Brain` to `Brain::Player(slot)` and route the player's control into it (revert on release/death). Deliberately **unrestricted** for now (justified as an endgame ability); the real game will gate it hard. The point is to **drive every actor's `ActionSet` through the human control path** — NPCs, enemies, bosses — so any unification oversight becomes explicit. Concretely: bosses currently `unreachable!()` on `Brain::Player` (`content/features/ecs/bosses.rs:558`), so possessing a boss immediately flushes that assumption (and any enemy/NPC verb a human never drove). **Decided (Jon): default = vacate / despawn the player body** — you fully *become* the possessed actor; the original body is gone until you un-possess. Make the body-handling a policy/knob (enum: Vacate / FreezeInPlace / GhostFollow) so other modes/games can pick a different rule later, but ship Vacate first. **Un-possess:** press `Down+Interact` again to leave (forced out on the possessed actor's death); the camera follows the possessed actor while possessing. Cheapest high-signal test of the universal-brain unification — see the "Universal brain interface" item in the drop-zone.

- [~] **Context-sensitive Interact + button-prompt overlay** `[V3/D3]` - _Partial: the held-item override + label exist — `InteractVariant` gained `ModeSwitch` ("Mode Switch") and `None` now reads "Context" (not "Interact"), and `resolve_interact` resolves item-override > nearby interactable > Context, with the **portal gun** making Interact toggle blue↔orange (`portal_toggle_system` gated on no nearby interactable). **Remaining:** make a no-nearby-interactable press a true no-op everywhere (not just label), and the polished on-screen button-prompt overlay._ Builds on the shipped affordance / contextual-button-label work (`player::affordances`, `update_button_glyph_from_active_input`, `docs/systems/control-affordances.md`; see "Contextual button label follow-ups" near the top of this file). Two changes: (1) `Interact` is a **no-op unless** something interactable is in range — gate it on an in-range `Interactable` (`interaction.rs:14`; its `prompt: String` is the label source) so a press with nothing nearby does nothing. (2) Certain held items **override** what the button does — e.g. the portal gun makes it "switch modes" instead of "interact" — so the button resolves through a small context stack (item override > nearby interactable > no-op). Surface the resolved label in the on-screen button overlay (OoT A-button style: "Talk" / "Open" / "Switch Mode" / nothing), reusing the existing affordance glyph overlay rather than a new one. Text first; icons/symbols to prettify later. Pays off as a live debugging aid and the seed of in-game tutorialization. Respect single-press interaction discipline (`[[feedback_interact_binding]]`).

- [~] **Puppy slug gun (friendly-summon held item) + faction generalization** `[V4/D4]` - _Shipped 2026-06-03 (`67a233a0`): `crate::puppy_slug_gun` — a `puppy_slug_gun` HeldItemSpec (no melee/ranged verb) whose plain Attack summons one **player-allied** (`ActorFaction::Player`) + **passive** puppy slug ahead of the player, capped at 3 alive (`PuppySlugAlly` marker), via the generalized `spawn_runtime_minion(..., faction, aggression)` (boss adds now pass Enemy/hostile). Player faction ⇒ `is_player_side()`, so the existing damage loop lets it hurt enemies + be hurt by them, never the player — **no new faction/targeting code** (the "ally that hunts" is a future `AggressionTarget` variant). The slugs just wander (passive), per Jon. The gun is a "pure throwable", so it was excluded from throw-on-plain-Attack (throws only on Shield+Attack) and the summon is gated off Shield+Attack — plain Attack summons, Shield+Attack discards the gun. Equips from the OoT menu (catalog `held_item_id`); debug ground item in the sandbox. 3 tests. **Remaining:** the puppy-slug-gun **prop sprite** (section B); smarter per-summon targeting for OTHER helper types later; persistence/decay tuning._ Original scope below. — A held item (`HeldItemSpec` with a ranged verb) that fires **friendly puppy slugs** — they damage actors hostile to the player but never the player. Reuses the existing `puppy_slug` / `PuppySlug` surface-walker actor, spawned as a **player-allied** summon. Forces a small generalization of the faction/aggression system: there is already an `ActorFaction` enum + a `can_damage` matrix with friendly-fire rules (`actor.rs:26`, test `can_damage_matrix_encodes_the_friendly_fire_rules` at `:212`), so the work is spawning the slug **player-allied** so it **damages** actors hostile to the player, **is** a valid target for hostiles, and never hits the player or other allies — the `can_damage` matrix does the rest. **Decided (Jon): the puppy slugs don't target — they just move** (their normal surface-walker wander); they're simply player-allied now. Smarter targeting can come for *other* helper summons later (the behavior may differ per summon type). Cap ~3 alive; persist until killed or you leave the room (no decay timer). This is the concrete use case for the **"Actor aggression / NPC-enemy distinction polish"** item below: model aggression as allegiance + an `aggressiveness` scalar, not a hardcoded species split (the `NpcRuntime`/`EnemyRuntime` it references are gone). Needs a sprite (separate task in section B).

- [~] **Merchant + buy/sell system & UI** `[V4/D4]` - _Working economy loop shipped 2026-06-03 (autonomous): the `merchant_seed` Yarn node is a real **dialogue-driven shop** (the design-intended shape) — buy Axe/Health Cell/Mana Cell (line-conditioned on `can_afford` so unaffordable rows grey out) and sell Axe/Health Cell (shown when owned), refunds + leave. `crate::shop::buy/sell` are pure primitives over `PlayerWallet` (`try_spend`/`add`) + `OwnedItems` (`grant`/`take`); Yarn `<<buy_item>>`/`<<sell_item>>` commands + `wallet_balance()`/`can_afford()` functions wrap them. The `npc_merchant_prototype` is already placed + dialogue-bound in `sandbox.ldtk`, so it's reachable in-game; bought items flow into the same `OwnedItems` the OoT grid renders. 6 shop tests. **Remaining:** a dedicated **shop overlay UI** (vs the dialogue list) if Jon prefers it; richer stock + provenance/"ethical currency" framing (Vault Keeper); balance pass._ Original scope below. — A merchant the player can trade with — first stock: an **axe** (the held item above), a **health potion**, and a **mana potion**. This unblocks the stubbed `merchant_seed` Yarn shop node (see "Implement the TODOS for the kernel NPC dialog tree" near the top of this file), which is explicitly waiting on the *economy decision* — **decided (Jon): a coin/credits `PlayerWallet`** (abstract money dropped by enemies / pickups, shown on the HUD), **not** item-as-currency; plus a heal command for the potions. Build the trade loop — spend/earn `PlayerWallet`, move items in/out of `PlayerInventory` / the equip grid, and a simple shop UI (stock list + price + buy/sell, reusing 2D `bevy_material_ui` + `inventory/input.rs` nav). Depends on the money/currency system + grid inventory. Potions consume to restore `Health` / `PlayerMana`. A *working* economy loop, not a balanced one.

- [~] **Healing / save-point shrine** `[V3/D3]` - _Vertical slice landed 2026-06-03 (`58b96e4`): `crates/ambition_sandbox/src/shrine.rs` — `HealShrine` + `heal_save_shrine_system`: one `Interact` heals to full health + mana and writes a save checkpoint (marks `SandboxSave` changed → existing autosave persists). LDtk-authored placement (`ShrineSpawn`) + obelisk prop sprite + SFX; 2 tests. **Remaining:** route the heal/save through the affordance/prompt system via an `Interactable` (it still uses a direct overlap + `Interact` check, with no prompt)._ An interactable shrine that (a) heals the player to full (`Health`, and likely `PlayerMana`) and (b) acts as a save point. **Decided (Jon): a single Interact does both** (heal to full + save) in one action. Game logic: an `Interactable` (heal-on-interact + trigger a save through the existing `SandboxSave` path) plus placement in test rooms. This is the concrete "dedicated save-point entity distinct from switches" the **"Save-point lab + persisted-switch test room"** item (test rooms, below) already calls for. Needs a sprite (separate task in section B).

- [~] **More enemy varieties across size and aggression bands** `[V4/D4]` - `EnemyArchetype` already covers several combat shapes. Finish the missing low/medium/high combinations only if they create distinct encounters, not just more enum values. Validate HP, aggro radius, damage, and LDtk brain IDs.

- [ ] **Actor aggression / NPC-enemy distinction polish** `[V4/D3]` - `issues.md` states the architecture should distinguish enemy vs NPC mostly by aggressiveness, not by separate conceptual species. Check current `Actor`, faction, `NpcRuntime`, hostility conversion, and `EnemyRuntime` paths; reduce special cases where possible.

### Test rooms and progression laboratories

- [~] **Save-point lab + persisted-switch test room** `[V4/D3]` - `switch_lab` demonstrates switch persistence, but a dedicated save-point entity and save/lab flow are still useful. Add a save-point interactable distinct from regular switches, a reset-switches sub-room, and a broader test-state schema for boss defeated / mob room cleared / save restoration.

- [ ] **Additional body-mode traversal rooms** `[V3/D3]` - Current body-mode vocabulary is available, but more authored rooms should prove crouch, crawl, slide, morph, stand-up rejection, and compact traversal under real LDtk collision.

- [ ] **Tutorial refresher / quest reminder rule** `[V3/D3]` - Design rule: the game should never permanently strand a player without a way to review tutorial controls, current quests, or what they were doing. Add a durable UI/UX plan and a small sandbox proof.

- [x] **Alice/Bob/Eve/Mallory/etc. NPC cast** `[V2/D3]` - Potential cryptography-themed NPC set: Bob the architect, Alice the cryptographer, Eve listener, Mallory malicious attacker, Trudy intruder, Craig cracker, Sybil identity attacker, Trent arbitrator, Victor/Peggy verifier/prover, Walter warden, Olivia oracle, Judy judge. Add these NPCs to various rooms in the sandbox.

## B - Audio, generated assets, and authoring tools

> **Sprite convention:** adding a sprite is its own task, separate from the feature that uses it (placeholder-but-presentable). We may ship a goofy debug build with no commissioned art, so the generated placeholder sprites are the shipping art until an artist pass — make them look a little nice.

- [ ] **Boss music binding extension** `[V4/D3]` - Extend adaptive music cue bindings beyond mob encounters so boss phases such as Intro / Phase1 / Transition / Phase2 / Stagger / Enrage can resolve to cue states. Mechanism exists in pieces; authored audio identity is the gating cost.

- [~] **Music level / mixing debug tools** `[V3/D3]` - _Offline level report shipped 2026-06-02 (autonomous):_ `tools/ambition_music_renderer/level_report.py` walks `assets/audio/music/generated/<cue>/full.ogg` and prints a sorted, fixed-precision, diff-friendly table — duration, RMS dBFS, sample peak, true peak (4x-oversampled dBTP), crest factor, target-RMS delta, optional integrated LUFS (if `pyloudnorm` is importable) — plus a summary (RMS min/mean/max/spread + CLIP/LOUD/QUIET tallies). Reuses `transition_audit`'s reviewed db/rms/peak; self-tested against a closed-form 1 kHz sine (`tests/test_level_report.py`, 4 passing). **First run already actionable:** 35 cues, 0 CLIP, but a **12.3 dB RMS spread** (-28.8..-16.5) with 18 cues >3 dB under the -20 dBFS target — the guild faction themes cluster quiet while boss/action themes sit near target, which is concrete evidence for the inter-cue level jumps noted elsewhere. **Remaining:** the optional **in-engine dev HUD** (live per-bank / per-slot gains + post-master dB while playing) — World-coupled + best validated by ear in a play session, so deferred to an interactive run.

- [ ] **Equal-power crossfade in runtime gain smoothing** `[V2/D2]` - The current per-slot smoothing can create a midpoint power dip. Investigate a phase-driven sin/cos equal-power transition when two banks crossfade. Validate by ear and with level-report output.

- [ ] **Renderer: master per-stem outputs** `[V3/D3]` - The full mix may receive postprocess treatment that individual stems do not. If stem-driven runtime playback returns, make sure per-stem outputs are mastered/audited enough that state-level stem-gain changes are audible and balanced.

- [ ] **Generalize encounter music registration** `[V4/D3]` - `crates/ambition_sandbox/src/music/first_goblin.rs` is intentionally specific today, but future encounters need a reusable structure. Avoid hard-coding every encounter cue by hand.

- [~] **Sprite / tile wiring batch** `[V3/D3]` - Keep a visible list of assets that exist or are planned but are not wired into runtime/LDtk visuals yet. Examples from old TODOs and current issue notes: switch armed/disabled sprites, lock-wall tile, water-surface tile, ladder/vine/climbable tile, acid/lava tile, circuit/background tiles, creator/wagon/lab props, and replacing placeholder raid-enforcer/general sprites where the wrong asset is used.

- [ ] **Generated tile sprites for IntGrid layers** `[V3/D3]` - Replace colored placeholder rectangles for climbable/water/hazard/solid layers with real tileset textures while preserving canonical LDtk/bevy_ldtk usage.

- [~] **Build the portal gun sprite (blue + orange, mode-reflecting)** `[V4/D3]` - _Done 2026-06-03: the blue/orange mode-reflecting portal-gun sprite (`portal_gun_blue`/`portal_gun_orange` props) is wired as the held gun (in-hand, aim-rotated, swaps blue↔orange on toggle) and the ground pickup at a consistent size; the portals themselves are now thin oriented blue/orange doorway bars (rim + core) instead of big quads. `regen_sprites.sh` installs the prop canonicals on a fresh clone._ **Remaining:** a **textured** portal sprite/VFX and an in-flight-shot VFX (vs the current colored bar / streak). Generate the portal-gun sprite via the sprite generator, mirroring the held-item/effect prop sprites (axe, javelin). Needs a **blue and an orange variant**: the gun's on-screen sprite must switch to match the active mode (`PortalGun.next_color`) so the player sees which portal they'll shoot next when they toggle (Interact). Today a placeholder chip floats above the player (`portal::sync_portal_mode_indicator`) — replace it with the real sprite swapping blue↔orange. Also: the **portals themselves and the in-flight shot** are colored placeholder quads (`portal::sync_portal_visuals`) and want real sprites/VFX (a blue and an orange portal). It's the visual for the single portal-gun pickup item (`PortalGunPickup`) and the held/active gun. Scoped separately from the mechanic. Keep `regen_sprites.sh` working on a fresh clone (`[[feedback_regen_invariant]]`).

- [ ] **Puppy slug gun sprite** `[V2/D2]` - Held-item prop sprite for the **Puppy slug gun** (section A), generated like the axe/javelin/portal-gun props. Placeholder but presentable. Keep `regen_sprites.sh` green on a fresh clone (`[[feedback_regen_invariant]]`).

## C - Bosses, encounters, and story-arranged slice

- [ ] **Sandbox-side boss controller hook for `BossMovementKind`** `[V4/D3]` - Engine pattern data can describe traversal beats, but the sandbox boss runtime needs to interpret `step.movement` into actual world transforms so bosses dash, orbit, reposition, or retreat instead of only firing attack verbs.

- [~] **Flying Spaghetti Monster boss fight** `[V4/D4]` - _Playable fight shipped 2026-06-03 (`fdc80007`): a fully data-driven FSM boss composed entirely from existing reusable attack profiles (no new hardcoded attacks) — OverfitVolley (meatball volley), SaddlePoint (rotating noodle cross), GradientCascade (summon lesser appendages), FullBodyPulse/SideSweep. `boss_profiles.ron` row (floaty AirSwoop, 3 phases, Scripted schedule) + `boss_encounters/flying_spaghetti_monster_boss.ron` (36 HP) + spec_types/bosses/profile constructors + AUTHORED registration + reward chest; `flying_spaghetti_monster_fight` music registered. New `flying_spaghetti_monster_arena` LDtk room (BossSpawn + bidirectional Hall-of-Bosses door, authored via ambition_ldtk_tools). 4 tests; render shows the spawn; headless runs the fight 300 ticks clean. **Remaining:** the FSM sprite has idle/drift rows (not boss-attack rows) so the boss renders via the **generic boss fallback** until its sheet is boss-wired (`BossSheetSpec` + `load_flying_spaghetti_sprite_in` + `all_boss_sprite_filenames` + game_assets, like smirking_behemoth); the **Will-Smith-eats-the-FSM finish**; per-phase music ramp; feel/balance tuning._ Original scope below. — Design + build an FSM boss and place it in the hall of bosses (`assets/sandbox_assets/builders/world.rs` — author the room/slot if it isn't there yet). First-pass goal is *interesting, not final* — we fully expect to tweak/redesign once it's in-game, so prioritize getting something playable over nailing the design. Lean on the data-driven path as far as it goes: a `boss_profiles.ron` row consumed by `BossBehaviorProfile::from_data` (tunings + `Scripted`/`Cycle` attack schedule), an `ActionSet`, a generated sprite, and an LDtk `BossSpawn`. Noodly-appendage flails / meatball volleys / "touched by His appendage" hazard fields fit the **Effect-primitive ability/attack vocabulary** model (section A) — express the attacks as data-authored primitives rather than new bespoke systems. Flag whatever still needs Rust (encounter `BossProfile` metadata, any genuinely bespoke set-piece) and route bespoke logic through the decoupled **boss-encounter plugin seam** (section D) instead of new cross-module special-cases.

- [~] **T-rex boss fight** `[V4/D4]` - _Playable fight shipped 2026-06-03 (`1882e583`): grounded, melee-centric `trex_boss` (charge via macro Approach, then stomp/bite/tail-sweep from FloorSlam/FullBodyPulse/SideSweep — contact-AABB only) in a new `trex_arena` (BossSpawn on the floor + Hall-of-Bosses door, authored via ambition_ldtk_tools). `boss_profiles.ron` + `boss_encounters/trex_boss.ron` (44 HP, `dinosaur_liberators` music) + constructors + AUTHORED + bone-relic chest. 2 tests; headless runs the fight 300 ticks clean. **Remaining:** boss-wire the `trex_enemy` sprite (renders via generic fallback now); a dedicated roar/stagger telegraph + screen feedback; feel/balance tuning._ Original scope below. — Design + build a bipedal T-rex boss that feels distinct from the floating bosses — grounded, gravity-affected, melee-centric (charge / stomp / bite / roar-stagger) rather than ranged volleys — placed in the hall of bosses. Doubles as the **player/boss unification flex**: a bipedal grounded actor stresses the shared `Brain` + `ActorControl` + `ActionSet` + `step_kinematic` path differently than the aerial bosses, which is the point. Promotes the existing "Develop a Boss to flex / force the player/boss unification (bipedal/trex)" note near the top of this file. Same data-driven discipline as the FSM boss (`boss_profiles.ron` + sprite + LDtk spawn first; bespoke beats via the encounter seam in section D). First pass interesting-not-final; expect iteration once in-game.

- [ ] **Boss intro sequence audit** `[V3/D2]` - `TODO-drafts.md` flags the boss music / intro sequence as possibly bugged. Reproduce first; do not assume the diagnosis. Check cutscene gating, encounter start, music request, and boss phase state.

- [ ] **Real central hub authoring** `[V3/D4]` - Resume story-arranged slice content once the sandbox bar is met.

- [ ] **Basement / first-zone Gridvania authoring** `[V3/D4]` - Prefer a connected layout with side-scrolling exits and branching routes over door-heavy lab chunks.

- [ ] **Vertical intro lab maze pass** `[V4/D5]` - Explore Jon's current story shape: the intro climbs vertically through a lab maze with side branches, factions fighting each other, the creator trying to escape with the player, and faction/boss pressure at the top. Keep this as design work until mechanics and rooms support it.

<!-- Gradient Sentinel item closed 2026-05-25 — see FEATURES.md "Gradient Sentinel boss fight". -->



- [ ] **Intro cutscene polish** `[V2/D3]` - Old note: include a "Hey you, you're finally awake" beat or equivalent intro hook, but only if it fits the current tone.

- [ ] **Dev menu: play intro vanity card + wire vanity-card playback** `[V3/D3]` - The vanity-card *art* exists (`assets/vanity_card/` — `final`, `panels`, `poses`, `vanity_card.gif`) but there is **no playback code** in the sandbox (grep `vanity` in `src` is empty). Wire up vanity-card playback as a game-start intro sequence (the studio/branding cards shown when the final build launches), and add a **developer-menu item to play the intro vanity card on demand** so there's a place to wire and test them now. Reuse the cutscene/banner presentation path (`presentation/cutscene.rs`) if it fits. Scoped so the dev-menu trigger + playback land first; the "show at game start in the final build" gating comes later.

## D - Engine, validation, and architecture

- [~] **CharacterAI authoritative migration** `[V3/D3]` - Movement half done as of 2026-05-21: every enemy archetype + every boss runs through the `ActorControlFrame` brain→sim seam and a uniform `step_kinematic` (commits `155171c`, `66c8b0b`). _Knob-table half verified done 2026-06-02 (autonomous):_ per-archetype enemy knobs (`chase_speed`, `attack_radius`, HP, aggro, etc.) live in `assets/data/enemy_archetypes.ron` (loaded once via `ENEMY_ARCHETYPE_REGISTRY` LazyLock); per-boss knobs + the full `attack_pattern: Scripted/Cycle` schedule live in `assets/data/boss_profiles.ron` (via `BossBehaviorProfile::from_data`). Adding an enemy/boss is now a RON edit, not a code change. The only `match self` arms left on `EnemyArchetype` are genuine code-side *classification* (`respawn_policy`, `enemy_key_for` RON-key mapping), not tunables. **What genuinely remains is one design call, not blind work:** whether the boss `Cycle`/`Scripted` attack-pattern *timer state machine* should fold into the brain-evaluator output or stay a layered driver — decide with a use case in hand. See `docs/systems/character-ai-refactor.md`.

- [~] **Player + multi-player unification onto `ActorControlFrame`** `[V4/D4]` - _Decomposition endgame verified complete 2026-06-02 (autonomous):_ the `ae::Player` struct no longer exists in `engine_core` (grep for `pub struct Player\b` / `&mut Player\b` finds only historical comments), every movement entry point is cluster-native (`update_player_*_with_tuning_scratch` over `PlayerClusterScratch`), and the three internal scratchpads + read-only snapshot consumers listed below were all converted. Items (a)/(b)/(c) of the 2026-05-28 snapshot are done. What genuinely remains is the broader **universal-brain EFFECTS-consumer flip** tracked separately (read `ActorActionMessage` instead of `update_player`); see the "Universal brain interface" item in the drop-zone. Original snapshot context kept below for history. - Brain → ActorControl seam landed for the player (2026-05-24): every player entity carries `Brain::Player(slot)` + `ActorControl` + `ActionSet`. **2026-05-28 push (phase 3d.1–3d.4):** every writable `&mut ae::Player` path in sandbox-side runtime has been refactored to `&mut PlayerClustersMut<'_>`. Touch sites converted: `player_control_phase`, `player_simulation_phase` (both now call `update_player_{control,simulation}_with_clusters` directly), `reset_sandbox`, `handle_player_events`, `is_riding`, `death_respawn_player`, `safe_respawn_player`, `apply_player_knockback`, `handle_player_damage_events`, `load_room`, `reload_ldtk_world_from_disk`, `start_attack`, `advance_attack`, `apply_player_body_profile`, `apply_movement_profile`, settings menu plumbing, `transition_for_player`, `remember_safe_player_position` (with new `classify_safety_from_kinematics` helper). `to_player()` / `write_from_player()` no longer called from sandbox-side runtime — only inside `engine_core/movement.rs` for its internal scratchpads. **What remains:** (a) three internal scratchpads in `update_player_{control,simulation}_with_clusters` (tick_active_ledge_grab, integrate_velocity, try_start_ledge_grab still take `&mut Player`); (b) read-only `&ae::Player` snapshot consumers in `dev/trace/*`, `dev/debug_overlay.rs`, conversion-tests, and runtime/setup constructors; (c) the actual `ae::Player` struct + legacy `update_player_*_with_tuning` entry points (deletable once a/b are done).

- [~] **Headless simulation for AI playtesting** `[V4/D4]` - `SandboxSim` and trace replay exist. _Reward-shaping examples landed 2026-06-02 (autonomous):_ `rl_sim::reward` provides pure, dependency-free shaping terms over an `(prev, cur)` observation transition — `survival` (tick reward / death penalty / damage-edge), `exploration` (new-room bonus + capped distance), `health_preservation`, and a `default_shaped` composite — with unit tests pinning the signs/caps. Research code (or a future PyO3 binding) can call or template off them. Remaining: PyO3 binding for Python research code, and a decision on whether to adopt `bevy_rl` or keep the custom adapter.

- [~] **Headless screenshot / visual verification path** `[V3/D4]` - Make the headless or test harness capable of rendering screenshots so agents can visually verify rooms, HUD, sprites, and regressions without manual desktop capture. _Geometry slice landed 2026-06-02 (autonomous):_ `cargo run -p ambition_sandbox --example render_room_geometry -- <ROOM_ID>` renders a room's collision world (Solid / OneWay / Hazard / PogoOrb / BlinkWall / Rebound + spawn) to a PNG with **no GPU/wgpu/window** (pure-Rust `image` pixel buffer, dev-dependency only). Agents can Read the PNG to verify room boundaries, mid-air doors, and authoring fixes — it already confirmed the `pirate_sky_arena` ceiling fix (#64). It also overlays every **authored** entity family from the `RoomSpec` (enemy/boss spawns, NPCs/switches, pickups, chests, breakables, hazard volumes, doors) as colored outlines, plus **moving platforms** (tan fill), **kinematic paths** (green polyline + waypoint dots + path AABB), and **camera zones** (violet outline) added 2026-06-02 — so platform/patrol routes and camera framing are reviewable. It also overlays each boss's **live rest-pose `damageable_volumes` hurtbox** (bright cyan, built from a transient `BossRuntime` + baked sheet registry) over the orange spawn box, so a boss whose hurtbox is a small head inside a big body envelope reads correctly (verified on GNU-ton). **Remaining:** (a) overlay *dynamically-spawned* mobs (encounter waves aren't in the static `RoomSpec`; enemy hurtboxes are just the body AABB so are already implied by the red spawn outline); (b) optional actual-sprite rendering via the `libvulkan_lvp.so` (Lavapipe software Vulkan) backend for art/HUD verification.

- [ ] **Headless collision-invariant fuzz oracle (find collision bugs)** `[V4/D3]` - The headless sim is deterministic (verified 0 RNG in the sim, fixed 60Hz, LCG-seeded policies) and the observation surface already exposes `player_pos/vel/size`, `on_ground`, `on_wall`, `body_mode`, `world_size`, `last_safe_pos` (`rl_sim/observation.rs:9`) — but the existing fuzzer (`tests/fuzz_random_walker.rs`) asserts only "no panic / no NaN / alive" and *deliberately permits* collision violations ("Player can fall outside `world_size`…", `:105`), so it can't catch the collision bugs known to exist. Add a per-step invariant oracle on top of the existing `SandboxSim`: player AABB never fully inside a Solid block (tunneled/clipped), never outside world bounds except through an authored gap/exit, not stuck (movement commanded but `vel≈0` against open space for N ticks), `on_ground` consistent with geometry below, no teleport (Δpos/tick ≤ max feasible speed). Sweep **all** rooms via `SandboxSimOptions::with_start_room(...)`, not just the cold-launch room. The oracle needs the collision world (Solid blocks), which the sim already holds. Runnable on today's build — no presentation-gating required. Precondition: verify no gameplay-affecting HashMap iteration order so failing seeds reproduce exactly (sim-layer `engine_core` is already iteration-free; spot-check the feature/encounter spawn layer). Cheapest path to actually surfacing the collision bugs.

- [ ] **Feature-gate presentation for a lean headless build** `[V4/D4]` - The headless sim already RUNS (`run_headless` / `SandboxSim` on `MinimalPlugins`), but the render/presentation stack is **compiled unconditionally**: `mod presentation` (`lib.rs:66`) has no `#[cfg]`, and 16 presentation files reference `Sprite`/`Camera2d`/`Mesh2d` with **0** `feature = "visible"` gates (the "visible only" split is enforced at runtime plugin-wiring, not compile time). So every headless/RL/CI build still pulls `bevy_render` + `wgpu` + all presentation code (full ~10-min build, GPU deps), and `--no-default-features` won't compile (`Sprite`/`Camera2d` are referenced unconditionally). Gate `mod presentation`, the render bits of `mod host`, and the `add_presentation_plugins` / `add_ldtk_runtime_plugin` wiring behind `#[cfg(feature = "visible")]`; fix whatever sim→presentation type imports leak (boundary is mostly clean — `run_headless` already omits these plugins at runtime). Payoff: a GPU-free, fast-compiling, deployable headless artifact for RL training + CI, and a compiler-policed sim/presentation boundary. Pairs with the collision-oracle item (that one works today; this makes the loop fast and shippable).

- [ ] **Unified `ControlFrame` consumer seam** `[V4/D4]` - Move menu navigation, inventory, cutscene dismissal, and other consumers away from raw `ActionState<SandboxAction>` where practical so keyboard, controller, and touch all pass through the same mode-gated abstraction.

- [ ] **Extract `input/` into a leaf `ambition_input_device` crate (invert the one core dependency)** `[V3/D3]` (DISCUSSED 2026-06-04 with Jon) - `input/` is nearly a self-contained leaf already: its **only** outward dependency *in the core* is `input/control.rs` → `crate::persistence::settings::ControlSettings` (deadzone, `DashInputMode`, `TriggerEdgeState`). The `game_mode` / `presentation::PlayerVisual` / `SandboxDevState` reaches all live in `app/input_systems.rs`, which is the **app glue/adapter** and correctly stays app-side on extraction. **The improvement is a dependency inversion:** deadzone/dash/trigger settings are *input-domain* concepts and belong in the input crate; `persistence` should depend on `input` to serialize/load them, not the reverse. Flip that one dependency and `input/` becomes a true leaf (bevy + leafwing only) → cleanly extractable to a reusable `ambition_input_device` crate that owns both intent frames (`ControlFrame` gameplay + `MenuControlFrame` menu) across all five modalities. Highest-value payoff of the move: a single home to **finish touch + on-screen virtual-gamepad into the frames** (the `mobile_input` bridge exists but isn't wired in). Cost is breadth not depth — ~88 consumer files need a mechanical `crate::input::` → `ambition_input_device::` rename — and the inversion itself is small and can land **in place first** (value on its own) before any crate split. Apps (the game; optionally the inventory demo) consume the crate; the reusable `ambition_inventory_ui` lib stays input-agnostic. Pairs with the `ControlFrame` consumer-seam item above.

- [ ] **Move dev hotkeys into settings/menu surfaces** `[V3/D3]` - Keep hotkeys for developer speed where useful, but every persistent toggle should have a canonical non-hotkey place in settings or dev UI. F-key overlap has caused confusion before.

- [ ] **Shared settings/menu intermediate representation (cube ↔ bevy-UI parity)** `[V3/D3]` (FINDING 2026-06-05, #31) - The cube System page (`crate::oot_cube::build_system_page`) and the bevy-UI pause/settings menu each **re-implement** the settings, so they diverge (the cube's Video submenu already differs from the grid's). Clean fix: one `SettingsMenuModel` IR — categories → options, each `{ label, kind: Toggle | Cycle(values) | Slider(range,step), current_value, description, apply: fn(&mut UserSettings) }` — built once from `UserSettings` (single source of truth). **Both** frontends render from it, so adding/changing a setting updates both in parity. The same `MenuModel`-per-screen pattern generalizes to the other menus. Staging: (1) the IR + builder, (2) the cube System page renders from it (does the layout work in the polish item below), (3) migrate the bevy-UI pause menu onto it (the invasive step). Mirrors the renderer-agnostic `MenuPageModel` seam the cube already uses for the items grid.

- [ ] **3D cube menu (#31) — remaining polish + content** `[V3/D2]` (2026-06-05) - The OoT 3D cube inventory (`crate::oot_cube`/`oot_cube_app`, lib `submodules/ambition_inventory_ui`) is integrated + interactive (items grid + equip/use, page rotation + OoT opening spin + faster close, keyboard/gamepad/mouse/touch nav, dim-scrim, HUD-in-front order-9 camera, nested System settings, cube-backend routing of Esc/inventory/map). Remaining: **(a) System-page touch layout** — bigger fonts + bigger (touch-sized) buttons, the description pane on the **bottom** not the right, centered + enlarged with more right-side room; must be ≥ as touch-usable as the grid (do it when the System page renders from the settings IR above). **(b) Confirm System mouse/touch clicks land** — dispatch logic is tested-correct, so a live dead-click (if any) is the perspective-camera raycast (`cube_3d_picking`) missing the thin System rows; the bigger pick targets in (a) should fix it, else fix the hit-test. **(c) Map + Quest page content** — still placeholders (edge buttons only); wire Map → world-map fragment state, Quest → quest log. **(d)** bumper page-turn = gamepad LB/RB + keyboard Q/E (A/D are the item cursor) — revisit if A/D wanted. **(e)** optional: literal per-material alpha crossfade on close (today it's a faster `amount` decay + earlier hide cutoff).

- [ ] **Promote `KinematicPath` to typed components + index** `[V2/D3]` - Moving platforms have a working path contract, but future path-authored hazards/platforms may need a generic typed runtime index.

- [ ] **Continue Bevy ECS slice extraction** `[V4/D4]` - Shrink procedural orchestration and ad-hoc SystemParam bundles when tests make it safe. Do not do broad architecture churn without a focused validation plan.

- [ ] **Time-domain consistency sweep (gameplay timers on `sim_dt`)** `[V4/D2]` - The `WorldTime` abstraction (ADR 0010/0011: `sim_dt`/`wall_dt`/`player_dt`/`entity_dt`) exists but several **gameplay** systems bypass it and read raw `time.delta_secs()`, so they tick on wall-clock instead of the sim clock. Verified offenders: `app/sim_systems.rs:111/112/130` decrement `combat.damage_invuln_timer` / `hitstun_timer` / `hitstop_timer` on `frame_dt` — so **i-frames and hitstun don't slow under bullet-time or freeze on pause** (latent gameplay bug today; a desync source under multiplayer). Also `app/sim_systems.rs:164` (double-tap window), `app/player_tick.rs:116/202`, `world/physics.rs:139/162`. Route every gameplay timer/state-machine through `WorldTime::sim_dt()`; leave genuinely wall-clock things (camera, hot-reload, music, UI fades) alone. Second half: ~6 sites write `sim_state.time_scale` directly (`app/sim_systems.rs:80`, `app/world_flow.rs:67/156/613/724`, `runtime/reset.rs:164`), bypassing the `ClockScaleRequest` → smoothing pipeline the project mandates — route them through the request channel or assert they are the deliberate hard-reset exceptions. Hardens the multiplayer/RL per-player-clock seam.

- [ ] **Move boss special-attack tunings into `boss_profiles.ron`** `[V3/D2]` - Completes the data-driven boss migration. Per-boss knobs and the attack *schedule* already live in `assets/data/boss_profiles.ron`, but each special attack's numeric tunings are still Rust `const`s in `content/features/bosses.rs:41–117` (`OVERFIT_VOLLEY_SHOT_SPEED`, `MINIMA_TRAP_DAMAGE`, `SADDLE_POINT_ARM_LENGTH`, `GRADIENT_CASCADE_MINION_COUNT`, apple-rain interval/speed/damage, …) — verified 0 occurrences in the RON, so even re-tuning an existing special needs a recompile. `SpecialActionSpec` (`brain/action_set.rs:266`) lacks `Deserialize` (unlike `MeleeActionSpec`/`RangedActionSpec`), which is why the consts can't be authored. Derive `Deserialize` on `SpecialActionSpec`, surface the per-special tunings on the boss profile RON, and have `boss_special_for_profile` (`bosses.rs:350`) read them instead of the consts. Shippable independently as the smaller precursor to (or first slice of) the **Effect-primitive ability/attack vocabulary** item in section A.

- [ ] **Boss-encounter plugin seam: data-driven authoring + decouple bespoke set-pieces** `[V4/D4]` - Make adding a boss/encounter (FSM, T-rex, and the growing hall-of-bosses roster) require as little core-code editing as possible, and stop bespoke encounters from coupling into the rest of the codebase. Two facets:
  - **Data-driven authoring:** tunings + attack schedule already come from `boss_profiles.ron` via `BossBehaviorProfile::from_data` (`content/features/bosses.rs:243`), but encounter-level metadata (phases / music / rewards) still lives in hardcoded `BossProfile` constructors registered in `AUTHORED_BOSS_PROFILES: &[(&str, fn() -> BossProfile)]` (`boss_encounter/profile.rs:123`), with pin-tests asserting "RON matches the hardcoded constructor." Move that metadata into RON too so a new boss is a data row + sprite + LDtk spawn, not a constructor + registry edit. (The special-attack-tunings half is tracked in the item above + the effect-primitive item.)
  - **Decouple bespoke set-pieces (the cut-rope lesson):** `boss_encounter/cut_rope.rs` is the cautionary example — its ~10 systems + `is_cut_rope_boss(id)` checks leak into `app/plugins.rs` (system registration), `app/sim_systems.rs`, `content/features/ecs/damage.rs` (kill-condition gating), `dialog/yarn_bindings.rs`, and the asset builders. A bespoke encounter should register *itself* through a stable seam — spawn hooks, encounter-id-gated systems, a kill-condition/damage hook, dialog hooks — so the core damage/schedule/dialog files don't grow a branch per encounter. Keeping cut-rope in the sandbox crate for now is fine (per Jon); the goal is to sever the *cross-module coupling* so an encounter is an implementation detail layered **on top of** the sandbox engine, and could later move to its own module/crate without touching core. This is the backbone that makes the hall-of-bosses roster cheap to grow.

- [ ] **Boss HP single source of truth** `[V3/D3]` - Boss HP currently lives in three reconciled places: `BossEncounterState.hp` (declared authoritative, `boss_encounter/registry.rs`), the cluster mirror `BossStatus.health` (also written on damage at `content/features/ecs/damage.rs:587`, and made authoritative by the `None` fallback at `:593` when no registry/test resource exists), and a "mostly idempotent" per-frame reconciliation loop (`boss_encounter/systems.rs:217–269`) copying `state.hp` back into the cluster. Two write paths + a reconciliation loop is the "which copy is the truth?" smell, and the `None` fallback means tests and the live game disagree on the source of truth — a latent replay/RL desync. Collapse to a single writable source (`BossEncounterState.hp`) with the cluster demoted to a one-directional read-model — the discipline the **enemy** path already follows correctly (`sync_actor_components_from_enemy` never writes back). Delete the direct-mutation `None` fallback (construct a registry in tests instead).

- [ ] **String-key enemies (drop the `EnemyArchetype` enum gate)** `[V3/D3]` - Adding an enemy still needs a closed-enum edit in 4 Rust sites — a variant in `EnemyArchetype` (`content/features/enemies.rs:103`), an arm in `archetype_data_key` (`:323`), an entry in `COMBAT_ALL` (`:370`), and a row in `BRAIN_NAME_TO_ARCHETYPE` (`:156`) — even though tuning is already fully data-driven via `enemy_archetypes.ron` and NPCs/bosses are already keyed by stable `String` id. Collapse enemies onto the same string-id model (`from_brain` already does a string lookup; the registry is already `HashMap<String, …>`), deleting the enum + the three parallel match/list sites; keep only genuine code-side classification (`respawn_policy`, sandbag checks) as small predicate maps. Result: new enemy = one RON row + sprite, zero Rust. (Continuation of the CharacterAI data-driven migration above.)

- [ ] **Kill per-entity String allocations in hot paths** `[V2/D2]` - Three per-frame/per-event `String` allocations worth removing: (a) `content/features/ecs/damage.rs:186–188`/`:248` does `format!("npc:{}"/"enemy:{}"/"boss:{}", id)` for every actor and boss on every hit event just to compare against `ignored_targets` — compare id slices by disposition instead, or make `ignored_targets` carry `(Faction, &str)`; (b) `content/features/ecs/view_index.rs:54` does `id.to_string()` per feature per frame because the index is `clear()`-ed and rebuilt (`:94`) — reuse keys / mark-and-sweep instead of clear+realloc; (c) the actor read-model mirror (`sync_actor_components_from_enemy` at `actors.rs:866`, `_from_npc` at `:201`) clones `id`+`name` into `ActorIdentity` every tick even though identity never changes — split stable identity from volatile health/combat, or compare-before-write (also stops spurious Bevy change-detection wakeups). Hot only at scale, but cheap and removes allocator churn (matters for RL stepping the sim millions of times).

- [ ] **`EnemyActorBundle` constructor (de-duplicate spawn literals)** `[V2/D2]` - The 15-field `EnemyActorBundle { base, identity, disposition, faction, target, pose, combat_kit, aggression, health, combat, intent, cooldowns, damageable_volumes, pogo_policy, pogo_target_volumes }` literal is hand-written 6× (`content/features/ecs/spawn_actors.rs:224/304/364/439`, `spawn_mounts.rs:156/203`), differing only in `faction`/`aggression`; adding a bundle field means editing all 6. Add `EnemyActorBundle::new(id, name, aabb, facing, faction, aggression, snapshot, combat_kit)` near the struct def (`content/features/components.rs`) that fills the boilerplate defaults. Note the NPC spawn path at `spawn_actors.rs:364` reuses the *enemy* bundle — give it its own constructor or a clarifying comment.

- [ ] **Dead-code & comment hygiene pass** `[V2/D1]` - (a) `ui_nav/list.rs:1` has a **file-level** `#![allow(dead_code)]` masking genuinely-unused helpers in a widely-imported module — remove it, `cargo check`, then narrowly `#[allow]` or delete only what actually warns; audit the other unconditional `#[allow(dead_code)]` sites (`dev/trace/model.rs:364`, `audio/runtime.rs:260`, `music/catalog.rs:246`, `presentation/fx.rs:175/181`, `presentation/character_sprites/sheets.rs:486/497/500`) for "still used?". (b) ~19 "OVERNIGHT-TODO #N" comments in `app/plugins.rs` + `player/components.rs` describe **already-completed** migration steps but read as open work and inflate `grep TODO` from ~25 real to 81 — reword to past tense or drop the token. Genuinely-open markers (kira underwater filter, gnu_ton head-hurtbox `#30`, crouch-sprite-row HACK) are fine as-is.

- [ ] **Documentation health remains green** `[V3/D1]` - Run doc checks after doc moves, rewrites, or archive changes.

```bash
python scripts/check_doc_links.py
python scripts/generate_agent_index.py
python scripts/check_agent_kb.py
```

## Proposed / agent drop-zone

Agents may append new ideas here freely. Jon promotes them into the accepted sections above or moves them to `TODO-drafts.md` / brainstorm docs.

> **Placement rule (for agents):** when Jon says "add this to TODO.md", the item goes in the **curated sections above** (S / A / B / C / D) — he has already reviewed it. This drop-zone is **only** for unreviewed ideas an agent surfaces on its own during an autonomous run.


- [~] **LDtk spatial-question tooling for LLM authoring** `[V3/D3]` - See `docs/concepts/llm-spatial-authoring-discipline.md`. _Progress 2026-06-02 (autonomous): three subcommands shipped._ (1) `intgrid query --px X,Y --size W,H` — read-only mirror of `intgrid erase` that lists the IntGrid values (Solid / OneWay / Hazard / …) present in a px/size rect, so an author can ask "what collision is here?" without opening the editor or grepping `intGridCsv` (`edit/intgrid.py::_cmd_query`). (2) `entity measure --level X --identifier foo` — an entity's size + center + nearest Solid distance in each direction (placement context), reusing `entity query`'s finder + the Collision IntGrid (`edit/measure.py`). (3) `gates audit --level X` — lists a level's gating / destructible elements (switches + action/target/prompt, runtime lock walls, encounter triggers, breakable platforms / pogo orbs) so a boss/encounter author sees the whole locking story in one view (`edit/gates.py`); switches that piggyback the activation bus without targeting an encounter (e.g. falling_sand spouts, consumed by id in `falling_sand.rs`) print `(none — consumed by id)` + their prompt rather than a misleading empty arrow. All verified on goblin_encounter / scroll_lab / falling_sand_room; `entity query` already covered the entity-finding half. **Remaining subcommand:** `paths describe --level X` (reachable exits from spawn + traversal kind) — deferred; reachability over a platformer collision grid (jump arcs, one-ways) is a non-trivial graph problem not worth a rough heuristic that could mislead an author.

- **Promote batch-2 crypto sketches to bespoke templates as needed** `[V2/D3]` - Batch 1 (Bob, Alice, Eve, Mallory, Trent, Judy) + batch 2 (Trudy, Craig, Sybil, Victor, Peggy, Walter, Olivia) all landed as toon-target sketches or bespoke templates. See `docs/concepts/cryptography-crew.md` → "Promotion criteria" for when to upgrade a sketch to a bespoke target. No specific promotion queued; do as story rooms demand.

- **`connect_to` should snap reciprocal Door zones to the destination floor by default** `[V2/D2]` (FINDING, 2026-06-03, autonomous) — `area create … connect_to` inserts the reciprocal `LoadingZone` at the raw `px:[x,y]` from the spec (the *source* door's height), and `snap_to_surface` is opt-in. Authoring the gradient_ascent / overfit_annex / proving_grounds hub entrances without that flag left all three `Door` zones ~280px in mid-air → the rooms were **unreachable from the hub** until a follow-up `entity move` dropped them onto the floor (commit 4e28928f). `ldtk validate` already *warns* on mid-air Doors, but the bug is easy to ship. **Proposed:** in `area_authoring.add_reciprocal_loading_zone`, default `snap_to_surface` to **on for `activation: Door`** (opt out with an explicit `false`); leave `walk` zones unsnapped. **Ripple to handle:** `tests/test_area_authoring_features.py::test_connect_to_inserts_reciprocal_loading_zone` copies the real `sandbox.ldtk` and places its probe door at (240,600) precisely to dodge overlaps — with default-snap it lands on the now-occupied (240,880) floor slot and trips the overlap guard, so that test must move its probe x or pass `snap_to_surface:false`. Left for review since it's a tool-default behavior change.

- **Loudness-normalize the music catalog to a consistent target** `[V3/D3]` (FINDING, 2026-06-02, autonomous) — `tools/ambition_music_renderer/level_report.py` (shipped this session) measures a **12.3 dB RMS spread across the 35 runtime cues** (-28.8 dBFS `long_lofi_drift` … -16.5 dBFS `how_to_kill_a_mockingbird`), with **18 cues >3 dB under the -20 dBFS reference** — structured, not random: the guild faction themes cluster quiet while boss/action themes sit near target. When the game crossfades between a near-target action cue and a -27 dBFS guild theme, that ~8-10 dB jump *is* the audible seam (see the "goblin encounter music transition sounds like a section swap" blocker). Normalizing each cue toward one target (ideally integrated LUFS — install `pyloudnorm` for that column) would close most of it; `level_report.py` is the before/after measuring stick and the `dRMS` column is the per-cue gain delta. **By-ear mastering call + re-renders committed OGGs, so left for an interactive session, not a blind autonomous pass.** 0 cues clip today (the `--check` gate passes).

## Closed-work policy

Do not add completed work here just to preserve history. Put durable history in `FEATURES.md`, `docs/history/progression-log.md`, `dev/journals/`, or `docs/archive/`. When closing an item, update the source doc too if it tracks the same state.
