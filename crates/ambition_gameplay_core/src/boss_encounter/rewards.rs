//! Boss reward placeholder module (intentionally code-free).
//!
//! Boss reward-chest sync now lives in `sync_boss_reward_chests_ecs`
//! (`crate::features`); this file is retained only as a module boundary so
//! `pub use rewards::*` in the parent keeps compiling.
