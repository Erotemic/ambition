[package]
name = "ambition_interaction"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Reusable, content-free interactive-world-object MODEL: the Interactable / InteractionKind vocabulary, Pickup / Chest / Breakable types + their state enums. Pure data over the actor + geometry foundations; the game's ECS + LDtk conversion consume it. Extracted from ambition_platformer2d_actor_monolith."

[lib]
crate-type = ["rlib"]

[dependencies]
ambition_characters = { path = "../ambition_characters" }
bevy_math = "0.19.1"
ambition_entity_catalog = { path = "../ambition_entity_catalog" }
serde = { workspace = true }
