# Long run — 2026-06-02 (autonomous, TODO.md)

Live progress window for Jon. Started ~05:12 UTC, 8-hour budget. This file is appended as work lands; newest section at the bottom.

## TL;DR (executive summary)

A long autonomous run. Everything verified (`cargo test -p ambition_sandbox` + `--all-targets`); **1226 → 1277 lib tests, all integration suites green.** No risky/blind changes — gameplay-feel, save-schema, and visual-only behavior were deliberately left for you (see "Findings flagged for Jon" near the bottom).

What landed, by theme:
- **Real fixes/features:** GNU-ton idle hurtbox now tracks the live animation frame (closes a hitbox-vs-animation blocker); dialogue↔inventory bridge (`inventory_has` reads + `give_item` grants the live inventory); HUD button glyphs follow the selected keyboard preset; the headless geometry renderer now overlays boss hurtboxes, moving platforms, kinematic paths, and camera zones.
- **#1 debt (wall-cling lock-wall teleport, V5):** thoroughly investigated — **no current reproduction** exists (synthetic fixtures + the one captured trace are exhausted; it may already be fixed by the `body_is_side_contact` predicate). Added repro guards and, crucially, a **trace auto-dump on teleport-class CollisionCorrection** so a *live* recurrence now produces a directly-usable trace (the old trace was aftermath-only). The actual budget-reject fix stays blocked on a repro per the project's own criteria.
- **Doc reconciliation:** the trackers had drifted well behind the code — closed/corrected **7 TODO items, 5 expressibility-checklist entries, and the combat-hit entries in `current/next.md`** (the `HitEvent` unification already landed; `DamageEvent`/`PlayerDamageEvent` are gone). New `docs/recipes/dialogue-authoring.md` reference. See the `project_todo_drift` memory.
- **Test + QA hardening (~50 tests):** filled real gaps across the full core-gameplay loop (combat hit matrix, pickup, hazard, NPC peaceful→hostile flip, switch toggle, item-use, projectile motion/bounce + charge mechanic) and the previously-untested data/state-machine/parser layers (encounter state machine, loading-zone/portal logic, LDtk field parsers, encounter-reward serde). Added a **room-spatial-integrity CI guard** (out-of-bounds entities / spawn-in-solid) + a `render_room_geometry -- report` mode.

Details and commit hashes in the dated updates below.

## Headline

Two threads: (1) genuine fixes/features where the correct behavior is unambiguous and unit-testable; (2) **TODO.md reconciliation** — the queue had drifted badly: **6 open `[ ]` items turned out already implemented + tested**, just never closed. See the `project_todo_drift` memory. All work verified with `cargo check -p ambition_sandbox --all-targets` + `cargo test -p ambition_sandbox` (1234 tests green).

## Commits (chronological)

**Real fixes / features**
- `cb4454ed` — **GNU-ton idle hurtbox follows the live animation frame.** Mechanism 1 of the "hitbox doesn't follow animation" blocker: the rest-pose head hurtbox was locked to frame 0 while idle (`damageable_volumes` sampled the rest anim at elapsed 0). Made `BossAnimationFrameSample.profile` an `Option`, emit an idle sample for the `Rest` row, and added `idle_animation_frame_index`. Regression test pins the frame-tracking. (Mechanisms 2+3 were already fixed/ruled-out.)
- `149155e1` — **Boss hurtboxes in the geometry renderer.** Bright-cyan rest-pose `damageable_volumes` over the orange spawn box, so "small head inside big body envelope" reads correctly.
- `cee17130` — **Moving platforms + kinematic paths + camera zones in the renderer.** Tan platform fills, green waypoint polylines (where a platform/patrol travels), violet camera-zone outlines.
- `63b084e6` + `76278f3f` — **Dialogue ↔ inventory bridge.** `inventory_has(item)` Yarn function now reads the live `PlayerInventory` (via a new `inventory_counts` mirror slice); `<<give_item kind count>>` now actually grants (was a logged stub). Loose item-id spelling, unit-tested. Merchant *spend* still needs an economy decision.
- `d0a5e4f6` — **HUD glyphs follow the selected keyboard preset** (was hardcoded Arrows+ZXC). New bounds-safe `KeyboardPreset::by_index`.

**TODO reconciliation (verified-done, closed + recorded in FEATURES)**
- `1dde0ba0` — GNU-ton apple-rain attack (built + scheduled in `boss_profiles.ron`) and per-boss data-driven attack schedules.
- `21271b40` — CharacterAI per-archetype knob tables (`enemy_archetypes.ron` / `boss_profiles.ron`).
- `d2deda8e` — ladders-pass-through-solids (`block_passable_during_climb_clusters` + 3 tests).
- `317b50c7` — menu hover-vs-keyboard focus policy (`ui_nav::pointer`).

**Scoping / hygiene**
- `94d950cc` — Empirically scoped the "no shadow below foot" CI assertion: probed all 107 sprites; the naive version false-positives on 58/107 (the foot anchor isn't the lowest pixel), so it's D3+ not D1 — recommend doing it generator-side. Saved a future agent the dead-end.
- `e0a9c352` — Moved a stray top-level doc into `docs/tools/`; all 3 doc-health checks green.

## Update 2 (~08:00 UTC)

- **HUD glyphs follow the selected keyboard preset** (`d0a5e4f6`) — was hardcoded Arrows+ZXC; new bounds-safe `KeyboardPreset::by_index`.
- **Menu hover-vs-keyboard focus** (`317b50c7`) — verified already implemented + tested in `ui_nav::pointer`; closed.
- **Renderer: platforms/paths/camera-zones** (`cee17130`) — tan platform fills, green waypoint polylines, violet camera-zone outlines. Verified on central_hub_complex + combat_calibration_lab.
- **Lock-wall teleport (HIGH debt) — thoroughly investigated, no blind fix** (`99a495b7`, `2b4386ac`, `f6c58f3d`). Added two full-world regression guards (real goblin_encounter geometry + appended lock wall, synthetic upward velocity AND faithful control+sim wall-jump). **Neither reproduces** — the upward sweep correctly stops below the top wall. Then ran `trace_replay` on the referenced trace: it's **aftermath-only** (first frame is already the stuck post-teleport state), so it can't reproduce either. **Conclusion: there is no current reproduction of this teleport; it may already be closed by the `body_is_side_contact` predicate.** Documented the exact next action (live re-test + fresh pre-teleport trace, else downgrade) in `tech-debt-log.md`. Did NOT land the documented budget-reject fix because (a) the project's own closure criteria require a reproduction first and (b) a blind collision-sweep change risks rejecting legitimate large repositions (blink/reset/room-load).

## Update 3 (~08:20 UTC)

- **Smirking Behemoth cut-the-rope boss** (`14868fea`) — verified fully built (dedicated world, cut-rope mechanic, EyeBeam attack, victory dialogue, replay command); closed (7th stale item).
- **Combat spawn-distribution extraction + tests** (`9a2bc988`, `923a03f2`) — pulled the two genuinely-tricky pure computations out of `brain_effects.rs` into tested helpers: GNU-ton apple-rain golden-ratio-spread + boss-body dodge (`apple_rain_spawn_x`), and the gradient-cascade even minion spread (`gradient_cascade_minion_x_offset`). The other spawn fns (eye-beam, overfit-volley, minima, saddle) are stateful target-lock/sampling machines with trivial per-projectile math — no clean pure extraction, left as-is.
- **Room review** — rendered all 43 rooms via the geometry tool and reviewed the intro slice; geometry is well-formed (vertical escape shaft, sensible enemy/pickup/hazard placement), no authoring bugs surfaced.

## Update 4 (~08:45 UTC)

Once clean code wins were exhausted, the highest-value remaining safe work was **doc/drift reconciliation** (the codebase is well ahead of its trackers) and **targeted test hardening**:

- **Dialogue authoring reference** (`80b042bd`) — new `docs/recipes/dialogue-authoring.md` listing every Yarn command/function (args, what each touches, live/stub status) + markup cues. There was no author-facing reference before.
- **Tracking-doc reconciliation** (`f4d6bcb7`, `8fc1a0c4`) — corrected stale entries in `expressibility-checklist.md` (ladder-passthrough, reward-shaping, headless-screenshot, falling-sand) and the combat-hit entries in both the checklist and `current/next.md`: **the combat-hit unification already landed** — `DamageEvent`/`PlayerDamageEvent` are deleted and everything flows through the canonical `HitEvent`; only the advanced `HitResult` fields (stagger/poise/elements/hitstop) remain, to add with the mechanics that need them.
- **Combat spawn extractions + core-loop test** (`9a2bc988`, `923a03f2`, `d76ccbff`) — pure `apple_rain_spawn_x` + `gradient_cascade_minion_x_offset` with tests, and an explicit test of the central player-slash-damages-enemy loop through `apply_feature_hit_events` (was untested).

## Update 5 (~08:46 UTC)

Filled genuine **core-loop test gaps** (several systems had no direct coverage), 1226 → 1242 lib tests:
- Combat hit matrix through the canonical `HitEvent`/`apply_feature_hit_events`: player-slash damages + kills an enemy, player-slash shatters a breakable (`d76ccbff`, `8322d5fc`).
- `collect_ecs_pickups` (pickup-on-overlap, no-player no-op) — `6082f3fa`.
- `update_ecs_hazards` (touch hazard → `HitSource::Hazard`, clear → none) — `58a3b93e`.

The remaining untested loops (NPC peaceful→hostile flip, switch-toggle interaction) need heavy `EnemyActorBundle` / `NextState<GameMode>` / `DialogState` setup that would be fragile to hand-reconstruct — left for a session that can drive the real spawn path. They're exercised indirectly by `scripted_gameplay` / `plugin_minimal_app`.

Then swept the codebase for **pure-logic 0-test gaps on core systems** and filled them (1226 → 1257 lib tests this run):
- `projectile/body.rs` — tick/gravity, lifetime expiry, aabb, fireball floor-bounce-vs-side-expire (`a9ea7c58`).
- `projectile/spec.rs` — the fireball charge mechanic: `tier_for_hold` thresholds, ×4-per-tier damage ramp + hitbox growth, non-Fireball ignore, normalized initial velocity (`809ae6d4`).
- `encounter/spec.rs` — the `reward` serde-default + PickupKind serde round-trip + lock-wall aabb (`89f57f3e`).
- `encounter/registry.rs` — `SwitchActivation` LDtk-payload parse/round-trip + malformed rejection (`1542f8a1`).
- Earlier: pickup collection, hazard damage, the combat hit matrix, and two spawn-distribution extractions.

The remaining 0-test modules are presentation (`morph_ball` image build, physics debris), data-loading (`registry`), or trivial — not pure-gameplay-logic gaps.

## Update 6 (~09:04 UTC) — interaction surface complete

Pushed through the two "heavy setup" gaps after all (worth it — built via the *real* spawn helpers so the tests track production, not hand-rolled bundles):
- **NPC peaceful→hostile flip** (`18100b62`) — `apply_npc_stimuli` flips a `RetaliatesWhenHit` NPC to `Hostile` once strikes reach the threshold (and stays peaceful below it). Design-central + guards the `make_entity_enemy` conversion.
- **Switch toggle** (`43a4f155`) — a buffered interact on an adjacent switch flips `SwitchOn` false→true via `interact_ecs_actors_and_switches`.

**Net for the run: 1226 → 1259 lib tests**, all integration suites green, all-targets clean (only pre-existing dead-code warnings remain). The full core-gameplay loop surface now has direct coverage: player→enemy/breakable damage, pickup, hazard, projectile motion/bounce + charge, NPC flip, switch toggle, chest open, encounter reward serde, switch-payload parse.

### Findings flagged for Jon (design decisions, not bugs I should fix blind)
- **Inventory does not persist across save/load.** `SandboxSave` persists flags/quests/bosses/dialog-visits but NOT `PlayerInventory` (a separate resource reset to `starter()` each session). This is inconsistent with quest/boss progress persisting — but whether inventory *should* persist is a deliberate design call (session-fresh-player vs persistent-equipment), so I left it. Easy + additive + testable if you want it.
- **No save-point/checkpoint interactable.** Saving is automatic (`autosave_sandbox_save` on `SandboxSave` change); there's auto-tracked "safe position" respawn but no explicit player-set checkpoint. The TODO "save-point lab" needs the checkpoint *semantics* decided first.
- **Dialogue `camera_zoom` / `spawn_chest` stubs** left — both need camera-composition / chest-placement+contents decisions I can't verify without playing.

## Update 7 (~09:12 UTC) — teleport auto-dump (advances the #1 debt)

`e606c93c` — the trace recorder now **auto-dumps the same frame an unexplained teleport-class `CollisionCorrection` fires** (`DumpReason::TeleportAuto`). This closes the exact gap my lock-wall investigation found: the OOB auto-dump misses a snap to `y=-23` (within `OOB_MARGIN`), so the only prior trace was a manual dump seconds later holding just the stuck aftermath. Now a *live* reproduction of the wall-cling teleport will produce a directly-usable trace with the pre-teleport frames intact — turning the previously-blocked "capture a fresh trace" step into "just trigger the bug." Safe (debug-infra only, rides the existing OOB-auto-dump path), tested.

## Update 8 (~09:25 UTC) — data / state-machine / parser layer

The movement+combat core was already heavily tested; the **data, state-machine, and conversion layers were not**. Filled the high-value gaps there (now **1226 → 1277 lib tests** for the run):
- **Encounter state machine** (`cac5e798`) — phase queries, persist round-trip, and the two core transitions (defeat last mob → Cleared+unlock; defeat last of a wave → advance + WaveStarted). Drives every encounter/boss.
- **Loading-zone readiness + portal phase machine** (`bc6e2227`) — `is_ready` encodes the interact-binding rule (Door needs Interact; EdgeExit/Walk fire on contact); `tick_portal_phase` Off→Opening→On→Closing with mid-open reverse.
- **LDtk field parsers** (`ae3b6c56`) — `parse_points`, `parse_path_mode`, `parse_pickup_kind`, `parse_enemy_brain`/`parse_boss_brain` (prefix dispatch + Custom fallback). Content-critical; parser-drift discipline.
- **`compact_path_name`** slug normalization (`e6353cfb`), **item-use heal** (`382808c2`), **cut-rope heavy-object cycle** (`e3b12683`).

All green (1277 lib + all integration suites). Remaining 0-test modules are presentation, audio (feel-tuned), generic settings helpers, or AI-construction detail — lower value than the gaps filled.

## Update 9 (~09:31 UTC) — room QA tooling + CI guard

- **Room anomaly report** (`4103f177`) — `render_room_geometry -- report` scans every lowered `RoomSpec` for runtime-projection bugs the LDtk validator can't see: authored entity centers outside the room bounds, and player spawns embedded in a Solid block. Confirmed **0 anomalies across all 43 rooms**.
- **Promoted to a CI guard** (`76447f68`) — `tests/room_spatial_integrity.rs` fails the build if a future room regresses, rather than only on a manual report run.

Also checked (no gap): the `boss_profiles.ron` / `enemy_archetypes.ron` LazyLocks panic on parse error and are exercised by existing tests, so RON drift is already caught; a `.yarn` compile test is impractical (Bevy-plugin + feature-gated + async) and this session's `kernel.yarn` edit was text-only (safe).

## Update 10 (~09:47 UTC) — disk recovery + test completion

- Hit the documented ENOSPC-at-link blocker mid-run (the disk genuinely filled — `/home/joncrall/ambition-target` was 19G). Freed it the documented way: `rm -rf /home/joncrall/ambition-target/debug/{incremental,examples}` (~5.3G, both rebuild on demand). Lib + integration suites green again; will re-free if it refills. Also cleared ~710M of disposable `debug_traces/` captures (kept the one the debt log references).
- Added enemy-AI brain-dispatch coverage (`5dbd8632`) and rounded out the encounter state machine (`25218a87`). **Run total: 1226 → 1283 lib tests.**

The remaining untested code is genuinely not worth blind coverage: channel-coupled audio gain smoothing (auditory correctness), inline settings cycling, and presentation. Movement, combat, save, and `Health` were already well-tested before this run.

## Notes for Jon

- **#1 debt (wall-cling lock-wall teleport):** the one concrete next action is now trivial — **play the goblin_encounter wall-cling-then-jump; if it still teleports, the trace auto-dumps a usable capture** (no setup) and the documented budget-reject fix can land against it. If it does *not* recur, downgrade/close (the `body_is_side_contact` predicate may have already fixed it). I did NOT land the fix blind — the project's own criteria require a repro first, and a blind collision-sweep change risks regressing legitimate large repositions.
- **Test coverage is comprehensive now.** The movement/combat/save/boss-phase cores were already well-tested; this run filled the genuine gaps in the data/state-machine/parser/AI/encounter/inventory layers and the friendly-fire matrix (1226 → ~1290 lib tests). The remaining untested code is World-coupled (settings apply, audio channels), trivial accessors, or presentation — not worth blind coverage.
- The genuinely-undone TODO remainder is mostly **feel/design-blind** (movement feel, audio mixing, boss balance, sprite art) or **large refactors** — not safe to land blind in an autonomous run. The dialogue `camera_zoom`/`spawn_chest` stubs were left because they need camera-composition / chest-placement decisions I can't verify without playing.
- `tools/ecs_inventory.py` has an **uncommitted** working-tree change from a prior session (tree-sitter migration) — I left it untouched; it's yours to review/commit.

## Update 11 (~10:31 UTC) — LDtk spatial-query tooling for LLM authoring (real TODO item)

With the clean Rust test gaps exhausted, shifted to the **disk-free, verifiable** TODO item *"LDtk spatial-question tooling for LLM authoring"* (`docs/concepts/llm-spatial-authoring-discipline.md` had three of these listed as **Wanted**). Shipped all three read-only subcommands an LLM map author needs *before* placing a gate, each reusing the existing `collect` / `load_project` / IntGrid helpers:

- **`intgrid query --px X,Y --size W,H`** (`d52c6ec7`) — read-only mirror of `intgrid erase`: what IntGrid values (Solid / OneWay / Hazard …) occupy a px/size rect. "What collision is already here?" without opening the editor.
- **`entity measure --level X --identifier foo`** (`baaf60e0`) — an entity's size + center + nearest-Solid distance (px) in each of the four directions. Kills the in-head pixel arithmetic and shows how much open space surrounds a spawn. Verified on goblin_encounter (Switch: `down=16px`, on the floor) and scroll_lab (EnemySpawn: `left=edge`).
- **`gates audit --level X`** (`e1d545e2`) — the whole locking story of a level in one view: switches (action / target / prompt), runtime lock walls, encounter triggers, breakable platforms / pogo orbs. Surfaced (and correctly classified) that the `falling_sand_room` spout switches carry `action=ResetEncounter` + empty target but are **consumed by id** in `falling_sand.rs` — not orphans — so the audit prints `(none — consumed by id)` + prompt instead of a misleading empty arrow.

The 4th wanted subcommand, **`paths describe`**, I deliberately **deferred** (documented in the TODO + the discipline doc): reachability over a platformer collision grid (jump arcs, one-way platforms) is a non-trivial graph problem, and a rough heuristic could mislead an author worse than reading the geometry by hand.

- **Documented** (`133ca019`) — moved the three from "Wanted" to "exists" in the discipline doc with module pointers, folded them into the width-matching rule, updated `implemented_by` frontmatter.
- **Tested** (`0b46a6bb`) — six smoke tests over a tiny in-memory LDtk fixture (`tests/test_spatial_query_tools.py`), following the existing `test_intgrid_paint.py` pattern. Full `ambition_ldtk_tools` suite: **73 passed**.

This vein is **complete**: 3 of 4 subcommands shipped + tested + documented, the 4th deferred with a written reason. All changes are Python tooling / markdown — zero Rust rebuild, safe under the tight disk. `sandbox.ldtk` confirmed untouched (all three commands are read-only).

## Update 12 (~10:48 UTC) — music cue level report (TODO: music level/mixing tools) + a high-signal finding

Built the **offline half** of the *"Music level / mixing debug tools"* TODO (`8102a55c`, `e14b78bb`): `tools/ambition_music_renderer/level_report.py` — a sorted, fixed-precision, **diff-friendly** table over every runtime `<cue>/full.ogg`: duration, RMS dBFS, sample peak, **true peak (4x-oversampled dBTP)**, crest factor, target-RMS delta, optional integrated **LUFS** (only if `pyloudnorm` importable — degrades cleanly, isn't installed), plus a summary (RMS min/mean/max/spread + CLIP/LOUD/QUIET tallies). Reuses `transition_audit`'s reviewed db/rms/peak; reads natively (no mono→stereo doubling that would bias LUFS). **Self-tested** against a closed-form 1 kHz sine (`tests/test_level_report.py`, 4 passing — exact RMS/peak/crest, true-peak ≥ sample-peak, dB scaling, flag thresholds). Distinct from the existing `transition_audit.py` (two-file seam) and `audit_cue_balance.py` (sections within one cue) — this one is the **inter-cue catalog** view; all three are now documented side-by-side in `docs/recipes/generated-music-workflow.md`.

**HIGH-SIGNAL FINDING for Jon** — the first run is immediately actionable: **35 cues, 0 clipping, but a 12.3 dB RMS spread** (−28.8 dBFS `long_lofi_drift` … −16.5 dBFS `how_to_kill_a_mockingbird`), with **18 of 35 cues >3 dB under the −20 dBFS target**. The pattern is structured, not random: the **guild faction themes cluster quiet** (−24…−28: mages/physicists/ninja/thieves/luddites/pirates/fighters guilds) while **boss + action themes sit near target** (−18…−19: crooked_ascent_boss, military_iron_resolve, smirking_behemoth_boss, violin_boss). This is concrete, measured evidence for the **"goblin encounter music transition still sounds like a section swap"** active blocker and the general inter-cue level-jump complaint — when the game crossfades from a near-target action cue into a −27 dBFS guild theme (or back), the ~8–10 dB jump *is* the audible seam. A loudness-normalization pass (target the catalog at one RMS/LUFS) would close most of it; `level_report.py` is now the before/after measuring stick. (Left as a finding, not a blind fix: re-leveling generated audio is a by-ear mastering call, and re-rendering touches committed OGGs — out of scope for an autonomous run.)

The **in-engine dev HUD** half (live per-bank/per-slot gains + post-master dB while playing) stays deferred — World-coupled and best judged by ear in a play session.

## Update 13 (~10:55 UTC) — asset-pipeline doc clarity + stale-path fixes (two TODO items closed)

Two D2/D3 documentation TODOs, both done by **reading the actual scripts + git state** rather than inventing (so every claim is verified):

- **`[x]` Clarify image/sprite generation workflow** (`a7ce6cd5`) — expanded `docs/tools/generated-visual-tools.md` from a thin renderer list into the verified publish pipeline: `regen_assets.sh` orchestration, the sprite **target registry** (`ambition_sprite2d_renderer list`/`publish`) as the source of truth, where the adapter-rig YAML actually lives, output dirs, and a no-one-off-forks / fresh-clone rule.
- **`[x]` Clarify audio asset staging vs production** (`8187e8c1`) — rewrote the music recipe's staging section into an explicit three-tier table. The buried key fact: the **entire `crates/ambition_sandbox/assets/audio/` tree is gitignored** (`.gitignore:66`) — music OGGs *and* `sfx.bank` are regenerated, never committed, so a fresh clone has no audio until `regen_assets.sh`. The opposite of sprites (committed). That makes "accidentally committing scratch" impossible and the edit/publish loop trivial.

**Two more stale-path bugs found + fixed along the way** (same class as the audit_cue_balance one — left by the `tools/audio/music_renderer` → `tools/ambition_music_renderer` rename and the sprite-renderer package nesting):
- `audit_cue_balance.py` defaulted `root` to a non-existent dir → no-arg run died `missing root`. Now resolves relative to the script (`92cfe145`).
- `regen_sprites.sh`'s header pointed adapter configs at `tools/ambition_sprite2d_renderer/configs/` — empty/non-existent; the YAML really lives in the inner-package `…/ambition_sprite2d_renderer/configs/`. Fixed the comment + pointed at `list` as authoritative (`a7ce6cd5`). This one actively misled me mid-task, which is exactly the friction these TODOs target.

Doc health stayed green throughout (`check_doc_links.py`, `check_agent_kb.py`). Net: the asset-pipeline corner of the docs now matches reality, and three rename-rot paths are repaired.

## Update 14 (~11:06 UTC) — fixed the broken coverage tool, then used it for a second Rust test round

The `tools/audio` → `tools/ambition_music_renderer` and `ambition_engine` → `ambition_sandbox/src/engine_core` renames had left **dev tools silently broken**:
- `tools/test_coverage_report.sh` scanned the deleted `crates/ambition_engine/src` → its `engine`/`both` modes found zero files. Repointed at `src/engine_core/` + collapsed to the single crate (`d1de5513`).
- `tools/optimization_report/collect_optimization_report.py` ran `cargo check -p ambition_engine` (no such package) → removed; the sandbox check covers it.

With the coverage tool working again, used it to find the **largest zero-test pure-logic files** and added a second test round (build was warm + cheap, ~15-20s; disk held at ~4.4G):
- **settings/model.rs** (`ecc90c50`, 10 tests) — the menu's page graph + row tables + action routing + label formatting. `apply_action` is World-coupled, but every helper it delegates to is pure; pinned the display-mode cycle (3-loop, prev⁻¹=next), `page_nav_outcome`, dup-free Back-terminated row tables, `apply_cycle`/`nudge_delta` direction routing, and the format helpers.
- **world/ldtk_world/intgrid.rs** (`1a8d1559`, 8 tests) — `merge_intgrid_rects`, the two-pass collision-rect coalescer behind every LDtk body (past floor-friction / wall-grind regressions lived here). Pinned floor→1 block + offset, wall→1 stacked block, staircase stays per-row, different values don't merge, length-mismatch errors, and **the every-cell-covered-once invariant** (total rect area == nonzero cell count).
- **rl_sim/action.rs** (`bbb202b6`, 8 tests) — the `AgentAction → ControlFrame` RL/scripted-driver seam, including the regression guard that a *held* `move_y` forwards the axis but must not synthesize a down *edge* (the crouch→MorphBall bug).

**Lib tests 1287 → 1313** this round (run total now ~1226 → 1313). These are all genuinely load-bearing: the collision-merge invariant, the RL input seam, and the settings navigation graph are the kind of pure logic where a silent regression is expensive and a unit test is cheap. The coverage tool itself is now reliable for whoever picks up test work next.

## Update 15 (~11:14 UTC) — finished the pure-logic test sweep + a clipping guard

Continued the coverage-tool-driven sweep through the remaining high-value zero-test pure-logic files (build stayed warm/cheap, disk held ~4.3G):
- **encounter/loading.rs** (`b0249f28`) — goblin wave escalation + the documented spawn-past-the-trigger spatial invariant (visible after the camera zoom), without over-pinning coordinates.
- **pause_menu/model.rs** (`8cfc2e5a`) — the pause overlay's destructive-item set (gates the single-tap confirm) and the enter/pop page-stack back-navigation.
- **world/rooms/spawn.rs** (`e08c19b7`) — spawn placement safety: clamp-into-room + the spiral search that pushes a player out of a solid (room-transition embedding is a real bug class; authored spawns already have a CI guard, this pins the *runtime repair*).

Also turned the music level report into a regression gate: **`level_report.py --check`** (`0dd34ccb`) exits non-zero if any cue clips (true peak > -1 dBTP) — the one unambiguous defect (currently 0, so it passes today and catches a future hot render). The loudness spread stays report-only (mastering call). Documented in the music recipe's validation section.

**Test-round total: lib 1287 → 1324** (six modules: settings nav, IntGrid collision-merge, RL input seam, encounter waves, pause-menu stack, spawn safety). Full lib suite re-confirmed green (1324 passed, 0 failed). Files that turned out to be data-only (e.g. character_catalog/entry.rs) or World-coupled systems were skipped rather than force-tested. The remaining zero-test files are ECS systems (need App harnesses), presentation, or feel-tuned code — past the point where a blind unit test adds more than it costs.
