# Archived agent handoff

This handoff is historical. It was compacted because long autonomous prompts are poor retrieval targets after the repo state changes.

Current agents should use:

- `docs/README.md` for navigation;
- `docs/current/state.md` and `docs/current/next.md` for live status;
- `.agent/manifest.yaml` and generated indexes for file/symbol lookup;
- focused `docs/systems/` / `docs/recipes/` pages for the task at hand.

Use git history if the full original handoff is needed.
