# Player ECS phase 0 — compact archive

**Status:** complete and folded into the full player ECS migration.

Phase 0 established safety rails before the authority cut: inventory the old aggregate readers, add focused regression tests, and make the intended cluster shape visible. The current player shape is documented in `docs/current/state.md` and the code under `crates/ambition_sandbox/src/player/`.

Use git history for the full checklist.
