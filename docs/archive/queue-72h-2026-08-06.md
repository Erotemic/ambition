# The 72-hour run, 2026-08-06 → 2026-08-09

**This file is the SPINE and the ledger `scripts/goal_guard.py` reads.** One of
its checks is `! grep -q '[closed]' docs/planning/queue-72h-2026-08-06.md`, so an open
row blocks the stop. That is the whole mechanism: **if every row here is `✔`, the
next item has not been WRITTEN DOWN yet** — go read `docs/planning`, find it, add
it, and get back to work.

Jon, 2026-08-06, setting this run: *"I also want you to set a new 72 hour work
goal with our script, after you finish with this push, then get back to working
on everything in docs/planning."*

Rows carry their reasoning and their evidence. A row is a CLAIM ABOUT THE CODE
and goes stale like any other: **re-measure before working it.**

⛔ **THE COMMONEST STALENESS IS A ON WORK THAT ALREADY LANDED**, and the PA
campaign that closes as this one opens hit it TWICE inside its own handoff — the
pause menu already declared `PAUSE_CONTEXT`, and the touch hit-test already
resolved through `touch_action_live`. Both were written as open by the session
that had just finished reading that code. ⭐ **the cheap tell: a row whose
SIBLING claims it landed.** Before working any `[closed]`, grep for the thing it says is
missing.

The three previous ledgers — `queue-72h-2026-08-03.md` (153 marks),
`queue-72h-2026-07-31.md` (106) and `queue-24h-2026-07-26.md` (50) — were
**DELETED on 2026-08-07**, harvested first; read them at `9a66996ce~1`. Lane E
records the audit: fourteen of fifteen rows verified already closed, every
referenced decision already a heading in `awaiting-maintainer-decision.md`, and
exactly two live rows recovered out of 1.1 MB. ⛔ **the lesson is about the MARK,
not those files**: `[closed]` was used for "a point I am making" as well as "a thing to
do", so a count of marks was never a count of work.

---

## ⇥ START HERE

```bash
python3 scripts/goal_guard.py --status          # confirm the run is armed
git status --short                              # expect only the two submodules
python3 scripts/check_absence_contracts.py --check   # 25/25, ~11s
```

**Standing state at the open of this run**, so nothing is re-derived:

- `tools/ambition_sprite2d_renderer` and `tools/ambition_music_renderer` are
  DIRTY with Jon's scratch work. ⛔ hands off, and `git add` by path only.
- ~~ONE known red~~ ✔ **FIXED 2026-08-06, and the diagnosis above was half
  right in the way that costs the most.** `app_it::rendered_identities_are_
  registered` did fail on `["special_patent_clerk"]`, and the single quotes in
  `patent_clerk.py` were the cause — but that was recorded as a fact about ONE
  ROW when it was a fact about the SCANNER. The parser matched `"character_id"`
  literally, so four targets were invisible (`patent_clerk`, `carl_stargan`,
  `bear_mauler`, `robot_heavy`), and it stopped at the first id per file, so
  second ids in family targets were too. ⛔ **do not "fix" a red by deleting the
  waiver it names**: that turns the check green while the blindness stays. The
  scan now reads all three spellings (`"k":`, `'k':`, `k=`) and every id per
  file, which surfaced `npc_super_sanic` (waived — the demo registers it) and
  `npc_robot_heavy` (a real orphan, now a row in `awaiting-maintainer-decision.md`).
- The gate is `cargo check -p ambition_app` (~25s), never per-crate.
- Feature-gated suites lie when run bare: `-p ambition_input` needs
  `--features input` (99 vs 48), `-p ambition_touch_input` needs `--all-features`
  (44 vs 4).
- ⚠ a new dependency edge fails the contracts job until
  `fixtures/minimal_game/Cargo.lock` is regenerated (`cargo tree --offline` in
  that directory) and committed WITH the manifest change.

---

## Lane A — what Jon picked, and what his rulings turned into work

The PA campaign (PA1–PA4) landed 2026-08-06; its record is
[`pa-campaign-handoff-2026-08-06.md`](pa-campaign-handoff-2026-08-06.md). Every
decision it was parked on is now closed in
[`maintainer-decisions.md`](maintainer-decisions.md), and three of the closures
are work rather than a stop.

✔ **A1 — PA3's remaining halves. Jon's explicit pick for next work.** All three
bullets discharged; the `MenuFrameConsume` decision below was TAKEN on
2026-08-07 as a nesting rather than the fold it was framed as.
The pointer half landed (`f8c69d7ea`): one `PressArm<T>`, every bevy_ui menu and
the tab bar activating on release with drag-cancel. What is left is the other two
thirds of "the standard UI module":
- ✔ **ONE semantic activation event** — landed `91f8dfa10` + `43301bcc0`. Three
  routes into `dispatch_menu_action` became one message; `system_focus_nav` shed
  five dispatch parameters and the game mode; the cube gained the consumer it
  never had.
- ✔ **Directional repeat / focus / wrap — NOT WORK, and this was checked rather
  than assumed.** ⛔ the row was written from the spec, and the spec was stale:
  **repeat** already lives behind `ambition_input::MenuInputState` (settings-tuned
  initial delay + interval, with its own tests); **windowing and scroll-into-view**
  already live in `ambition_ui_nav::list` (`visible_window_start`,
  `scroll_into_view`, `visible_row_index`, `ListCursor`); **wrap** is
  `step_system_row`, a six-line pure function with a wrap-or-clamp flag that is
  ALREADY shared by both backends through `system_focus_nav` — one
  implementation, not two. Moving it into `ambition_ui_nav` would need a
  clamping-cursor API that does not exist and would make the call site longer.
  What is genuinely per-backend is the focus GEOMETRY — `move_items_cursor`
  walks a 2D item grid, the cube traverses face edge-buttons — and those have one
  consumer each, so unifying them is generalizing without a consumer. ⚠
  `MenuControlFrame` consumers keep their nav-axis semantics either way (locked
  decision 2 in `engine/character-actions.md`).
- ✔ **the `MenuFrameCutsceneSkip` vs `MenuNavConsume` fragmentation — TAKEN,
  2026-08-07, and the row's binary was a false one.** It framed the choice as
  fold-or-don't, and folding is what carried the cost: `MenuNavConsume` is pinned
  `.after` by the menu-backend switch, so a folded set would make that switch wait
  on cutscene skip as well. ⭐ **nesting has no such cost.** `MenuFrameConsume` is
  now an UMBRELLA over both; a frame writer says `.before(MenuFrameConsume)` once
  and the switch still pins `.after(MenuNavConsume)` alone. `ambition_touch_input`
  went from naming both readers to naming neither — and a writer that enumerates
  its readers is one that silently stops covering them the day a third is added,
  which is the actual defect under the complaint.

  ⚠ **the prerequisite nobody had checked, and it could have made the whole thing
  decorative.** A Bevy set node belongs to ONE schedule, so an umbrella over
  members that live elsewhere constrains nothing — and both configuration sites
  already warn that this app really does produce empty nodes (*"under
  `Fixed60Hz`/`Ggrs` this creates an empty node here and constrains nothing"*).
  So "is the set present" is the wrong question and "does it have MEMBERS here"
  is the right one. Measured before writing the umbrella, by asking the schedule
  graph rather than reading the plugins: both readers populate `Update` and only
  `Update`. `the_menu_frame_reader_sets_are_co_scheduled` keeps that true, and
  would also have reported the pre-existing possibility that one of the touch
  adapter's two `.before`s was already vacuous.

  ⭐ and the mechanism itself is PROVEN, not assumed: nesting propagating
  `.before` to members is a claim about Bevy, so
  `a_before_pinned_against_an_umbrella_orders_every_member` exercises it on a
  three-system app — verified by flipping the pin to `.after`, which reports
  `["reader_two", "reader_one", "writer"]`. Had that claim been false, swapping
  two pins for one would have silently unordered every gesture write against the
  menu readers, and nothing in the repo would have said so.
D3's sequencing puts this BEFORE the P5 rebind-capture UI, so the rebind rows are
built on the module rather than rebuilt twice. Jon confirmed that order by
picking this row.

**⇢ A1's activation half is SCOPED (2026-08-06). Do not re-derive this.**
`dispatch_menu_action` is ALREADY one dispatcher — the duplication is one step
earlier, at "which action did the user just choose", and there are THREE routes
into it where there should be one message:

1. **pointer** — `MenuActionActivated<MenuPageAction>` (published by the
   renderer's bridge) → `grid_menu_action_activated` → dispatch. ✔ already an
   event; this is the shape the other two should take.
2. **keyboard/controller, Items page** — `grid_menu_nav` resolves
   `owned_item_action(idx)` from the cursor and dispatches INLINE
   (`grid_backend.rs` ~564). The cube has its counterpart.
3. **keyboard/controller, System page** — `system_focus_nav`, which is already
   SHARED by both backends (one function, so the two frontends do not disagree)
   but dispatches internally.

⭐ so the edit is: routes 2 and 3 PUBLISH `MenuActionActivated<Action>` instead of
dispatching, and the existing consumer becomes the only dispatcher. That also
shrinks `system_focus_nav`'s ~18-parameter signature by every param it holds
purely to dispatch (`owned`, `commands`, `players`, `mana_q`, `heals`, and the
dispatch half of `sfx`), which is the real reason both backends bundle
`SystemParam` structs to stay under Bevy's 16-param ceiling.

⚠ **NOT every submit is an activation.** `grid_backend.rs:528` — SELECT on the
tab bar drops focus into the body — is structural navigation and must stay in
nav. The distinction is worth encoding rather than discovering per-branch.

⚠ **ordering**: `grid_menu_nav` runs in `MenuNavConsume`; the activation
consumers run `.after(BevyUiMenuInteractionSet)`. A message published by nav must
be consumed the SAME frame, per the `InputSet` contract ("an edge produced this
frame is consumed this frame, never at worst a frame late") — so check the
relative order of those two sets before assuming it holds.

✔ **A2 — D5: any seat pauses, and that seat drives the menu.** Landed
`860e7c31e`. `ShellPauseMenu` gained an owner; the first seat in SLOT ORDER to
press Start wins (deterministic under a simultaneous press), and only that seat's
frame is read while it is open. Releasing the owner lives inside `close()`,
because four sites fold the menu shut and a stranded owner would keep the pause
button away from everyone. The confirmed detail below was right and worth having
checked.
⚠ today's behaviour is NOT what the handoff first claimed. The pause menu reads
`MenuControlFrame`, which `populate_menu_control_frame_from_actions` fills from
the PRIMARY seat ALONE — so player two can neither open the pause menu nor move
its cursor. `SeatMenuFrames` is the per-seat channel and the smash select screen
already reads it. The world still stops for everyone; the one piece of new state
is which seat OWNS the open menu, so `drive_shell_pause_menu` reads that seat's
frame instead of the global one.

✔ **A3 — D2: input FILTERING is keyed to the pad, bindings stay machine-wide.**
Landed `6c7a9a7d7`. `ControlFilters` is the calibration half of `ControlSettings`,
`Copy` so the per-seat rebuild costs nothing. Three lines drawn deliberately:
preferences (dash mode, inverted aim) stay machine-wide; an EXPLICIT
`controller_profile` outranks detection, because a settings screen the game
rewrites is not a settings screen; and ⛔ `Xbox360`'s table is unreachable from
detection, since a vendor id cannot tell a worn 360 pad from a new Series one and
guessing would widen the deadzone on good hardware. The original text follows.
`populate_secondary_slot_controls` and `populate_control_frame_from_actions` both
pass the one global `&user_settings.controls`, so player two's drifty 360 pad
runs on player one's DualSense-tuned deadzone. `ControllerProfileId` already
carries the per-pad calibration (360 = wider deadzones + worn-trigger hysteresis;
PlayStation = tighter) — it is the CHOICE that is global, not the table.
⛔ this row CLOSES PA1's "per-seat presets" item by narrowing. Do not build
per-seat presets or per-seat binding overrides; Jon ruled those stay
machine-wide.

✔ **A4 — D4: dialogue claims the TALKER, not the world.** Landed `b2ef17bd5`.
`allows_gameplay` keeps meaning "may input route"; the new `stops_the_world`
means "does the clock freeze", and both routers plus `gameplay_suspended` ask the
second. `DialogueStopsTheWorld` is the per-experience opt-in Jon's 2026-08-03
ruling required. The pinning test is re-aimed and grew two halves it lacked — a
PAUSE still freezes an untouched seat, and the opt-in restores the modal beat —
without which this could have deleted the pause and the suite would have agreed.
The original text follows.

**A4's original wording, kept for its detail:**
`GameMode::Dialogue` leaves the suspend set (`apply_suspended_time_scale_system`
under the `gameplay_suspended` run condition, `player_schedule.rs`), and
`DIALOGUE_CONTEXT` — already declared by `declare_in_session_input_contexts` —
becomes the whole gate. ⚠ `dialogue_still_stops_the_world_as_well_as_claiming_
the_input` pins the OLD behaviour and must be re-aimed, not deleted. The
per-experience opt-in to stop the world STAYS (Jon 2026-08-03: both must be
expressible). ⚠ `RoomTransition` and `Cutscene` are explicitly not the same
question.

**A5 — dialogue CONTINUITY, which is what A4 costs.** Design and Jon's verbatim
words: [`engine/dialogue-continuity.md`](engine/dialogue-continuity.md). Once the
world keeps running, a conversation has to keep being POSSIBLE:
- it BREAKS on damage and on the participants separating;
- it HOLDS its participants in station if they are capable of it;
- a break BARKS, because a conversation that silently vanishes reads as a bug.
⭐ symmetric, from the actors' side — *"both characters should hover"*. Any
implementation that asks whether the PLAYER can still talk has already got it
wrong. The five open sub-questions are in the design doc; answer them explicitly
rather than defaulting.

**Progress (2026-08-06):**
- ✔ **the participants seam** — `756795024`. `DialogState::participants()` yields
  BOTH bodies; a conversation used to hold only the NPC as an entity and the
  other side as a character-id STRING, so no continuity question could be asked
  at all. ⚠ landmine recorded there rather than renamed: `speaker_entity` is the
  NPC while the interaction site's `speaker_id` is the body that walked UP. The
  two spellings of "speaker" have always meant opposite things; the fix is a
  separate rename with a `<<challenge>>` consumer to move.
- ✔ **the "can this body hold station" authority** — `454d7e455`, and it needed
  NO new flag. The row said it was missing because `can_fly` lives only in the
  SMASH brain config; that was the wrong place to look. `AbilitySet::fly` is the
  actor-level capability, and holding station is what being grounded OR being
  able to fly already means, so `can_hold_station` is DERIVED
  (`ambition_platformer2d_core::abilities`). ⚠ still open from the design doc's
  fourth sub-question: whether the HOLD is applied by exercising a hover the body
  already has or by suppressing its gravity — prefer the former, because a body
  held still by a lie shows up the first time something reads its velocity.
- ✔ **the break rules** — `7387d5626`. `DialogueBreak::evaluate` is the pure
  rule in `ambition_dialog`; `break_dialogue_on_hit_or_separation` gathers the
  facts where `BodyCombat` and AABBs live. Knockback (the recoil/hitstun lock),
  not damage — a body that flashes damage without being MOVED keeps its
  conversation, and the test pins that. Separation borrows the interaction's own
  `strict_intersects` reach rather than authoring a second range.
- ⛔ **A5's BREAK-ON-DAMAGE RULE CANNOT FIRE IN PRODUCTION (found 2026-08-07).**
  The most consequential instance of the limit below, and it invalidates a
  shipped feature rather than merely leaving a system off:
  * `break_dialogue_on_hit_or_separation` asks `BodyCombat::recoil_lock_timer` and
    `hitstun_timer` — knockback, not damage, deliberately.
  * both are written by the damage pipeline, whose entry
    `apply_feature_hit_events` is `.run_if(gameplay_allowed)`
    (`combat_schedule.rs:283`).
  * `gameplay_allowed` is `allows_gameplay()` is `matches!(self, Self::Playing)`,
    and `interact.rs` sets `GameMode::Dialogue` when a conversation opens.
  ⇒ **during a conversation no hit is ever applied, so neither timer is ever set,
  so `any_struck` is permanently false.** The rule is implemented, unit-tested
  with hand-set timers, and unreachable in the game.
  ⭐ **the SEPARATION half is fine** — bodies keep integrating because A4 moved the
  clock onto `stops_the_world` — so the feature is half-live, which is exactly the
  shape that survives review: its tests pass and one of its two triggers is dead.
  ⛔⛔ **AND "just run the damage pipeline during dialogue" IS WRONG — the gate was
  put there ON PURPOSE, for another of Jon's reports.** Its own comment at the
  site: *"Otherwise a body pinned overlapping an actor while a conversation runs
  keeps registering hits (strikes, FX) on it — the dialog half of the 'continuous
  hit' report."* ⚠ **and R1's HOLD makes that worse, not better**:
  `project_conversation_hold` pins the talker in place, overlapping the initiator,
  which is exactly the geometry that produced the continuous-hit bug.
  ⭐ **so the two halves of Jon's own continuity design are in TENSION, and the
  current code resolved it silently by killing one of them.** "A conversation
  breaks when somebody is knocked about" needs hits during dialogue; "a held body
  must not register a hit every tick" is why hits are off. Neither requirement is
  wrong and they cannot both be met by moving one run condition.
  **the decision, restated honestly.** The fork is not "gate or ungate". It is:
  * **(a)** ungate the damage pipeline for dialogue AND give contact damage the
    per-target dedup / invulnerability window that stops a held overlap from
    re-firing — the continuous-hit bug's real fix rather than its avoidance;
  * **(b)** leave hits off and give the break rule a different signal for "you
    were knocked about" that survives a gated pipeline;
  * **(c)** accept that break-on-damage is separation-only in practice, and DELETE
    the struck branch so the code stops claiming a trigger it does not have.
  ⚠ **(a) may be far cheaper than it sounds, and this is worth checking before
  costing it.** Two dedup mechanisms already exist: the player *"gates re-hits on
  the same `BodyCombat.damage_invuln_timer`"* (`damage/actor_hit.rs:109`), and a
  slash already dedups per target per swing (`record_dedup` / `hit_targets` in
  `damage/mod.rs`). So "a held overlap damages the player every tick" is NOT what
  was happening — the gate's own comment says *"registering hits (strikes, FX) on
  IT"*, i.e. attacker-side, on the actor.
  ✔ **CONTACT damage is already i-frame bounded — verified to a definition, not
  inferred.** `apply_actor_contact_damage` gates each hit on
  `crate::combat::util::body_vulnerable(...)`, which ANDs in
  `BodyCombat::vulnerable()`, which is literally `self.damage_invuln_timer <= 0.0`
  (`ambition_characters/src/actor/body.rs:284`). Three named functions ending in a
  comparison, so a held overlap CANNOT damage the player every tick — it fires on
  the i-frame cadence like any other contact. ⭐ that removes the expensive half of
  option (a): no dedup system needs building for contact.
  ⛔ **what the gate's comment actually names is attacker-side — "registering hits
  (strikes, FX) on IT", the ACTOR — and which path re-fired there is NOT
  established. Reading further only produces confident guesses** — twice today that shape has been wrong. What
  settles it is a REPRODUCTION: hold a conversation with the bodies overlapping,
  ungate the pipeline, and watch what repeats. That is one test, and it turns a
  three-way design fork into a measurement.
  ⛔ **do not sweep all 37** — the row below says so and it is still right.
- **A4's reach has a LIMIT, measured 2026-08-06 — record before trusting the
  commit message.** A4 moved the CLOCK (`gameplay_suspended`) and the two input
  routers off `allows_gameplay`. It did NOT move the **37 other `gameplay_allowed`
  run conditions**, so during a conversation those systems are still off: pickup
  magnetise/collect, the deferred-challenge grace (`tick_pending_challenges`,
  whose comment explicitly wants "only in `Playing`, after the dialog box
  closes"), room transitions, and a portal maintenance set.
  ⚠ **this is not a regression** — every one of them was already off during
  dialogue, twice over. What changed is the REASON: it used to be "the world is
  stopped", and now it is "a run condition happens to say `Playing` only". Some
  of those are RIGHT to stay off (the challenge grace names dialogue in its own
  comment); some are probably just inherited. ⛔ do not sweep them: decide each,
  or leave them and say so. The wrong move is another blanket predicate swap.
- ✔ **the break bark** — `6849a7a8a`, and reading the existing vocabulary changed
  its shape twice. ⛔ the `bark_pool` STRINGS this row proposed were a second
  table: `BarkSituation` has always been the bark vocabulary, with per-situation
  pools on the catalog row. Deleted. ⛔ and the STRUCK break wants no bark at all
  — `npc_hit_bark_line` already fires on every strike and falls back to a generic
  line, so a second bubble for one event is worse than none. Only walking out was
  a new occasion, so `BarkSituation::ConversationCut` is the one variant, with a
  `serde(default)` pool. ⚠ **no lines authored, and that is FINISHED rather than
  missing** — an empty pool is silence exactly as `Idle`/`Hall` document, and what
  a character says about being left is Jon's voice to write.
- ✔ **the HOLD** — `d6d20ff8f`. ⭐ the measurement this row DEMANDED came back the
  other way: **NPC brains DO run during a conversation.** `tick_actor_brains`
  sits in `WorldPrep` with no `gameplay_allowed` condition, so the only thing
  holding an NPC still mid-sentence was the clock A4 removed. One rule — a
  participant's movement INTENT goes to zero — with two mechanisms, because the
  two bodies are driven from different places: `DIALOGUE_CONTEXT` already
  neutralises the talker, `ScriptedControl` blanks the NPC's brain. That is not
  player-centrism and the test says so, asserting the talker is NOT marked.
  `HeldByConversation` is the claimant `ScriptedControl`'s own doc asks for; the
  release answers from the dialogue state rather than mirroring the insert,
  because a conversation ends in more ways than the break rule and a stranded
  marker is a permanently frozen NPC.

✔ **A6 — the P5 rebind-capture UI.** Engine half `893ca4a2f` + `11636a458`;
screen and capture loop `2d1405b1b`. A player can set an override without editing
a file, and it reaches behaviour, glyphs and touch in one frame.
⛔ **the trap, recorded because ordering LOOKED like the fix:** the frame that
ARMS a row must not capture it — selecting is itself a key press and is still in
`get_just_pressed` when the capture runs later that frame. Running the capture
after the dispatch does NOT solve it; a just-pressed set is a fact about the
frame, not about where in it you read. Without `RebindCapture::settle`,
confirming a rebind row binds that action to the confirm key and the whole list
ends up on Enter.
⭐ the screen is ATTACHED (`SystemMenuModel::with_rebind`), not built and not a
fourth `build` parameter: a composition with no input stack has no bindings and
should show no row rather than an empty screen, so presence FOLLOWS capability.
The parity guard pins both sides.

**A6's original text:** `893ca4a2f` + `11636a458` put the parts with policy in them in
`ambition_input::rebind`, pure and tested: `capture` (first bindable press wins;
⛔ `PhysicalControl::Other` REFUSED, because that arm exists so the projection
can be total and an override must be constructible), `also_bound_to` (a
collision is REPORTED, not refused — Escape is deliberately both `Start` and
`MenuBack`, so refusing collisions would forbid a shape the game ships), and
`pressed_controls_this_frame` (⛔ reads the keyboard RAW, since routing through
the `InputMap` would make the rebindable set exactly the already-bound set;
sorted, because `ButtonInput` is hash-ordered and a rebind landing on a different
key each run is worse than one that refuses).

⚠ **the screen is NOT a `SettingsOptionId` per action** — that vocabulary is one
option per settings FIELD, and there are 20+ rebindable actions. It is a list
screen: rows from `SeatBindings::for_seat(seat).all()` (already canonically
ordered), a label per row from `PhysicalControl::label_for(style)` (⛔ **"already
vendor-correct after PA4" is WRONG — see A7/R4.** It re-spells whatever control is
FIRST, so a keyboard `Z` bound ahead of the pad binding stays `Z` under a
DualSense. Build this row on `ActionBindings::control_for(action, device)` once
R4 lands), an armed row, and `ControlSettings::set_binding_
override` on capture. Everything downstream of that call already works in one
frame — behaviour, glyphs and the touch overlay all follow the recipe rebuild.
⚠ remember `curated_options` if any row DOES join the settings IR; it is the
touchpoint that gets missed.

**A6's original text:** the persisted override model landed
(`9d2386b2e`) and NOTHING in the game can set one, so the whole feature is
reachable only by hand-editing a settings file. Built ON A1's module, per D3.

✔ **A7 — the GPT 5.6 review through `c32e690`. JON'S CURRENT PICK — DISCHARGED.**
Its own ledger carries `R1 ✔ R2 ✔ R3 ✔ R4 ✔ R5 ✔` and **zero** open marks
(counted 2026-08-07), and the three deliberately-not-work findings stayed not
work: the Patent Clerk regen is still blocked inside the hands-off submodule,
generic feature-volume presentation is still deferred, and the Hall syntax
scanner gained no parsing cases.
⚠ **this row was `[closed]` while its own ledger said done** — the staleness this file's
header names, in the row the run was opened on.

**A7's original text** — the GPT 5.6 review through `c32e690`. JON'S CURRENT PICK** (2026-08-06,
handed over with a second round of reviewer feedback on my plan; another agent
holds `main`, so this is worked on `review-fixes-2026-08-06` in the write-ahead
worktree). Verification, the reviewer's adjustments recorded verbatim, and the
sequenced rows are in
[`review-gpt56-through-c32e690.md`](review-gpt56-through-c32e690.md).

Six of the eight findings were confirmed against the tree before planning; the
`file:line` evidence is in the ledger. In the reviewer's order:

- ✔ **R1 — the conversation authority.** A5 shipped a state machine split across
  two authorities: `ScriptedControl` is rollback-registered, `HeldByConversation`
  and `DialogState` are not, and both hold systems run in `sim_schedule()`. A
  rewind can leave an NPC "held" with no control override, and `dialogue.close()`
  on a resimulated tick is an unrewindable write. Fix is a rollback-owned
  `ActiveConversation`; the markers become projections deterministically repaired
  from it. ⚠ this is A5's defect, not new work beside it.
- ✔ **R2 — dialogue claims every seat.** `declare_in_session_input_contexts`
  claims `DIALOGUE_CONTEXT` on all participants; its own doc says that was staged
  on purpose and names this function as where it changes. Owner becomes EXPLICIT
  (no implicit "claim everyone"), derived from the initiator's
  `Brain::Player(slot)`.
- ✔ **R3 — per-seat menu calibration.** A3 gave gameplay a per-pad filter and left
  `decode_menu_frame` on the machine-wide deadzone, so a couch seat's menu and its
  gameplay disagree about its own stick.
- ✔ **R4 — device-aware prompts.** `label_for` picks the FIRST binding; its
  `GamepadStyle` parameter cannot even express "this seat is on a keyboard". One
  `control_for(action, device)` primitive shared by labels and glyphs.
- ✔ **R5 — the `ParticipantId` ↔ `PlayerSlot` mapping seam.** No rename (deferred
  by the reviewer), just one place holding the correspondence so the future split
  is localized.

⚠ **`--features visible` and the plain feature set THRASH one target dir.**
Alternating `-p ambition_demo_mary_o_app --features visible` with
`-p ambition_app` forces a full rebuild each way — feature unification changes
the fingerprint of every shared crate. Two runs that each take three minutes warm
take twenty when interleaved. Batch by feature set, not by question.

**R1 left three rows behind it** (detail and evidence in the ledger; they are
here so the guard tracks them rather than the ledger swallowing them):

- ✔ **the narrative→sim edge was a non-rewound read.** The sim POLLED
  `DialogState::active()` to learn the Yarn runner had finished, so a resimulated
  tick read the live runner rather than the runner as it was, and a rewind to
  before a conversation ended would end it again immediately. The end is an
  EVENT now — `ConversationEnded`, written once by presentation, consumed by the
  sim, `clear_message_on_rollback` so a rewound end is not re-consumed. **A tick
  told nothing changes nothing**, which is what a replayed tick must do. ⚠ the
  message NAMES its conversation, or a late end closes the conversation that
  replaced it. ⚠ **this does NOT make the Yarn runner deterministic or rewound**:
  that means the advance presses replayed like device frames and the runner
  ticking inside the sim, which is much larger work and is written down rather
  than claimed.
- ✔ **`DialogState`'s entity API was dead weight.** `set_speaker_entity`,
  `set_initiator_entity` and `participants()` had no production caller left once
  the Yarn commands moved; deleted in `7bced88dc`, which is the "one authority"
  payoff rather than a tidy-up.
- ✔ **`dialog/yarn_bindings.rs` asked `DialogState::speaker_entity()`** in
  `<<challenge>>`, `<<use_brain>>` and `<<restore_brain>>` — and `<<challenge>>`
  starts a FIGHT, so a simulation effect was keyed off view state. Repointed at
  `ActiveConversation::talker()` in `259560298`.

✔ **A7's app-level verification, and the reds it SEPARATED rather than assumed.**
`app_it` at `7bced88dc`: 299 passed, 9 failed. ⭐ exactly ONE was mine —
`rollback_schema_baseline`, because the rollback schema has TWO baselines and R1
updated only the JSON one the contracts job reads (fixed `765cab597`).

⛔ **the other eight were MEASURED against the base commit, not reasoned about.**
Two of them plausibly touched this campaign — a GGRS checksum mismatch
(`rollback_match_activation` ×2; R1 adds a rollback-registered resource) and
`smash_in_the_host::a_keyboard_player_and_a_pad_player_drive_different_fighters`
(input routing; R2/R3 edit input systems) — so "the other agent's area" was a
hypothesis, not a finding. Detaching the worktree to `b24ccb6b2` and running the
same six test files produced **the identical 8 failures**: 8 failed at base, 8
failed on the branch, same names. None are this campaign's.

⚠ **they are still RED, and somebody owns them.** Six share one symptom — melee
not landing (*"PCA should throw repeated melee swings (got 0)"*, *"the lethal hit
did not reach seat 0"*, *"the 3-HP player should have died"*) — which is the area
the parallel agent was probing in `main` while this ran. Carried as a row for
whoever merges, not as a claim about who broke it.

⛔ **three findings are deliberately NOT work**: Patent Clerk regeneration is
blocked inside the hands-off renderer submodule (diagnosis recorded); generic
feature-volume presentation is deferred by the review's own priority; the Hall
syntax scanner is FROZEN — no new parsing cases, its replacement needs a manifest
from that same submodule.

---

## Lane B — Jon's own file

✔ **A8 — two experiences each claimed to be the SOLE publisher of both match
globals.** Not a planned row: the absence-contract check was red at
`a-second-writer-of-a-match-global-must-answer-ownership`, and reading WHY it was
red found a different and worse defect than the one it reported.

⭐ **the red itself was stale, and stale in the most misleading possible way.**
The contract excludes the one legitimate `ActiveMatch` publisher by PATH, naming
`seating.rs`. `ef28aea6f` split preparation out of seating and the publisher moved
to `prepared_match.rs`, so the contract went red pointing at the LEGITIMATE
writer — which is exactly what a genuine second writer looks like. Verified a move
rather than an addition by reading the commit: the `insert_resource` line is a `-`
in `seating.rs`'s diff, and no non-excluded writer remains. The exclusion now
names `prepared_match.rs`, and it carries the DISCRIMINATING QUESTION for the next
reader — *grep the path this list already names; if the old site no longer writes,
the publisher moved.* Without that, the cheapest response to this red is to add
the new path, which is also the response that would hide a real second writer.

⛔ **and the real defect was in the direction the contract cannot see.** Its regex
matches `commands.insert_resource` / `remove_resource` only. A shell experience
scope deletes a global by DECLARING `releasing::<R>()`, and both Versus
(`versus.rs:987`) and Smash (`ambition_demo_smash/src/lib.rs:1231`) declared it on
`ActiveMatch` **and** on `PreparedMatch` — in a host that registers both
(`shell_host.rs:165`). Whichever experience left its routes first deleted the
other's live match and the plan it was built from.

⭐⭐ **the shape of the miss is the interesting part.** `releasing`'s own doc says
*"a resource this provider ALONE publishes"*, and the scope module's headline rule
says *"release is OWNER-SCOPED, never 'remove the resource' … a scope that removed
it unconditionally would be one game deleting another's match."* Both files
CORRECTLY apply that rule to `MatchParticipantRoster` — with a comment saying so —
and then break it on the next two lines. Each declaration reads as right in
isolation; the contradiction exists only across two files nothing compared. This
is the roster's `published_by` lesson for the FOURTH time.

**The fix, and why it is not the roster's fix.** `ActiveMatch` is rollback state
(`resource.active_match`) whose doc says it holds *"plain data with no identity in
it"* — stamping a shell experience id into it would put frontend identity on the
rollback wire to answer a teardown question, and bump the wire format. So:
`PreparedMatch` (NOT rollback state) inherits `published_by` from the roster it was
prepared from, costing no new authoring anywhere, and the activation is released on
the word of the plan it came from — a new `ExperienceScopeBuilder::releasing_witnessed`
that still reports `ReleaseKind::OwnedRemoval`, because that is truthfully what it
is. ⚠ givebacks run in declaration order, so a witness released first would answer
"not mine" forever — a release that silently STOPS WORKING. That ordering is
invisible at the call site, so `releasing_witnessed` panics at declaration if the
witness is already released in the same scope.

**The probe, written first and verified red** (`app_it::experience_scope_ownership`):
composes the shipped host and asserts no resource is `SoleRemoval`-released by two
owners. It named both resources and both owners — `smash, ambition_versus` — before
any fix existed. It ships with a FLOOR test, because an invariant over a registry
is worth what the registry contains: a composition registering no scopes would
satisfy it by having nothing to check. Plus a guard PAIR in `versus.rs` against the
real declaration and the real release system — mine is released, a stranger's
survives — because deleting the release passes the second alone and leaving it
unconditional passes the first alone.

⛔⛔ **AND THE POISON CAUGHT MY OWN INVARIANT BEING TOO WEAK — twice over.**
Both artifacts were wrong in their first form and both were GREEN when wrong:

- the FLOOR test asserted *"more than one experience declares a sole-removal"*.
  That is not a floor, it is the BUG: the fix converts smash's two claims to
  owner-scoped, so the assertion demanded the defect stay and went red on the
  repair. Replaced with what actually has to hold — both match experiences are in
  the composed registry with givebacks declared, so the day smash stops being
  listed in `shell_host.rs` the main assertion cannot go green by not looking.
- the MAIN rule was *"no two experiences claim SOLE ownership of one resource"*.
  Poisoning versus back to `releasing::<ActiveMatch>()` left smash owner-scoped,
  so there was exactly ONE sole claim, nothing was contested — and the assertion
  stayed green while the unit test next door failed with `left: None, right:
  Some(4)`, versus deleting smash's live match. ⭐ **the rule that holds is
  weaker-sounding and much stronger: a resource that MORE THAN ONE experience
  removes may not be removed by type by any of them.** A second giveback is
  itself the evidence of a second publisher — nobody writes teardown for state
  they never create — so one `SoleRemoval` among them is a defect however polite
  the others are. Re-verified red under the same poison before restoring.

⚠ the tell for both: each first draft was written to describe the defect I had
just found, rather than the property that must hold. The poison is what separates
those, and neither error was visible any other way.

⭐ the general lesson, and the reason the test asks the composed registry rather
than the source: **`ReleaseKind` makes an ownership CLAIM into recorded data.**
Before it, "this provider alone publishes X" was a claim each call site made and
nothing could compare.

✔ **A9 — a red test was asking for the rule Jon rejected by name.** Found while
verifying A8: `ambition_demo_smash` was 49/1, and the failure was not mine.
`two_cpus_and_nobody_playing_is_not_a_match` asserted that a CPU-vs-CPU match must
not start, which is precisely what Jon asked for — *"it does not let me make a CPU
vs CPU match, and it is very important that that is expressible and easy to do."*
`ready()`'s `humans_decided() >= 1` clause was removed on that instruction and the
test pinning it was left standing.

⛔ **making the red test pass would have re-broken a thing Jon named.** ⭐ the tell
costs one grep: the test demanded a blocker string — *"At least one slot must be a
controller player"* — that appears NOWHERE in the source. **A red test whose
expected value is absent from the tree is describing a product that was decided
against, not a fix that is owed.** Grep the expected literal before writing a fix.

⚠ **and a stale COMMENT is what made it read as owed work.** `smash_in_the_host`'s
doc still said *"`SmashSelect::ready()` requires `humans_decided() >= 1`, so START
is inert"* — written while that test was failing, and read by anyone arriving
later as current behaviour. Now past tense rather than deleted: the reason a rule
was dropped is what stops it coming back.

Kept rather than deleted — its second half was always right, and flipping the
contradicted half makes it the only UNIT-level pin of Jon's rule (the other guard
is a full lobby drive, too coarse to fail when `ready()` grows a clause back).
Verified by restoring the clause: it fails with *"a CPU-vs-CPU match could not
start, which Jon asked for by name"*. smash 50/50.

✔ **B2 — the 1-2 elevator shaft, AUTHORING half.** Jon: *"moving platforms that
move vertically down and up like an elevator. When they go OOB … they can
teleport to the top / bottom of the screen to make an infinite elevator
effect."* ⭐ **the engine half landed** — `MovingPlatformMotion::Loop` wraps where
`Sweep` and `Path` reverse, and its carry is its TRAVEL rather than its position
difference, so the wrap cannot throw a rider the length of the shaft. What is
left is CONTENT, and the two things standing in the way are measured rather than
guessed:

1. ✔ **the `loop_dy` field def is in**, on `MovingPlatform` in BOTH `mary_o.ldtk`
   and `sandbox.ldtk` (purely additive; every existing platform reads `None`).
   ⛔ **and 1-2's only existing `MovingPlatform` is the Underground FERRY**
   (`mary_o_1_2_ferry`, `sweep_dx: 64`, `speed: 90`) — an existing gameplay
   element that other code finds BY NAME. It carries you across; it is not a
   spare platform to repurpose. The elevator needs NEW instances.
1b. ✔ **the shaft needed an ANCHOR, and `loop_dy` alone could not express a
   conveyor.** `loop_min_y` landed, with the field def in both worlds; a probe
   pins that three staggered platforms sharing one shaft stay in it for 60 steps
   and stay three distinct platforms. Without it `lift_2` ran to 410 in a
   100..400 shaft by step 10. `loop_dy` is measured from the platform's own position, so three
   staggered platforms get three DIFFERENT shafts and drift into separate bands
   instead of chasing each other round one. A conveyor is N platforms sharing one
   shaft at different PHASES, which needs the shaft stated independently of the
   platform — an optional `loop_min_y` (falling back to the entity's own y, so
   the single-platform case stays one field). ⭐ this is a gap in the authoring
   model rather than in the engine: `MovingPlatformState::from_vertical_loop`
   already takes an explicit `min_y`/`max_y` and does the right thing; only the
   spec cannot say it.
2. ✔ **a shaft may leave the room — CHECKED, not assumed.**
   `moving_platforms_for_room` is a straight `clone()` of the room's list and
   `world_with_moving_platforms` just extends the block list, so nothing culls or
   clamps a platform by room bounds; the shaft is numbers on a spec and the
   platform is an ordinary solid wherever it is. ⚠ it stays SOLID while outside,
   which is what makes the effect work and also what a level with an EdgeExit on
   that side should be authored around.
2b. ✔ **+y is DOWN, and the API said the opposite.**
   `from_vertical_loop` took `rising: bool` and set `dir = +1` for it, while the
   LDtk conversion does not flip the axis — a falling body's y GROWS (Mary-O left
   1-2 at y 648 in a 448-tall room). So a positive `loop_dy` descends, and every
   elevator authored against the old name would have run the wrong way, silently,
   visible only by watching it. Renamed `downward`, with the convention stated at
   the enum, the spec field and the constructor.
2c. ✔ **the shaft is authored in 1-2** — three lifts at x 1332 (the free column
   between the ledge ending 1280 and the stairs at 1488), sharing one shaft
   anchored at y −96 and 640 tall so both wrap points are outside the room.
   Spaced 213 apart (640/3) so the run reads as a conveyor. ⚠ the run was shifted
   up to y 6/219/432 because the validator caught the lowest lift's bottom edge
   at 462 in a 448-tall level — the spacing is preserved, the bottom lift now
   sits exactly on the floor line.
2d. ✔ **and the app suite confirms it loads and plays** — `-p
   ambition_demo_mary_o_app --features visible` 36 passed with the shaft in the
   level, which is the empirical half of "a shaft may leave the room" rather
   than the read-the-code half.
2e. ✔ **the FEEL is PINNED, 2026-08-07** — `every_lift_wraps_outside_the_room`.
   Jon's parenthesis is the requirement: *"when they go OOB (far enough so they
   are off screen of the player in normal gameplay)"*. A wrap the player can see
   is not an elevator, it is a glitch, and it is the one failure mode no other
   check catches — the platform is in bounds, the shaft is well formed, the ride
   works, and the effect is simply ruined.
   ⭐ **the room's own extent is the right line, and no viewport size is needed.**
   Under `CameraClampMode::RoomBounds` the target is clamped to
   `[-H/2 + half_view, +H/2 - half_view]`, so the visible band is exactly
   `[-H/2, +H/2]` — the room and nothing past it. A lift wholly outside the room
   at both wrap points cannot be on screen at either. ⚠ two escapes are STATED
   rather than guarded, both visible in `clamp_camera_target` and neither
   reachable from what 1-2 authors: a room SHORTER than the viewport falls back to
   centring, and portal padding can expand the bounds.
   ⚠ **this bullet's own "first thing to check" was already stale** — bullet 2
   above had discharged the culling question by reading `moving_platforms_for_room`.
   The real open question was only ever visibility.
   ⛔ **and the floor test earned itself immediately.** The first draft lived in
   `app_it::room_spatial_integrity`, which loads the AMBITION project — it found
   zero looping platforms and would have been green forever while looking at the
   wrong world. It now lives in `ambition_demo_mary_o`, where the lifts are, and
   asserts it sees at least three. Verified by poisoning the CONVERTER
   (`with_loop_anchor(None)`, the pre-`loop_min_y` behaviour): all three lifts are
   named with their exact edges, *"the player watches it blink out"*.

⭐ 1-2 already has ONE `MovingPlatform` at px (960, 280), 96x16 — the shaft can
reuse its size so the run looks like a set. ⚠ **the stagger is the feel**: one
platform that vanishes and reappears is a bug, three evenly spaced is an
elevator.


**B1 — [`JONS_OBSERVATIONS_BUGS_AND_ISSUES.md`](JONS_OBSERVATIONS_BUGS_AND_ISSUES.md)**,
7 open rows. Jon's direct observations outrank inferred work: read it before Lane
C and promote anything in it that is cheap or that another lane would otherwise
step on.

✔ **B2 — the two new SVG characters** (Jon, live, 2026-08-06: *"there are new svg
characters for stargan and the patent clerk, stargan needs to replace the old
one and patent clerk is new: note we want to use the svg clerk not the older
one"*). Stargan's sheets were re-published from his new SVG (the tree's copy was
from 07-26). The patent clerk is IN: a `special_patent_clerk` catalog row
spliced from the target's own `ACTOR_METADATA`, a Hall pedestal from
`generate_hall_of_characters`, and the waiver that excused him deleted.
⭐ **three separate silent-drop defects fell out of it**, each of which had been
reporting success:
  * `character_notes.fallback_pool` folded `dialogue_hints` into the catalog from
    a hard-coded list of THREE key spellings, under a comment saying not to make
    a character "mute over a key name". `patent_clerk` and `python_goras` spell
    theirs `fallback_lines`, so six lines each were read as nothing — and because
    both also author `barks`, the pool came back non-empty and no gap was
    reported. It now reads every key under `dialogue_hints`; Python Goras got his
    four lines back.
  * `patent_clerk` was in `scripts/regen/sprites.sh`'s render batch and NOT in its
    expected-output postcondition, so a run that failed to draw him still
    reported every output present.
  * `rendered_identities_are_registered` — see the START HERE note above.
**what is left is Jon's**: `./scripts/regen/sprites.sh --target patent_clerk` dies in
his own live rig work (`SVG view 'Patent Clerk - Side Left' does not have
one-to-one drawable ownership: multiply_assigned={'path1115': ['torso',
'neck']}`), so the sheets in the tree are the 13:58 render installed from
`generated/` and a fresh clone cannot reproduce them. ⛔ hands off — the fix is
in the dirty submodule.

◐ **B3 — Mary-O 1-2 is built out and the flying snakes are in it**, from Jon's
own file: *"World 1-2 also needs to be built out a lot more, its very plain, and
there are no enemies. I would like to add the flying snakes on a plane as
enemies in this world."* 1-2 was a rectangular box with one shelf; it now has
ceiling teeth, a second shelf, a ledge that makes the hidden block reachable, a
staircase to the pole, and two of each plane snake in the airspace.
⭐ **an archetype is not an enemy, and the 08-06 ledger row said it was.** That
row closed with *"the whole enemy is a TABLE"* because the movement kernel
already flew. Placing one proved the claim was about BEHAVIOUR: they drew red
placeholder rectangles until `plane::register_snakes_on_a_plane_sheets` existed,
because this demo publishes its own enemy art (`snake.rs` and `ai_slop.rs` each
carry theirs). ⚠ the sheet TARGET is not the catalog id for these two, which
`solid_snake` and `ai_slop` never had to notice because theirs are the same
string.
⭐ **two more comments were measurably wrong and are corrected in place**: the
archetype's *"the `Aerial` template ignores `aggro_radius`/`attack_range`"* (it
reads both — the zeros are what make these roam instead of dive), and 1-2's
*"the only terrain shelf clear of every wall"* (eight of them now; the coin
shelf is identified by the coins that span it, which survives further building).
⚠ **the second pit I added came back OUT.** `the_chasm_is_only_crossable_by_the_
platform` states the room's design rule — one hole, crossed by the ferry — and
that rule is Jon's, not the test's.
✔ **the progression half landed too, and it uncovered a bug Jon then hit in
play.** The round trip is four deleted zones; 1-2 is entered only by finishing
1-1 and left only by finishing 1-2.
⛔ **finishing a level KILLED you, in both directions**, and had since the flag
route shipped on 08-05 — deleting the shortcut is only what made it the only
road. `FlagSequence::driven` is a position in the room the pole was in, and
`run_flag_sequence` writes it onto the body through `constrain_body_pose` every
tick the phase is not `Idle`. `cycle_level_on_flag_tally` deliberately STAYS
`Tallied` while departing so the transition keeps being asked for — so the
commit placed her at the target's spawn and the next driver tick put her back at
the SOURCE room's pole coordinates, outside the new geometry, where she fell and
the death beat restarted the level.
⭐ **the fix is an authority, not an ordering.** `follow_the_active_room` already
owns "the active room changed" (it is why the pole and the destination are
re-derived there), so it clears the sequence — and it is adjacent to the driver
in the same `.chain()`, so no commit can land between them.
⚠ **the test that caught it nearly hid it.** The arrival assertion read the body
on the frame the room id flipped; the first rewrite was going to settle 120
frames and re-read, which would have gone GREEN the moment the death-reset
happened to put her inside the room. It waits for the body and reports the last
position instead, and `the_run_survives_the_crossing` catches the lost life —
two independent symptoms of the one cause.
✔ **and one more 1-2 row is untouched**: Jon wants vertical elevator platforms
that wrap top-to-bottom for an infinite-elevator effect. — **STALE, closed
2026-08-07.** That is B2 directly above, which is done: three lifts on one shaft
in 1-2, wrapping outside the room, pinned by `every_lift_wraps_outside_the_room`.
⭐ **this is exactly the cheap tell this file's header names** — *"a row whose
SIBLING claims it landed"* — with the sibling four paragraphs up its own page.

---

## Lane C — the architecture spine

Prefer items that make the NEXT ten cheapest: a seam, an authority, a lifecycle
boundary, or a measurement that can move.

◐ **C1 — [`tracks.md`](tracks.md)** — **8 open rows when this was written, 6
now** (counted 2026-08-07; ⚠ I first wrote 5 here, in the commit that was about
counts rotting — the count is at lines 83, 112, 616, 671, 762 and 839, and TWO of
them were added TODAY by this run's own findings, which is why the number went
down by less than the number closed): the guardrail-sequester row resolved (⛔ and its remaining
prescription was WRONG — sequestering the rollback sweep into agent tooling would
file 19 tests of the ADR-0023 determinism contract as agent tooling), the base-SHA
landing rule landed in `AGENTS.md`, and the cutscene design row was describing
code that already exists. ⚠ the count in a row is itself a claim that rots: this
one was four days old and off by three.
✔ **C2 — [`smash-siblings-plan.md`](../archive/smash-siblings-plan.md)** — **all six rows
were already LANDED** (verified against the tree 2026-08-07, row by row, before
touching anything). Not one of the six needed work; §0 was even marked "FIRST",
blocking the other five, and its own analysis — a saturating `damage_taken()`, a
`DeathPolicy` that had to move down a crate, nothing in production selecting
`Unbounded` — is false of the current code in every clause. ⭐ **this is the
staleness this ledger's own header warns about, at the scale of a whole plan
file**: six rows reading as the next six pieces of work, describing a game mode
that ships. ⚠ §5 is marked ◐ rather than ✔ because it deviates twice from what it
asked for — the HUD prints a NAME and a `2/3` text count where the row asked for a
portrait and one icon per stock. Neither is a defect and a photograph settles
both, but a row that asked for icons and got text should say so rather than be
ticked.
Jon's ruling stands: 3 stock, no items, character select with orb-drag, one
battlefield-like 3-platform level, 1–4 players, back to select on end.
◐ **C3 — [`authoring-loop-program-2026-07-31.md`](authoring-loop-program-2026-07-31.md)** —
worked 2026-08-07 by taking the migration ledger's cheapest-looking remaining
family, the fighter brain ladder, and finding it was not a migration at all.
⛔ **every other open row there is the two-readers-of-one-file shape; this one had
ZERO production readers.** Ambition authored a nine-rung difficulty ladder in July
and the running game had never read a row of it, while
`FighterBrainProfile::for_level` — which documents itself as *"a FLOOR … a game
that cares ships its own nine rows and this is never consulted"* — was consulted
at every call site. ⭐ the consequence is not a number: `for_level` hands every
rung `UtilityWeights::default()`, and `default()` is `v1()` is the authored level
NINE, so a level-1 CPU prices a kill move exactly as the hardest one does and the
ladder's entire "what a difficulty NOTICES" axis is absent from the shipped game.
Landed: the arbitration seam (`profile_for_level`), the schema, and the load —
the ladder is now declared, validated and lowered, asserted against the PACK
rather than the file. ✔ **consumption CLOSED 2026-08-07** — and the finding is
sharper than the row was: **the wire runs to the wrong game.** Measured, not
assumed: Ambition's own 24 archetypes contain ZERO `Fighter` brains (12 `Smash`,
3 `Skirmisher`, 4 `StandStill`, the rest one-offs), while all 7 `Fighter` rows in
the workspace are `ambition_demo_smash`'s roster plus its
`"duelist": Fighter(level: 5)`. `ambition_demo_smash` depends on
`ambition_platformer2d` alone — never on `ambition_content` — so the ladder is
AUTHORED in the game with no fighters and CONSUMED by the game that cannot see
it. `ambition_app` is the only composition where the two meet, and
`authored_fighter_ladder.rs` now pins that composition end to end (real
`brain_from_preset` → one step of the real app → the authored rung), probed red
BOTH ways: projection unregistered, and the insert `#[cfg(any())]`-ed out.
⚠ **level 5 on purpose** — the authored level 9 row is byte-identical to
`UtilityWeights::v1()` and level 1's `reaction_ms` is 500.0 in both ladder and
floor, so a probe on either of those rungs is green whether or not any of this
exists. The test asserts `authored != floor` before it asserts anything else.

**C3a — the standalone smash demo still runs on the engine floor**, and so do
both instruments built to measure the ladder. `ladder_probe` and `ladder_rig`
live in `ambition_demo_smash_app`, which is behind the demo gate (*"a demo app
depends on `ambition_platformer2d`, never on `ambition_app`"*) and therefore
cannot see `ambition_content`'s pack. ⭐ **so the two binaries whose entire job is
to measure difficulty rungs have been measuring the FLOOR** — the same class as
`ladder_probe` measuring the engine floor once already this campaign, one layer
out. ⚠ the fix is a layering decision, not a wire: either the ladder is Ambition
content that smash borrows (today's shape, and the demo gate says it cannot), or
**a difficulty ladder belongs to the game that has fighters** and
`fighter_brain_ladder.ron` should move to `ambition_demo_smash` with Ambition
reading it rather than owning it. The second reads correct — the file is titled
"Ambition's fighter-brain difficulty ladder" and Ambition has no fighters — but
it moves authored content between games, so it is written down for Jon rather
than done. ⛔ do NOT "fix" this by adding an `ambition_content` dep to the demo;
that deletes the gate.

**C3's remaining rows** —
12 open rows: the content-authoring loop.
◐ **C4 — the engine design docs** in [`engine/`](engine/). `roadmap.md`,
`vision.md` and `status.md` frame them. An engine doc with no queue row is
exactly the "not written down yet" case this ledger exists to catch.

⭐ **SWEPT 2026-08-07, and the method is the reusable part.** Asked which of the
35 `engine/` docs no planning file references at all — four:
`character-bone-rig-conversion.md`, `frame-awareness.md`, `sprite-renderer.md`,
`unified-movement-kernel.md`. Two are position papers rather than work
(`frame-awareness` is a manifesto Jon wrote verbatim; `sprite-renderer` states a
principle whose tool is the hands-off submodule). ⛔ **and `unified-movement-kernel`
carries a section titled "Residual debt (honest)" with FIVE numbered items and no
row anywhere.** That is precisely the case this lane exists to catch: honest debt,
written down by the person who left it, in a file nothing points at.

**Each measured before being copied here, because a debt list ages like any other
row:**

* ✅ **item 2 is STALE — the gravity resources are accounted for.** It says
  `GravityField`/`GravityZones`/`ForceZones` "remain unregistered snapshot
  resources". All three are in the frozen baseline: `ForceZones` and
  `GravityZones` as `derived` (rebuilt per tick by `collect_force_zones` and from
  authoritative components), and **`GravityField` as `resource-canonical`** with a
  canonical checksum projection. That also closes the item's own caveat —
  *"rewinding a mid-rewind gravity switch is not covered"* — because it now is.
* ✅ **item 3 — CLOSED, and its remaining half is REFUTED rather than deferred.**
  The remedy landed: four seams construct `ScreenAxes`, and
  `movement/input.rs` states the property — *"Raw `ScreenAxes` never appear below
  the seam."* ⛔ **and `ControlFrame` must not hold one**: `ScreenAxes`'s own doc
  says passing it below the controller seam is an architecture error, and
  `ControlFrame` goes exactly there — mirrored into `SlotControls`, read as brain
  state, and serialized as the recorded input stream. ⚠ it would also cost the
  replay format: `ScreenAxes` derives no serde, and `input_stream.rs` is explicit
  that only ADDING a field is free, so reshaping the two axes into a nested field
  invalidates every stream recorded at `INPUT_STREAM_VERSION = 1`. ⭐ a typed
  wrapper stops WHOLE-VECTOR confusion where two frames could be mistaken for one
  another; `ControlFrame`'s axes are screen-space by definition and have no rival
  frame at that point.
* ✅ **item 4 — CLOSED same day.** `SurfaceMomentumMotion` grew
  `set_tangential_speed` / `scale_tangential_speed` / `tangential_speed`, and
  Sanic's two reach-ins use them. ⚠ **the doc's name for the fix was wrong** — it
  asked for a tangential-IMPULSE op and neither operation is one: the launch SETS
  and the brake SCALES, which is also why they are two ops rather than one
  (scaling preserves direction, setting does not). ⭐ the win is not tidiness: the
  kernel's sign convention lived in three comments inside the demo and now lives
  on the op, and "what does AIRBORNE mean here" was a branch each caller wrote
  separately — the launch has an answer, the brake does not — which is now a
  returned `bool`. core 354, sanic 77, contracts 25/25.
* ✅ **item 1 — CLOSED by reading the transit policy**, which is what the first
  pass correctly said it needed. The item claims transit orientation reads the
  presentation `GravityField` mirror "rather than per-body frames". It does not:
  `projectile/systems.rs` calls `gravity.dir_for(kin.aabb())`, and
  `GravityCtx::dir_for` resolves the body's AABB against the authored zones,
  using the field only as the BASE when a room authors none — the per-body port
  the item asked for. ⭐ and the transit CORE needs no frame at all: it is pure
  portal geometry, momentum rotated by the pair's transform. ⚠ the item's own
  escape survives as an EXERCISE gap rather than a code gap — portal rooms author
  no nested gravity zones, so the zone branch is correct and unrun there. That is
  a test, not a port.
* **item 5 — block→chain crawl transfer.** A behaviour claim (a block-attached
  crawler does not migrate onto an overlapping chain without detaching), so a grep
  cannot settle it either. ⚠ it neighbours the adhesive-crawler contact bug Jon
  has left open by his own call, and should not be worked before that one.

⭐⭐ **THE BETTER SWEEP, and it changes the lane's conclusion.** "No planning file
references it" is a weak proxy — it finds only the fully-unreferenced. The sharper
question is which docs carry an open-work SECTION HEADING (`residual`, `debt`,
`open question`, `remaining`, `not done`, `gap`): **eleven do**, and **nine of
them are cited nowhere in this active ledger**:
`character-definition-design`, `closeout-review-followups-2026-07-20`,
`competitive-2d-platformer-engine-roadmap`, `fighter-brain`,
`immutable-content-and-transactional-construction`, `participant-action-system`,
`participant-input`, `room-transition-loading`, `unified-actors`.

⛔ **but the risk is the opposite of what that number suggests, and the evidence
is today's own work.** `unified-movement-kernel`'s five-item list read as five
pieces of owed work and was **four-fifths closed**: two items had already been
fixed by work that never came back to say so, one asked for something the type it
names forbids, and one I did. Sampling `unified-actors`'s "Convergence audit (the
debt baseline)" shows the same shape — CONVERGED / DONE entries with struck-through
resolutions, a progress record rather than a backlog.

⭐ **so the finding is not "nine docs of un-rowed work". It is that these sections
READ as open and are largely historical**, which is worse in a specific way: a
sweep that trusts them re-derives closed questions, and this session nearly did it
twice. Whoever works these should expect to spend most of the effort CLOSING items
rather than doing them, and should treat "it says it is open" as a claim about the
code that ages like any other row — the same rule this ledger's own header states.

◐ **the remaining work here is nine READS**, one per doc, each producing either a
row or a closure. It is not nine implementation slices, and estimating it as such
is how the lane would get deferred.

**FOUR DONE (2026-08-07), and every one was a closure rather than a row** — which
is now an evidenced pattern rather than a guess:

* `unified-movement-kernel.md` — 4 of 5 residual items closed (two already fixed
  by work that never reported back, one refuted, one done).
* `participant-input.md` — 5 of 8 "deliberately not done" items landed. ⚠ a
  NON-GOALS list is the worst shape to leave stale: it reads as a standing
  decision when it was one slice's scope, and it invites someone to DECLINE work
  that is wanted.
* `participant-action-system.md` — two "gaps" had been decided against since
  (bindings machine-wide per A3/D2; menu repeat global per R3). ⛔ **this is the
  dangerous direction**: a stale backlog row invites redoing finished work and you
  notice in minutes; a stale GAP row invites UNDOING a decision, with the argument
  for undoing it sitting in a document that reads as current.
* `room-transition-loading.md` — the whole "Current source-backed gap" section,
  every claim false: the path composes `ambition_load`, commits behind
  `ROOM_READY_BARRIER` a later tick, and the presentation crate's shell coupling
  is isolated in `shell_adapter.rs`. ⭐ that heading is the most authoritative
  phrasing a design doc has — it claims to be a measurement of the tree — which is
  exactly what makes it least likely to be rechecked.
* `character-definition-design.md` — 3 of 4 "Attack intent gaps (block Smash)"
  closed, including the headline one: `attack_dir_from_axis` has a `Forward`
  branch, so jab ≢ ftilt. ⭐ **a gap list titled "block X" is worth rechecking the
  day X ships**, and Smash shipped without anyone revisiting it.

* `unified-actors.md` — all three recorded DECISIONS landed
  (`AggressionMode::HostileToPlayer` is gone, `ProjectileFaction` retired,
  `WorldHitPolicy` on the spec), and ⛔ **two code comments still named the deleted
  variant**, one of them asserting the player-centrism the audit removed. Fixed.
  ⚠ the audit's one MEASUREMENT drifted the wrong way: "~15 genuine
  `With<PlayerEntity>`/`With<PrimaryPlayer>` filters" re-measures at ~17.
* `fighter-brain.md` — the four-features gap closed via option (1)
  (`MoveFrameData::max_damage`, `UtilityWeights::expected_payoff`). ⭐ and it lands
  on today's ladder finding: the feature shipped, the per-rung CALIBRATION shipped
  in `fighter_brain_ladder.ron`, and the game read neither until today.
* `immutable-content-and-transactional-construction.md`,
  `closeout-review-followups-2026-07-20.md`,
  `competitive-2d-platformer-engine-roadmap.md` — **no open work in any of them.**
  The first's "Remaining families … Still outside the planner" table is nine rows
  ALL struck through as migrated (⚠ its heading and intro now contradict its
  content, and its verification line cites "908 monolith lib tests" against
  today's 1196). The second's "Remaining rule" is a POLICY, not a gap. The third's
  "Phase 0" delegates outright: *"Keep `tracks.md` authoritative for the exact
  remaining slices."*

⭐⭐ **ONE GENUINE OPEN FINDING out of nine reads, and it is a live one.**
`closeout-review-followups-2026-07-20.md` §2 says the portal mapping convention is
a PROCESS GLOBAL, and it still is:
`ambition_platformer2d_shared_tangle::math` holds
`static PORTAL_MAP_ROTATION: AtomicBool`, `ambition_portal2d::tuning` writes it
from live tuning, and FOUR sites in `ambition_portal2d/src/placement.rs`
(45, 130, 154, 179) read it purely to produce the `MapConvention` argument the
core's pure functions **already take** (`cast.rs:182`).
⛔ **this is the class that bit twice today**: a process-global two compositions
share, which the doc's own evidence names — *"prevents two Apps/providers in one
process from choosing independent conventions, contaminates tests, and leaves a
simulation rule outside the session authority."*
◐ **PARTLY FIXED 2026-08-07 — and the fix's own commit message OVERCLAIMED,
which is corrected here.**

✔ **what landed**: `transfer_step` now reads `tuning.convention.is_rotation()`
instead of the global for `roll_delta`, `facing_flip` and `input_warp`. The value
was already in the argument list — `PortalTuning::convention` exists and the pure
layer already exposed `*_for_convention` forms — so the fix is one `let` and three
call swaps. Probed first and verified red (left `0.0`, right `3.1415927`).
⚠ **the probe took four fixtures**, each rejection informative: floor→wall gives
π/2 under BOTH conventions; two FACING walls give `atan2(0, 1) = 0` under both;
the convention only enters as `if !rotation && wall_to_wall(..) { return 0.0 }`,
so it needs a wall↔wall pair with a nonzero roll — two SAME-facing walls,
`atan2(0, -1) = π`. The anti-vacuity assert caught the first two.

⛔ **CORRECTION — that commit said "the transit that decides where a body lands
and which way it faces no longer consults it". The FACING half is true and the
LANDS half is false.** `transfer_step`'s `pos` goes through `map_point` and its
`vel` through `rotate_velocity_between_normals`, and **both call
`portal_map_vec`, which reads the global.** So what moved onto tuning is three
DERIVED facts; the core map did not.

**what remains, measured**: `portal_map_vec` is the single dispatch point — its
own doc calls it *"one orthogonal map shared by velocity, position, AABB, input,
and rays so they always agree"* — with only 5 non-test call sites (`map_point`,
`map_aabb` ×2, `rotate_velocity_between_normals`, and a gun-visuals presentation
read). ⚠ but each of those three helpers has its own callers, which is where a
convention parameter would widen — **the same shape that cascaded on the fighter
ladder today**. Because the map is ONE function, a session-scoped convention read
or an explicit parameter threaded from `PortalTuning` are both plausible; decide
by measuring `map_point`/`map_aabb`/`rotate_velocity_between_normals` callers
first, not by starting at `portal_map_vec`.

---

## Lane D — decisions that are still Jon's

✔ **D1 — DISCHARGED 2026-08-07.** Every row re-checked for whether its stated
facts still hold; two additions (`SessionScopedEntity` provenance, the portal
convention global) and one promotion (the PCA aerial question, which was sitting
in a review ledger where decisions are not read). ⛔ **and the file decayed in
BOTH directions**, which is why this is not clerical: the dialogue row said
`the game-level default stays open` three lines under Jon's recorded answer to
exactly that, and an "unconfirmed" body half-width turned out to be the expected
state rather than a symptom. ⚠ an unanswered question going stale is visible; an
ANSWERED one whose body was never updated is not, because the heading says CLOSED
and nobody reads on.

**D1's original text** — [`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md),
6 open rows: 5 after the dialogue one closed today, plus the robot heavies added
on 08-06 when the identity scan stopped being blind to them. ⚠ a row here is NOT work: the
job is to keep each one answerable — state the mechanism, the cost of each
branch, and what is blocked — and to notice when one has become answerable
without Jon, which is how the dialogue row spent three days blocked on a question
whose engine half was already settled.

---

## Lane E — carried residue, by POINTER

Never by copy. Re-measure before working any of these; they are the oldest claims
in the tree and the most likely to be stale.

✔ **E1–E3 — the three carried queues are DELETED (2026-08-07), harvested first.**
`queue-72h-2026-08-03.md`, `queue-72h-2026-07-31.md` and `queue-24h-2026-07-26.md`
were 1.1 MB — 39% of everything under `docs/planning` and **86% of every `[closed]` mark
in it** (309 of 361). Read them at `9a66996ce~1`; git is the archive.

⭐ **the sample that opened this lane held up under a real audit.** E1 drew four
rows and found four closed. Verifying the concrete ones by identifier found ten
of eleven already done: `G4` (delayed ranged aim — `MovePlayback::aim` plus
`a_move_started_aiming_up_fires_up_after_its_request_is_cleared`), `G7` (Sanic's
`sandbox_all()` inheritance — gone), `G8` (*"`MovePlayback`'s checksum omits
`hit_targets`"* — `snapshot_impls.rs` says **"`hit_targets` is in the CHECKSUM
now"**), `PICK 18` (`respawn_room_visuals_on_request` — registered at
`rendering/mod.rs:404`, "was the straggler"), Jon's pipe-linking schema
(`ldtk_vocabulary.rs` carries the portal-style `link` he asked for), and
*"THE ORPHAN TEST HAS NO MIRROR"* (`rendered_identities_are_registered.rs` opens
*"The orphan check's MIRROR"*). **Fourteen of fifteen rows verified closed.**

⭐ **and the decisions were never at risk**: every decision the queues referenced
— #9, #11, #13, #14, #15 — is already a heading in
[`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md). Jon's
verbatim asks were either landed (`PICK 15`'s five-part LDtk polish is ✔ but for
nothing) or carried better elsewhere — his snake/slop sizing report lives in
[`JONS_OBSERVATIONS_BUGS_AND_ISSUES.md`](JONS_OBSERVATIONS_BUGS_AND_ISSUES.md)
with a measured pixel table the queue never had.

**TWO rows survived the harvest.** They are the entire live yield of 1.1 MB:

**E1a — no test that a drop dies with its room.** From Jon's 2026-08-05
playtest: an enemy drop was SESSION-scoped while its visual is a `RoomVisual` and
therefore room-scoped, so a coin survived a room change and its picture did not —
`no render family claimed coin:EnemySpawn-…`, eight per transition, forever,
because no real visual was ever coming. Fixed in `dd73a3087` and **the fix is
unguarded**. Needs a real room-unload fixture, not the hand-built worlds the drop
tests use. ⭐ same family as *"drops outliving a reset"*.

**E1b — how many of the 30 `#[ignore]`d tests are hiding a red?** Counted
2026-08-07: **30** across `crates/` and `game/`, including `rollback_coverage`,
`collision_invariant_oracle`, `rollback_exit_oracle`, `hall_scale_spread`,
`enemy_body_scale` and `dive_drill_reachability`. Three were surfaced this way
before. ⚠ the cheap version is to run them, not to read them — an `#[ignore]`
whose reason has expired is indistinguishable from one still earning it until it
executes.

◐ **E4 — the review ledgers**: `review-gpt56-through-1a05b98.md`,
`review-gpt56-through-32eb27a.md`, and `triage/`.
✔ **counted 2026-08-07: 0, 1 and 0 open marks.** ⭐ including
`review-gpt56-through-c32e690.md` at **zero**, which is the A7 review this run
was opened on — R1–R5 are discharged by its own ledger, not just by mine.
✔ **the ONE open mark is DONE** — the only carried mark this run found that was
genuinely open. `ArchetypeSpec::is_aerial` is `Option<bool>`; four authored rows
are `Some(..)`; **three** readers resolve absence with `unwrap_or(false)` (my
estimate said two — the compiler found the third, which builds
`ActorTuning::is_aerial`, and a grep had shown it while I discounted it because
TWO other fields share that name).
⭐⭐ **and it answered part of the question it was meant to make answerable.**
`cellular_automaton_fighter` reads `is_aerial: Some(false)` — a DELIBERATE
grounded claim — against a catalog saying `Floating`, so the two sources really
do contradict and *"the catalog wins because the archetype is silent"* is not an
available resolution. That was unknowable with a bare bool.
⛔ the rejection half deliberately did not ship (it would refuse the PCA today),
and the product question is promoted to
[`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md) — *"Does the
Perfect Cellular Automaton fly when it fights?"* — with three options, because a
decision sitting in a review ledger is one nobody reads.

---

## F — the item that makes the next ten cheapest (added 2026-08-07, with its reasoning)

✔ **F1 — a planning doc's backticked names should be checkable, not just its DONE
rows — MEASURED AND REJECTED. The measurement is below and it is the point.**

**Why this and not something else.** This run found stale planning claims TEN
times, in five different shapes: a whole plan file open on a shipped game mode
(smash-siblings, 6/6 rows); 8 of 9 engine docs whose "open" sections describe a
tree that no longer exists; a decision file saying `still open` three lines
under Jon's recorded answer; 4 of 4 sampled rows in a carried queue already
closed; and two code comments naming an enum variant the codebase deleted. Each
cost a read to disprove, and two nearly sent me to undo a decision or redo
finished work.

⭐ **the repo already has the rule, and already has the function.**
`scripts/check_roadmap_evidence.py` states it outright — *"A BACKTICKED name is a
CLAIM THAT IT EXISTS. That is the whole authoring rule, and it is what lets a
correction paragraph name a retired file without the guard calling it rot: write
the dead name as plain prose."* — and implements `identifier_exists(name)`, which
greps SOURCE ONLY (deliberately, because its first version searched everything
and reduced to "does this document still contain its own text").

⛔ **what it does NOT cover is the direction that actually rots.** It reads two
shapes — the engine roadmap's `### Task N` + `**Status … MET**`, and `status.md`'s
verdict table — and only rows CLAIMING DONE. Pointed at `unified-actors.md` it
reports *"no completion claims found at all"* and says so honestly rather than
passing. But every failure above was an OPEN claim that had gone false, in a doc
with neither shape.

**The work.** A mode that takes any planning doc, extracts backticked
Rust-ish identifiers, and reports the ones `identifier_exists` cannot find. Not
new machinery — the rule, the function, the source-only discipline and the
anti-vacuity reporting all exist; this is a second entry point to them.

⛔⛔ **MEASURED 2026-08-07, AND NOT WORTH BUILDING. The premise fails at the
decisive point.** Prototyped against the real corpus before writing anything
permanent, and three results in order of severity:

1. ⛔ **it would not have caught a single one of the ten failures that motivated
   it.** Every one is a SITUATION claim — "still open: the layer-2 silence", "the
   ordinary room-transition path does not compose those pieces", "the
   game-level default stays open" — and a situation names no identifier to look
   for. That is exactly what `check_roadmap_evidence.py`'s own docstring already
   says it cannot do, and I proposed the extension without noticing the
   limitation was the same one.
2. ⛔ **`Type::member` citations are a false-positive factory.** Across 35 engine
   docs the naive scan reported 190 absent names, and the very first two I
   verified — `MoveFrameData::max_damage` and `UtilityWeights::expected_payoff` —
   both EXIST. Rust never spells a field access as `Type::field`, so grepping the
   literal is a category error. Restricting to simple names drops it to 118.
3. ⚠ **and the 118 are dominated by CORRECT writing.** `unified-actors.md`
   backticks `BossStatus` and `EnemyArchetypeSpec` in the sentences recording that
   they were retired. A design document names dead things constantly — that is
   what a design history IS — so the rule *"a backticked name is a claim it
   exists"* is a ROADMAP convention, not a docs-wide one, and nothing taught the
   other 34 files about it.

⭐ **the narrow version is honest and nearly pointless**: on the two ACTIVE files
it finds 5 names in 263 (2%), all of them correction prose — including
`two_cpus_and_nobody_playing_is_not_a_match`, a test renamed today, named in the
row that records the rename. Reviewable, and catching nothing wrong.

✔ **so F1 is CLOSED as measured-and-rejected**, and the measurement is the
deliverable: it saves the next person building a checker that would have caught
0 of 10. ⭐ what the prototype DID buy is real and already committed — chasing it
is what found that `check_roadmap_evidence.py` was wired in without `--check` and
could never fail.

⚠ **the false-positive answer is already the convention**: a correction paragraph
names a retired thing in PLAIN PROSE, which is what today's sweep did without
knowing the rule (`HostileToPlayer` and `prewarm-neighbor-room` are written
unbackticked in the notes recording their removal).

⚠ **and it must report its own vacuity**, like its sibling: a doc with zero
backticked identifiers is not clean, it is unreadable by this check.

---

**F2 — the convention that WOULD have prevented the ten, written where it is
read.** ✔ **done 2026-08-07**, and the reasoning is F1's rejection: situation
claims are what rot, no tool can see them, and the repo already knows the answer.
`check_roadmap_evidence.py`'s docstring holds the authoring rule — *"a BACKTICKED
name is a CLAIM THAT IT EXISTS"* — and it lives in a script an agent never opens.
Neither `AGENTS.md` nor `docs/planning/README.md` mentioned it.
⭐ Added to the README's **Evidence discipline** section, where planning docs are
governed, with the measured contrast: *"the path does not compose those pieces"*
is uncheckable and was false for weeks; *"`ROOM_READY_BARRIER` does not exist"* is
one grep. ⚠ and it says explicitly not to answer this with a scanner, citing the
prototype's numbers — because Living-plan rule 5 already forbids exactly that and
F1 proved it right.

---

## The rule when this file looks finished

**There is no completion criterion.** If every row above is `✔`, the next item
has not been written down yet — go read `docs/planning`, add it as a row WITH its
reasoning, and work it. Do not write a completion-shaped summary while time
remains; that prose is the exact artifact that ended a run in 84 minutes on
2026-07-25.
