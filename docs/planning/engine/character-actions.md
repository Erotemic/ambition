# Character actions — merged into current owners

**Status:** MERGED 2026-08-30.

The action-authoring migration that created body-owned repertoires, semantic
slots, control prompts, binding overrides, authored move labels and per-character
`MovesetContract` values is complete enough that this file no longer needs to be
an independent planning authority.

Current ownership:

- character/action authoring and the two remaining trigger-based repertoire
  questions: [`character-authoring-package.md`](character-authoring-package.md);
- participant, binding, provider-action and UI-context work:
  [`participant-action-system.md`](participant-action-system.md);
- durable control/action model:
  [`../../concepts/engine-mental-model.md`](../../concepts/engine-mental-model.md).

Do not add new execution history here.
