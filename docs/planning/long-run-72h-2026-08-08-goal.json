{
  "_comment": [
    "72-hour run, prepared 2026-08-08 on Jon's instruction: 'Restart the 72 hour",
    "goal when you get a chance before you stop, and have it continue doing what",
    "you are doing, integrating the new SR code in",
    "untracked/ambition-twintrack-relativity-festival-overlay-2026-08-06-1538.zip,",
    "and then working on docs/planning starting with the new monolith crate",
    "decomposition task, which takes priority after your current review task, the",
    "SR unpack task, and the monolith decomp.'",
    "",
    "The deadline below is an ABSOLUTE wall-clock stamp -- re-arming after a",
    "session is replaced does NOT restart the 72 hours, which is deliberate. Only",
    "Jon ends this run.",
    "",
    "The lane order is Jon's, verbatim: A the review campaign already in flight, B",
    "the SR/TwinTrack overlay, C the actor-monolith decomposition, D everything",
    "else in docs/planning. A1-A4 landed before this run was armed; A5 is the",
    "architecture headline, because combat participation turns out to have TWO",
    "proxies rather than the one the review named.",
    "",
    "The ledger check names THIS run's spine. Refill it rather than letting it",
    "empty: an empty ledger means the next item has not been written down.",
    "",
    "Check 5 is new and it is the one that would have caught today's red. The",
    "rollback wire format is a ratchet with a baseline nobody regenerates",
    "automatically, and a registration added without recording it leaves the",
    "contracts job red for whoever runs it next. It is an EXIT CODE on a real",
    "artifact, not a doc marker."
  ],
  "goal": "[STATUS 2026-08-08, PREPENDED — the lane text below is the ORIGINAL CHARTER and parts of it are now stale; it is kept verbatim rather than rewritten. LANE A IS DISCHARGED: A5 landed (ActiveCombatant replaces BOTH proxies — CombatStanding::of takes the marker, and the stand-down guard's comment records that it was Has<MatchSeat>), A6, A7 and A8 likewise; the write-up is docs/planning/review-gpt56-through-43373f7.md, 298 lines. LANE B IS DISCHARGED (ac0bf991c, verdict in twintrack-overlay-merge-2026-08-08.md). LANE C is measured out and blocked on the maintainer. READ docs/planning/queue-72h-2026-08-08.md for what is actually open — this description is not the ledger.] Work continuously for 72 hours on docs/planning/queue-72h-2026-08-08.md, in Jon's lane order. A: FINISH the GPT 5.6 review campaign through 43373f72d (plan: ~/.claude/plans/lively-drifting-conway.md). A1-A4 landed (conversation instance identity, the narrative-input ledger, presentation attachment, every gameplay-bearing Yarn command routed through it). Remaining: A5 ONE authority for combat participation -- an explicit component on the body, Jon's call, replacing BOTH current proxies (RulesetOwnsDeath in CombatStanding::of AND Has<MatchSeat> in the stand-down guard), so that 'active combatant, human controlled, socially non-hostile, damageable, able to attack' becomes representable; A6 a RULE not a type against new ParticipantId==PlayerSlot arithmetic, plus one tracks.md row for the missing SessionSeatId/ControlChannelId; A7 the duplicate asset-root default in scripts/regen_music_registry.py and a provenance trim limited to files this campaign already edited; A8 docs/planning/review-gpt56-through-43373f7.md, including the three things the review did not know (ordinary play is a GGRS host, PendingChallenge was not rollback state, combat participation had two proxies). B: the SR/TwinTrack overlay in untracked/ambition-twintrack-relativity-festival-overlay-2026-08-06-1538.zip. It is stamped 2026-08-06 and main has moved, so it is a MERGE and not an unpack -- DIFF all 30 paths first and write the verdict down before writing any file. Four of them (rollback/registry.rs, tests/rollback_schema_baseline.txt, docs/planning/tracks.md, slice-evidence/rollback-schema-baseline.json) have changed since the zip was cut, and overwriting them silently reverts landed work. C: docs/planning/engine/actor-monolith-decomposition.md -- re-measure it against the tree first, then carve by DEPENDENCY DIRECTION rather than line count, writing each carve's accounting into the doc BEFORE moving a file. The conversation module already carries one, and it predates the ledger, so re-derive it. D: ONCE LANES A-C ARE DISCHARGED, EVERYTHING ELSE IN docs/planning IS IN SCOPE -- read the roadmap, vision, status, tracks and the engine design docs, pick the item that makes the next ten cheapest, and add it to the spine as a row WITH its reasoning before working it. Do not invent work outside docs/planning. Keep awaiting-maintainer-decision.md ANSWERABLE and notice when a row has become answerable without Jon; Jon's own observations file outranks inferred work. BEFORE WORKING ANY OPEN ROW, grep for the thing it says is missing: the commonest staleness is an open row on work that already landed. THERE IS NO COMPLETION CONDITION -- only Jon ends this run.",
  "deadline_utc": "2026-08-11T01:00:00Z",
  "max_run_hours": 74,
  "max_stalled_blocks": 40,
  "checks": [
    {
      "name": "the ledger has no open rows (refill it rather than stopping)",
      "cmd": "! grep -q '▢' docs/planning/queue-72h-2026-08-08.md",
      "timeout": 60
    },
    {
      "name": "the app compiles (the gate, not a per-crate check)",
      "cmd": "$HOME/.cargo/bin/cargo check -p ambition_app --quiet",
      "timeout": 900
    },
    {
      "name": "the architectural absence contracts hold",
      "cmd": "python3 scripts/check_absence_contracts.py --check",
      "timeout": 300
    },
    {
      "name": "the app integration suite is green",
      "cmd": "$HOME/.cargo/bin/cargo test -p ambition_app --test app_it --quiet",
      "timeout": 1800
    },
    {
      "name": "nothing is left uncommitted",
      "cmd": "test -z \"$(git status --porcelain -- ':!.llm_resource_tally' ':!.goal' ':!untracked' ':!tools/ambition_sprite2d_renderer' ':!tools/ambition_music_renderer' ':!ambition-agent-source-*' ':!*.zip' ':!docs/planning/gpt-diff.patch' ':!assets/manual-art')\"",
      "timeout": 60
    }
  ]
}