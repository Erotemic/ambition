# Maintainer decisions

This file records decisions Jon made explicitly. It exists to distinguish
maintainer intent from agent analysis, consensus drafts, and inferred design.
Agent-written records may explain a decision, but they do not become Jon's
decisions unless they are represented here or Jon says so directly.

Confidence is Jon's current confidence in the decision, not a permanence promise:

- **High** — proceed on this basis; do not reopen without new concrete evidence.
- **Medium** — current direction, but implementation may reveal a better shape.
- **Low** — tentative preference or deliberately deferred naming/design choice.

Do not backfill confidence for older decisions by guessing. Add or revise a row
when Jon states a decision or changes his confidence.

| Date | Decision | Confidence | Notes |
|---|---|---:|---|
| 2026-07-16 | Perform the identified content evictions. | High | Each eviction must end in an open provider-owned catalog, registration, or presentation seam rather than moving a closed engine-owned table. |
| 2026-07-16 | Extract the reusable programmatic simulation surface as `ambition_sim_harness`. | High | Reset/step, typed actions and observations, headless testing, RL, replay, and fuzzing belong below `ambition_app`. |
| 2026-07-16 | Extract the platformer-provider lifecycle from the `ambition_platformer2d` facade and consolidate the repeated provider protocol. | High | Exact crate name remains open; `ambition_platformer2d_provider` is the working name. |
| 2026-07-16 | Keep cutscenes and encounters as separate domain systems. | High | Cutscenes are scripted with limited interaction; encounters are interactive with limited scripting. Shared micro-primitives are allowed only when naturally demonstrated. |
| 2026-07-16 | Keep provider registration explicit in the host composition root. | High | The explicit dependency plus plugin registration is intentional; do not add opaque plugin discovery. |
| 2026-07-16 | Defer any boss crate carve until boss behavior converges onto the canonical moveset/action path. | High | Reassess afterward whether a separate boss crate still exists as a coherent subsystem. |
| 2026-07-16 | Reject the proposed named-content scanner and stop adding poison-test ceremony by default. | High | Prefer Rust types, APIs, crate boundaries, visibility, and behavioral tests. A new policy test must justify why those cannot enforce the invariant. |
| 2026-07-16 | Keep the compiler term **lowering** for authored world IR becoming live ECS state. | High | Deserialization/import produces the IR; lowering materializes its canonical runtime representation. |
| 2026-07-16 | Repository-wide knowledge-base hygiene checks are CI/maintainer tools, not routine local validation. | High | Agent-facing docs should not attach them to ordinary code changes. |
| 2026-07-16 | Preserve historical journals as historical records during documentation cleanup. | High | Do not rewrite old journals merely to modernize present-day guidance. |
| 2026-07-16 | A full rename of `ambition_platformer2d_actor_monolith/src/features/` may be worthwhile, but the name `sim` is not settled and the work is low priority. | Low | Do not perform a partial rename or let naming block architectural work. |
| 2026-07-29 | **Label occlusion / transition nameplates are LOW PRIORITY** — not touched until combat is good. | Low | Jon: *"This is more of a debugging feature… a real game probably doesn't use it in this capacity. It's more like there's a door, and the player knows where it goes based on a map."* Engine SUGAR to make a game define its own presentation is welcome; the always-on nameplate is a dev affordance. Queue Z′13 / AC12 stay open at low priority; the two OCCLUSION defects (AB1, blink_run) were fixed anyway because a covered protagonist is not a naming question. |
| 2026-07-29 | **Do NOT gate or redesign `Interact` yet — it needs a design discussion.** | High | Jon: *"A smash game (versus) shouldn't have the concept of 'interact', in fact neither does Mary-O, or Sanic. Only Ambition has an 'interact'… it's something the ambition game might want, and its general to that game, but maybe not to the engine. There is certainly an architecture issue here, and I don't want to make a rash decision and introduce a worse abstraction."* So queue Z′4 is NOT a prompt-gating task: the real question is whether `interact` belongs to the ENGINE at all or to Ambition-the-game. Blocked on that discussion, deliberately. |
| 2026-07-29 | **Build the portrait target → art resolver.** | High | Jon: *"the resolver makes sense. The portraits are not a hub feature… a character portrait for a dialog box is ubiquitous for platformer 2d games, so it makes sense the engine has a mechanism to make it easy, although like most things with the engine, it should always be possible to ignore some part of it and roll your own. This is the Bevy ECS way."* So: engine mechanism, opt-out by construction — not a required path. Queue Y″6. |
| 2026-07-29 | **Replace the invented lab dummy enemies with real ones that already exist.** | High | Jon: *"The lab striker was an agent invention. I asked it to just use a goblin as the enemy there until I decided what I wanted to do about the Nazis… Just replace those enemies with a real enemy that already exist. The entire intro sequence is unpolished slop anyway."* Done: `Puppy Slug` / `Salvage Guard` / `Lab Raider`, each keeping its original brain so the room still teaches patrol / guard / striker. Queue AB5. |
| 2026-07-29 | **DI matters — Smash-style physics is wanted in Ambition itself, not only in versus.** | High | Jon: *"In smash DI is critical! I probably want to use some smash physics in ambition itself too, because I want that game to feel like a cross between smash subspace emissary and hollow knight."* ⚠ he did not name a NUMBER, so the exact `di_max_angle` remains a feel value for him; the decision recorded here is that DI is on and that Smash physics is a direction for the flagship game, not a versus-only affordance. Queue F0-J1. |
| 2026-07-29 | **Generic versus ends on HEALTH. Smash Siblings is a separate, specified mode.** | High | Jon: *"For a generic 'versus' fighting proof of concept I don't care. Probably use health, to make it a generic fighter. For Smash Siblings 3 stock, no items… character select screen, ability to have 1-4 players, have them toggle between real player or cpu, use a smash like, drag an orb onto your character to select, and then the fight boots into a single battlefield like 3 platform level. Its 3 stocks, and then when the game ends it goes back to the character select screen. We don't need items in a first pass."* So `DeathPolicy::Unbounded` stays uncalled for versus. Queue F0-J2. |
| 2026-07-29 | **Smash Siblings HUD: per-character portrait, stock icons, percentage. No score.** | High | Jon: *"A smash game would have a character portrait on the bottom for each character with an icon for each stock and their current percentage. There is no score, when you lose your stock you are dead."* This answers Z′10 by making it moot: the reserved-surround branch was about placing a SCOREBOARD, and there is no score. |
| 2026-07-31 | **Each robot version is a DIFFERENT CHARACTER. Intended.** | High | Jon: *"I did rule on the robot lineage btw, each robot version is a different character. Intended."* This closes the product half of queue AF4b: `player_robot_v2` and `player_robot_v3` are not two renderings of one identity, so version-specific facts (display name, sheet, dialogue, physical properties) are legitimately per-character CONTENT rather than a lineage table's to override, and introducing v4 cannot mutate or delete v3 because it is a new character and not a new value. What the ruling does NOT decide is per-field precedence when `player_robot_lineage.rs` and `character_catalog.ron` both author the same version — that stays an ordinary duplicate-authority question (Rust owns reusable lineage COMPOSITION, content owns the facts), and it is not blocked on Jon. |
| 2026-08-06 | **Input FILTERING is keyed to the PAD; BINDINGS stay machine-wide.** | High | Jon picked *"Filtering per pad, bindings shared"* over full per-seat profiles and over the status quo. Deadzones and trigger thresholds are a fact about the CONTROLLER, not the person holding it, and `ControllerProfileId` already carries the per-pad calibration tables (Xbox360 = wider deadzones + worn-trigger hysteresis; PlayStation = tighter). What this fixes: every couch seat currently reads the one global `user_settings.controls`, so player two's drifty 360 pad runs on player one's DualSense-tuned deadzone (`populate_secondary_slot_controls` passes `&user_settings.controls`). What it does NOT authorize: per-seat presets or per-seat binding overrides — those stay machine-wide until somebody asks, so PA1's "per-seat presets" item is answered by narrowing rather than by building. This closes D2. |
| 2026-08-06 | **Dialogue claims only the TALKER's input by default. It no longer stops the world.** | High | Jon picked *"Build it, default to per-seat"* — ⚠ NOT the recommended option, which was to build the policy and keep today's stop-the-world default. So this is a deliberate change to how every existing dialogue scene plays: NPCs keep patrolling and hazards keep ticking during a conversation, and on a couch one player talking no longer freezes the other mid-jump. The engine half was already settled by his 2026-08-03 ruling (*"dialogue should have the option to stop the world. I'm not decided on what I want it to do in game"*) — both must be expressible, so this decides the DEFAULT and leaves the per-experience opt-in intact. Mechanism: `GameMode::Dialogue` leaves the suspend set (`apply_suspended_time_scale_system` / `gameplay_suspended`), and `DIALOGUE_CONTEXT` — already declared by `declare_in_session_input_contexts` — becomes the whole gate. ⚠ `RoomTransition` and `Cutscene` are explicitly NOT the same question and stay global. This closes D4. |
| 2026-08-06 | **A conversation is SUSTAINED, not modal: it breaks on damage or separation, holds its participants if they are capable of it, and barks when it breaks.** | High | Jon, verbatim: *"I think I do want time to not stop when you are in dialog. So if you get hit in dialog, dialog needs to be interrupted I think. Or say you are falling and you talk to a the flying parrot, if you fall away from them dialog should also break, but if both character are capable of flying and hoverying and you stop to talk, then both characters should hover so they can have the dialog. A broken dialog can have some bark to indicate that it was broken."* This is what the D4 row above COSTS, and it supersedes its shape: once the world keeps running, a conversation has to keep being POSSIBLE. ⭐ the hover rule is stated from the ACTORS' side and symmetrically — "both characters should hover", not "the NPC waits for the player" — so any implementation that asks whether the PLAYER can still talk has already got it wrong. Design + the open sub-questions in `engine/dialogue-continuity.md`. ⛔ the one genuinely missing authority is "can this body hold station": `can_fly` today exists only as a SMASH brain tuning field. |
| 2026-08-06 | **Any seat may pause, and the seat that paused drives the menu.** | High | Jon picked *"Any seat pauses and navigates"*. ⚠ the handoff described today's behaviour wrongly: it is not "any seat pauses, primary navigates" — the pause menu reads `MenuControlFrame`, which `populate_menu_control_frame_from_actions` fills from the PRIMARY seat ALONE, so player two can neither open the pause menu nor move its cursor. The per-seat channel `SeatMenuFrames` already exists and the smash select screen already uses it. The world still stops for everyone; what becomes per-seat is who OPENED the menu and therefore who drives it, which is the one piece of new state. This closes D5. |
| 2026-07-30 | Defer the `bevy_ggrs` patch-table leak; revisit once upstream merges the `GgrsFrameTiming` accessor to crates.io. | High | The 2026-07-30 blind-agent run ranked this the highest-cost finding in the API campaign — a third party cannot compile the engine without copying `[patch.crates-io]` out of the workspace root, and it blocks before any API question. Deferred anyway, because the only way to drop the fork is the parallel accumulator `sample_ggrs_accumulator_phase` already rejected (it diverges during run-slow catch-up, stalls and rollback resimulation). Stating the rule IS done — `docs/sdk/README.md` carries the required entry and the pinned rev. Removing the need waits on upstream. Do NOT let this block slice B. |
| 2026-08-08 | **YES — a game may compose this engine WITHOUT a given capability. Capabilities are OPTIONAL.** | High | Jon, verbatim: *"it's a resounding yes on compose without capability. I think the portal crate is close to that. but we need to carefully design it. I don't want it to add overhead. it should add conceptual clarity."* This upgrades his 2026-08-08 provisional *"I think… yes, but I don't entirely understand the question yet"* to a decision, and selects option **(a)** — a movement-only game should not inherit the fifteen capability crates it never asked for. ⛔ **the two constraints are the whole design brief and they exclude the obvious implementation**: *no overhead* and *it should add conceptual clarity* rule out sprinkling `#[cfg]` through the interact dispatch, the input capture and ten `rollback/domains/*.rs` registrations until the number moves. A feature seam has to be a place the engine genuinely divides, not a compile switch bolted across one. ⭐ **and he named the exemplar: the portal crate is "close to that"** — so the first move is to read what `ambition_portal2d*` already does right and generalise THAT, rather than designing optionality from scratch. ⚠ **what this does NOT settle**: whether any given capability (dialogue, cutscenes, menus) is one of the divisions, or the ORDER. It settles that the answer to "may a game go without" is yes. ⚠ and it is orthogonal to carving — the measurement that broadened this question showed **no carve, of anything, moves the footprint number**; only `optional = true` does, in the runtime as much as in the monolith. |
| 2026-08-08 | **AMENDMENT to the bbox route: the target figure height is an AUTHORED number with a per-character override — not a compiled constant.** | High | Jon agreed to this framing when he asked whether the bbox design is *"an authored number to tweak… not a compiled one"*: **the collapse must remove the OBLIGATION to compensate, not the ABILITY to deviate.** ⛔ so "all 116 `collision_scale` values collapse to ONE global constant" must not be implemented as a Rust `const` — it lives where the sheets do, tweakable without a rebuild, and a character may still state a deliberate figure height. ⭐ **the evidence that the override is REQUIRED and not merely nice**: across 733 generated sheets the authored scales fan out to 1.0 (52 sheets), 1.05, 1.12, 1.5, 1.65, 1.68, 1.78, 1.8, 1.85, 2.0, 2.15, 2.4. Most of that spread is padding compensation the route deletes — but not all of it. The player robot v3 is deliberately **chibi**, which is a low figure height stated on purpose; collapsing to a single constant with no override would silently retune it to everyone else. ⚠ **authoring surface**: `collision_scale` lives in each character's `..._spritesheet.yaml`; the `.ron` is auto-emitted and says so in its first line, so **a RON edit is discarded on regeneration**. The new global belongs on the same authored side, and `body_pixel_bbox` stays what it is today — MEASURED from the art by the generator, never hand-written. ⭐ the practical test for the implementation: after the change, a sheet that wants ordinary proportions should author NOTHING, and Jon should be able to move every character's size together by editing one value and re-running the generator — no `cargo build`. |
| 2026-08-08 | **TAKE THE BBOX ROUTE — size and crop the character quad from `body_pixel_bbox`, not the padded frame.** | High | Jon, verbatim: *"I think the bbox route sounds ok."* ⚠ **recorded as a softer yes than the capability ruling the same day** (*"a resounding yes"*), so it authorises the route without authorising haste. What it decides: `collision_scale` is a reciprocal-of-padding fudge — `collision_scale = target / fill` — so with `fill = 1.0` by construction all **116 authored values collapse to ONE global constant**, the target figure height. That is why answering this DELETES an authored field rather than retuning it, and why two of his reports (*"the Hall characters are inconsistent sizes"*, *"the snake's sprite does not match its box"*) are one decision: 13 scientist sheets render at figure height 0.84 while 33 high-fill sheets render at 1.42 — two populations of people in one Hall, **1.69x apart**. ⛔ **FOUR coupled sites, and site 2 is load-bearing**: (1) `sheets/geometry.rs:25` height off the constant, width off `bbox.w / bbox.h`; (2) the sprite must draw the bbox **SUB-RECT** — without it, `Sprite::custom_size` scales the whole padded frame into the smaller quad and the art stretches per-axis, measured first-try as a **2.20x vertical squash on the snake** and a **0.65x horizontal squash on Mary-O**, both matching the arithmetic to measurement error; (3) `feet_anchor_norm`, frame-normalised; (4) **`sprite_offset`** (`rendering/actors/mod.rs:345,:771`) — the site list was an undercount, and it is the one that actually breaks the snake and Mary-O, who take the `sprite_offset` branch and never read `feet_anchor_norm` at all. ⚠ **doing fewer than all four is the stretched-sprite failure this repo has already paid for.** ⭐ **it also has a second, independent motivation from Jon that nobody had connected**: his untriaged note that the player V3 collision box is larger than the sprite. Measured — visible art 25.5x35.4 world units against a 30x48 collision, so **1.18x wide and 1.36x tall** — and one scalar cannot fix it, because the art's aspect (0.720) differs from the collision's (0.625). The bbox route gives the two degrees of freedom that a single `collision_scale` cannot. |
| 2026-08-08 | **Run a MUCH smaller suite on a docs-only change. Bias toward running fewer tests.** | High | Jon, verbatim: *"I want to bias towards running less tests to balance out the agent urge to run more. So yes run a much much smaller suite for docs only changes. We will catch regressions eventually."* ⭐ **the WHY is a correction to agent behaviour, not a speed optimisation** — the default pull is to run more, and this is a deliberate counterweight. ⚠ **he accepted the trade I flagged**: the failure mode of a smaller gate is a regression caught later rather than at the turn that caused it, and *"we will catch regressions eventually"* is that cost being taken knowingly. So an implementation must NOT hedge it back — a "small" suite that quietly stays large is disobeying the ruling. Measured basis: `app_it` is **94.6%** of all Stop-check time (158.4 s of 161.1 s), `cargo check` is 0.5 s warm, and 114 checks in 11h17m was ≈5 h of suite, most of it on turns that edited only prose. ⛔ **shape**: whitelist, not blacklist — anything outside `docs/` forces the full suite, so a generated file, a `.ron` the tests read, or a submodule pointer can never be mistaken for prose. The failure mode of the RULE is a slower turn; the failure mode of the SUITE is a later catch, and he owns the second one. |
| 2026-08-08 | **"Declared no abilities" does NOT mean "inherit the dev kit". Character capabilities are AUTHORED, EXPLICIT and COMPOSABLE.** | High | Jon, verbatim: *"declared no abilities probably does not mean give it the dev kit. The capabilities of a character in a context should likely be authored and explicit and composable."* ⚠ *"probably"* / *"likely"* — a direction, not a mandate to sweep tomorrow. This selects option **(a)** over the current behaviour and then goes further than the fork offered: the fork asked *what should absence mean*; his answer is that **capability should not be resolved by absence at all**. ⛔ so today's `character_catalog.ability_set(..).unwrap_or_else(\|\| editable_abilities.as_engine())` is wrong in its SHAPE, not merely in its default — a fallback is an inheritance rule, and he asked for authorship. ⭐ **"in a context" and "composable" are the load-bearing words**: the same character may be capable of different things in the sandbox than in Mary-O, and the answer should COMPOSE (an intrinsic body set, plus an explicit session/dev grant or restriction) rather than one table shadowing another. That is the same shape the decomposition brief asked the character-preparation seam to support. ⚠ **this supersedes the narrow question but NOT his Sanic ruling** — whether Sanic keeps blink stays a product call about one character (`JONS_OBSERVATIONS_BUGS_AND_ISSUES.md`, *"say the word either way"*); this decides how any character's kit is DECIDED, not what Sanic's kit is. |
| 2026-08-08 | **The Perfect Cellular Automaton CAN fly.** | High | Jon, verbatim: *"pca should have the ability to fly."* Closes the open question directly. ⚠ note this is a CAPABILITY ruling and lands the same day as *"capabilities of a character in a context should be authored and explicit and composable"* — so flight belongs in the PCA's authored kit, not granted by a brain tuning field or inherited from a shared set. ⚠ it also touches the open duel rule *"can a flying fighter shield?"*, which he has NOT answered; a flying PCA makes that question live rather than hypothetical. |
| 2026-08-08 | **Sanic does NOT have blink.** | High | Jon, verbatim: *"sanic should not have blink."* Answers his own long-standing note (`JONS_OBSERVATIONS_BUGS_AND_ISSUES.md`: *"whether Sanic should keep blink is product, not repair — say the word either way"*). ⭐ **and it is consistent with the capability ruling the same day**: today Sanic gets blink by INHERITING the dev `EditableAbilitySet` because he declares no intrinsic abilities, which is exactly the resolve-by-absence shape Jon rejected. So this is not a special case to hard-code — Sanic's kit becomes authored, and blink is simply not in it. Doing it as a one-off exception would re-create the defect. |
| 2026-08-08 | **Hitstun needs a REDESIGN in its own session, not a tweak.** | High | Jon, verbatim: *"I think hit stun is pretty messed up right now so we probably need to redesign it it's own session."* This supersedes the narrow open question (*"hitstun keeps ATTACK and strips RECOVERY — right for a body knocked OFF the stage?"*) by rejecting its premise: the question assumed the model was sound and one arm was miscalibrated. ⛔ **so do not answer the narrow question or tune the arm** — it is scoped work awaiting a session, and a patch now would be tuning a model he has said is wrong. |
| 2026-08-08 | **The three named robot heavies are DEPRIORITISED, not decided.** | Low | Jon, verbatim: *"I don't remember what the robot heavy variants are. They likely are not important."* ⚠ **recorded as what it is** — not a ruling that they are variant art, and not a ruling that they are characters. He does not have the context and does not consider it worth acquiring. ⭐ **the actionable half is mine, not his**: a question the maintainer cannot recognise is a question I wrote badly. If this matters later it comes back naming the CHARACTERS and what changes for a player, not the casting abstraction. |
| 2026-08-08 | **⭐⭐ THE ROLLBACK WIRE FORMAT IS UNSTABLE BY POLICY. The latest build is compatible with itself and nothing else. STOP ASKING.** | High | Jon, verbatim: *"I'm not concerned with saved replays and net play right now. We need to maintain no backwards compatibility there and we can say that the latest build is only compatible with itself. Much later in the project we will change this decision and stabilize the rollback format, but there is no restriction on that right now and there is no need to persist any replays or have any that play at all. In fact we don't have any net play yet. Agents have asked me this question in different variants many times and it's not worth perseverating on."* ⛔ **so rename crates freely, change registrations freely, bump the schema version and move on** — no migration, no compatibility shim, no deferral of a good change to protect a replay that does not exist. ⚠ **the standing cost this removes**: several open questions and at least one whole campaign have been shaped by protecting a wire format nobody consumes. ⭐ **and he named a process problem, not just a technical one** — *"agents have asked me this question in different variants many times"*. It needs one clear document, and **explicitly NOT `AGENTS.md`**: *"it's too specific to bloat up that document."* |
| 2026-08-08 | **Portal orientation is AUTHORED per portal, in LDtk. No global setting. Rotation is the default; scale inversion is opt-in and may be unsupported per game.** | High | Jon, verbatim: *"portal orientation should be explicitly authored, and there should be a nice way to do that in ldtk. There should not be any global setting of orientation. The default authoring should be the standard rotation, the author would need to opt into scale inversion, and it's likely games might not even support that."* ⭐ **his reasoning is the design, and it is worth keeping**: *"the sort of changes the world you come out into, or it changes yourself. With the rotation convention we don't have that problem."* — i.e. mirror/inversion semantics force a question about WHOSE frame changed, and rotation does not. ⛔ this kills `static PORTAL_MAP_ROTATION: AtomicBool` outright rather than relocating it: the answer is not "a better owner for the global" but "per-portal authored data with a default". ⚠ **and it does NOT close scale**: *"changing world scale or player scale with portal size is still something I'm interested in exploring. Particularly having a big portal go into a smaller portal."* So the authoring surface must leave room for a size relationship even though inversion is opt-in and rotation is the default. |
| 2026-08-08 | **It is FINE that 1-1's first `?`-block drops its wand into a pit.** | Low | Jon, verbatim: *"it's fine if the wand drops into a pit."* Not a bug; closed. |
| 2026-08-08 | **You SHOULD be able to hit GNU-ton during its special.** ⚠ and the boss's authoring is stale generally — not worth deep work unless it is a symptom of an architecture problem. | High | Jon, verbatim: *"I don't see a reason why you shouldn't be able to hit gnu Ton during its special. I think the authoring on that boss has gotten pretty out of date and it's inconsistent. The boss currently doesn't play very well. Not that it ever really played very well. But it's not something I'm interested in digging into too deeply unless it's just a symptom of a deeper architecture issue we actually are interested in solving here."* ⭐ **this answers the narrow question by rejecting the premise it shared with the art.** The open question was whether `apple_rain` should sample the authored `head_down` hurtbox (hittable only on a head-sized box, ~5% of the frame) or keep the body-box fallback. His answer is the fallback's BEHAVIOUR, reached from feel rather than from the sheet: hitting the boss should work. ⚠ **so do not "honour the art" here** — the descending head hurtbox the author drew is exactly what he is declining. ⚠ **and the scope fence is explicit**: fix it if it falls out of an architecture problem worth solving; do not open a GNU-ton authoring campaign on its own. |
| 2026-08-08 | **The world keeps living while you die — but it must KNOW you are dying and stop attacking you.** | High | Jon, verbatim: *"the world can live while you die. We just have to be careful that, the world understands that you are dying and doesn't try to keep killing you or whatever."* So: **no freeze**, consistent with his per-seat dialogue ruling — on a couch, one player dying must not stop the other. ⭐ **the caveat is the actual work, and the mechanism landed TODAY**: a body that cannot be hurt any more is already expressible as `DamageableVolumes::intangible()` (published-and-empty), and `body_is_corpse` is the one "is this an intangible corpse" rule. D23 and D25 made melee, hostile projectiles and the player-faction hit predictor all honour it. ⚠ **so the death beat needs the dying body to publish intangibility for the dwell**, not a new concept — and the open question is whether "dying" (mid-beat) and "dead" (corpse) should be the same state to the damage path. ⛔ what he is NOT asking for is enemies freezing; they keep patrolling, they just stop landing hits on a corpse. |
| 2026-08-08 | **YES — a crawler's collision volume orients with its attachment, and it is the SAME concept as a body under different gravity.** | High | Jon, verbatim: *"yes a crawler does need orient with its attachment. It's similar to how another body would Orient under different gravity."* ⭐⭐ **the second sentence is the design instruction and it should decide the implementation**: the engine already has gravity-relative orientation (feet = the +gravity face; `body_extents_for` already swaps extents on a vertical face, which is what fixed the slug's MOTION). ⛔ **so this is not a new adhesive-crawler special case — it is the existing frame concept reaching the CONTACT path, which was left in world axes while motion moved to the surface frame.** An implementation that adds a crawler-specific rotation has misread him. This closes the long-deferred F5 (open by his call since 2026-07-29) and the adhesive-crawler contact bug together. |
| 2026-08-08 | **Make most of the Hall cast dormant.** | Medium | Jon, verbatim: *"yes probably make most of the cast in the hall dormant especially if that reduces lag."* ⚠ **stronger than the standing recommendation**, which was *bosses yes, Hall wait* pending a frame profile. He is authorising the Hall too — 129 `NpcSpawn`s, all `stand_still`, all ticking a no-op brain. ⚠ **"especially if that reduces lag" is a condition, not decoration**: measure the frame cost before and after, and if it does not move, say so rather than banking an unmeasured win. ⭐ safe by construction for these: `tick_npc_idle_barks` carries no dormancy filter, so a sleeping statue keeps its voice. |
| 2026-08-08 | **Defer all GNU-ton work.** | Low | Jon, verbatim: *"just defer any gnu ton action for now it's probably not important."* ⚠ this REPLACES the ruling above it in scope, not in content: he still says you should be able to hit the boss during its special, but nothing is to be done about it now. ⛔ so the apple_rain hurtbox stays as it is; do not "honour the art", and do not open the authoring cleanup either. |
| 2026-08-08 | **⭐ MARK staleness where it is found, with EVIDENCE. Sweep later, separately.** | High | Jon, verbatim: *"we probably need something that starts off by marking which items in planning documents are actually stale or marking entire documents as stale in case we make a mistake. They can record the evidence that they are stale and then we can make a sweep up later. It also prevents people from reading them and taking them as up to date in the meantime."* ⭐⭐ **this reframes the long-open "which of the 33 engine design documents have become history" question and is a better answer than it asked for.** That question wanted a verdict — archive or keep — from *"someone who knows what is live"*, which is why it sat unanswered: nobody has that whole picture at once. Marking needs only LOCAL knowledge: whoever trips over a stale claim records it, right there, with what they saw. **Four properties he named, each load-bearing:** (1) **two granularities** — an item or a whole document; (2) **evidence, not just a flag** — a mark says WHY it is believed stale, so the eventual sweep is reviewable rather than trusted; (3) **non-destructive** — *"in case we make a mistake"*, so nothing is deleted on one reader's say-so; (4) **it protects READERS in the meantime**, which is the real cost of staleness — this repo's docs are believed, and today alone a stale doc claim and a stale section heading each nearly drove a wrong decision. ⛔ **the mark must be visible at the point of reading** — top of the document, or inline at the item. A separate index of stale things does not stop anyone reading the thing. |
| 2026-08-08 | **(b) Fix the one site; leave the authored-id rule to prose. Revisit the refactor only if it becomes important.** | Medium | Jon, verbatim: *"Sure b for authored-id collisions. We can revisit the refactor later if it becomes important."* So `spawn_split_offspring` stops minting `SimId::placement(..)` — the AUTHORED namespace — for a runtime spawn, and mints `SimId::spawned(parent, n)` like every other runtime spawner. ⛔ **the ~70-site `PlacementId` newtype is NOT authorised** and should not be started as a "while we're here". ⭐ **the framing that got there**: spawning an authored THING is fine and useful — copying an authored enemy ten times is ordinary. Sharing its IDENTITY is the bug, because rollback matches a snapshot back to an entity by `SimId` and two entities holding one id are indistinguishable to it. Photocopying a form is fine; the copy carrying the same serial number is not. ⚠ no symptom exists today, and his rollback-format ruling the same day removed the pressure that made this feel urgent. |
| 2026-08-08 | **⚙ OPERATING NOTE: Jon does not have design intuitions about rollback, and has delegated that design to agents.** | High | Jon, verbatim: *"I've left rollback design to agents. I don't have a good design sense on that issue. I've never needed to design something that needs this sort of data structure before. I'll have to learn more about it to give better insight later."* ⛔ **so do not put a rollback DESIGN question to him cold and wait** — that is how the `SessionScopedEntity` row sat unanswered, and how at least two others were framed. ⭐ **what to do instead**: make the call, record the reasoning where he can overrule it, and bring him the ANSWER plus what it costs — not the fork. ⚠ this is scoped to rollback mechanics specifically; it is NOT licence to decide feel, content, product or architecture direction on his behalf, all of which he rules on readily and in detail. ⚠ and it is explicitly temporary — *"I'll have to learn more about it to give better insight later"* — so keep the reasoning legible rather than compressing it into a verdict. |
| 2026-08-10 | **⭐⭐ A CHARACTER IS A REUSABLE AUTHORED TEMPLATE, NOT A SINGLETON PERSON — and the enemy-archetype system is deleted.** (answers queue D48; opened D73, closed 2026-08-13) | High | Jon, verbatim: *"A character is a reusable authored template, not a singleton person … `spawn Goblin` / `spawn Fretjaw` twice are the same engine operation."* ⇒ **D48 resolves as (a) and then goes past it**: the endpoint is ONE character authority, not `CharacterCatalog` for half the facts, `PreparedCharacterDefinition` for another half and `ArchetypeSpec` for a third set reached through a field called `brain`. Two refinements he arrived at from reading the code: (1) ⛔ **do not migrate `ArchetypeSpec` into `CharacterDefinition` wholesale** — it is a god-object holding three authorities (intrinsic body facts, autonomous-controller policy, placement/session policy); split them and delete it; (2) `PreparedCharacterDefinition` is nearly the right endpoint but is still finalised by consulting the catalog and keeps a `HostCode` escape hatch. ⭐ **his emphasis, verbatim**: *"`character_archetypes.ron` is not merely an enemy tuning file to be renamed. The fact that `respawn`, `attacks_player`, body capabilities, AI strategy, and mount semantics all coexist in one row is precisely the evidence that it should be decomposed and then deleted."* ⭐ **and why now**: *"the current tree is unusually favorable … the generic ECS runtime underneath it has already been unified."* ⚠ a character definition MAY name a default autonomous profile — that is authoring ergonomics, not intrinsic identity; possession changes who drives a Goblin, not what a Goblin is. ⛔ **do not stop when the new path works beside the old one.** Full brief, verbatim and durable, in [archived D73 character-template brief](../archive/planning-superseded/2026-08-13/character-template-architecture-2026-08-10.md); eight phases, ~2,437 lines of obvious legacy to delete, 18-point definition of done. |
| 2026-08-13 | **⭐⭐ THERE IS NO SEPARATE "CAN FIGHT" CHARACTER PROPERTY. A character can fight exactly to the extent that its body has abilities/capabilities that can produce combat effects.** | High | Jon, verbatim: *"There is no separate 'can fight' character property. A character can fight exactly to the extent that its body has abilities/capabilities that can produce combat effects."* His decomposition, verbatim: ***Body/character:** owns abilities and moves. If the body has punches, projectiles, damaging specials, etc., those are available.* / ***Controller/brain:** decides whether and when to use those available abilities.* / ***Disposition/relationship/placement:** determines who is considered friendly, hostile, allied, etc.* / ***Ruleset:** may restrict available verbs but does not create combat abilities that the body lacks.* ⛔⛔ **so do NOT introduce a `can_fight`, `combatant`, or peaceful-vs-fighter taxonomy while completing character bodies.** ⭐ **the two consequences he spelled out**: *"a normally peaceful NPC is not inherently 'non-combatant'. If its body has a damaging ability and circumstances/controller policy call for it, it can fight. Conversely, a body with no damaging abilities simply has nothing useful to execute when asked to attack."* ⚠ **this is an ARCHITECTURAL INVARIANT and it supersedes his own Carl-specific phrasing** — he issued it as the general rule after stating the Carl case, explicitly: *"that is the cleaner rule, and it should supersede my Carl-specific phrasing."* It answers the open row *"should seven peaceful characters on the Smash grid be re-authored as fighters"* by rejecting the category: authoring a body's abilities is the only question. |
| 2026-08-13 | **Carl Stargan does NOT fly. He fights — because his BODY has abilities, not because of a fighter flag.** (closes D96 5) | High | Jon, verbatim: *"Carl **does not have the fly ability**. Carl **can fight**. His combat capability is intrinsic to the character/body, not something granted by being controlled or by entering a particular mode. In ordinary gameplay, if Carl is accompanying the controlled body and a hostile mob attacks, his autonomous policy should allow him to help fight back. Author his locomotion and combat capabilities explicitly in the character definition so the body is complete. Do not infer flight from his art, body kind, NPC role, or any legacy archetype. Do not make him permanently peaceful/passive merely because he may normally appear as a friendly NPC. His placement/disposition can be friendly while his body still possesses combat capabilities and his autonomous policy can defend allies."* ⭐ **the split he named**: *"friendly companion is contextual disposition/policy; ability to fight is a body capability; flight is absent."* ⚠ he then generalised it — read the no-`can_fight` invariant row, which he says supersedes this phrasing; only two CONTENT facts survive here: **Carl does not fly**, and **Carl has whatever combat abilities are authored for him**. ⛔ do not add a fly capability to keep an old construction path or test working, and do not invent additional Carl combat capabilities merely to close migration metrics. |
| 2026-08-13 | **Skitters ARE Puppy Slug.** `SmallSkitter` and `under_town_skitter` are authored as `npc_puppy_slug`. (closes 2 of D96 1/3/3b) | High | Jon, verbatim: *"All of the generic 'skitter' concepts were AI-invented and Jon does not care about preserving them as distinct identities. Skitters are Puppy Slug. `SmallSkitter` should resolve to / be authored as `npc_puppy_slug`. `under_town_skitter` should resolve to / be authored as `npc_puppy_slug`. Any other ordinary legacy 'skitter' identity encountered during this migration should preferentially become Puppy Slug rather than preserving a generic skitter archetype, unless there is clearly hand-authored content establishing a genuinely distinct character. Do not create a new Skitter character merely to preserve AI-generated taxonomy. This decision should remove the legacy `medium_striker` / skitter justification where that is its only remaining purpose."* ⚠ **placement/session differences are preserved; the reusable character/body IDENTITY is Puppy Slug.** ⭐ this agrees with what the repo already did once — the proving grounds' `pg_skitter` placement is cast as `npc_puppy_slug` today. |
| 2026-08-13 | **`large_brute` becomes a REAL authored reusable character — a Goblin Brute, with its own Python sprite generator.** (closes the goblin-lab heavy casting call) | High | Jon, verbatim: *"This one SHOULD become a real authored reusable character. Author a distinct Goblin Brute character for `large_brute`. It should be a separate reusable character identity rather than a generic archetype row or an alias for an existing named goblin. It may initially use the same broad goblin visual design/template, scaled/designed as a bigger brute. However, its sprite must have a SEPARATE Python sprite generator/target, even if that generator initially shares helpers or visual vocabulary with the ordinary goblin. The reason for the separate generator is to preserve a clean future path for making the brute visually distinct without having to separate shared assets later. Do not use image-generation tooling. Ambition sprites remain code-generated through the repository's Python sprite pipeline. The brute's initial gameplay/body values may preserve the current `large_brute` behavior closely; exact balance is tunable later. Give it an explicit complete character definition/body rather than retaining `combatant` as an unresolved casting fallback."* ⚠ **pick a durable id consistent with repo naming conventions after checking existing identifiers for collisions.** ⭐ this is the one of the four uncast identifiers that earns a character rather than a deletion. |
| 2026-08-13 | **The dive-drill and its anonymous `Target` may be DELETED. Deletion over architecture for disposable AI-authored content.** | High | Jon, verbatim: *"The dive-drill content is AI-invented placeholder content that Jon does not care about preserving and expects to redesign eventually. Remove the dive-drill and its anonymous Target if doing so simplifies the character/construction migration. Do not create a real character, fixture architecture, compatibility exception, or other permanent machinery merely to preserve it. Delete associated content/tests that exist only to preserve this throwaway authored scenario, adjusting broader tests if necessary to test the underlying engine behavior somewhere more appropriate. Do not let dive-drill block making ordinary authored EnemySpawn character-first or required-character-id work."* ⚠ **the adjustment clause is the careful half**: an engine behaviour that only this scenario exercised gets tested somewhere more appropriate, not dropped with the content. |
| 2026-08-13 | **Previously unauthored body health is TUNING, not a blocked product decision — pick reasonable numbers and AUTHOR them.** (closes D96 7 and D96 8) | High | Jon, verbatim: *"Jon does not regard the exact current numbers as an important product decision. These are ordinary tuning values that can be changed later if they feel wrong in play. Pick reasonable explicit health values and AUTHOR THEM. Do not retain fallback health or incomplete body definitions because we are waiting for balance decisions. Treat health as a body/character fact, not a provocation or controller fact. Provocation/control changes must preserve current body health."* **His initial values, to use unless nearby authored precedent gives a stronger reason** — ordinary humanoid/NPC baseline **4 HP**; ordinary pirate **4 HP**; heavy/large pirate variants **6 HP**; Patent Clerk **6 HP**. ⚠ **the distinction he drew explicitly**: these NUMBERS are initial tuning choices, intentionally easy to retune; the ARCHITECTURAL decision — that they become explicit authored body facts now, owned by the body — is the invariant. ⛔ so an incomplete body definition may no longer be justified by "waiting on balance". |
| 2026-08-13 | **Camera orientation is a per-view observer policy. Preserve the existing world-fixed/external-observer camera and add an optional controlled-body/view-subject-relative mode.** | High | Jon: the existing camera modes remain valid options; depending on the game/context a camera may behave as a distant/external observer or as part of the controlled body's reference frame. In the subject-relative mode, gravity changes should visually rotate the world while the controlled body's local frame stays upright, and movement/aim should use coherent body-relative input semantics. This is presentation/control policy layered over the same simulation — do not add gravity-side camera affordances, do not make the policy process-global, and do not privilege `PrimaryPlayer`. Future multiview may choose a different policy per local view. Exact naming, transition/easing, clamp semantics and user/game/context precedence remain open; see [`engine/camera-reference-frame-policy.md`](engine/camera-reference-frame-policy.md). |
⭐ **the 2026-08-13 block above is one handoff**, given while the Authority
Convergence campaign was running. Read it as a whole: the no-`can_fight`
invariant is the rule, the five content rulings are its application, and Jon
separated them himself — *"distinguish clearly between architectural invariants,
which should persist, and initial tuning choices such as HP, which are
intentionally easy to retune later."* His closing instruction was that these
**unblock the deletion path** and must not be reopened: *"Do not invent
replacement compatibility machinery for rows whose only consumers disappear or
become character-first."*

The fuller multi-agent recon consensus, including accepted campaigns and explicit
non-goals, is in
[the archived 2026-07-16 recon consensus](../archive/planning-superseded/2026-08-13/engine/decisions-2026-07-16.md).

## 2026-08-15 — item/reset semantics: the CHECKPOINT is the reset baseline

⭐⭐ **Jon's decision, recorded verbatim in substance before any work moved on it:**

> Treat **checkpoint state as the reset baseline**, rather than giving key items a
> special "always persist" rule.
>
> - Ordinary traversal/unload preserves current world occurrence/disposition.
> - Death/retry restores the latest committed checkpoint baseline.
> - If an item is picked up after checkpoint C0 and the body dies before another
>   checkpoint, it returns to its C0 location.
> - If acquiring the item commits checkpoint C1, subsequent death preserves that
>   acquisition and the original placement stays empty.
> - A temporary/disposable item picked up after C1 can still revert to its C1
>   baseline on death.

⛔⛔ **DO NOT encode this as `KeyItem => survives reset`.** The persistence of a key
item is a *consequence* of when a checkpoint committed, not a property of the item
kind. An item-kind rule would be a second authority that disagrees with the
checkpoint the moment content changes.

⇒ **so D125 must distinguish THREE horizons**, and this is the architectural
content of the decision:

1. **current occurrence state** — what is true right now;
2. **state committed at the current reset/checkpoint horizon** — what a death
   restores to;
3. **durable save state** — eventually, and still a separate concern.

⭐ **the acceptance fixture Jon named:**

```text
C0 key on pedestal → pick up → die            → key RETURNS to the pedestal
pick up again → commit C1 → die               → key stays acquired, pedestal EMPTY
pick up a temporary item after C1 → die       → key stays acquired, temporary RESETS
```

⚠ that fixture is doing real work: its third line is what makes the rule
*checkpoint-shaped* rather than *item-shaped*. A `KeyItem` special case satisfies
lines one and two and fails line three.

## 2026-08-17 — four rulings given in one handoff

⭐ Recorded verbatim before any work moved on them. Two of these **close ledger
rows that agents had marked closed on their own** (D114, D160), and the pattern
in both is the same: a row claimed a resolution the tree did not carry.

| Date | Decision | Confidence | Notes |
|---|---|---:|---|
| 2026-08-17 | **⭐⭐ KEEP THE LANDED PER-BODY HITLAG FREEZE. The old *"do not reintroduce a per-body zero-dt"* prohibition is SUPERSEDED. D114 is CLOSED.** | High | Jon, verbatim: *"keep the landed fix and overrule the old prohibition. Close the decision. The older 'per-body zero-dt makes AI-vs-AI degenerate' judgement predates D155, when Smash launch/landing behavior was materially broken; the D128 record itself now treats those pre-D155 feel judgements as invalid. More importantly, hitlag is a combat/body semantic, not something that should depend on whether a body happens to occupy the primary local-control road. CPU-vs-CPU now has real exchanges, the match still terminates, and the later capture exercises corrected knockback. Keep sim_dt = 0.0 during that body's hitlag. Mark the old prohibition superseded. If hitlag later feels too sticky, tune its duration/shape rather than restoring a controlled-body/actor asymmetry."* ⭐⭐ **the load-bearing sentence is the SECOND reason, not the first**: *"hitlag is a combat/body semantic, not something that should depend on whether a body happens to occupy the primary local-control road."* ⇒ the asymmetry WAS the defect, and the old row's three options — no extra freeze · a proper-time treatment at the ADR 0011 seam · extend the global 0.125 beat — are all void, because every one of them preserved a per-road distinction. ⛔⛔ **the future repair for a feel complaint is DURATION/SHAPE; reintroducing a controlled-body/actor split is explicitly forbidden.** ⚠ the earlier judgement was not wrong when made — it was formed on a build where every authored launch direction was vertically inverted and a tumbling launch resolved as a landing (D155), i.e. where nobody was knocked anywhere. ⇒ **a feel verdict inherits the build it was formed on, and D155 invalidated every one that predates it.** |
| 2026-08-17 | **⭐⭐ KEEP THE PER-TURN GATE SMALL — `cargo test --workspace --lib` stays OUT of `gate_suite.py` DELIBERATELY.** "Gate" continues to mean an EXECUTABLE gate; the pre-push checklist is a separate validation tier. **D160 CLOSED as an intentional policy choice, and `awaiting-maintainer-decision.md` §9 is ANSWERED.** | High | Jon, verbatim: *"keep the per-turn gate small. Close D160 as an intentional policy choice, not by redefining 'gate.' gate_suite.py should remain the cheap executable per-turn gate; do not add cargo test --workspace --lib to every turn. The workspace lib suite remains a required pre-push/finalization check. Likewise, feature-gated suites should be run when the affected subsystem is touched, not wholesale every turn. 'Gate' should continue to mean an executable gate; the pre-push checklist is a separate validation tier. This is consistent with the August 8 ruling to bias agents toward fewer tests. So §9 is answered: the absence of --workspace --lib from the per-turn gate is deliberate."* ⭐ **THREE TIERS, and naming them is the decision**: (1) the **per-turn executable gate** — `scripts/gate_suite.py`, cheap, and it stays cheap; (2) the **pre-push / finalization check** — `cargo test --workspace --lib`, REQUIRED, and required is not the same as gated; (3) **feature-gated suites** — run when you touch the subsystem, never wholesale. ⛔ **so D160's defect was never the missing line in `gate_suite.py`** — it was a ledger row claiming a gate behaviour that did not exist. ⭐ *"not by redefining 'gate'"* is the instruction to agents: do not relabel a documented obligation AS a gate to make a row true, and do not enlarge the gate to make one true either. ⚠ this extends his 2026-08-08 *"bias towards running less tests… we will catch regressions eventually"* rather than revisiting it. |
| 2026-08-17 | **ACCEPT the CPU showcase's current pacing. Do NOT retune stock count, knockback or damage. D128's pacing/product-acceptance blocker is CLEARED** — D128 stays open for its engineering/presentation defects. | High | Jon, verbatim: *"accept the current pacing for the CPU showcase. The stronger evidence is not merely '180%/124% with 3/3 stocks at 20 seconds': by 2400 ticks, roughly 40 seconds, the match has completed and returned to character select. A three-stock CPU showcase finishing in under ~40 seconds is certainly not too long; if anything it is brisk. Do not retune stock count, knockback, or damage around that partial 20-second frame. Clear the pacing/product-acceptance blocker. Human-vs-human balance can be judged separately later. D128 itself can remain open for its actual engineering/presentation defects—seat-independent respawn placement, remaining presentation issues, standalone Smash asset composition, etc.—but pacing should no longer block it."* ⭐⭐ **he read the evidence better than the row presented it, and that is the reusable lesson**: the row led with a MID-MATCH frame (1200 ticks, 180%/124%, nobody dead) and buried the WHOLE-MATCH fact (2400 ticks, completed, back at CHOOSE YOUR FIGHTER) two paragraphs down — so a partial observation was framed as the finding while the complete one sat underneath it. ⇒ **when a capture sweeps time, the acceptance question is answered by the LAST frame, not the most alarming one.** ⚠ **the scope fence is explicit**: this accepts CPU-showcase pacing ONLY; human-vs-human balance is a separate later judgement, and it is not licence to close D128. |
| 2026-08-17 | **REPLACE the `central_hub_main` developer-note sign — do NOT delete it.** The hub wants an orientation sign; only the authoring-language content was wrong. | High | Jon, verbatim: *"replace it, don't delete it. The hub benefits from an orientation sign; only the authoring-language content is wrong. I would use: `// CENTRAL HUB — ROUTES OUTWARD; BASEMENT ACCESS BELOW`. That fits the established diegetic lab-sign voice, communicates useful navigation, and contains no LDtk/chunk/authoring vocabulary. Make the change through the LDtk authoring tooling, not by editing the JSON directly."* ⭐ **his wording keeps the `//` prefix on purpose** — it is the house style already carried by 42 other signs (`// PERIMETER BREACH — TWO HEADINGS`), the same voice the D161 sweep nearly filed as a defect. ⇒ the discriminator stands: **a dev note talks about the AUTHORING ARTIFACT (LDtk, chunks, levels); diegetic text talks about the FICTION** — and the replacement had to satisfy the second while still being useful navigation. ⭐ **and he named the ROUTE as part of the decision**: through `tools/ambition_ldtk_tools`, never by re-serialising the JSON. ✔ landed on `DebugLabel-0019` in `central_hub_main` via `entity set-field`; `text` is the only player-facing field, so `name` stays an authoring id exactly as in `intro_wake_room`. |
| 2026-08-17 | **Inventory becomes Morrowind-style: the OCCURRENCE owns, and inventory is a SET WITH A COUNT — not a count.** | Medium | Jon, verbatim: *"eventually we are going to switch to a Morrowind style inventory, so the occurrence is the owner, but inventory likely isn't a count it's a set with a count. I suppose it will depend on it the item is unique or not."* ⭐ this answers D125/D132's "which authority owns the nine dual-nature items" and **reframes the other five classes too**: the fix is not "occurrence for nine, count for the rest" but one representation — a set of occurrences that REPORTS a count — with UNIQUENESS deciding whether a stack's members need distinct identities. ⚠ *"eventually"* and *"likely"* are his words: the direction is settled, the schedule and the exact stacking rule are not. ⛔ so do not build the general set-with-count today; the executable slice is still D132's named gate — `OwnedItems` joins the checkpoint baseline and the mint SPENDS the granted row — chosen because it is the step that is right under either shape. |
| 2026-08-17 | **A line count is a PROXY. Decompose where it makes sense, and stop making the monolith worse.** | High | Jon, verbatim: *"like count is a proxy, decompose as it makes sense. try not to dump things into it to make the problem worse."* ⇒ **the compile ratchet is an instrument, not a target**, so D33's re-freeze question is answered by refusing its premise: do not schedule work to move a number, and do not re-freeze to make a tool quiet. Carve when ownership genuinely says so. ⭐ the operative half is the second clause and it is a STANDING rule on every other row: **new work does not land in `ambition_platformer2d_actor_monolith` because that is where the neighbours are.** A feature whose owner is elsewhere goes to its owner even when the monolith is the cheaper edit. |
| 2026-08-17 | **Split-screen layout is ADAPTIVE WITH HYSTERESIS.** | Medium | Jon picked BG3-style — one shared framing while participants are close, splitting into viewports as they separate, with hysteresis so it cannot flap at the boundary. ⇒ this answers the half of decision 11 that was reserved for him (the LAYOUT POLICY), and it settles the engineering fork under it by implication: **duplicate per view** is the only option compatible with a layout that can split at any moment, so `parallax.rs` — which still has no view concept and reads `WINDOW_W`/`WINDOW_H` — must gain one. ⚠ the distance threshold and the hysteresis band are FEEL values he has not named. ⛔ and adaptive layout makes the silent-wrong fallback a real defect: with several cameras, label layout and nameplates currently fall back to a world-origin focus rather than declining to draw. |
| 2026-08-17 | **Sprite sizing: give every scale a SHARED UNIT first, then revisit the quad-from-bbox route.** | High | Jon picked the smaller change over the structural one. Today each `collision_scale` multiplies its OWN sheet's frame size, so the numbers are not comparable across characters — heavies 1.95, other pirates 1.60, `robot` 2.10, and the robot's is the LARGEST while he reads chibi. Making every scale multiply one shared reference unit makes the sizes legible and the mis-tunings obvious **without** committing to the four coupled sites the bbox route needs. ⛔ do NOT delete `collision_scale` in this slice, and ⛔ do not size a quad from the bbox without also cropping the drawn region — that was tried and reverted because it stretches the art. ⇒ three of his reports (snake/AI-slop, Sanic small at home, cove pirates) are the customers; if the shared unit does not settle them, the bbox route comes back with evidence. |

Questions that are WAITING for a decision — scoped far enough that the choice is
real and the work after it is small — are collected in
[`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md). Nothing
there is a decision; a row moves into **one of this file's tables** when one is
made — the dated section it was answered in, or the main table above.
