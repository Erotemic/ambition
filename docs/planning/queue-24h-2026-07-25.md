# Work queue — 2026-07-25

A ranked, exit-criteria'd queue over the live backlog. Jon's instruction
(2026-07-25): **nothing is off the table except the netplay deferral.** This file
exists so a goal can be named and not sidestepped — every item below states what
CLOSES it, in terms of something observable, so "I did an adjacent easier thing"
is visible as a miss.

Sources, all live: [`tracks.md`](tracks.md),
[`demos/super-mary-o.md`](demos/super-mary-o.md), [`demos/sanic.md`](demos/sanic.md),
[`JONS_OBSERVATIONS_BUGS_AND_ISSUES.md`](JONS_OBSERVATIONS_BUGS_AND_ISSUES.md),
[`triage/`](triage/), `dev/journals/code_smells.md`. Nothing here is invented;
this only ranks and sharpens.

**Executor grade.** `[opus]` throughout `docs/planning` means Opus 4.8. This run
is Opus 5 and takes `[fable]`-grade work. No item is scoped, deferred, or
excluded on grounds of difficulty — only on payoff order.

**Standing rule for whoever runs this:** finishing an item is not a stopping
point. The sentence after "committed" starts the next one.

---

## A. The current 24h run — headline chosen by Jon

### A1. "A demo can have two rooms" + Mary-O World 1-2 (≈12h) — **LANDED 2026-07-25**

`c07f20e4c`, `835bade2c`, `b7cad2934`, `08592316f`, `aa911fda3`. Exit met: the
demo's own binary walks 1-1 → 1-2 by playing (pipe verb, then walking to the
shaft, no placement after the vault), poison-tested; and a body standing on
1-2's ferry is carried by it, which is the only way across its chasm.

⚠ **One survey claim in this file was WRONG and is corrected here rather than
edited away:** it said the composer could not live in `ambition_runtime` because
it needs `platformer::lifecycle` and the provider depends on runtime. But
`ambition::platformer` is `ambition_platformer_primitives`, not the provider —
there was no cycle, and `ambition_runtime` is where it went, carried by
`PlatformerEnginePlugins` exactly like §2.5's replay consumer.

Also unpredicted: the engine group now supplies `AmbitionLoadPlugin`, which made
both demo hosts and three test hosts panic on a duplicate plugin. One owner now.

The blocker was never a dependency. It was ONE call: the commit drew the new
room itself (`spawn_room_visuals`), which named `ambition_render`.

| # | Slice | Exit |
|---|---|---|
| A1.1 | ✅ Composer → `ambition_runtime::room_transition`, carried by `PlatformerEnginePlugins`. | Done. |
| A1.2 | ✅ The commit ASKS for the redraw (`RespawnRoomVisualsRequested`) instead of drawing it — the channel the sandbox reset and the room stager already used. That one call was the whole blocker. | Done; `spawn_room_visuals` no longer has a commit-time caller. |
| A1.3 | ✅ Mary-O **World 1-2**, a real second `RoomSpec`, linked both ways. | Done. |
| A1.4 | ✅ The 1-2 ferry, and a proof a body rides it. | Done. |
| A1.5 | ▢ **OPEN** — continuity: score, coins, lives, and worn power across the transition. Assert against the BODY, never the emitter's bookkeeping (triage §3). | The next thing to do in this queue. |

### A2. The transformation moment — **LANDED 2026-07-25**

`730a3d9be`, `1b51b490e`, `65d32b4c3`, `7372e6f68`.
`ambition_actors::features::transform_beat` is the one beat; each game owns only
WHICH identity change counts (stepping up a tier / becoming super — a downgrade
or a loss gets none) and its numbers. The multiplayer clause is structural: the
beat writes a `ClockScaleRequest` and the active `RegimePolicy` answers, so a
regime that cannot afford one participant bending everyone's clock denies it. It
hands the clock back by asking for 1.0 rather than writing it.

Two things the card did not predict: `untouchable` was a flag with no reader
until it was routed through the existing `Health::invulnerable` seam (a second
intangibility mechanism would have been the bifurcation), and Sanic's body is
ALREADY invulnerable when his beat starts — so the beat borrows and gives back
rather than clearing. Both tested.

Still owed: a dedicated `grow` sheet row (a generator change), and Jon's feel
pass — both demos ship BLIND.

<details><summary>the original card</summary>


Jon, twice: Mary-O needs a growing animation, and *"in a multi-player setting the
time slow needs to be agreed upon by all players"*; Sanic needs the same. ONE
engine beat on the worn-identity edge — pose pin, i-frames, and a clock-scale
request through `ClockScaleRequest` (never a direct `time_scale` write), so the
multiplayer agreement is structural. Mary-O's `sync_grown_form` and Sanic's
`sync_super_form_traits` are the two existing edges; both get it for free.

**Exit:** one beat, two demos, zero demo-local time manipulation. Feel ships BLIND.
</details>

### A3. "Choose Game": the title screen (≈4h)

Jon's longest observation. `basic_presentation.rs` draws 24/28px text with
keyboard-only selection: mouse-over does nothing, the touch menu icon does
nothing, buttons are not touch-sized, "Play" should read "Choose Game".

- pointer + touch hit targets and hover, through the participant input path —
  the shell still reads no raw devices;
- type scale up on title and footer;
- a **global** settings surface reachable from the title (mute first), a small
  shell-owned IR that Ambition *extends* for its in-game system menu. Not
  kaleidoscope — lunex has no wasm.

**Exit:** the title screen is operable by touch and by mouse, and one settings IR
has two consumers.

### A4. Small, independently landable (≈3h)

1. ✅ **Sanic clears the act and then dies off the end of it** — FIXED
   (`914a7ee3d`), and the triage's diagnosis was wrong: a hazard strip authored
   144px PAST the goal, not running out of level. Folding the replay assertion
   back into `act_completion.rs` is now UNBLOCKED and still open.
2. ◐ **Pit B opens into the secret vault** — verified NOT live at HEAD and
   pinned (`63316dcdb`); lengthening the level closed it and nobody noticed. The
   "weird spot" return-pipe placement is a separate, still-open half.
3. ◐ **The blossom is unobtainable in 1-1** — sharpened, not solved. Nothing has
   ever bonked a ?-block while GROWN; a standing jump from underneath does not
   reach one, and the acceptance run MOUNTS them instead. See
   [`demos/super-mary-o.md`](demos/super-mary-o.md).
4. **Rings tunnel at speed** — `collect_ecs_pickups` uses a per-frame overlap and
   the 130px magnet masks it. `cast::aabb_path_contacts` is the swept route
   already named in `pickup/mod.rs`.
5. **50/100-ring milestones** (extra life / jingle).

---

## B. The backlog, ranked — all of it is fair game

Ordered by payoff × unblocking power. Take from the top unless a named goal says
otherwise.

### B1. FB6 — the fighter-rollout redesign (track 6) [fable]

FB6 as written depends on the DELETED snapshot engine and is unimplementable at
HEAD. Choose a deterministic work budget or a recorded-input model, and define a
rollout state built ONLY from allowed `Perceived` facts, on GGRS-era machinery.

**Exit:** the determinism and no-cheat contracts are explicit enough that an L3
implementation cannot accidentally violate either — *and then build L3*, so the
run ends with a fighter that thinks, not a design doc.

### B2. Participant-action PA1–PA7
[`engine/participant-action-system.md`](engine/participant-action-system.md).
Per-seat input authorities; the foundation local-N Super Smash Siblings needs.
**Exit:** two participants, two seats, one session, each owning its own actions.

### B3. The 311-site `add_systems(Update, …)` everything-schedule
code_smells.md 2026-07-23; 10–18% executor self-time in every phase, title screen
included. **Exit:** a measured before/after on the same scene, and the everything-
schedule population is a fraction of 311 — not a doc describing the problem.

### B4. K2b — direct entry becomes a shell host whose initial route is gameplay
tracks.md carries a five-edit trace and a three-stage plan (settle helper first,
then delete the build-time root, then delete `AmbitionShellHosted`). Two named
structural risks: ~35 integration files flip sync→async, and
`SessionGatedSimulation` semantics flip for the headless/RL harnesses.
**Exit:** the hand-built `SessionRoot` at `app/resources.rs:295-322` is deleted.

### B5. Track 9 — Jon's player-facing repair list
morph-ball/worn presentation follows the body's active mode as the general rule;
shrine + glider sprite repair; kernel-guide NPC peaceful patrol;
possession-aware dialog (speaker/listener from the actors, not "the player");
`AMBITION_START_CHARACTER=sanic` granting blink/fireballs and losing move/jump.
**Exit:** each demonstrated in the real app.

### B6. Track 7 — role evictions from the sim heart
`actors/src/menu/` to the game side; `affordances/` inverted behind a sim_view
read model; `character_sprites/` anim ladder toward sim_view; the ~73
`pub use ambition_*` facades repointed; the ruled content evictions
(`ambition_items::Item` closed enum → provider-registered catalog,
`deep_dream_strength`, `puppy_slug_gun.rs`).
**Exit:** actors' out-degree drops (no menu/settings_menu/ui_nav edges) and the
oracle holds for items — add an item without editing core.

### B7. Phase 6 remainder — the construction campaign's last slice
Visible-shell half (needs a display) + the task-7 workflow measurements.
Task-7 error diagnostics landed 2026-07-24.

### B8. Falling-sand fluids — ⛔ GATED on Jon's explicit go-ahead
Water/oil are shelved on the frame-driven external crate; the unblock is an
explicit rewrite/fork decision. Jon: *"getting falling sand to work right is part
of the engine."* Ask before starting; everything else here, don't ask.

### B9. Boss-fight QUALITY grammar — wants Jon's calibration in the loop
Beyond validation: the open iteration loop in `engine/boss-design.md`.

### B10. Cutscene authority — design-before-code
Write the semantic-playback state shape (beat index, deterministic elapsed,
advance/skip edges through participant input) vs derived presentation FIRST;
hold-to-skip stays a local accumulator; modes ride `ControlFrame`. Then implement.

### B11. The bounded boss animator fold
`BossAnim` → `CharacterAnim` vocabulary; retire obsolete `target_pos` mirrors.
Do NOT reopen boss body integration.

### B12. WASM repair — [`repair_wasm.md`](repair_wasm.md)
Includes Jon's constraint: no kaleidoscope on web, because lunex has no wasm.

### B13. Sanic + Mary-O remaining content
Additional authored acts/levels beyond the first; the high-route-beats-safe-route
CONTEST that `act_completion.rs` still does not drive; the ring sheet's `collect`
row; the per-game prop-catalog contribution seam (smell #19).

### B14. Standing hygiene, real but never a substitute for the above
- the 17 files with inline `#[cfg(test)]` ≥200 lines (per-file judgement, the one
  red KB-linter item);
- the glob-import untangle (30-site worklist);
- `BodyOffense.damage_multiplier` — dead, misnamed field; delete or wire;
- `EXTRA_ENTITY_CONVERTERS` — a live first-wins `OnceLock` one call from
  `to_room_set`, with the silently-dropped `Err` K2a condemned. Dormant, latent;
- declared-id resolution checks (triage; boot-time pass REJECTED, `error_once!`
  at the ~6 miss sites + extending the `every_*` registry tests);
- duplicate schedule registration is silent in demo hosts (room-replay triage §2);
- emit-observing tests as a pattern (triage §3) — a bounded sweep of other
  request/consumer seams;
- `ui_nav` adoption in `ambition_menu`/`ambition_settings_menu`; the shared
  input-suspended gate for cutscene/encounter;
- the ONE deletion-heavy docs pass, then STOP.

---

## Excluded — one item, by Jon's ruling

**Matchbox / P2P netplay and the two-peer predicted-A/corrected-B oracle.**
DEFERRED to the Super Smash Siblings era (Jon, 2026-07-24). The transport, the
coordinated peer barrier, and the External rebase seam stay documented seams, not
queue items. *Agents keep drifting here — pick a different lane.* The sync-test
rollback foundation is enough until a game needs more.
