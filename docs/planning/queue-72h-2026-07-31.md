# 72h queue — 2026-07-31 → 2026-08-03

**Armed:** 2026-07-31T01:30Z. **Deadline:** 2026-08-03T01:30Z, enforced by
`scripts/goal_guard.py` (`docs/planning/long-run-72h-2026-07-31-goal.json`).

**Jon's goal, in his words:** *run continuously for 72 hours — there is so much
to do we won't get it all done, so there is no reason to set a stopping
criterion. Take items from `docs/planning` in any order, but **the priority
should be on architecture items that make future work easier**.*

That last clause is the ordering rule for this run, and it is narrower than the
previous run's. When two open rows compete, the one that makes the NEXT ten rows
cheaper wins — a seam, an authority, a lifecycle boundary, a measurement that
can move. A row that only makes one game better waits behind a row that makes
every game cheaper to build. This does not forbid gameplay work: it says the
gameplay work worth doing first is the kind whose absence is an architectural
hole (the damage model is one — smash percent is blocked on a body-generic
damage authority, not on a HUD).

`▢` open · `✔` closed · `⊘` deliberately not doing (needs a stated reason)

> **This file is the open-items ledger the guard reads.** A row is only `✔` when
> a NAMED TEST or a deleted code path proves it, never because it feels done.
> **If every row here is `✔`, that means the next item has not been WRITTEN DOWN
> yet, not that the work is done** — go read `docs/planning`, find it, add it,
> and get back to work.

> **Backlog:** everything still `▢` in
> [queue-24h-2026-07-26.md](queue-24h-2026-07-26.md) remains open and in scope.
> That file is not superseded; this one is the SPINE and it is where new rows
> land. The handoff rules, the localizer and the recurring defect class live in
> [queue-24h-2026-07-26-handoff.md](queue-24h-2026-07-26-handoff.md).

---

## ⇥ START HERE (status 2026-07-31, evening)

**The GPT 5.6 review is DISCHARGED** — all five findings closed, each probed red
first, plus its whole "constrain now" list (S11). The spine S1–S7 was already
closed when it landed.

**What has happened since, in one line each.** Everything below the review's five
is new this evening and every row carries its probe:

* **S8** the exit oracle's coverage is PINNED (it was 3 sessions, all setup — the
  pin is a delta, not a literal).
* **S9** one id with two display names is a reported authority conflict now.
* **S10** three things the suite found about ITSELF — a doc ratchet, a feature
  job that aborts on this CPU, and a standing guard that was FLAKY.
* **S11** the pure half of composition moved before the first mutation; three
  provisional claims narrowed to what is true.
* **S12** a second biome had no sky in any composition but the shipped app —
  and the backdrop that did spawn never moved.
* **S13** that class got an INSTRUMENT, which found more the moment it existed.
* **S14** ⛔ the disk filled (110G of incremental cache); the runner sets
  `CARGO_INCREMENTAL=0` now. **Watch `df -h /` in a long run.**
* **S15** the robot lineage stops authoring what the catalog already says —
  `Incarnation` is `id` + `replaces`, and a guard that read a Rust literal now
  asks the definition.
* **S16** the instrument's budget reaches **0**: `sync_cutscene_ui` is WAIVED on
  the standard rather than moved — generic, but no demo opens a cutscene and
  moving it would reorder the shipped host's UI chain to serve a consumer that
  does not exist.
* **S17** 2,187 lines of closed ledger archived by a rule a reader can check.
* **S18/S20** the CONSUMER and the DEMO now ask whether the engine drew it —
  Outlander and Sanic both assert the backdrop exists and MOVES, both probed.
* **S19** `cargo check --workspace --all-targets` emits **zero warnings**.
* **S21/S22** ⛔ the fighter brain thread, and it ends somewhere useful:
  `ladder_probe` runs 3 seeds and prints where the body died and how fast;
  `AMBITION_FIGHTER_TRACE=1` prints one line per decision. Two real perception
  bugs fixed (blind at the lip; "offstage" meant the ROOM), one real sentinel bug
  fixed (`move_id: String` whose EMPTY value meant "no attack", so every rollout
  armed a press naming no move) — and the diagnosis: **attacks ARE the movement**,
  445 of 665 decisions inside an attack, each one lunging. The shadow model does
  not roll them.

* **S23** Smash is on the title screen — and the seam that let it be is
  **entry route ≠ session route**: a launcher row may lead to a QUESTION
  (character select) rather than to a game. Five playtest defects closed with it,
  including menu text that FLASHED on every arrow press and hover (spawned at the
  reference size, resized a frame later), a select screen a lone player could not
  start, and the protagonist's 7-frame attack telegraph. ⛔ **the defect that
  actually blocked the stage was in ANOTHER GAME**: `MatchParticipantRoster` is
  global and had no owner, so versus's exit rule deleted smash's match every
  frame. It has an owner now.

* **S24/S26** the GPT 5.6 review: findings 1 (rollback dropped the percent meter
  and death policy) and 3 (Blink re-pressed every tick) are CLOSED and probed;
  finding 2's seam landed and its end-to-end acceptance is the first row of S26,
  which is where every open thread from this session is written down — including
  ⛔ **Jon cannot enter doors** and three probes that say the cause is not where
  anybody has looked yet.
* **S25** five goal checks were failing at the LINKER, not in the code
  (mold + incremental); `.cargo/config.toml` turns incremental off, with the
  diagnosis at the setting.

**Still open and worth doing next:** the backlog below, by the review's standard.
S15 is closed and the instrument's budget is **zero** — every engine
presentation system a game registers alone is now either moved or waived with a
reason.

---

Its standard is worth adopting verbatim: before assigning an
architectural review item, require a concrete current failure, two authorities
producing contradictory behaviour, a public contract about to become expensive
to change, a cheap repair that removes a recurring bug class, or a real consumer
blocked. And prefer **one targeted failing test → bounded repair → continue**
over a prolonged defensive campaign.

**All five are CLOSED (2026-07-31).** Each repair was probed red before it was
made; item 5's answer was MEASURED and the public claim narrowed to match it
rather than the architecture widened to meet the claim.

1. ✔ **HIGH — a newly mounted module can modify the PREVIOUS module's
   experience.** `ModuleDraft`'s cursor now clears on both sides of `define`, so
   it is module-local. Held by
   `app::tests::a_module_that_routes_before_declaring_an_experience_is_refused`
   (`cargo test -p ambition_platformer2d --lib`). **Probe:** with the two clears removed the
   test reports stage `Assembly` instead of `Declaration` — module B's route had
   landed on experience A and the composition got all the way to assembly before
   failing for an unrelated reason. (`crates/ambition_platformer2d/src/app.rs`)
2. ✔ **HIGH — rollback health reports a STOPPED session as healthy.**
   `health()` now asks `session_is_active` BEFORE any read model, and
   `RollbackHealth::Healthy`/`Desynced` carry the session `generation` (frames
   restart at zero, so a frame number cannot tell a restart from a rewind). The
   diagnosis still survives teardown — a desynced-then-stopped host reports
   `Invalidated`, via the same `carried_from` prose the next session would
   inherit. `ambition_platformer2d::rollback::stop` is the public teardown that makes the
   lifecycle observable at all. Held by
   `a_stopped_session_is_not_healthy_and_a_restart_is_a_new_timeline`
   (`fixtures/external_consumer`, 11/11 green). **Probe:** with the liveness
   check disabled it reports `Healthy { frame: 31, generation: 0 }` after stop.
3. ✔ **HIGH — the public rollback session can report a participant count the
   running session does not have.** `PlatformerApp::rollback` now REFUSES a
   count outside `1..=SlotControls::MAX_SLOTS` at the declaration pass, and
   `rollback::start` reports `SyncTestSettings::player_count()` — the value GGRS
   actually built with — rather than the request. Held by
   `app::tests::a_participant_count_the_session_cannot_seat_is_refused`. The
   session-topology redesign is NOT started; that stays with atomic activation.
4. ✔ **MEDIUM, conditional — physical retraction keeps the outgoing persona's
   mass and max-health.** Already repaired AFTER the reviewed range:
   `DisplacedPhysicals` records what a persona displaced, per field, at the
   first persona to take it, and `PhysicalRetraction` puts back what the
   incoming character leaves unclaimed. The A→B half was held; the HOT RELOAD
   half the review also named was not, and now is —
   `deleting_an_override_in_a_hot_reload_gives_the_body_its_own_numbers_back`
   re-publishes the same character with the mass and health lines deleted and
   watches the body go back to 100/1.0. **Probe:** with `PhysicalRetraction::
   NONE` in the projection it stays at 60/2.0.
5. ✔ **CONDITIONAL — ONE narrow test, and its answer.**
   `the_second_mounted_experience_launches_and_its_asset_policy_is_the_primarys`
   mounts Sanic + Mary-O and routes into the SECOND. It **launches**: the route
   activates under Mary-O's own experience id with a prepared session, and her
   sprite is in the asset catalog (catalog fragments merge, so the cast is
   shared and correct). What follows the PRIMARY: the music fold, the
   `SfxBankAssetPath` attribution, and the startup room theme.
   ⚠ **and the music half was RE-CHECKED and is weaker than first written**
   (same day): a track the catalog does not carry still resolves, through its own
   `asset_path` or the `audio/music/generated/{id}/full.ogg` convention, so what
   is lost is the catalog's path policy — source prefixes, quality variants —
   and not the audio. The shipped host folds one provider's music the same way.
   The room theme is the one with a visible consequence. Both the doc and the
   test say this now; the first version said "no asset path" and left a reader
   to conclude "no music", which would have sent them at the wrong fix.
   So the claim is NARROWED — `ModuleDraft::experience` now states all three, and the test pins
   the music limit so widening it is a failing test rather than stale prose.
   Multi-experience asset virtualization stays deferred: no consumer is blocked.

**Constrain, do not solve** (the review is explicit): `install_into` mutating the
app before validation completes; opaque order-sensitive capability closures; the
settle-tick rollback startup; consumer-supplied participant counts. Keep each
provisional, do not promise it as a 1.0 contract, do not open a campaign.

**Deferred until a real consumer needs it:** a capability dependency/conflict
solver, universal declaration-order proofs, proving every rollback descriptor has
a live population, full multi-experience asset virtualization, transactional
recovery from failed installation, further blind-agent exercises before the
above are repaired.

### Done this run — do not reopen without new evidence

* **Campaign 2 (rollback domain adapters) COMPLETE, R1–R5.** `rollback/mod.rs`
  1,870 → 690 lines, nine domain adapters, schema dump byte-identical at every
  step, held by `central-rollback-does-not-enumerate-domains`. R1's crate
  extraction was DECLINED on evidence (see the campaign doc).
* **Campaign 3A + 3B COMPLETE.** `RoundScopedEntity` is the round lifetime;
  every reset path goes through `reset_body_clusters` and announces
  `ae::BodyRestarted`.
* **Campaign 4's criterion holds for BOTH operations and is tested.** Its shared
  trait is declined: both already share `RoomConstructionPlan`.
* **API row (g):** `SeatMenuFrames` (seat-keyed input), plus
  `MatchParticipantRoster::unsatisfiable_seats` and `MatchSeatingRefused`.
* **Smash demo**: select screen → lock-in → stocks match, end to end.
* Full suite **24/24 jobs green** (`./run_tests.sh`).

### ⚠ Two things you will otherwise re-learn the hard way

* **`rustfmt` on a `lib.rs`/`mod.rs` cascades into its submodules** — and it is
  PREVENTABLE, which is worth more than the warning was. It caught three commits
  on 07-31 and four more on 08-01, each needing a revert.

  ```bash
  rustfmt --edition 2024 --config skip_children=true path/to/mod.rs
  ```

  `--skip-children` is not a flag (rustfmt rejects it); it is a CONFIG key.
  Verified: with it, formatting `features/mod.rs` leaves every submodule
  untouched.
  ⚠ **it still reformats the target file wholesale**, so a `mod.rs` whose imports
  rustfmt wants to reorder will still show churn — but in ONE file you can read,
  instead of thirty you cannot. Check `git status` against what you edited before
  staging either way.
* **`ladder_probe` numbers compare only WITHIN one build.** A survival figure
  moved 23.5s → 5.4s purely because the stocks respawn was fixed — not because
  the brain changed.

⊘ **Abandoned deliberately, same day:** a `with_cpu_brains` capability claim on
`AuthoredCatalogFragments` (Campaign 5's shape, extended to the character group).
The condition available at preparation — "the roster's brain keys are empty" —
does not express what it needs to ("THIS provider registered archetypes"), so the
guard was weak and its test failed. Backed out rather than shipped. The seam that
DID land for the same bug is `unsatisfiable_seats`, checked per provider.


## ⇥ THE NEW SPINE (Jon, 2026-07-31 late): the authoring loop

Jon assigned a three-program architecture campaign that supersedes S1–S26 as the
ordering rule for what comes next. Full plan, with the survey of what already
exists and the evidence for each gap:
**[authoring-loop-program-2026-07-31.md](authoring-loop-program-2026-07-31.md).**

> author → validate → control → simulate → explain

Do not read the three programs as three backlogs. They share prepared
identities, they land as bounded vertical slices with real consumers, and each
slice DELETES the path it displaces.

The survey found the substrates are further along than the brief assumes — and
found four places where they stop being authorities. Those four are the rows:

✔/▢ **P1a+P1b. The compiler EXISTS and the acceptance criterion is MEASURED**
(2026-07-31). `ContentPackDraft` was already the facade's named next slice
(`crates/ambition_platformer2d/src/app.rs:435`, ADR 0032), deferred because *"nothing can yet
validate it"*. `crates/ambition_content_pack` is that validator, and
`crates/ambition_content_cli` is its front door.

**The number:** validating Ambition's real 148 KB catalog — 140 characters, 9
brain presets, 8 action-set presets, 280 references, 265 assets — takes
**~5 ms**, against the ~10 minute rebuild it replaces. Held by
`validating_a_character_edit_does_not_rebuild_rust`, which spawns the BUILT
binary, adds a character, and asserts the pack FINGERPRINT moved — a binary that
had embedded the catalog would report the old one forever.

* Thirteen probed refusals, one per hard error, each asserting the code and the
  stage rather than the prose.
* ⚠ **`UnknownField` is an error, not a warning.** An authored field nothing
  consumes is the most expensive content bug there is: everything looks authored
  and nothing happens. `deny_unknown_fields` landed on the catalog types and the
  shipped catalog survived it — no stale fields.
* Every refusal names **the checks that did not run**. Same lesson as ADR 0032's
  composition funnel: the funnel is structural, and what is fixable is that it
  was silent.
* The fingerprint is hand-rolled FNV-1a over CANONICAL bytes, not file bytes:
  reflowing a comment must not move it, changing a value must. `DefaultHasher`
  is randomised per process, which would make it useless here.

⛔ **IT FOUND THREE BROKEN CHARACTERS ON ITS FIRST RUN, IN 18 ms.**
`npc_giant_gnu_hands` (sheet present, `.ron` manifest missing),
`npc_hypatia_prime` and `npc_le_beast` (no art at all) — and `git ls-files`
finds ZERO tracked files for any of them, so the TEXT manifests were never
committed either. All three have pedestals in `hall_of_characters.ldtk`.
**Content rows awaiting art, for Jon — not a bug in the tool** (a control row,
`goblin`, resolves).

⚠ **which forced a real decision about severity, and the ratchet forced a
second.** AGENTS.md says binary payloads are git-ignored but present and a
feature owes only *"degrade visibly when a file is absent"* — so on a fresh
clone missing art is a DOCUMENTED state. A compiler refusing it by default would
contradict a project rule and get waived, which this repo has already learned
costs more than the check is worth. So `AssetSource::severity` is a choice the
CALLER states: `DirectoryAssets` refuses (packaging), `AdvisoryAssets` warns
(authoring). Both REPORT. And `the_strict_asset_path_still_refuses_on_real_
content` is the probe, pinning that the strict path DISCRIMINATES without
pinning WHICH files are missing — a known-missing list is an allowlist and rots
the moment the art lands.

⚠ **`capability-footprint-may-not-grow` went RED and was right.**
`ambition_content_pack` entered `fixtures/minimal_game`'s linked closure,
because `ambition_characters` depended on it unconditionally. §2e exactly: a
movement-only game must not link a content compiler it never asked for. The dep
is `optional` and default-OFF now (slice H's pattern), the CLI turns it on, and
20/20 contracts hold at 38 crates. ⚠ the suite auto-generates
`ambition_characters [content_pack]`, so the five gated tests still run — the
skip that would have made this a silent un-run was checked, not assumed.

→ ▢ **still open on this row, and it is the honest half:** the RUNTIME still
reads `include_str!`. Nothing consumes a `PreparedContentPack` yet, so today the
compiler proves content correct without being the path that loads it. That is
deliberate — one migration at a time — and it is the next row, not a finished
one. The catalog's own header has claimed *"No Rust changes needed"* since long
before this; it is true for the first time.

✔/▢ **P1d. The causal substrate EXISTS and the first required question is
answered headlessly** (2026-07-31). `crates/ambition_causal`: typed
`CausalFact`s with tick / subject / participant / cause / generation /
`Execution` / prepared content id, a bounded `CausalLog`, `RecordingPolicy`,
`explain(tick, subject)`, causal chains, and a deterministic dump. 12 tests.

The first real publisher is the fighter's decision, and it REPLACED the
`eprintln!` rather than sitting beside it — the stderr line is rendered from the
same fact, so the two cannot drift. Held by
`the_inspector_answers_why_this_fighter_chose_this_action`, which asks the real
`tick_fighter` which verb it chose, what was offered, what the rollout vetoed,
and what reached the body — **asserting on FIELDS, never on the summary
sentence**, so improving the wording is not dangerous.

⚠ **the observer is proved not to change what it observes.**
`recording_changes_nothing_about_what_the_brain_does` runs the same seed twice
and compares 20 frames. Under a rollback host a brain that decided differently
while being watched would be a desync.

⚠ **the scoped sink is a hidden channel and the safety argument is stated, not
assumed.** `tick_state_machine` has 44 call sites; threading a recorder through
all of them to instrument one domain is the churn the program warns against, and
it would recur per domain. So the sink is thread-local — and it is **write-only
by construction**: `record` returns `()`, there is no getter, so a simulation
cannot read back what it published and branch on it. ⛔ **if this module ever
grows a read path that argument is gone** and parameter threading becomes right.

⚠ **the tick is stamped by the scope owner.** A brain five hops below the ECS
does not know the world's clock, and a decision counter guessed there would be a
second clock nothing could join against.

→ ▢ **still open, and the honest list:**
* the brain's facts carry **no subject** — it has no stable sim id at that
  depth, so they are world-facts today. `SubjectKey::Unstable` exists and is
  documented as a recorded API leak rather than a design. The subject arrives
  when the integration layer publishes.
* ✔ **the rollback-identity publisher IS written (2026-07-31).**
  `ambition_platformer2d_runtime::causal` (feature `causal`, default-off) owns the log as a
  resource, stamps the host's tick, and publishes `tick_execution` with
  `Execution` + `generation` read from `SimulationReplayState` and
  `ConfirmedFrameBoundary`. Held by
  `an_original_tick_and_a_resimulated_one_are_different_facts`. Published with
  NO subject, so it explains every body on that tick — a resimulated frame is
  resimulated for all of them.
* ⛔ **and building it found a soundness bug in the sink I shipped hours
  earlier.** `with_sink` is THREAD-LOCAL; Bevy runs systems across worker
  threads, so a system publishing through it would publish into nothing. That is
  sound for what the sink was built for (a pure call tree driven from one
  thread) and wrong as a general ECS mechanism. **Silent loss was the
  unacceptable part**, so `facts_lost_offthread()` counts it and
  `assert_no_offthread_loss()` turns the count into a failure. An ECS domain
  uses `ResMut<CausalRecording>`; the sink stays for headless/pure use where it
  is proven. Held by
  `a_fact_published_off_thread_is_counted_rather_than_vanishing`, which spawns a
  real worker thread.
* ⚠ `ambition_platformer2d_runtime` LEFT `SKIP_FEATURE_JOB` in the same commit, which its own
  rule requires — *"adding a `#[cfg(feature = ...)]` test to a skipped crate
  must remove the skip in the same commit; a stale entry silently un-runs
  tests."* **Measured cost: 2m00s** for the `causal,portal` variant, 72 tests.
* ✔ **the resource moved so a DOMAIN can publish** (2026-07-31).
  `CausalRecording` lives in `ambition_causal` behind a `bevy` feature that is
  `bevy_ecs` ALONE — no renderer, no app, no domain types, so the crate keeps
  the property that lets any domain depend on it without depending on another.
  A host-owned resource would have forced a movement fact in `ambition_platformer2d_actor_monolith`
  to depend on `ambition_platformer2d_runtime`, which is backwards.
* ✔ **the MOVEMENT publisher is written (2026-07-31).**
  `ambition_platformer2d_actor_monolith::causal::record_player_movement_intent` answers the
  inspector's FIRST required question — *why did this body move this tick* —
  keyed by `SubjectKey::Seat`. Not an `Entity` (indices are recycled) and not a
  `SimId` (bodies do not carry one); a seat survives death and respawn, which is
  exactly the window an investigation spans.
  * **OBSERVER BY SIGNATURE.** Every component is taken immutably, so it cannot
    affect the tick — a property, not a promise, and it matters because a
    rollback host resimulates and an instrument that nudged state would desync
    precisely when somebody was using it. Registered `.after(tick_player_brains)`
    in the same set rather than threaded through it.
  * It records the EMITTED intent beside the body state, because "asked for
    nothing" and "asked and was refused" are different findings and a position
    sample cannot tell them apart.
* ✔ **the LIFECYCLE publisher is written (2026-08-01).**
  `ambition_combat::causal::record_stock_lifecycle` answers *why did a body lose
  a stock, get eliminated, or end the match* — `body_knocked_out`,
  `stock_spent`, `match_decided` — keyed by seat, and it is an OBSERVER OVER
  MESSAGES rather than instrumentation inside the rules. No rule gained a
  parameter; a reader holds no authority over its writer; it stays correct when
  the rules change because it observes what they DECIDED. ⚠ it drains its
  readers even while recording is OFF, or the first frame somebody enables the
  instrument reports a backlog of old knockouts stamped with the current tick.
* ✔ **the DAMAGE publisher is written (2026-08-01), and the choice was a NEW
  MESSAGE.** `BodyHitResolution` — `Ignored` / `Blocked` / `Armored` /
  `WalletShielded` / `Damaged{damage, died}` — is already the exact "why was this
  hit accepted or rejected" vocabulary; it was reaching nobody but the caller.
  `BodyHitResolved` lets it out, written from both hit paths at the moment the
  shared resolver returns, and `record_hit_resolutions` publishes the facts.
  * The message and both writes are `#[cfg(feature = "causal")]`, so a build
    with no inspector pays nothing. Instrumenting the resolver itself was the
    alternative and was refused: it would put an observer's concern inside the
    one function every body's damage passes through.
  * ⚠ **the raw damage travels beside the outcome**: "asked for 30 and dealt 0"
    and "asked for 0" are different findings that the outcome alone cannot tell
    apart.
  * ⚠ **published BEFORE the `Ignored` early return.** A publisher placed after
    it would explain every hit except the ones that did nothing — which are the
    puzzling ones and the reason somebody opens an inspector.
  * ⛔ **and the writer is `Option`, which is the opposite of `BodyKnockedOut`'s
    rule and deliberately so.** A knockout is a message the SIMULATION acts on,
    so an unregistered one is a broken composition that should say so. This one
    is read by an instrument and nothing else, so a hand-built fixture or a game
    with no inspector must publish nothing rather than panic. Found the hard
    way: four `ambition_platformer2d_actor_monolith` fixtures died with "Message not initialized"
    the moment a hit landed. **An instrument that can take down a composition is
    worse than no instrument.**

→ ✔ **KNOCKBACK too (2026-08-01), and the inspector's damage questions are
both answered.** `apply_body_hit_reaction` RETURNS what it decided —
velocity, the victim's DI, hitstun, and whether the hit carried knockback at all
— and `record_hit_reactions` publishes it as `knockback_applied`.

⛔ **the velocity alone cannot answer the question**, which is the whole reason
the fact carries its inputs: a short launch is a weak hit, a well-DI'd one, or a
hit with no knockback at all, and those are three different findings that look
identical on screen. `steered` and `had_knockback` separate them, and the
"hurt but not launched" summary tells a reader chasing a launch to stop.

⚠ **returned rather than published from inside the reaction.** That function
takes a `&mut Vec2` and no writer; threading one in would put an observer's
parameter through the reaction every body in the game shares. The caller has the
writer, so the caller publishes.

→ ✔ **the PLAYER path publishes it too (2026-08-01)**, so both bodies that can
be launched explain their launches. ⚠ `publish_reaction` exists in BOTH feature
configurations — a no-op without `causal` — so neither call site carries a `cfg`
and the returned reaction is never an unused binding. Gating the function
instead produced two conditional blocks at the call sites AND a warning in the
build that has no inspector, which is how a warning gets ignored. Both
configurations compile warning-free.
* ⚠ **`ambition_platformer2d_actor_monolith` and `ambition_platformer2d_runtime` both LEFT `SKIP_FEATURE_JOB`**,
  which that set's own rule requires once a crate gains a `#[cfg(feature)]`
  test. ⛔ **and the actors entry was mispriced.** It was justified by "tens of
  GB / 488s"; that cost belonged entirely to `profile` (which forwards
  `bevy/trace_tracy`, whose initializer ABORTS on a CPU without an invariant
  TSC). `profile` is denied now — the skip's own comment predicted this exact
  requirement — and the restored job runs **1137 tests in 2.8 s**. A whole
  feature variant had been un-run for a cost that was never its own.
* ✔ **the brain's facts carry their SUBJECT, and stop landing in nothing**
  (`a510ee92b`). Both halves the row predicted, and they were one defect:
  a decision fact nobody owned and nobody received.
  * **Subject.** `BrainSnapshot` is pure body state, so the brain genuinely
    cannot know which body it is — its own test called that "a leak, not a
    design". Identity now arrives through the world-in port exactly like
    `attack_kit` and `actor_aerial`: `subject: Option<String>`, filled by the
    actors-side snapshot builder from `config.id`, the id the rest of the actor
    system already names a body by. The brain is TOLD who it is and still cannot
    branch on it.
    **PROBED**: unattributed, asking about `fighter_left` returns **4**
    decisions belonging to the other fighter — not two. An unattributed fact is
    returned for EVERY subject, so both streams merge into every query.
  * **Sink.** The row said the publisher "must use `ResMut<CausalRecording>`".
    ⊘ **it could not**, and the reason is worth more than the prescription: the
    publisher is a BRAIN, five hops below the ECS, and threading a recorder into
    `tick_with_actions` would put the log on the simulation's own signatures —
    which this crate already refused once, running the movement observer AFTER
    the brain tick so *"a system that only reads cannot be the thing that broke
    the tick"*. So the HOST lends its log to whichever worker thread the tick
    lands on (`CausalRecording::lend_to_thread`), and the library keeps its plain
    `record` call. Same outcome, and the refusal that motivated it survives.
  * ⚠ the parameter slot is `CausalLend<'_>`, a cfg'd alias: `ambition_causal` is
    optional and default-off (§2e) and a `#[cfg]` cannot be written on a tuple
    element, so the shipped build gets a zero-sized `PhantomData`.
  * ⊘ a second off-thread-loss test was written and DELETED. It duplicated
    `a_fact_published_off_thread_is_counted_rather_than_vanishing`, which owns
    that claim in more detail — and racing it on the process-global counter made
    that one fail (2 where it expected 1). The counter caught a test, not a bug.
* no `app_it` module, no runtime panel, no CLI dump command.
* `ambition_gameplay_trace`'s `GameplayTraceFrame` is still `PlayerTraceState` —
  player-centrism in the observation layer, untouched by this row.

✔/▢ **P2a. `ActiveInputContext` is per-participant — the SEAM landed
2026-07-31.** ⛔ **two shipped authorities disagreed**: `ParticipantContexts` is
per-seat, and `resolve_active_input_context` folded it to ONE global resource
read off `ParticipantId::PRIMARY`. Same defect class `SeatMenuFrames` already
fixed one layer down, and now the same shape:

* `SeatInputContexts` is the resource — `BTreeMap<slot, ActiveInputContext>`.
  `ActiveInputContext` survives as the per-seat VALUE, so no reader had to learn
  a new vocabulary, only which seat it means.
* `.primary()` is the named answer for the four consumers that genuinely mean
  the local primary — the `ControlFrame` bridge, the trail toggle, the
  control-prompt HUD, the touch overlay. They say so now instead of being the
  only readers of a global.
* `populate_secondary_slot_controls` asks **each seat's own** context. That is
  the actual repair.
* **Probed red:** restoring `active_context.primary()` in that one line makes
  `a_seat_browsing_a_menu_stops_driving_its_slot_and_the_others_keep_playing`
  fail with seat 1 still jumping.
* ⚠ **pause is NOT the counterexample it looks like.** "Seat 0 pauses, seat 1
  keeps playing" would be wrong and this does not cause it: pausing is a
  `GameMode` transition every router gates on independently. The test pins both
  halves so a later reader does not "fix" the wrong one.
* ⚠ a departed seat is DROPPED from the map rather than left holding its last
  answer — unplugging a pad mid-match must not leave a slot the router still
  believes is driven.

→ ▢ **still open on this row:** nothing DECLARES a per-seat claim yet. Every
declarer (`declare_gameplay_input_context`, `declare_in_session_input_contexts`,
the shell's two) loops `for mut contexts in &mut participants` and writes the
SAME claim to every one, so the new keying is exercised only by
`a_seat_browsing_a_menu_stops_driving_its_slot_and_the_others_keep_playing`,
which hand-declares the divergence.

⛔ **and the select screen is NOT the consumer this row predicted** (checked
2026-08-01). `drive_the_select_screen` takes `SmashSelect`, the router, the seat
frames and the device order — never `SeatInputContexts` — and dispatches on
`match select.seat(seat)`. The per-seat state machine IS the authority for what
a seat's press does, and a browsing seat and a locked-in seat already diverge
correctly without any context involved.

Declaring a per-seat `SELECT_CONTEXT` there would be a **second authority over
the same question**, which is the defect class this whole queue has been
removing — and an inert one, because nothing would read it. `SELECT_CONTEXT` is
defined in `participant.rs` and has no declarer for exactly this reason.

→ **what a real first consumer looks like**, so the next attempt does not build
the inert one: input contexts arbitrate between SUBSYSTEMS competing for a
seat's input (gameplay vs menu vs dialogue), not between states inside one
screen. The genuine case is a seat that opens a surface MID-MATCH while the
others keep fighting. The nearest shipped candidate is dialogue —
`declare_in_session_input_contexts` currently captures EVERY seat when any seat
talks to an NPC — and it is blocked on the world-stop product question in
[awaiting-maintainer-decision.md](awaiting-maintainer-decision.md), not on this
seam. **The seam is built ahead of its first real consumer; that consumer is
one product decision away, not one slice away.**

✔/▢ **P2b. The in-session surfaces are CLAIMS, not `GameMode` matches**
(2026-07-31). Six ids landed WITH a declarer, because dead vocabulary is a smell
this repo names: `debug` (195), `cutscene` (180), `dialogue` (150), `inventory`
(140), `select` (130), all between the launcher (200) and gameplay (100).
`declare_in_session_input_contexts` syncs dialogue and cutscene claims, and
`populate_control_frame_from_actions` lost both of its duplicate gates — it has
one now instead of three.

**Behaviour-identical today ON PURPOSE**: the declarer claims on every
participant exactly as the global gates did. What the move buys is that the
per-seat version is a change at ONE function rather than a rewrite at every
router — held by `an_in_session_surface_claims_input_and_can_claim_it_for_one_
seat`, whose second half puts seat 0 in a conversation on a RUNNING world and
watches seat 1 keep jumping. **Probed red:** with the declarer out of the chain
the seat's owner is `gameplay` instead of `dialogue`.

⚠ **pause is deliberately NOT in the declarer.** The paused path writes a MENU
frame into `ControlFrame` rather than a neutral one, so a paused seat can still
navigate; folding it into a capturing claim would silently delete that.

⚠ one behaviour DID change and it is named in the code: the dash edge is reset
on a cutscene now, which it was not before. That is the pause and dialogue rule
applied to the third case rather than a fourth policy.

→ ⊘ **THE SPLIT IS A PRODUCT DECISION, and it is Jon's** (moved to
[awaiting-maintainer-decision.md](awaiting-maintainer-decision.md), 2026-08-01).
Investigated to the mechanism: the suspend is
`apply_suspended_time_scale_system` under the `gameplay_suspended` run
condition, setting `time_scale = 0.0`. So splitting `Dialogue` out means NPCs
keep patrolling and hazards keep ticking during every conversation — a change to
how existing scenes play, not a multiplayer bug fix.
⚠ **I first concluded the opposite**: `allows_gameplay()`'s call sites are almost
all input routing, and the run conditions built on it look unused. They are not
— `player_schedule.rs` uses `gameplay_suspended`, and that one line IS the pause.

→ ⛔ **the split is HALF done, and the remaining half is PINNED AS A TEST rather
than left as prose.** `dialogue_still_stops_the_world_as_well_as_claiming_the_
input` asserts that a seat whose context IS gameplay is suppressed anyway,
because `GameMode::Dialogue` still answers "the world is stopped" too and
`allows_gameplay()` overrides. Without that pin the next person builds the
per-seat declarer, sees no effect, and concludes the context seam is broken. It
is not; this is. ⚠ the repair is NOT deleting the `allows_gameplay` gate —
pausing must keep stopping everybody. It is stopping `Dialogue` from claiming to
stop the world when what it wants is one participant's attention.

⛔ **and a THIRD authority conflict was found while sizing this row, in the same
family as P2a's.** `participant.rs:16` states the rule: *"nothing derives input
ownership from `GameMode` or from the presence of a controlled body"*. But
`input_systems.rs` derives exactly that, four times — `mode.get().
allows_gameplay()` at :221, :315 and :392, and `matches!(mode.get(), GameMode::
Dialogue)` at :280 — and 18 sites across the crates read `allows_gameplay()` or
match a suppressing `GameMode`. So pause, dialogue and cutscene suppress input
through `GameMode` while the doc says they suppress it through a context claim.

⚠ **this is not simply a bug to fix.** Part of it is CORRECT and P2a's commit
depends on it being correct: pausing stops the world, which is not a per-seat
fact, and `world_running` at :392 is the right shape. What is wrong is that
*"the world is stopped"* and *"this seat's input belongs to a menu"* are the
same switch today, so a surface that wants the second without the first cannot
express it — which is exactly what a select screen, a dialogue box and an
inventory need. Splitting them is the row; deleting the `GameMode` gate is not.

✔/▢ **P2c. Bindings are ONE authority — `SeatBindings` (2026-07-31).**
Projected from the very `InputMap<SandboxAction>` the router reads, keyed by
participant slot (the third seam in this family, after `SeatMenuFrames` and
`SeatInputContexts`). `ActionBindings::from_map` gives a pure caller the same
answer without a `World`.

⛔ **the parallel authority it deletes NAMED ITSELF.** `gamepad_glyph`'s own
comment: *"today this is a static table that matches the bindings authored in
`KeyboardPreset::input_map`; if those bindings change the table here needs to
follow."* It never followed automatically, and the failure mode is a prompt
telling a player to press a button that does nothing — from the player's side,
indistinguishable from a broken binding.

The table answered two questions and only one was its business:
* **WHICH control** → the binding projection. Nothing to keep in step.
* **HOW TO DRAW IT** → `vendor_label(button, kind)`, a real presentation
  concern: `GamepadButton::South` is "A" on Xbox, "Cross" on a DualShock and
  "B" on a Switch pad, because Nintendo mirrors the positions.

**The device tests did not change their expectations** — Z, X, C, Space, J, K,
A, Cross, X, Square all still pass — which is the strongest available evidence
that one authority now produces exactly what two used to agree on.

Also deleted: `key_glyph`, a THIRD key→label table, displaced by the same move.
⚠ `KeyboardPreset::action_label()` turned out to have zero callers — it was
already dead.

⚠ one real bug found on the way: `presets::key_name` returns `"?"` for any key
it does not list, so a rebind to an unlisted key would print a question mark.
Unnamed keys fall through to the `KeyCode`'s debug form now (`F13`) — ugly and
TRUE, the same rule as `PhysicalControl::Other`.

→ ✔ **CLOSED 2026-07-31 — and the repair was composition, as predicted.**
`PromptEntry::binding` carries the key now, read from `SeatBindings` via
`label_for_slot`. The verb and the binding stay two fields with two owners: what
the slot DOES (the action scheme) and which control presses it (the input map).

* `ambition_input::action_for_slot` is the ONE `ControlSlot → SandboxAction`.
  It lives in `ambition_input` — which gained a downward edge to
  `ambition_entity_catalog`, a pure serde/ron leaf — because `ControlSlot` in
  the catalog cannot see `SandboxAction`, and the correspondence is an input
  fact. No new crate entered the closure; 20/20 contracts still hold.
* `touch_action_to_sandbox_action` COMPOSES through it and the second table is
  gone.
* ⚠ **the first version of that composition reintroduced a swept anti-pattern.**
  It returned `SandboxAction::Jump` behind a `debug_assert!(false, ..)` for a
  slot-less button — release drove JUMP and the assert WAS the handling, which
  is precisely the class swept a day earlier. It returns `Option` now: an
  unclassifiable button gets no glyph and no press, which is true and needs no
  alarm.
* ⛔ **and the cache would have made the projection pointless.**
  `rebuild_control_prompt` skips on an unchanged key, so without
  `bindings.is_changed()` the entries kept the glyph they were built with — a
  prompt telling the player to press the old key, which is the exact staleness
  the projection exists to prevent, reintroduced one layer up. **Probed red:**
  the test reports `Z` where `F13` was expected.

The historical note, for the record — this is what the row looked like before:

⛔ **there is no `ControlSlot → SandboxAction` mapping anywhere.** What exists
is two hand-maintained maps side by side in `ambition_touch_input/src/
bevy_plugin.rs` — `touch_button_slot` (`TouchActionButton → ControlSlot`, :966)
and `touch_action_to_sandbox_action` (`TouchActionButton → SandboxAction`,
:1211). They agree by hand only, and a prompt asking "which control is the
Attack SLOT on" would need a third.

→ the repair is to compose rather than add: ONE `ControlSlot → SandboxAction`
owned where the slot vocabulary is, with touch's second map derived through it
(`TouchActionButton → ControlSlot → SandboxAction`). That deletes a parallel map
AND unblocks the glyph. ⚠ the home needs a moment's thought: `ControlSlot` lives
in `ambition_entity_catalog`, a LEAF, so it cannot see `SandboxAction`; the
mapping belongs in `ambition_input` (which may depend on the leaf) rather than
in the catalog.

✔/▢ **P2d. The flow reaches its END (2026-07-31).**
`two_participants_start_a_match_and_can_still_pause_it` walks launcher → Smash
row → two seated participants → Escape → pause menu open, at the HOST level
where the whole input stack runs. **Probed red** against the old `single()`
fold: the pause menu stays shut.

⚠ **it presses a KEY, not `MenuControlFrame`.** Setting the frame by hand is
what the select screen's original tests did, and it is exactly how that screen
came to be fully tested and completely inert — an injected value skips the
system that was broken.

⚠ two attempts failed first, and both are the same trap the select screen's own
tests carry a note about: a hand-spawned `InputParticipant` is despawned (the
host derives seats), and an inserted `DeclaredInputSeats` is clobbered (the
select screen declares it from the live pads). **The pads are the fact.**

⚠ **the test lives in `app_it`, not in the demo**, because a demo app depends on
`ambition_platformer2d` alone — that is the demo gate — and neither `ShellPauseMenu` nor
leafwing is on the facade. Recorded rather than worked around: widening the
facade to let a demo assert on pause state is a real decision, not a test fix.

→ ▢ still open on this row: the SELECT half and the PAUSE half are proved
separately (the demo's `two_players_join_choose_and_lock_in_and_the_battle_starts`
and this one). Nothing yet walks one app through both, and "neither steals the
other's input" is proved for gameplay (`four_pads_each_move_their_own_fighter…`)
but not for a menu.

▢ **P1d residue**, re-checked 2026-08-01 against the code rather than against
the last version of this row — two of its four items had already closed and it
was still asking for them.
* ✔ **publishers** for movement, damage and lifecycle exist
  (`ambition_platformer2d_actor_monolith::causal`, `ambition_combat::causal`), and rollback identity
  is `record_execution_identity`.
* ✔ **a subject on the brain's facts** — closed above (`a510ee92b`). The
  `SubjectKey::Unstable` pointer this row carried is stale for the brain;
  `capability_demo` still uses that variant, correctly and by its own admission.
* ✔ **`Execution`/`generation` ARE filled from a live host.** `CausalPlugin`
  runs `stamp_causal_frame` in `First` and `record_execution_identity` reads
  back what it decided, so every fact in a frame agrees by construction rather
  than by two systems reading the same resources and hoping.
* ✔/▢ **the `app_it` module exists now** —
  `tests/causal_explains_the_real_app.rs`, and writing it found why it had never
  been written.
  ⛔ **`causal` was enabled NOWHERE.** Measured 2026-08-01: it resolves false for
  every crate in the default workspace build, and `ambition_app` had no such
  feature at all. The chain is complete from the facade down (platformer2d →
  runtime → monolith + combat → characters) and nothing turned it on — so every
  domain recorder was compiled out of every build the app produces, and
  "the substrate now works" was true only of the substrate's own tests.
  ⚠ **that is why this row read as two easy surfaces for days.** The per-crate
  feature jobs exercise the sink, the tick stamp and the explainer; none of them
  asks whether the GAME publishes anything, so nothing could distinguish "works"
  from "compiled out".
  → `ambition_app` gained a `causal` feature (opt-in: the shipped game has no use
  for recording, what was missing was a DOOR), plus a dedicated `run_tests.py`
  job, because the crate is in `SKIP_FEATURE_JOB` and the test would otherwise
  be dark — the rule that set states about itself.
* ▢ **still open**: the dump command. Now genuinely a surface on a substrate
  that is reachable from the app, which is what this row claimed before it was.
  ⚠ and the first real consumer should be the ladder trace (the emit→body seam
  above), not a demo — that question is open, the instrument now exists, and
  pointing it at a live question is worth more than a CLI nobody has asked for.

✔/▢ **P1e. The RUNTIME loads THROUGH the compiler (2026-07-31).**
`ambition_content::character_catalog::load_catalog` compiles the embedded pack
and takes what the character schema LOWERED, instead of calling `parse_catalog`
on the same bytes. Two readers of one file, with nothing making them agree, is
gone. Held by `the_shipped_cast_is_what_the_compiler_prepared`.

Three pieces:
* `ContentPackDraft::from_sources` — a draft from `include_str!`s, no
  filesystem. **One manifest, two source origins**: a shipped binary must embed
  (there is no asset directory beside a wasm bundle) and the CLI must read a
  directory, but the PIPELINE that judges them is the same one. `pack.ron` is
  `include_str!`d by the provider and read off disk by the CLI.
* `FacetOutcome::lower` / `PreparedContentPack::lowered::<T>` — the artifact
  `RuntimeDisposition::Runtime` was always promising. `Arc<dyn Any>` because the
  compiler must not name a domain type; the owning capability downcasts.
* ⚠ **a refused pack hands out NO runtime value.** `out.lower` is gated on
  `!out.failed()`, and `a_refused_pack_hands_out_no_runtime_value` pins the
  direction that matters — invalid content reaching a running game while the
  validator says it refused is the whole failure this row exists to prevent.

⚠ assets are UNCHECKED on the runtime path, deliberately: a fresh clone's art is
legitimately absent, and a compiler that stops the game over a missing sheet
becomes the thing that breaks it rather than the thing that explains it. The
CLI's strict mode is where art is a gate.

⚠ a bad catalog now PANICS at composition with the compiler's full diagnostic —
naming the character and the field — rather than falling back at spawn. The repo
has chosen between these once already: *"a silent partial start would be worse
than a loud stop."*

→ ▢ **still open:** editing content still needs a rebuild for the SHIPPED
binary, because it is embedded — unavoidable while it is. What the disk path
buys is a dev/hot-reload origin that now exists and is unused
(`ContentPackDraft::read_from_dir` + the same `compile`). And only the character
family is migrated; music, dialogue, quests, encounters and worlds still go
through `validate_content_graph`.

✔/▢ **P3's ACTION half is unblocked (2026-08-01).**
`ambition_input::semantic` is the open vocabulary: `SemanticActionId` is a
`&'static str` like `InputContextId` and the compiler's `SchemaId`, and
`ActionRegistry` answers *what can be pressed in this context* and *what does
this capability contribute*. Held by
`a_capability_registers_its_own_action_without_touching_the_engine_enum`.

⛔ **the registry IS the vocabulary, not a description of one.** Every
`SandboxAction` is registered, and `every_device_action_is_registered`
enumerates the enum THROUGH REFLECTION (`Actionlike: Reflect + Typed`; leafwing
0.20 has no `variants()`) so the list cannot be hand-maintained and cannot go
stale. **Probed red**: deleting `blink` fails with *"these `SandboxAction`
variants have no semantic entry, so nothing that asks the registry can see
them"*. Without that test this would be a parallel list, which is the thing it
replaces.

→ ▢ **still open, and it is the migration not the design.** A capability action
can be DECLARED and looked up; it cannot yet carry a device binding of its own,
because that needs `InputMap<SemanticAction>` in place of
`InputMap<SandboxAction>` — hundreds of call sites, whose ordering is written
down in the program doc. Declaring the vocabulary first is what makes that
migration mechanical rather than a redesign.

✔/▢ **P3 PROVED — `examples/capability_demo` (2026-08-01).** A shockwave mechanic,
small on purpose, that contributes ALL FOUR halves without editing anything
central. 7 tests, one per claim:
* **schema** — `pulse_schema()` registered into a `SchemaRegistry` the compiler
  built with no pulse knowledge; its own refusals (a zero radius, an unconsumed
  field) come out in the compiler's vocabulary rather than as a panic.
* **action** — `PULSE_ACTION` sits beside `jump` and `attack` in
  `for_context(GAMEPLAY)`. `SandboxAction` was not touched.
* **rollback** — `PulseCooldown` registered through the public
  `AmbitionRollbackApp`, under the CAPABILITY's own owner label.
  `rollback/domains/` gained no file. ⚠ a cooldown that was not rewound would
  let a pulse fire twice from one charge on a resimulated frame.
* **causal facts** — `pulse_fired` quotes the AUTHORED profile
  (`pulse:pulse_profile/gentle`), which is what *"which content supplied the
  active value"* means. And a refused pulse says WHY: *"I pressed it and nothing
  happened"* is the question an inspector exists for.

⚠ **the crate defines its own `PulseBody`/`PulseAffected`** rather than
borrowing `BodyKinematics`. That is the compiler enforcing the seam: with no
direct `ambition_platformer2d_actor_monolith` dependency, an actor-crate item cannot even be named.

⛔ **AND THE PROOF HAS A MEASURED HOLE, which the sentinel exists to find.**
`ambition_platformer2d_actor_monolith` IS in the transitive closure, via `ambition_platformer2d_runtime`.
Rollback registration lives in the runtime, and R1's decision is that only a
crate ABOVE it owns a schema directly — so **a capability that wants its own
rollback state links the whole simulation whether it uses it or not.** My first
draft of the manifest claimed "no `ambition_platformer2d_actor_monolith`" flatly; that was an
overclaim and is corrected in place.

→ ✔ **CLOSED the same day, and the fix was NOT moving the trait.** The other
two contributions already had the right shape: a capability OFFERS a schema and
OFFERS an action, and whoever composes installs them, because the registry
belongs to the composition. Rollback is the same kind of thing, and the sentinel
had simply broken the pattern by self-registering.

`ROLLBACK_STATE` is the offer; `ambition_platformer2d_runtime` is a **dev-dependency**, so
the one-line registration is exercised by a test and absent from the crate's
real closure.

**Measured: 133 → 7.** `capability_demo` used to link every ambition crate in
`ambition_platformer2d_runtime`'s closure; it now links EIGHT foundations, with ZERO
occurrences of `ambition_platformer2d_actor_monolith` or `ambition_platformer2d_runtime` under
`cargo tree --edges normal`.

⚠ **and the offer is pinned honest from both sides.**
`the_plugin_alone_registers_no_rollback_state` asserts the plugin does NOT
quietly register it — otherwise the offer is a lie and the dependency returns
the first time somebody assumes the plugin was enough. ⚠ omitting the
registration is a DESYNC, not a missing feature: a rewind that restores the body
and not the cooldown lets a pulse fire twice from one charge.

→ ✔ **CLOSED (2026-08-01).** `RequiredRollbackState` lives in
`ambition_platformer2d_core::snapshot` — a foundation with no Bevy app and no GGRS —
so the DECLARING end costs a capability nothing.
`RollbackRegistry::missing_required_state` is the check, and it returns the
capability's own `why` rather than a bare name: a host that hits this needs to
know whether it is looking at a desync or an optional extra, and only the
capability knows which.

Exactly the content compiler's shape for `RuntimeDisposition::Runtime` —
declare the obligation next to the thing that has it, let the assembler refuse
when it is unmet.

⚠ **the OWNER is part of the requirement**, and its own test says why: two
capabilities may both reasonably want a `cooldown`, so a name registered by
somebody else must NOT satisfy this one. That would be the worst kind of pass —
reporting safety while the desync is still there.

→ ✔ **WIRED (2026-08-01).** `ModuleDraft::requires_rollback` declares it and
`install_into` REFUSES at `CompositionStage::Assembly`, naming the state, its
owner and the capability's own `why`.

⚠ **declared by the MODULE, not carried by the capability.** A capability is
deliberately an opaque closure with no identity here, and `capability()`'s own
docs refuse to guess an identity model before a real consumer needs one. So the
module that knows it mounted the thing says what it needs.

⚠ **it runs FIRST among the post-capability checks**, because it is the only one
whose failure is SILENT at runtime: an unregistered route refuses to activate
immediately and loudly, a missing rewind produces a desync much later and far
from its cause.

⛔ **and the first gate was WRONG in the direction that matters.** I gated on
"a `RollbackRegistry` resource exists" — and the headless foundation installs
one unconditionally, so every composition looked rollback-capable and a headless
game with no session was refused for a hazard it cannot have. The gate is the
DECLARATION (`rollback(n)`) now. Both directions have a test, and the negative
one carries the note that the registry is still present, which is exactly why
its presence proved nothing.

→ ▢ **also still hand-wired:** `PULSE_ACTION` is DECLARED and cannot carry a
device binding, so a consumer writes `PulseRequested` directly. That is the
`InputMap<SemanticAction>` migration, unchanged.

---

## ⇥ GPT 5.6 review of `a1b8da9..edaa355` — all six addressed (2026-07-31)

Every finding was VERIFIED against source before repair, and every repair was
probed. Two of them found further defects the review could not have seen.

1. ✔ **HIGH — production registration bypassed the compiler.** Correct, and my
   ledger claim was premature: `register()` still called
   `CharacterCatalogFragment::from_ron`, so the CLI/tests/`load_catalog` went
   through the compiler while the running game went through the legacy parser.
   `from_prepared` takes the LOWERED catalog with no re-parse and no second
   validation — running the old validator again would only create a second place
   for the two to disagree. Held by
   `the_registered_app_catalog_is_the_compilers_artifact`.
2. ✔ **HIGH — `RuntimeDisposition::Runtime` did not require lowering.** It does
   now, and the review's sharpest observation was that MY OWN toy schemas were
   `Runtime` and never lowered. `critter` is `AuthoringOnly` (it is the family
   the reference/asset/duplicate tests use, several of which need two sources)
   and `habitat` is the runtime family and lowers. The same handler compiles
   under one disposition and refuses under the other, which is the test.
3. ✔ **HIGH — same-schema last-wins.** Refused now, and the message names the
   missing contract: only the HANDLER knows whether two of its artifacts union,
   override or conflict, so a generic merge would be the compiler guessing.
4. ✔ **HIGH — the global menu frame died with two participants.**
   `populate_menu_control_frame_from_actions` used `single()`, which returns
   `Err` the moment a second participant exists, so pressing Start opened
   nothing. The comment claimed it folded every participant; it discarded all of
   them. The policy is STATED now — the primary seat owns the global shell
   controls, a per-seat surface reads `SeatMenuFrames` — and the same defect in
   the trail toggle went with it. **Probed red at 1, 2 and 4 seats.**
5. ✔ **MEDIUM — the fingerprint carried the checkout path.**
   `DirectoryAssets::label()` is a list of absolute roots and it was in the
   canonical bytes, so the same content fingerprinted differently per machine.
   The authored path is identity; WHERE it resolved is diagnostics and stays on
   `AssetProvenance`. Held by a test that compiles the same pack from two
   directories and first asserts the two roots really differed.
6. ✔ **MEDIUM — explanations merged generations and executions.** `explain` now
   returns ONE `ExecutionKey` group and `explanations` returns every one;
   `execution()`/`generation()` read the explanation's own key instead of
   whichever fact sorted first. ⚠ two resimulations within one generation still
   group together because nothing publishes an ATTEMPT counter — recorded, not
   hidden.
7. ✔ **THE PARALLEL-SCHEDULE PROOF, and it FOUND TWO BUGS.** The ECS path is
   sound (Bevy will not run two conflicting `ResMut` systems concurrently, so
   publishes are serialised and ordered by the scheduler). But writing the proof
   caught that the frame stamp ran in `Last` — **after every publisher** — so
   facts carried the PREVIOUS frame's tick; and that a publisher cannot know
   whether the host is resimulating, so every movement fact would have claimed
   `Original`. `stamp_causal_frame` now runs at the head of the sim phase and
   stamps tick + execution + generation, with `RecordingSet::Publish` ordering
   every publisher after it.

⛔ **AND THE DISK FILLED AGAIN — S14, four hours later.** 387G at 100%, 295G of
it in `target/debug/deps`, ALL of it from today. The S14 fix
(`CARGO_INCREMENTAL=0`) addressed the 110G incremental half and not the other:
every feature job is a full variant of the graph and cargo never prunes the
previous one, so ~10G/job × 28 jobs refilled the volume the same day. The suite
REFUSES below 40G free now, naming `cargo clean`, and carries `free_gb_at_start`
in the status file so a long run can watch the headroom fall instead of
discovering it at zero. ⚠ **a mid-suite ENOSPC reports as a wall of unrelated
compile errors** — the cause appears nowhere — which is why this is a refusal
and not a note.

---

✔ **THE COMPOSITION CLAIM IS PROVED (2026-08-01).**
`three_domains_answer_one_question_about_one_body_on_one_tick` runs the real
publishers in the real order and asks one question. The answer:

```text
why seat:1 changed on tick 60 (generation 0, original) — 4 fact(s)
  t60 g0 original [rollback]  original execution        (resimulated=false generation=0)
  t60 g0 original [damage]    took 40 and died          (outcome=damaged raw_damage=40 source=LeftTheWorld)
  t60 g0 original [lifecycle] spent its last stock      (remaining=0 eliminated=true)
  t60 g0 original [movement]  asked for lateral +0.00   (locomotion_x=0.00 on_ground=false …)
```

Every publisher already had a test proving it PUBLISHES. None proved they
COMPOSE, and that is the inspector's entire premise — three unrelated logs is
what it exists to replace. The test asserts one tick and one execution across
all four, because domains stamping their own clocks would produce four
explanations of four moments, which is exactly what makes a multi-domain log
unreadable.

⚠ **it lives in `ambition_platformer2d_runtime`, not `app_it`**, and deliberately:
`ambition_app` is still in `SKIP_FEATURE_JOB`, so a `causal`-gated test there
would never run in the suite. That rule has now caught three changes this
session; the runtime has a real feature job and is where the causal plugin
lives anyway.

✔ **THE INSPECTOR IS ON THE PUBLIC FACADE (2026-08-01).** `ambition_platformer2d::causal`
re-exports the vocabulary and the host half behind a default-OFF `causal`
feature, and `causal_sdk_tests` uses ONLY that path — no `ambition_causal`, no
`ambition_platformer2d_runtime`, no `ambition_platformer2d_actor_monolith`. The program's standard says an agent
should inspect *"without importing internal-shaped engine modules"*; if a
consumer had to reach past the facade, that is now a compile error rather than a
paragraph to believe.

⛔ **and writing it found a real SDK gap.** `CausalPlugin` installed only the
resource, on the reasoning that the sim schedule owns the frame stamp — so a
consumer who added the plugin to a plain `App` and published a fact got one
stamped tick ZERO, silently. **An SDK entry point that only works inside this
engine's own schedule is not an entry point.** The plugin stamps in `First` now;
`player_schedule` still stamps at the head of the sim phase, and that is not
redundant — `SimulationReplayState` is only meaningful there, so the sim-phase
stamp is the one that can tell an original tick from a resimulated one.

⚠ **`ambition_platformer2d` left `SKIP_FEATURE_JOB`**, for the fourth time this session that
rule has caught a change. Its entry said *"the facade's own tests gate on no
feature"*, which the SDK module made false. Cost MEASURED rather than assumed,
because the comment implied ruin: **6m59s and 2.1 GB** for the 17-feature
variant — real, and not the "tens of GB" that phrase suggested, which belonged
to `ambition_platformer2d_actor_monolith`' `profile` feature and is denied.

✔ **THE COMPILER IS ON THE PUBLIC FACADE TOO (2026-08-01).**
`ambition_platformer2d::content` re-exports the pipeline behind a default-OFF `content_pack`
feature, and `content_sdk_tests` composes a pack, validates it and reads a
refusal using ONLY that path — no `ambition_content_pack`, no
`ambition_characters`.

⚠ **`engine_schemas()` is the piece that made it an SDK rather than a
re-export.** A consumer cannot assemble the engine's own registry without
knowing which crate owns which schema — which is exactly the internal topology
the facade exists to hide. Now a game writes
`engine_schemas().register(my_schema())` and never learns that the character
catalog lives in `ambition_characters`.

Both ends of the loop are now public: `ambition_platformer2d::content` validates and
`ambition_platformer2d::causal` explains, each with an SDK test that would fail to COMPILE if
a consumer had to reach past the facade.

⚠ the facade's feature job picks `content_pack` up automatically now that
`ambition_platformer2d` is out of `SKIP_FEATURE_JOB` — the fix for the last trap prevented
the next one.

✔ **THE ACTION REGISTRY HAS A HOME (2026-08-01).** It was a value nobody held:
`ActionRegistry::with_engine_actions()` existed and no composition built one, so
a capability's action had nowhere to arrive. `ModuleDraft::actions(..)` declares
them, `install_into` builds `InstalledActions` from the engine's vocabulary plus
the modules', and a prompt / help screen / rebind UI asks ONE question and gets
both.

⚠ **two owners for one action id is a composition REFUSAL** naming the action
and both claimants — the same rule the content compiler applies to an ambiguous
schema, for the same reason: letting it through means iteration order picks the
winner.

So all three registries now follow one shape — **a capability OFFERS, the
composition INSTALLS, and a conflict refuses**: content schemas, rollback state,
and now semantic actions.

→ ▢ **still the migration, not the design**: an action can be declared and
queried, and cannot yet carry a device binding. `InputMap<SemanticAction>` is
the remaining step and its ordering is in the program doc.

✔ **P3 IS PROVED THROUGH THE PUBLIC SDK (2026-08-01).**
`composed_through_the_sdk` is the integration an author actually writes: ONE
module that mounts the capability and declares what it brings.

```rust
module.capability(capability_demo::PulsePlugin)
      .actions(&[capability_demo::PULSE_ACTION])
      .requires_rollback(capability_demo::REQUIRED_ROLLBACK);
```

Three lines, three contributions, and the composition installs all of them. The
test asserts the action reached `InstalledActions` beside the engine's, the
mechanic's own resources exist, and — ⚠ **the interesting one** — that the
composition REFUSED because nothing registered `pulse.cooldown`, naming the
capability's own *"twice from one charge"* reason. A capability whose rewind
state is skipped desyncs, and this is the composition catching it rather than a
player discovering it.

A second test validates the capability's authored content through
`ambition_platformer2d::content` alone — `engine_schemas().register(pulse_schema())` — with
no `ambition_content_pack` import anywhere.

⚠ **`ambition_platformer2d` is a DEV-dependency of the sentinel.** A mechanic must not depend
on the facade that mounts it, and the capability's real closure is still SEVEN
foundation crates with zero `ambition_platformer2d_actor_monolith` or `ambition_platformer2d_runtime`. Verified
after adding the test, not assumed.

✔ **THE CAPABILITY RECIPE (2026-08-01).**
[`docs/recipes/adding-a-capability.md`](../recipes/adding-a-capability.md), with
`examples/capability_demo` as the WORKED EXAMPLE rather than a snippet — the brief
asks for *"executable recipes and complete examples over architecture essays"*
and a reference implementation that compiles beats a page of prose.

It leads with the rule the whole session converged on — **a capability OFFERS,
the composition INSTALLS, a conflict refuses** — and names the trap that costs
most: do NOT register your own rollback state. `capability_demo` linked 133
crates that way and links 8 now (the eighth is
`ambition_platformer2d_shared_tangle`, for the SIM SCHEDULE seam — see S28), and the
recipe gives the one-line measurement
(`cargo tree --edges normal | grep -c ambition_platformer2d_actor_monolith`) that tells an author
which of the two they built.

⚠ every symbol it names was CHECKED against the crate, not written from memory.

→ ✔ **all six of the brief's agent questions are answerable from
`docs/recipes/index.md` now.** The sixth — *"how do I test it headlessly?"* —
turned out NOT to be a missing doc: `planning/engine/headless-verification.md`
already answers it in 113 lines and is good. The gap was DISCOVERABILITY, so it
is linked from the recipes index and from the capability recipe rather than
restated.

⚠ **writing a second page describing the same substrate would have been the
defect this session keeps removing** — two authorities over one subject, which
drift. The link says so out loud so the next person does not "helpfully" write
the duplicate.

⚠ and the capability recipe adds the half `headless-verification.md` cannot
know: a capability's OWN tests should not need the sandbox at all.
`capability_demo` builds a bare `App` and steps it — a mechanic that needed the
whole sim to be testable would be telling you its seams are wrong.

▢ **KB hygiene, standing — and MEASURED 2026-08-01, which changes the answer.**
30,708 lines against a 10,500 soft budget. Where they actually are:

```text
  docs/planning/*.md   11,508   the queues and live plans
  docs/planning/engine  15,257   33 design documents
  docs/planning/triage   2,713
  docs/planning/demos      899
  docs/planning/game       231
```

⛔ **the mechanical rule does not reach the problem.** S17's test — "a section
with no `▢` rows is archivable" — applies to LEDGERS, and the ledgers are the
smaller half. Applied to `queue-24h-2026-07-26.md` (4,466 lines) it frees **251**.
S24 was archived by it today and freed 46. That is ~1% of the overage.

**The budget is dominated by `engine/`: 15,257 lines of DESIGN documents**, which
have no `▢` rows because they were never ledgers. Archiving one means judging
that a design is built and stable enough to stop being read — a judgement about
the engine's own record, not a checkable property, and the kind of thing that
loses rationale nobody notices is gone until they need it.

→ **so this is a decision for Jon, not a pass to schedule**: which of the 33
design documents have become history. Recorded in
[awaiting-maintainer-decision.md](awaiting-maintainer-decision.md).
⚠ **the warning is non-fatal and should stay that way** until there is a rule
that can answer it, otherwise the pressure lands on trimming live plans — the one
thing the budget's own message says not to do.

---

## The spine — six architecture items, in dependency order

Each is a guard check. The ordering is by *how much later work it unblocks*,
not by how loud the defect is.

### ✔ S1. Slice H — merged and verified from the main checkout (2026-07-30/31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S2. Match activation is a TRANSACTION (`activation_transaction`, 2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S3. AC24 exists and was PROBED RED (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S4. The damage authority and the stocks loop (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S5. AE6 — a match DECLARES its rules; `VersusBorrowedRules` is deleted (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S6. `check_agent_kb.py` is GREEN (2026-07-30, `89e1c5426`)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S7. FB4b — the fighter brain emits inputs (2026-07-31) — ⚠ recorded gaps

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S8. AC22 — the exit oracle's COVERAGE moves with authored content (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S9. AF4b's residue — one id, two DISPLAY NAMES, and nothing checks it (2026-07-31)

**Why it is architecture:** it is the goal's *authority* case, and the class is
already named and half-instrumented, which makes the repair cheap. Jon ruled on
the product half (*"each robot version is a different character. Intended"*), so
what is left is an ordinary duplicate-authority question:
`player_robot_lineage.rs` and `character_catalog.ron` both author a version's
display name and sheet, and *nothing decides which wins per field*.

**Surveyed 2026-07-31 — two of the three fields are already handled, and the
third is the gap:**

* **voice** — CLOSED earlier this run, and it closed by finding a DEAD authority:
  the catalog outranks a definition's voice, so `player_robot_v2`'s Rust lines
  could never be heard and the test asserting otherwise read the struct instead
  of the runtime.
* **sheet** — guarded. `CharacterAuthorityConflict::SheetDisagreement` compares
  the two authorities as canonical sheet TARGETS (comparing the raw strings had
  the audit permanently red for ten characters).
* **display name** — was **UNGUARDED, and it is the same shape** (closed below).
  `audit_character_authority_parity` checks *one name claimed by many ids*
  (`AmbiguousDisplayName`); it does not check *one id presenting under two
  names*. `PreparedCharacterDefinition::display_name` comes from the Rust
  definition; `CatalogEntry::display_name` comes from the RON row; both are
  live, and different consumers read different ones —
  `id_for_display_name` resolves content addressing through the registry while
  a pedestal label reads the catalog. Rename a version in the RON alone and
  content that addresses it by name silently stops resolving.

**The repair:** a fourth `CharacterAuthorityConflict` variant — one id, two
display names — with the message naming both and which one each consumer reads.
Then fix whatever the shipped cast turns out to disagree about (measure; do not
assume it is only the robots).

**Guard:** the new variant's unit test in `character_runtime::audit`, plus the
existing parity audit staying empty for the shipped cast.

**✔ DONE.** `CharacterAuthorityConflict::DisplayNameDisagreement` compares the
prepared registry's name against the catalog row's for every id both authorities
declare (~10 in the shipped cast), and its message names which consumer reads
which — content addressing resolves through the registry, labels through the
catalog. Two unit tests: the disagreement is reported, and agreement is NOT
(the failure mode `SheetDisagreement` already had once, when it compared raw
strings and was permanently red for ten characters, which is to say it fired on
agreement).

**Measured:** `the_shipped_cast_has_one_authority_per_character` is still green,
so nothing in the cast disagrees TODAY — the guard landed before an offender did,
which is the cheap direction. `authority_parity_tests` 7/7.

~~▢ what is deliberately NOT done: removing the duplication.~~ **DONE the same
evening — see S15.** The objection recorded here ("registration happens before
the catalog is necessarily loaded") turned out not to apply:
`character_catalog::load_catalog()` is a pure parse of an `include_str!`
constant, so there was never an ordering reason for the Rust side to keep its own
copy.

### ✔ S10. Three things the suite found about ITSELF (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S11. The review's "constrain now" list, done as constraints (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S12. A second biome had no sky in every composition but one (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S13. The recurring class got an INSTRUMENT (2026-07-31)

Three instances in four days — the world-label pass (AE1), the parallax theme
load and the parallax layer sync (S12) — every one found by accident, every one
silent, every one the same shape: *the engine owns the code, the shipped app owns
the REGISTRATION, and every other composition runs an engine that half-runs.*
Finding the fourth by accident too would be a choice.

`scripts/check_engine_systems_are_engine_installed.py` asks the question
mechanically: for every `add_systems` call in a game crate, take the systems
whose path names the render crate, and report the ones no engine crate registers
anywhere. It runs in the suite through `pytest scripts/tests`.

**It found more the moment it existed**, and they are budgeted rather than
hidden: projectile visuals (`sync_projectile_visuals`,
`sync_projectile_charge_visuals` — a demo that fires one gets the simulation and
no sprite), the cutscene UI (`sync_cutscene_ui`), and
`sync_resolved_visual_quality` (without it `ResolvedVisualQuality` never leaves
its default).

✔ **all four are resolved and `UNCLAIMED_BUDGET` is 0** (verified 2026-08-01 by
running the script, not by reading this row). Three were MOVED to the engine
crate that owns the family — `sync_projectile_visuals` and
`sync_projectile_charge_visuals` to `ambition_platformer2d_host`,
`sync_resolved_visual_quality` to `ambition_render::quality` — and
`sync_cutscene_ui` is a WAIVER with its reason. `ambition_app` keeps only an
`.after(..)` edge on the projectile one, which is the ordering-is-not-
registration case the script now gets right.

⚠ **the ratchet fails BOTH ways, and that is what keeps the 0 real**: over is a
new offender, under is a fix that did not tighten the budget in the same commit.
A budget nobody tightens becomes a permanent allowance, and at 0 there is
nothing left to spend.

⛔ **and it reported two FALSE POSITIVES, which is the failure mode worth naming
because it is the expensive one.** `sync_bubble_shield_visual` and
`sync_morph_ball_visual` are registered by `ambition_render` and always were;
what the app writes is `.after(morph_ball::sync_morph_ball_visual)` on its dev
sprite overrides. An ORDERING EDGE is not a registration — the same mistake as
reading `run_if(in_mode(..))` as a system — and a guard that sends you to fix
something already correct burns more than a guard that says nothing. Caught by
checking before fixing; both are stripped now, with a test for each.

✔ **The projectile pair is FIXED and the budget is down to 2** (2026-07-31), and
the fix taught the thing worth carrying out of this whole thread:

⚠ **moving an app-local system engine-wide surfaces its hidden REQUIREMENTS as
panics, not as skips** — twice, in one slice:

* `sync_projectile_visuals` takes `Res<SheetRegistry>`, and the registry that
  holds sprite metadata was app-local TOO. Eight tests died on *"Parameter
  `Res<SheetRegistry>` failed validation"*. That is the same class one layer
  down: not a system nobody installed, a RESOURCE nobody installed.
  `SheetRegistryPlugin` is idempotent now (`is_unique() -> false` plus a marker,
  the `WorldLabelLayoutPlugin` pattern) and `HostProjectileVisualsPlugin` adds
  it, because a plugin that registers systems needing a resource should install
  the plugin that provides it rather than hope.
* `refresh_parallax_layers_on_quality_change` took two `SessionWorldRef` —
  `Single<..>` params, which PANIC rather than skip when nothing matches. They
  were always satisfied while only the shipped app ran the system; installing it
  engine-side (S12) ran it against session roots carrying neither, and killed
  the `ambition_platformer2d_host` job and both consumer fixtures. Both are `Option` now: a
  world with no room geometry has no parallax to refresh, which is an ordinary
  state.

The pair lives in `ambition_platformer2d_host`, not in `PlatformerPresentationPlugin`, for a
layering reason: the ordering edges name `ambition_platformer2d_runtime::projectile_schedule`
and `ambition_sim_view::PresentedPoseSet`, and `ambition_render` depends on
neither. The host crate's own description says it MAY name render, runtime and
sim_view — `camera_follow` is already there for the same reason.

**Three narrowings, each paid for:**

* **presentation only.** The first version asked the question of every engine
  crate and reported 42, most of them a game legitimately composing its own sim,
  HUD and menus. A guard with 42 waivers is a guard nobody reads. `--all` keeps
  the wide question available as a REPORT that never gates.
  ⚠ **the wide report is 18 once ordering edges are stripped**, and it is mostly
  Ambition's own game (map menu, inventory save/restore, mana regen, the startup
  censuses, the versus rules projection). ▢ **the two worth a second look are
  `dialog_input` and `dialog_reveal_tick`** — dialogue is an ENGINE capability
  (`ambition_dialog`), so a demo that opens a conversation would get no reveal
  tick and no input. Not acted on: no demo uses dialogue today, and the review's
  standard is that a missing architecture needs a blocked consumer.
* **run conditions are not systems.** It read `run_if(in_mode(..))` as a
  registration and reported four predicates as missing presentation.
* **comments are stripped.** The S12 commit left `// sync_parallax_layers left
  this list` in `plugins.rs`, and a guard that reads that as a registration is
  the exact recurrence the absence checker documents three times over.

**Guard:** `scripts/tests/test_engine_systems_are_engine_installed.py` — seven
tests, and the waiver-staleness one **fired on its first run**, catching two
waivers written for systems the render-scoped question never asks about.

### ⛔ S14. The disk filled up, and the suite is what filled it (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S15. AF4b's duplication is COLLAPSED (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S17. 2,187 lines of closed ledger left the live tree (2026-07-31)

`check_agent_kb.py` has been warning that `docs/planning` is 32k lines against a
10.5k soft budget, and says what to do: *"archive sections that are long done
rather than trimming live plans"*. The 24h ledger was 6,585 of them.

→ every section of `queue-24h-2026-07-26.md` with **zero open rows** moved
verbatim to `docs/archive/queue-24h-2026-07-26-closed-sections.md`, with a
one-line pointer left where each stood. 20 sections, 2,187 lines; the live file
is 4,438 and still holds all 57 open rows.

⚠ **the rule is checkable, which is the point.** "No `▢` in the section" is
something a reader can verify in one grep, and it is why this is safe to do
mechanically — no judgement about what still matters, and nothing edited on the
way past. Two sections share a letter with a later one (`F`, `H`, `AE` all
appear twice), so the move is by INDEX, not by title.

`check_doc_links.py` green; `check_agent_kb.py` still warns (30,100 lines — the
tree is bigger than one ledger) and still passes.

### ✔ S18. The consumer asks whether the engine DREW it (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S19. AC21 — the workspace builds with ZERO warnings (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ✔ S20. Sanic's own suite now says its sky moves (2026-07-31)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).

### ⛔ S21. The ladder probe measured ONE match, and the rollout HURTS (2026-07-31)

`ladder_probe` is the only instrument this repo has for "does the fighter brain's
rollout buy anything", and it ran each configuration **once**. The brain's noise
stream is seeded from the LEVEL, so one configuration meant one match, and its
time-to-first-self-KO was reported as the answer. Two such numbers then get read
as a difference.

→ it runs `--seeds N` (default 3) per configuration now, applying the seed to
`FighterState::noise` the same way it already applies the depth override —
asserting it FOUND a brain, because an override that reaches nothing turns N runs
into N copies of one and prints the spread as zero. Columns print
`median [min-max]`, and a seed where the event never happened is counted, never
averaged in as a large number.

⛔ **and the result contradicts what this file claimed this morning.** Six hours
earlier the same A/B read `d0 5.0s` against `d12 9.8s` — the evidence
`l3_earns_its_depth` was waiting for. Tonight, over three seeds:

```text
  9/d0      5.2s to first self-KO   (identical on every seed)
  9/d12     2.7s to first self-KO   (identical on every seed)
```

**The rollout makes this fighter die sooner, and the seeds agree.** Levels
1/3/5/6 DO vary with the seed, so the override works and level 9's stability is a
property of that profile rather than a broken knob — which is what makes this a
finding rather than a coin flip. The morning's numbers are not comparable to the
evening's (the ladder's own caution, applied to two tables six hours apart), and
`l3_earns_its_depth` cannot be authored off either of them.

→ **two more columns, and they rule out the obvious readings.** WHERE it died
and how fast it was going:

```text
  1      died at x≈-119 (3L/0R), vmax 400        5      x≈-116 (3L/0R), vmax 658
  6      died at x≈756  (0L/3R), vmax 609        9      x≈758  (0L/3R), vmax 752
  9/d0   died at x≈752  (0L/3R), vmax 773        9/d12  x≈758  (0L/3R), vmax 752
```

The platform spans x 110..530 of a 640 stage, blast margin 120 — so `-119` is
off the left lip and `756` off the right.

* **the direction belongs to the LEVEL, not the rollout.** 1–5 leave left, 6–9
  leave right, and level 9 leaves right at both depths. The rollout is not
  steering.
* **`vmax` is DASH speed at both depths** (dash is 760; a run caps at 270). The
  fighter dashes off the stage with the rollout on and with it off. Depth
  changes when, not what.

~~▢ the open question is now narrow: why does the movement veto let a dash
through?~~ **ANSWERED, and the hypothesis below was WRONG** — the brain never
chooses `Dash` at all (zero occurrences in the trace). The body reaches dash
speed through its own ATTACK LUNGES; see S22. The reasoning is kept because it
is a fair record of what a plausible-and-wrong hypothesis looks like next to the
measurement that replaced it: the shadow models a dash as `dash_speed` held for
`dash_time` and then coasts under `ShadowIntent::Hold`, which zeroes lateral
velocity instantly **when grounded** — while a real body that dashes off a lip
becomes AIRBORNE, where `AIR_FRICTION` (650) lets 760 px/s carry it hundreds of
px further. That was a hypothesis, not a finding, and it needed a per-decision
trace of one level-9 match before anybody touches the veto: the last two attempts
here were a paralysis that read as a 3× improvement and a fix that made the
number worse.

### ✔ S22. The fighter goes blind at the lip, and "offstage" was the wrong question (2026-07-31)

The trace from S21 answered its own question in two lines, and both were
perception bugs rather than rollout bugs.

**1. `supporting_floor` matched the body's CENTRE.** A fighter standing on the
very lip of a platform — centre a few px past the edge, feet still on it,
`on_ground` still true — matched no solid, so `floor_ahead` returned `None`,
which every ledge question reads as *"no ledge here"*. From the trace:
`x=496 floor_edge=Some(34)` then `x=537 floor_edge=None`, platform ending at
530. It matches the FOOTPRINT now, and being past the edge is a NEGATIVE
distance — which both consumers already handle (`cornered` against a margin,
`walks_off` against a body width).

**2. `Recovery` asked whether the body had left the ROOM.** `StageView` is the
room box; on a platform stage the floor is a subset of it, so a fighter that
walked off a 420px platform inside a 640px room was *"on stage"* for another
hundred px of falling. L1 answered `Neutral`, L2 offered Approach/Retreat/Jump,
and `Recover` — the one verb meaning "get back" — was not on the list. The trace
caught it twice in one death: airborne at x=582 and x=627, drifting right at
532 px/s, offered `[Retreat, Jump]`, taking `Retreat` both times.
→ new `WorldView::ground_below`, and Recovery is now *offstage OR (airborne with
nothing underneath)*. ⚠ gated on `!terrain.is_empty()`: a view with no solids is
a composition that does not publish terrain, not a body over an abyss — the
`juggle_escape` fixture is exactly that and read as `Recovery` the moment this
landed, which is the trap `floor_ahead`'s own doc warns about.

**Measured, same build, before → after:**

```text
  level 1   9.0s → 12.8s survived      level 5   7.0s →  8.8s
  level 3   8.7s →  9.4s               level 9   5.4s →  6.5s
  9/d0      8.0s →  8.9s               9/d12     5.4s →  6.5s
```

and levels 1–5 now die at x≈89–150 instead of x≈-117 — at the platform's own
edge rather than deep in the blast zone, which is what a recovery that engages
and *nearly* works looks like.

⛔ **still not a fix.** Every rung still loses all three stocks, and d12 is still
worse than d0. What is closed is that the brain can now SEE the lip and knows it
has nowhere to land; what remains is what it does about it.

▢ **and the re-trace moved the question OUT of the brain, which is the most
useful thing this thread has produced.** With Recovery firing, the trace now
reads:

```text
  x=549 vx=478 ... chose=Some(Recover) emit_x=-1.0
  x=598 vx=588 ... chose=Some(Recover) emit_x=-1.0
  x=647 vx=577 ... chose=Some(Recover) emit_x=-1.0
```

**The brain emits FULL LEFT for three decisions and the body keeps accelerating
right.** `emit_x` is the lateral input the decision actually wrote, so this is
not a scoring question, a veto question or a situation question any more —
whatever is happening is between the emitted frame and the body. ⚠ **and `Dash` appears ZERO times in the whole trace** — the brain never chooses
it — while the probe measures `vmax` at 750, which is dash speed (a run caps at
270). So the body reaches dash velocity through something that is not the
brain's decision. Candidates, in the order worth checking: a dash armed
elsewhere, an airborne input gate dropping the emitted frame, or a locomotion
frame convention.

✔ **the instrument this row asked for EXISTS** (`a9be6f2a0`).
`record_body_control_frame` publishes the frame as it sits ON the body, keyed by
`SubjectKey::Sim(identity.id)` — the same subject the brain's decision fact
carries since `a510ee92b` — so one `explain(tick, subject)` returns what the
brain asked for and what the body was holding, side by side. It carries
`dash_charges` / `dash_cooldown` for the first named candidate specifically: a
dash armed by something other than the decision reads as a spent charge and a
live cooldown with `dash_pressed` false.

⚠ it covers what `record_player_movement_intent` cannot — that one is keyed by
SEAT and skips any body without a player slot, which is every AI fighter.

⛔ **and it is the SECOND observer of that seam** — found while trying to point
it at a rollout, which is why pointing it at one was worth doing before
believing the row. `ladder_probe` already prints a `[seam]` line from a
hand-rolled query, added the same day the trace above was captured. They cannot
disagree about values (same components) but they can drift in COVERAGE, and both
sites now carry a note saying which is authoritative and why the other is still
there.

✔ **RESOLVED — the probe reads the fact and its hand query is gone**
(`fa5bdb5c2`). `ambition_demo_smash_app` gained a default-off `causal` feature;
`AMBITION_FIGHTER_TRACE=1` without it now SAYS the `[seam]` half is unavailable
rather than silently omitting half the trace.

## ✔ And the thread has its first real answer (2026-08-01)

**Running it found three defects before it found anything about the fighter**,
and the third is the one worth reading.

1. **The inspector could not be turned on.** `ladder_probe --features causal`
   panicked on tick one — *"Message not initialized"*. The causal observers read
   five message types and the composition registered ONE; every other
   registration came from whichever gameplay plugin happened to WRITE that
   message. An instrument now registers what it READS — the reader half of the
   rule this repo learned on the writer side.
2. **The BRAIN domain was compiled out of every host.** `ambition_platformer2d_actor_monolith`'
   `causal` feature forwarded to `ambition_combat` and not to
   `ambition_characters`, so the fighter decisions Program C exists to explain
   published nothing, in any composition, ever. ⚠ **the stderr trace still
   printed, which is what hid it**: the line appeared and the fact did not, and
   nothing distinguishes *"the brain published nothing"* from *"the brain
   cannot"*.
3. ⛔ **the observer was ordered a whole phase BEFORE its writer.** Registered in
   `PlayerInputSet::Brain`, while actor brains write `ActorControl` in
   `WorldPrep`. So it read the PREVIOUS tick's frame and printed it beside this
   tick's decision — **378 rows of "the brain asked left and the body held
   right", which is the exact shape of the bug it was built to find.** It was
   nearly recorded as the answer. This thread has been fooled by a measurement
   artifact before.

**Correctly ordered, over a level-9 run, 2279 decision rows:**

```text
  asked != holding        0        the emitted frame reaches the body INTACT
  direction disagreements 378      holding=-1.00 while vx=+270.00
```

⛔ **and the 378 are NOT a defect either.** That was the fourth artifact, caught
by asking the obvious follow-up instead of stopping at a suggestive number:

```text
  consecutive disagreement pairs   170
  decelerating toward what it holds 167
  accelerating AWAY                   3
```

A body at the 270 run cap cannot reverse in one tick. Every one of those rows is
a body that has just been told to go the other way and is slowing down to do it —
ordinary momentum, and the correct behaviour. Facing does not explain the sign
either (the flips split 210/168 across both facings, and the same `holding=-1.00`
gives `vx=+270` at facing `+1` and `-1` alike), which is what made the number
look like a convention bug for a second time.

→ **what this run actually establishes**, which is less than the row wanted and
is the first thing here that is MEASURED rather than argued:
* ✔ the emitted frame reaches the body intact — 0 of 2279.
* ✔ the input/velocity disagreements are momentum, 167/170 decelerating.
* ⊘ **no defect is visible on this configuration.** The 2026-07-31 observation
  was three decisions of full-left while the body ACCELERATED right; only 3 of
  170 pairs here accelerate away, so either that state is rare, configuration-
  specific, or was itself an artifact of the stale-frame trace it was read from.
* ⊘ `vmax` 750 does not appear — this run caps at 270 with `dash_charges` 0
  throughout, so the dash-armed-elsewhere suspicion is UNCONFIRMED here rather
  than disproven.

✔ **RETIRED — and the three survivors did not survive either.** The probe runs
levels 1/3/5/6/9 and both depth rows in ONE process, so the table above already
covers every configuration this thread ever cited. Inspecting the 3
`accelerating AWAY` pairs:

```text
  t=200  holding=+1.00  vx= -53.33
  t=202  holding=-1.00  vx= +55.00
```

The HELD INPUT reversed between the two samples. `|vx|` "grew" 53.33 → 55.00
only because both signs flipped — the body decelerated through zero and is now
moving right at 55 while being told to go left, which is the same reversal
everything else on this list is. **Zero accelerating-away rows survive.**

→ **the 2026-07-31 claim is retired, with a cause.** That trace compared the
brain's `emit_x` against a `vx` printed by the brain's OWN trace — both from the
brain's side of the seam, with the body's held frame never in it. There was no
body-side observation in the repository at all until `a9be6f2a0`. So *"the brain
emits full left and the body accelerates right"* was never measured; it was
inferred from one side.

**What is true, measured over 2279 decisions across every rung:** the emitted
frame reaches the body intact every time, and the only input/velocity
disagreements are a body reversing under a 270 run cap. ⛔ **whatever makes the
fighter lose its stocks, it is not this.** The next person on this thread should
start somewhere else, and now has a reason rather than a hunch.

⚠ five suggestive numbers on this thread have now turned out to be measurement
artifacts. Four of them were produced today, by the instrument built to end
exactly that.

---

### ✔ S23. A launcher row may lead to a QUESTION — Smash is on the title screen (2026-07-31)

Jon: *"Can we add the smash game to the shell launcher?"* The blocker was not the
Cargo dependency. Every provider before this one advertised its GAMEPLAY route as
its launcher entry (`register_experience` asserted the two were the same string),
and Smash opens on character select — so listing it the old way would have
activated the stage with no roster and dropped a lone duelist onto the platform
with nobody to fight.

**The seam: entry route ≠ session route.**
* `ExperienceRegistration::entered_at(route)` — where the launcher sends the
  player, when that is not the route that owns the session. The registration
  assert is now *"the entry route is this session route, OR one somebody already
  registered"*, and the refusal names the registered routes, because a launcher
  row pointing at an unknown route is a dead row that fails when a player presses
  it.
* `PlatformerExperienceAuthoring::entered_at` passes it through.
* ⚠ **the entry route needs its own EXPERIENCE id.** A route carrying the
  gameplay id starts a gameplay session, and a screen has no prepared world:
  `activate_prepared_platformer_sessions` would panic with *"requires an exact
  prepared-session publication"* before a panel drew. So `smash.select` is a
  frontend experience of the provider's own.
* `ShellComposition` no longer overwrites a launcher route the experience already
  registered — that is what made the standalone demo draw its select panels on
  top of the launcher's list of one game.

**Four playtest defects closed with it** (all Jon, same session):
1. **Menu text flashed — grew, then shrank — on every arrow press and every
   mouse hover**, on the title screen and in Sanic's pause menu. Menu text nodes
   are SPAWNED at the 1080p reference size and `resolve_menu_text_size` makes
   them the window's; in `Update` those were two different frames, and the
   launcher rebuilds its whole tree whenever the cursor moves. Moved to
   `PostUpdate`, before `UiSystems::Content` — the wrong size is never presented.
   Probed red before the fix (54px vs 36px).
2. **No way to have player 2 be a CPU** — and worse, no way to start at all
   alone: seats were offered one per PAD, a match needed two DECIDED seats, and
   every decided seat was a human. New `SeatSelection::Cpu`; Down adds one to the
   lowest empty seat (never the presser's own), Up takes the last back off.
   ⚠ **the first cut bound it to `start`, which does not exist on a keyboard** —
   `SandboxAction::Start` is Escape, which opens the pause menu. Down/Up are the
   two directions the screen has no other use for.
   `ready()` now also requires one HUMAN locked in, or the second CPU added
   started a match nobody was in.
3. **Seats without a controller were HIDDEN.** Right fix for the wrong problem:
   the complaint was a chair nobody could sit in saying "press confirm to join",
   and hiding it also hid the only other thing that chair can be. Every seat is
   shown; `manned` changes the WORDS.
4. **No quit to title.** In the host there now is one: a demo's mode does not
   suppress the universal pause menu (only Ambition's own rooms do), so
   Quit to Title returns to the launcher — asserted in
   `tests/smash_in_the_host.rs` rather than argued.

**⛔ and the defect that actually blocked it was in ANOTHER GAME.** The stage
never opened: the select screen published its roster, asked for the route, and
the shell re-minted a preparation transaction EVERY FRAME (`shell.smash_gameplay.1`
… `.40`). The versus stage's exit rule reads *"not on my route and a roster
exists → remove it"*, which was exactly right while it was the only publisher of
the global `MatchParticipantRoster` and became **"delete the other game's match"**
the day a second stage published one. Neither stage was wrong on its own; the
resource had no owner.
→ `MatchParticipantRoster::published_by` (an experience id) + `is_published_by`.
Versus now gates BOTH arms on `mine` rather than `is_some()`, so it neither
deletes a foreign roster nor seats one. **Only visible by running the whole host**
— every unit test on both sides passed throughout.

**And a rematch works.** The published roster is a plain resource that outlives
the session and every seat stayed locked in, so returning to select found it
already decided and already spent. Arriving at the screen now clears both —
reachable only from a host that can return there, which is why it lands here.

**⚠ and a LOBBY had no way to be seated at all.** Input participants are seated
from the `MatchParticipantRoster` — deliberately, so a controller left plugged in
does not silently become a second player in every game — and a character select
is what PRODUCES the roster. So only the primary participant existed while the
screen was up: four people at four pads shared one cursor, and three panels
invited a join no controller could reach.
→ `ambition_input::DeclaredInputSeats(u8)`: how many seats a FRONTEND surface is
offering right now. `seat_input_participants_for_roster` unions it with the
roster's human seats, so the same sweep retires both, and
`assign_local_seat_devices` then binds pad *i* to slot *i* exactly as it does
mid-match. The screen sets it on arrival and drops it on the way out.

**⚠ the select screen's systems were UNORDERED against the input producer.** A
windowed host rebuilds `SeatMenuFrames` from its participants every frame
(clearing first), so whether the screen saw a press at all depended on where Bevy
put it; the demo's own app never installs that producer, which is why it always
worked there. Now one chain in `InputSet::Consume`.

`./run_game.sh smash` launches the standalone shell, whose home is the select
screen.

**Also this session, from the same playtest:** the protagonist's swipe had a
**0.12s (7-frame) windup** — `default_player_action_set` — so every attack
telegraphed before it came out. A windup is a promise made to an opponent, which
is right for a Striker and wrong for the hand the player is holding. Now `0.0`:
the Startup phase still exists (anim + read-model swing use it) and is zero
seconds long, so the volume, the slash arc and the cue all land on the frame of
the press.

✔ **CLOSED (2026-08-01, `ca5042eba`) — and the seam it needed was already
built.** With the pause menu open OVER the select screen the arrows drove both,
because the two read different channels and neither could consume the other's
edge. The row asked for *"a suppression seam the facade exposes, not a feature
edge added to a demo"*; that seam is input CONTEXTS, and it had no production
consumer.

* `PAUSE_CONTEXT` at priority **190** — above cutscene, dialogue, select and
  gameplay, all four being things a player pauses out of, and below `DEBUG`,
  because an inspector a pause could hide is useless exactly when it is wanted.
* the pause menu declares it CAPTURING while open, the same shape
  `declare_launcher_context` already used.
* the select screen declares `SELECT_CONTEXT` and asks whether it still owns
  each seat.

`resolved()` stops the open list at the first capturing claim, so the pause claim
closes the screen's context and the screen stops driving. **Neither side names
the other** — the test names both only because it stands in for the host.

⚠ **this is the per-seat resolution's FIRST PRODUCTION CONSUMER.** See the P2a
row, which recorded that every declarer wrote the same claim to every seat and
that `SELECT_CONTEXT` had no declarer at all. It has one now — and note it is
still a whole-screen claim, not a per-seat divergence, so the *per-seat* half of
that row stays open and its analysis stands.

PROBED: with the gate ignored the pause test reports `left: 1` — the CPU still
added, the original bug. The sibling test is the CONTROL: it asserts the screen
does drive when nothing is over it, so a gate that never opened could not pass
as a fix.

✔ **CLOSED 2026-08-01 — and it was already fixed; what was missing was the
evidence.** `shell_frame_key` excludes `launcher.selected` and says why in a
comment, but nothing measured it, so "the churn is still there" survived in this
queue as a belief. `moving_the_launcher_cursor_does_not_respawn_the_ui_tree`
(`ambition_game_shell/src/tests.rs`) now asserts the launcher root is the SAME
ENTITY after a cursor move.

⚠ **identity, not a node count** — a rebuild that happened to spawn the same
number of nodes passes a count and has still thrown the tree away.

⚠ **the first version of the test was VACUOUS and the probe caught it.** With
one `app.update()` after the command, restoring `launcher.selected` to the
rebuild key still passed: the command is consumed and the cursor moves in that
frame, but `render_basic_shell` has already computed its key, so the rebuild
lands on the NEXT frame. Two updates, and the probe fails exactly as it should.
⚠ also gated `#[cfg(feature = "basic_presentation")]`, because a bare
`cargo test -p ambition_game_shell` renders no launcher at all — its 38 green
tests never touch this presentation. The runner's per-crate feature job runs it.

---

### ✔ S24. The GPT 5.6 review of `7fb5c9a..a1b8da9` — all THREE closed (2026-08-01)

Closed; moved verbatim to [the archive](../archive/queue-72h-2026-07-31-closed-sections.md).
Finding 2 — *the fighter scores a move and executes a generic attack* — was the
last one open and closed in two commits: the emission (`3361f5cdf`) and the
acceptance test that asserts `MovePlayback.spec.id` rather than `melee_pressed`
(`b8a133b3d`).


### ✔ S25. Five goal checks were failing at the LINKER, not in the code (2026-07-31)

`rollback_match_activation::rewinds_across_the_activation`, `stocks`,
`resolved_combat_tuning` and the two that build the app's test binary all
reported red while every one of them passed when run by hand with
`CARGO_INCREMENTAL=0`. The failure was three screens of

```text
mold: error: undefined symbol: anon.<hash>.llvm.<hash>
>>> referenced by libambition_runtime-<hash>.rlib(…rcgu.o)
```

— a link error with no source cause, cured only by discarding the incremental
cache, and hit THREE times in one afternoon. A guard that reads the repository
cannot tell that apart from broken code, so a green tree reported itself red and
the reason sat in a linker dump nobody reads.

`.cargo/config.toml` now sets `incremental = false`, with the diagnosis written
where the next person will look. Two other facts point the same way:
`./run_tests.sh` already exported `CARGO_INCREMENTAL=0` (S14 — the disk filled at
110G, and this cache was **25G** of it), so an ad-hoc `cargo test` now agrees with
the suite; and with mold on this box a cold workspace build is ~3 minutes against
the three full rebuilds the bad links cost.

⚠ **the `▢` check is not a failure.** `! grep -q '▢' queue-72h…` is the ledger's
"there is still work" signal, and the goal's own text says to refill it rather
than stop. It stays red while rows are open, which is the point.

⚠ **a `▢` whose own first bullet says `✔ CLOSED` is the commonest stale row.**
Four of them today, three marked HIGH, all in the backlog queue — the file nobody
re-reads because the spine is elsewhere. The closure gets written UNDER the
marker and the marker never turns. Scan for it rather than re-reading every row:

```python
# ▢ lines with a nearby ✔ that says landed/closed
for i, l in enumerate(lines):
    if '▢' in l and any('✔' in c and ('landed' in c or 'CLOSED' in c)
                        for c in lines[max(0, i - 2):i + 7]):
        print(i + 1, l)
```

⊘ **deliberately NOT a guard script.** The `✔/▢` prefix is a real convention here
for a row with a closed half and an open one, so a check would flag correct rows
constantly and get waived — which is worse than no check. A reviewer can tell the
two apart in a second and a regex cannot.

⚠ **and do not read the COUNT as a progress bar.** The check greps the whole
file, so prose that mentions the marker — a row explaining that `engine/` has no
`▢` rows, for instance — counts as one. On 2026-08-01 the count went 40 → 43
across three commits that closed five rows. The check answers "is there work",
which is all it is for; anything finer needs to count list items, not characters.

---

### ▢ S26. Open rows the 07-31 session left behind — FIVE of six closed 08-01

Logged at Jon's ask. Nothing here was speculative: each was a thing that had been
OBSERVED and not closed. Items 1, 3, 4 and 5 are now closed, item 6 became a
decision for Jon, and item 2 is narrowed from four un-ruled-out causes to three.

**1. ✔ CLOSED 2026-08-01 (`b8a133b3d`) — the fighter's chosen move EXECUTES.**
`scored_move_tests.rs` asserts `MovePlayback.spec.id` and reaches it through the
production chain: the real kit builder, the real decision, the real emission,
then `resolve_attack_gestures` + `trigger_moveset_moves`. The fixture's premise
is REACH, not names (a jab reaching 16 vs an up-tilt reaching 84 at a 70-unit
gap), and it was PROBED against the original bug — the emission's axis forced
back to `Vec2::ZERO` reports `left: "jab", right: "uptilt"`. The close-range
test is the control and stayed green under that probe, so the pair pins that the
direction is the SCORED one and not a hard-coded `Up`.

~~The binding seam
landed (candidate carries its press, the kit enumerates presses, `PendingAttack`
carries the choice through the jitter, the checksum covers it) and the emission
is tested. What does not exist is the review's acceptance condition:

```text
a body owning two materially different moves
brain scores move B above move A
the pending action matures
→ MovePlayback.spec.id == B
```

⚠ **do not accept a test that observes `melee_pressed`** — that is the
disconnected seam itself. (Honoured: the shipped test asserts playback.)
~~The cheapest home is probably an `ambition_platformer2d_actor_monolith`
ECS fixture: `fighter_harness.rs` already builds a real headless App around
`ActorControlFrame`, but it does not install the gesture resolver or
`trigger_moveset_moves`, so those have to be added to it (and that is worth
doing — the harness is the natural place to prove any brain→moveset claim).

**2. ⛔ Jon cannot enter doors, and three probes say the mechanism is elsewhere.**
Reported 2026-07-31 ("in the last build I can't seem to enter doors anymore?").
`tests/door_entry.rs` now drives all three real paths — the sim harness's
`interact`, the SHIPPED HOST pressing the key the live `InputMap` binds, and the
deliberate double-tap Up (plus "one press of Up opens nothing") — and all three
pass. So it is not the buffer, the gesture, the binding, the input context or the
room graph.

✔ **and the rollback-host candidate is ELIMINATED** (2026-08-01, `410d26443`).
It was a real coverage gap — the door probes ran on hosts with no
`ConfirmedFrameBoundary`, so they took the EAGER branch every time, while
`rollback_room_transition.rs` drove the DEFERRED branch through an `EdgeExit`,
which fires on overlap. A door is the one activation needing a buffered interact
press, and the deferred branch clears that buffer before recording its intent.
With the combination covered, the door opens.

⛔ **and the first version of that test REPRODUCED the bug** — 600 frames, room
unchanged, `PendingLifecycleCommit { pending: None }`. It was the test.
`stand_in_a_door` places the body with a direct `world_mut()` write, which is
right for a host with no rollback and silently wrong here: position is ROLLBACK
STATE, so the body was restored out of the door before any frame the system saw.
`sim.teleport_player` rebases the baseline; the existing rollback test says so in
one line and I did not read it first. **Sixth measurement artifact of the day and
the worst-shaped one — it would have filed a false bug report against a symptom a
real person reported.**

→ **still un-ruled-out**: a specific room's authored zone, the pause/dialogue
`gameplay_allowed` gate, or the prompt not showing so the wrong key was pressed.
**Needs from Jon: which room, which door, and whether the prompt appeared.**

**3. ✔ CLOSED 2026-08-01 (`aaeced7d3`) — an arrow press moves the highlight, not
the tree.** The colour was BAKED at spawn by `control_bg`, so respawning was the
only way to change it. Two of the three pieces are engine-wide rather than
launcher-specific: `MenuVisualState` now carries the authored `important` (not
runtime state, and there on purpose — without it a restyle has to reach back into
the page data, which is the coupling that forced the rebuild), and
`restyle_bevy_ui_menu_controls` recomputes the background from that component on
`Changed<..>` for **every menu in the app**. The launcher then writes the
component from its cursor and drops `selected` from the frame key.
⚠ **the frame-key test is the one that matters.** The in-place path keeps working
if the key regresses, so the churn would come back silently. PROBED: restoring
`launcher.selected` fails with `left: "launcher:0:Ambition:[]"` /
`right: "launcher:3:Ambition:[]"`, and a control asserts the key still moves when
the ROWS change.

**4. ✔ CLOSED 2026-08-01 (`ca5042eba`) — one press moves one thing.** The
"suppression seam the facade exposes" this row asked for already existed and had
no production consumer: input CONTEXTS. `PAUSE_CONTEXT` at priority 190, declared
CAPTURING by the pause menu; the select screen declares `SELECT_CONTEXT` and asks
whether it still owns each seat. `resolved()` stops the open list at the first
capturing claim, so the pause claim closes the screen's context — **and neither
side names the other**, which is what kept the oracle rule intact. PROBED: with
the gate ignored the test reports `left: 1`, the original bug; its sibling is the
control, asserting the screen DOES drive when nothing is over it.

~~The screen reads
`SeatMenuFrames` and the menu reads `MenuControlFrame`, so neither can consume
the other's edge, and arrows navigate the menu AND add/remove CPUs behind it. The
demo cannot even NAME `ShellPauseMenu` (`basic_shell_presentation` is not in
`all_capabilities`, which is the oracle rule working). The fix is a suppression
seam the facade exposes — "a frontend surface is covered" — not a feature edge
added to a demo.~~

**5. ✔ CLOSED 2026-08-01 — the fixture authors a 100-point pool.** The percent
test reported *"went into the rewind at 3760%"* — 188 damage over a max of 5.
Correct for what it asserts (percent is preserved, and percent is NOT health) and
nonsense to anybody who opened it. It sets `health.max = 100` now, so the number
is **188%** — a Smash percentage a reader recognises, still above 100%, still
proving the meter is not the pool.
⚠ **and the number is PINNED, not just bounded.** The old assertion was
`before_percent > 1.0`, which is why 3760% sat there for weeks: nothing said what
the value was supposed to BE. A change to the seeded pool cannot quietly turn it
back into nonsense.

**6. `docs/planning` is over its soft budget — MEASURED 2026-08-01, and the
measurement moved it to Jon.** 30,708 lines against 10,500, of which **15,257 are
the 33 design documents under `engine/`**. S17's rule — archive what a reader can
CHECK is closed — applies to ledgers, and applied to every ledger it frees about
1% of the overage (251 lines in the backlog queue; 46 taken from S24 today).
`engine/` has no `▢` rows because those files were never ledgers, so archiving
one means judging that a design has become history. That is recorded as a
decision in [awaiting-maintainer-decision.md](awaiting-maintainer-decision.md)
with the file sizes and a plausible first candidate.
⚠ **not to be resolved by trimming live plans**, which is what the pressure
produces when the only reachable lines are the ones still in use — and not by
raising the budget to silence the warning.

---

## Standing hygiene, checked every turn

- ▢ **the app compiles** — `cargo check -p ambition_app` is the gate, not a
  per-crate check. A green per-crate check is encouragement, never evidence.
- ▢ **nothing is left uncommitted** — finished work is committed work; the
  guard's stall counter reads COMMITS, not turns.

---

## Backlog, carried from the 24h ledger (all still open)

Not repeated here in full — read the named row in
[queue-24h-2026-07-26.md](queue-24h-2026-07-26.md). Listed so the spine's
completion is never mistaken for the queue's.

- ✔ **API finding (a)** — an inert rollback registration is now DETECTED
  (2026-07-31). It cannot be made unrepresentable in the type system: whether a
  registration applies depends on which entities carry the component, which is a
  runtime fact. So `rollback_coverage` gained the inverse sweep — for every
  entity in the populations it already builds, a component registered as
  snapshot STATE on an entity carrying no `require_rollback` anchor is named,
  because the registry lists it, the existing sweep counts it "accounted", and
  nothing restores it. Probed red on an injected mistake before being trusted.
  Two waivers with stated reasons (a room visual, an authored pedestal) and ONE
  RULE that needs no maintenance: an entity whose only registered components are
  construction provenance (ADR 0030 — written once, never again) is outside the
  envelope by construction. The moment such a prop gains a component that
  CHANGES, its stranded set stops being provenance-only and the sweep speaks.
- ✔ **API finding (g), input half** — CLOSED 2026-07-31, and the fix was a
  re-export. `drive_seat_frame` has existed in `ambition_platformer2d_runtime::rollback` since
  queue Y1, fully built and tested; it was never surfaced through the facade, so
  "no public seam drives input to a named seat" was true of the SDK and FALSE of
  the engine. That is the worse of the two shapes and the harder to notice —
  nothing is missing, nothing fails, the capability is simply unreachable from
  where a consumer stands, and the finding it produced described the facade while
  reading like a gap in the engine. `ambition_platformer2d::sim::drive_seat_frame` sits beside
  `drive_control_frame` now, and `fixtures/minimal_game` asserts a consumer can
  name BOTH without leaving the SDK. Found by needing it: the smash demo's select
  screen has four people pressing their own buttons.
- ✔ **AD8** — a registered character's voice IS the situation-blind floor now
  (2026-07-31). `npc_hit_bark_line` and `npc_hostile_bark_line` never consulted
  the prepared registry, so a registered-only character that got hit said "Hey."
  and one that turned hostile said "That's it!" — the engine's words in a
  character's mouth, which is the exact failure the registered-character voice
  seam exists to end. Both now take the registry and consult it BELOW the catalog
  and ABOVE the generic text, which is where the doc always said the floor was.
  The catalog still outranks it (a test pins that), and the new test opens with a
  vacuity check asserting the engine speaks without the registry — the state this
  closed.
- ✔ **AC23** — the session install no longer LAUNDERS an unhealthy timeline
  (2026-07-31). `install_session_with_ownership` wrote
  `RollbackSessionStatus::default()` unconditionally, so a divergence reported on
  the old timeline vanished the instant a new session replaced it; exactly one of
  four call sites guarded against that, with a comment explaining why it had to —
  a seam asking every caller to remember a rule the seam can enforce. It carries
  now: an unhealthy timeline hands its reason to the one replacing it, and
  `acknowledge_and_clear` is the only way out, named for what it is. ⚠ the
  mismatch FRAME NUMBERS deliberately do not carry — frames restart at zero per
  session, so carrying them would report a mismatch at frames the new timeline
  has not reached. The reason survives as prose, which is the part a reader acts
  on. Four tests in `ac23_tests`; the `lifecycle_commit` caller's own guard is now
  belt-and-braces rather than the only thing standing there.
- ✔/▢ **AC19 / AC21 / AC22** — the three `ambition_app` warnings are CLEARED
  (2026-07-31): a duplicated `#[test]` in the rollback observatory, an unused
  `bevy::prelude::*` in the versus roster tests, and `PropBox.y` in the exit
  oracle — recorded and never read, so the standoff it feeds is a purely
  HORIZONTAL question and carrying a centre height made a reader think otherwise.
  `cargo check -p ambition_app --all-targets` is warning-clean.
  ⚠ **still open:** demote the DebugLabel spacer to a lint, and give the exit
  oracle a fixture room it owns.
  → **surveyed 2026-07-31: the seam for that already exists.**
  `SandboxSim::build(options, compose)` takes an arbitrary composition closure —
  `ambition_sim_composition` is just the default one — so a test can compose
  Ambition's content and then append or override a room it authored. The
  oracle's dependence on `combat_calibration_lab` is a CHOICE, not a constraint.
  ⚠ and the docstring already records what that choice cost: the route "DID stop
  doing two of the four things and stayed green for it" when a block's top face
  moved. A checksum oracle whose route is content-shaped drifts silently every
  time the content moves.
  The work is authoring the fixture room (a patrol enemy, a striker pair, a
  breakable, a switch, one floor route) as a `RoomSpec` the test owns. Bounded,
  and it also gives §8's owed FB4b scenario suite the pattern it needs — an RL
  or ladder scenario should not be shopping in the shipped world either.
- ✔ **AF4b** — the robot lineage's duplicate voice authority is RESOLVED
  (2026-07-31), and the duplicate turned out to be asymmetric. `Incarnation`
  authored `voice` while `character_catalog.ron` authored `barks`/
  `fallback_dialogue`, with v0's lines duplicated verbatim between them — but the
  catalog OUTRANKS a definition's voice (`npc_ambient_bark_line` asks
  `catalog.bark_line` first), and `CatalogEntry::bark` falls through the
  situation pool to `fallback_dialogue`. So `player_robot_v2`, which authored
  both, could never reach its Rust voice at all: it was DEAD, and
  `every_incarnation_says_something` was green over it because it asserted on a
  Rust literal rather than on what anybody hears. v0 and v3 authored only
  `barks.hall`, so their lines spoke everywhere EXCEPT a pedestal — the one place
  they are usually seen. Per Jon's ruling the content row wins: both rows gained
  a `fallback_dialogue` carrying exactly the lines they had, `Incarnation` lost
  the field, and the test now asks the RUNTIME (`bark_line` for Hall AND Idle).
  Probed red by removing a `fallback_dialogue`.
- ✔ **AD7 residue — CLOSED by looking (2026-08-01), and there was never an
  owner to tell.** The two untracked configs naming retired ids live in
  `.claude/worktrees/agent-a2793691aa9a17c3f/` — an abandoned AGENT worktree, not
  a person's working copy. `git worktree list` shows two such trees on their own
  branches; nothing regenerates from them and nobody is going to fix them.
  The LIVE tree's only `npc_player_robot_v2` hit is a doc comment in
  `player_robot_lineage.rs` explaining why that id is kept — the lineage design,
  not a stale reference.
  ⚠ **worth remembering when a grep looks alarming**: `.claude/worktrees/` is
  inside the repo and full of old copies of everything. A survey that does not
  exclude it reports the past as the present, which is how this row was written.
- ✔ **Campaign 3A — the ROUND is a lifetime** (2026-07-31).
  `begin_round` opened with one hand-written query despawning `LiveProjectile`.
  Every transient family added after it — a strike volume, a summon, a lingering
  hitbox — needed another query THERE, and forgetting one fails silently: the
  entity is simply still there in a round that never asked for it.
  * `lifecycle/round.rs`: `RoundScopeId`, `RoundScopedEntity`,
    `ActiveRoundScope`, `RoundSpawnScope`, `RoundScopePlugin`. Mirrors
    `session.rs` exactly — same id-carrying component, same captured-at-spawn
    scope, same `apply_to`. Named `RoundScopedEntity` and not the campaign
    document's `OwnedByRound`: three scope markers that read alike are one
    concept, and the doc is overrulable.
  * ⚠ **round scope is a LIFETIME, not a provenance.** `SpawnOrigin::Dynamic`
    already classifies projectiles/summons/drops and culling on it would be
    cheaper — and wrong. A dropped item is dynamic and should SURVIVE a round.
    Where an entity came from does not say how long it should live.
  * ⚠ **the first round had no scope.** `begin_round` runs at a TRANSITION, so
    minting there leaves round one unscoped and its debris survives into round
    two — the exact leak, reintroduced by the shape of the state machine. The
    round now opens where the countdown reaches zero, which is the one place a
    round actually goes live, and `begin_round` only ENDS one.
  * ⚠ **the existing test's fixture was not a projectile.** It hand-spawned a
    bare `LiveProjectile` marker, which the old `With<LiveProjectile>` sweep
    caught because a marker was all it looked at. Membership is stamped by the
    SPAWNER now, so the fixture fires through `SpawnProjectile` — otherwise it
    tests a population the game never produces and reports a leak that is not
    there.
  * Probed red twice: disabling the culler, and removing the spawner's stamp.
- ✔ **3A residue — ANSWERED, and the answer is NO** (2026-07-31). The row asked
  whether strike volumes and summons should defer to the round scope. They
  should not, and adding `RoundScopedEntity` to them would be the mistake:
  * **Strike volumes are already round-bounded, transitively and by
    derivation.** `retire_orphaned_strike_volumes` holds a volume alive iff its
    owner's `MovePlayback` is inside the window that spawned it, and
    `begin_round` removes `MovePlayback` from every fighter — so at a boundary
    every volume is orphaned and despawns on its own. Round-scoping them would
    be a SECOND authority over one lifetime, which the versus code already warns
    about in as many words: *"a second authority over a volume's lifetime is how
    the two come to disagree."*
  * **Summons are actors with their own lifetime** — a respawn policy, a room
    scope, an encounter that may span rounds. A boss's minion is not debris.
    Where a game DOES want a round-scoped summon, `RoundSpawnScope` is already
    the API and the summoner states it at spawn, which is the right place for a
    per-summon decision.
  The general rule this leaves: **round-scope things whose lifetime nothing else
  derives.** A projectile in flight is owned by nobody once fired; a strike
  volume is owned by a playback; a summon is owned by itself.
- ✔/▢ **Campaign 3B — the "one helper" half is BUILT, and better than the doc's
  design; the survey found a path that skipped it** (2026-07-31).
  * The doc proposes a `BodyLifecycleTransition` message every restart path
    emits. The code already does something stronger: the announcement is
    **DERIVED** — `reset_body_clusters` raises `BodyLifetime::restart_pending`
    and `announce_body_restarts` turns it into `ae::BodyRestarted` once per tick.
    A caller that has never heard of the event announces correctly, which a
    message-per-path design cannot promise. *"A doc comment cannot make seven
    call sites remember."*
  * ⛔ **and the smash stocks respawn skipped it — code written the same day.**
    It used `transit_body`, whose contract is that "axis maneuver state (coyote,
    buffers, dash timers) is deliberately KEPT — those are time facts, not place
    facts". Right for a blink, wrong for a knockout: a fighter came back holding
    the dash timer and buffered jump it died with, and no PROVIDER heard about
    the respawn either. **This is the versus stage's 2026-07-28 bug reintroduced
    in a new demo three days later** — which is the argument for the derivation:
    it made the fix one line instead of a hunt through every provider.
  * ▢ **still open:** the `reason` and `generation` half of 3B's sketch
    (`BodyRestartReason`, `old_generation`/`new_generation`). Not built, and not
    obviously needed — no consumer today asks WHY a body restarted. Build it when
    a consumer needs to branch on the reason, not before; a field nobody reads is
    a field that drifts.
  * ✔ **the survey found a second defect, in the authority itself.**
    `reset_body_clusters` restored air jumps from `DEFAULT_TUNING`, so a body
    under non-default tuning came back from every reset with default jumps.
    FOUR of five call sites knew and followed with
    `refresh_movement_resources_clusters(.., tuning.air_jumps)`; one did not, and
    nothing said so. **An authority that requires a follow-up call is not an
    authority** — it is a two-step ritual, and the second step is the one people
    forget. The count is now a PARAMETER, every caller states it, and all four
    follow-up calls are deleted because the reset already restores dash charges
    from the same `BodyAbilities`.
    ⚠ the test for it needed an ability set with `double_jump` — `air_jump_count`
    returns 0 without it, so a default fixture passes under the bug.
  * ✔ **the two ENEMY paths are converted** (2026-07-31) — the revive and the
    respawn in `features/enemies/integration.rs`, both of which called
    `transit_body` under "respawn is a discrete transit (ADR 0024 authority)".
    Right about the POSE and silent about the rest. `reset_body_clusters` transits
    internally, so saying the stronger thing loses nothing that comment protected.
  * ⛔ **and converting them exposed a live trap: `BodyBaseSize::default()` is
    the default PLAYER size, and NOTHING on the enemy spawn path ever wrote it.**
    Every enemy body in the game carries a base size that is not its own. It was
    invisible because nothing reset an enemy through the authority that restores
    it — so the first conversion would have silently resized every enemy to a
    player. The respawn records the identity size before resetting; the test
    asserts it and goes red without it.
  * ✔ **the trap is closed at CONSTRUCTION** (2026-07-31).
    `ActorBody::from_kit` takes the identity size and records it, so a
    non-player body cannot be built without one — the same "ask at the call
    site" move as the air-jump parameter, and the answer was already in scope at
    every caller. The two local workarounds at the respawn sites are DELETED:
    construction carries it now, and the test that pinned them still passes
    without them (and goes red if `from_kit` stops recording it).
  * ✔ **the boss revive is converted too**, which the construction fix is what
    made safe: `reset_body_clusters` restores `base_size`, so converting it
    earlier would have resized every boss on every room reset.
    `boss_revives_after_a_room_reset` and 22 boss tests hold.
  * **Every reset path in the engine now goes through one verb**, and every one
    of them announces. That is Campaign 3B's completion criterion, reached by
    fixing the authority rather than by adding an event.
- ✔/▢ **Campaign 2 — the before/after HARNESS exists** (2026-07-31). Its gate
  ("must not start while another authority migration is in flight") is clear:
  Campaign 3's body-lifecycle migration closed the same day.
  * `rollback_schema_baseline.txt` records all **351 registrations** as
    `deterministic_dump` writes them, and the test names the exact added/removed
    LINES rather than reporting a changed hash. `schema_fingerprint()` hashes
    this same text, so the readable form is the one worth committing — during a
    migration the question is always *which* line moved.
  * A second test pins the promise the whole migration leans on: the dump is
    identical from a differently-composed app. If it were order-sensitive, every
    migration commit would look like a schema change.
  * Probed red by deleting one baseline line: it reports that line by name.
  * **What the baseline SHOWS is the problem statement, measured: 336 of 351
    registrations are owned by `ambition_platformer2d_runtime`.** The other 15 are
    `ambition_content`'s bosses — which is proof the domain-owned path already
    works and nothing structural blocks the migration.
  * ✔ **R3: the projectile domain is MIGRATED, and the dump is byte-identical**
    (2026-07-31). `rollback/domains/projectiles.rs` owns its 14 registrations;
    the central function names zero projectile types. The baseline is what says
    the move changed nothing.
  * ⛔ **and the harness had to be fixed BEFORE the first migration could pass.**
    `schema_fingerprint` hashed the registration OWNER — an organisational label
    nothing reads — so relocating a registration changed the WIRE FORMAT and two
    peers running identical snapshot logic would have been declared incompatible
    for having moved a line between modules. R3 asks each migration to "verify
    the fingerprint is unchanged", which was impossible while that held. The
    fingerprint now hashes `schema_dump()` (name, kind, type, projection) and
    `GGRS_ROLLBACK_SCHEMA_VERSION` bumped 4 → 5, because peers on v4 computed a
    different number over the same schema and must not think they agree.
  * The collected domain surfaced a duplicate: `SpawnProjectile` was registered
    twice, once via `ambition_projectiles::SpawnProjectile` and once via
    `ambition_projectiles::spawn_message::SpawnProjectile` — the same type by two
    paths. The registry deduplicates identical descriptors, so it was invisible
    and harmless; collecting the domain in one place is what showed it.
  * ⚠ `projectile.gameplay` stays central: its type belongs to
    `ambition_platformer2d_shared_tangle`, not to this domain. Moving a registration
    is not the same as moving a TYPE.
  * ✔ **R1 ANSWERED: decline the extraction.** The question was whether the
    schema vocabulary can leave `ambition_platformer2d_runtime` so domains own their adapters
    for real. Three facts settle it:
    1. **The vocabulary is ALREADY public and already used from outside.**
       `ambition_platformer2d::runtime::rollback::AmbitionRollbackApp` is re-exported through
       the facade, and four crates register through it today —
       `ambition_content`'s bosses and portal, `ambition_demo_sanic`, and
       `ambition_app`'s versus. Those are the 15 non-runtime rows in the
       baseline. Domain ownership is not blocked; it is demonstrated.
    2. **Every remaining domain sits BELOW the runtime.** `ambition_platformer2d_actor_monolith`,
       `_combat`, `_portal`, `_characters` and `_projectiles` have no
       `ambition_platformer2d_runtime` dependency — the runtime depends on THEM. So none can
       own an adapter without inverting the graph.
    3. **Extraction does not fix (2), it relocates it.** A registration installs
       `ComponentSnapshotPlugin::<CanonicalCodecStrategy<T>>` — generic over the
       type, so the type and the GGRS vocabulary must meet at the same call site.
       No descriptor-plus-enum encoding avoids that: "install for T" is a
       closure, not data. An extracted schema crate would therefore drag
       `bevy_ggrs` into every primitive that registers.
    → **So: modules inside `ambition_platformer2d_runtime` for the five domains below it, and
    real crate ownership for anything above it.** That is what R4 actually asks
    for — the central function aggregates rather than enumerates — and it costs
    no dependency. Reopen only if a crate BELOW the runtime must register
    something the runtime cannot name, which has not happened yet.
  * ✔ **portal (37), encounter (18) and combat (72) migrated too** (2026-07-31),
    each byte-identical. `rollback/mod.rs` is 1,410 lines from ~1,870.
  * ⚠ **extracted MECHANICALLY, not retyped.** A script matches each chained
    call by type path and moves the whole span; the baseline is what says it
    arrived intact. Retyping 72 registrations is exactly the mistake that passes
    review and fails a peer — and it would have, twice: combat's move left a
    dangling chain head, and its adapter needed a checksum helper that was
    private to the central module.
  * ✔ **actors migrated too — 99 registrations, byte-identical.**
    `rollback/mod.rs` is **1,026 lines from ~1,870**, and the five domain
    modules hold 838 between them.
  * ⚠ **the script cannot finish the job alone, and actors is why.** Four
    separate chains lost their HEAD to the move and left an orphaned `.method`
    behind, because the central function is written as long method chains and a
    domain's registrations are interleaved with other crates'. Each needed a
    human decision about which crate owns the remaining head. A mechanical
    extraction plus a compiler plus the baseline is the right combination: the
    script does not mistype, the compiler finds the broken chains, and the
    baseline proves the schema did not move.
  * ✔ **characters (28) and primitives (28) migrated — EVERY domain is out.**
    `rollback/mod.rs` is **819 lines from ~1,870**, and what it still names is
    runtime-adjacent: `ambition_vfx` (11), `ambition_platformer2d_core` (8), world,
    time, sim_view, persistence, sfx. Seven domain modules hold the rest, and the
    schema dump never changed once across all seven moves.
  * ✔ **the projections followed their domains** (its own commit, because R3
    forbids a relocation commit doing anything else). `room_set_checksum` and
    `ldtk_runtime_index_checksum` live in `domains::actors`,
    `pending_player_hits_checksum` in `domains::combat`, and they are private
    again rather than `pub(super)`.
    **`rollback/mod.rs` is 723 lines from ~1,870, and it names neither
    `ambition_platformer2d_actor_monolith` nor `ambition_combat` anywhere** — what is left is
    engine_core, items, persistence, sfx, sim_view, time, vfx, world.
  * ✔ **R4 IS MET.** vfx (11) and items (1) migrated; `rollback/mod.rs` is **690
    lines from ~1,870** and names `engine_core`, `persistence`, `sfx`, `sim_view`,
    `time` and `world` — **not one of the component families R4 forbids**
    ("actor, combat, item, portal, boss or provider-specific"). Nine domain
    modules hold 1,556 lines between them. The schema dump is unchanged across
    all nine moves.
  * ✔ **R5 built: `central-rollback-does-not-enumerate-domains`.** An absence
    contract forbidding `rollback/mod.rs` from naming any of the nine migrated
    crates, with the reason and the instruction ("a new registration belongs in
    `rollback/domains/<domain>.rs`; if the domain has no module yet, adding one
    is the work"). 20 of 20 contracts hold. Probed red by putting one
    registration back — and the wire-format ratchet went red beside it, which is
    the two instruments agreeing.

**→ CAMPAIGN 2 IS COMPLETE (R1–R5), 2026-07-31.** R1 answered by declining the
extraction on evidence; R2/R3 nine domain adapters with a byte-identical schema
at every step; R4 the central function names no gameplay domain; R5 a guard that
says so. Its completion criteria — *"rollback schema remains behaviorally
unchanged through migration; every gameplay domain registers its own
authoritative state; the central runtime aggregates rather than enumerates"* —
are met and instrumented rather than asserted.
- ▢ **Boss animator residue** — the bounded `BossAnim`→`CharacterAnim` fold.
  Do not reopen the already-shared body integration path.
  ⚠ **sized 2026-07-31: 24 files touch `BossAnim`**, and the type is
  `ambition_sprite_sheet::boss::BossAnimFrame` — a SIM-owned cursor whose derived
  sample gameplay geometry reads (`BossAnimationFrameSample`), with a render-side
  `BossAnimator` mirroring it. So the fold crosses the sim/render seam and the
  attack-geometry derivation, not just a sprite table. Not a sitting-down job;
  budget it as its own slice and start from what READS the sample.

  ✔ **that survey is done (2026-08-01), and it makes the slice much smaller than
  "24 files".** Those 24 are the whole `BossAnim` ROW vocabulary. The
  sim-visible surface — `BossAnimationFrameSample`, the only part gameplay reads
  — has **ONE production writer and FOUR production readers**:

  ```text
  writes   features/ecs/anim_helpers.rs            (3 construction sites, one system)
  reads    boss_encounter/attack_geometry/mod.rs   BossVolumeContext.animation_frame
           features/ecs/target_volumes.rs
           features/ecs/damage_predicates.rs
           items/pickup/mod.rs
  ```

  And the sample carries exactly three fields: `profile: Option<BossAttackProfile>`,
  `frame_index: usize`, `animation_key: Option<String>`.

  → **so the fold splits into two slices that can land separately**, which is
  what makes it startable:
  1. **the SAMPLE** — give the character path an equivalent of those three
     fields and point the four readers at it. Bounded, sim-side only, and every
     reader is already `Option<&..>` so a body with neither still works.
  2. **the ROW vocabulary** — `BossAnim`/`BossAnimator`/`BossAnimFrame` → the
     character equivalents. This is the 24 files and it crosses into render.

  ⚠ **do slice 1 first and separately.** It is the half gameplay depends on, and
  finishing it means the attack-geometry derivation no longer names a boss type —
  which is the actual point of the fold. Slice 2 is then a sprite-table rename
  with no gameplay consequence.

  **And slice 1's design resolves, with ONE risk that is the whole job.**
  ⚠ **corrected 2026-08-01 by probing: there are THREE identity checks, not two,
  and the third is not expressible by key at all.** The first survey read the
  code; the probe read the behaviour, and they disagreed. The sample's `profile`
  is never read as data — every appearance is an IDENTITY CHECK:

  ```text
    mod.rs    AnimationSelection::live_frame_index   hurtboxes
    frame.rs  authored_animation_frame_index         hitboxes
    mod.rs    the same expression's `None` arm       the IDLE pose
  ```

  ⛔ **the idle arm is the one that breaks the clean design.** It reads
  `sample.profile.is_none().then_some(sample.frame_index)` — "the rendered row is
  the rest pose", expressed as an ABSENT profile, so a rest-pose hurtbox bobs
  with the breathing animation. `animation_key` cannot say that: an absent key
  means *the renderer could not resolve one*, which is a different fact. So a
  key-only sample loses the idle case, and slice 1 needs a third state
  (`Idle | Row(key) | Unresolved`) rather than an `Option<String>`.

  Original note, still true of the other two sites:

  ```rust
  if sample.profile.as_ref() == Some(profile) { … }   // frame.rs:87 and :107
  ```

  "is the row being rendered the one this profile drives". The sample already
  carries the generic form of that identity — `animation_key` — and
  `boss_animation_keys_for_profile` already maps a profile to its keys two lines
  below the second check. So the generic sample is
  `{ animation_key: Option<String>, frame_index: usize }`, the boss mapping stays
  in `boss_encounter::behavior` where it belongs, and `BossAttackProfile` leaves
  the gameplay geometry path entirely.

  ⛔ **the risk, and it is stated by the field's own doc:** `animation_key` is
  *"redundant with `profile` for most rows"*, and it exists to prevent *"future
  profile↔row alias drift from silently selecting a fallback box"* — i.e. the two
  CAN disagree, and today the profile is the authority for this check. Swapping
  to the key is therefore a **behaviour change on the attack-geometry path**, not
  a rename. It needs a test that pins the aliasing case FIRST — a row whose key
  and profile disagree — or the fold silently changes which hitbox a boss swings.

  ✔ **that test exists** (`a_samples_profile_decides_the_frame_even_when_its_
  animation_key_does_not_match`), and writing it is what found the third site.
  It guards the HURTBOX path; probing `mod.rs` fails it with
  `Got Vec2(0.0, 25.0)`, and probing `frame.rs` does NOT — which is the evidence
  that a first reading had missed a site. Its docstring says which path it covers
  rather than claiming the fold is guarded.
  ✔ **all THREE sites are pinned now, each probed against its own site** — which
  is the point, because probing one site left the other's test green and that is
  how the third was found in the first place.

  ```text
    a_samples_profile_decides_…_animation_key_does_not_match   hurtboxes
    the_hitbox_path_also_takes_its_frame_from_the_profile…     hitboxes
    an_idle_sample_carries_its_frame_and_an_absent_key…        the idle arm
  ```

  ⛔ **and then a FOURTH site, which is the one that actually matters.** Three
  successive readings of this code said two sites, then three, then four; the
  count only stopped moving when each claim was probed. The fourth is
  `runtime_animation_keys`:

  ```rust
  if sample.profile.as_ref() == Some(profile) {
      push_unique_animation_key(&mut keys, animation_key);   // the sample's OWN key
  }
  ```

  **It puts the sample's own key into the list the other checks would compare
  against.** So "is the sample's row among the rows being sampled" is CIRCULAR —
  it is true whenever the profile matched, because the profile matching is what
  put the key in the list. A probe of that rule passes all 21 derivation tests
  and proves nothing.

  ⊘ **so the `animation_key` design is not ruled out — it is not yet TESTABLE**,
  and the earlier note here claiming the idle arm ruled it out was wrong for a
  different reason: the writer emits `animation_key: Some("rest")` for idle, so
  idle IS expressible by key; the probe that "proved" otherwise used
  `animation_key.is_none()`, which is not the faithful equivalent.

  → **what the writer actually guarantees, checked**: `anim_helpers.rs` derives
  `animation_key` FROM the profile (`boss_animation_key_for_sample(catalog,
  profile, anim)`) and emits `Some("rest")` for the rest pose. The two identities
  cannot disagree in production TODAY. Whether that is a property to depend on is
  the question slice 1 turns on, and it is a question about
  `boss_animation_key_for_sample` vs `boss_animation_keys_for_profile` — not
  about the readers at all.

  ✔ **step (1) is ANSWERED, and the answer is yes for everything the engine
  names** (`every_hardcoded_sample_key_names_a_row_its_profile_claims`). The
  writer overrides the key for four GNU-ton profiles — deliberately, so *"the
  damageable head/body box follows the rendered row"* — and every override still
  lands inside the profile's own key list:

  ```text
    head_descent          → "head_down"   ∈ [gnu_head_descent, head_down]
    converging_shockwave  → "hand_slam"   ∈ [gnu_shockwave, hand_slam]
    hand_slam             → "hand_slam"   ∈ [gnu_hand_slam, hand_slam]
    hand_sweep            → "hand_sweep"  ∈ [gnu_hand_sweep, hand_sweep]
  ```

  So the two identities differ as STRINGS and agree as SETS, which is exactly
  what a key-based check needs. PROBED: pointing one override at a row the
  profile does not claim fails the test with the offending pair named.

  ⛔ **and `apple_rain` is the ONE that fails — step (1b), answered against the
  shipped catalog** (`apple_rain_claims_no_animation_rows_which_is_why_the_fold_
  is_blocked`, in `ambition_content` because only content can answer it).

  `apple_rain` is a `Special`, so its rows come from the app's
  `special_animation_keys()` map — **and it is not in that map.** The profile
  claims NOTHING while `boss_animation_key_for_sample` emits `"head_down"` for
  it. So its hurtbox row is reachable today ONLY through the profile identity:
  `runtime_animation_keys` pushes the sample's own key into the list because the
  profile matched. A key rule would look for `"head_down"` in an EMPTY list, miss,
  and fall back to elapsed-time sampling — a different hurtbox on a live boss.

  → **the fold's first slice is blocked on one line of CONTENT**, not on the
  engine and not on the four readers. Two ways out, and the test fails on either
  so neither happens silently:
  1. give `apple_rain` its rows in `special_animation_keys()` — then every
     identity agrees as a set and the swap is a rename;
  2. keep the profile identity and accept that the sample carries two facts.

  ⚠ **the circularity is what has been hiding this.** `runtime_animation_keys`
  feeds the sample's own key into the list the other checks compare against, so
  `apple_rain` works today *because of* the check a fold would remove. Any
  key-based rule must be tested with that push disabled or it proves nothing —
  which is exactly what happened here once already.

  ⛔ **and that push had NO TEST AT ALL until 2026-08-01.** Deleting it left all
  21 derivation tests green — a behaviour this whole design decision turns on,
  unguarded. `a_profile_claiming_no_rows_still_finds_the_row_its_sample_names`
  covers it now, reproducing `apple_rain`'s situation with an unregistered
  `Special` (which yields the same empty key list the shipped catalog gives it).
  PROBED: with the push deleted it fails with `Got Vec2(0.0, 0.0)` — the body
  box, the boss's damageable shape changed.

  ▢ **remaining for slice 1**: decide (1) or (2) — a content call — then break
  the circularity, then move the type. **Five pins are in place, so each step is
  checkable.**

- ✔ **THE SELECT SCREEN WAS INERT, and that is the whole reason it existed**
  (2026-07-31). Jon's ask was "the smash demo must start with the character
  select screen and then start the battle when up to 4 players are locked in".
  `SmashSelect` shipped with every state transition unit-tested — join, browse,
  lock in, the cancel LADDER, the ready rule — and **nothing in the app ever
  wrote to it**. `ready()` could not become true, so the battle could not start.
  All twenty-four tests were green because every one of them drove the resource
  directly. A state machine with no driver has never run.
  * `drive_the_select_screen` is that driver, and
    `the_screen_decides.rs` presses buttons instead of setting state. Probed by
    removing the driver: two of three fail.
  * ⚠ one of those tests was WRONG in a way that mattered: it joined a third
    player AFTER two seats had locked in, and `ready()` is instantaneous — the
    match had already started. The rule "a joined-but-browsing seat holds the
    match" only bites if they joined FIRST. Order is the test.
- ✔ **API-(g) seat-keyed input: `SeatMenuFrames`** (2026-07-31), which the above
  needed and nothing had. `MenuControlFrame` is ONE global answer — right for a
  pause menu (whoever reached for a controller pauses the game), useless for a
  select screen where four people navigate four cursors and "who pressed
  lock-in" has no answer in a folded frame. Keyed by participant SLOT, which is
  also the seat and also the device index, because two numberings that must
  agree eventually disagree.
  * Repeat state is per seat too. One shared `MenuInputState` makes seat 2
    holding a direction reset seat 1's repeat clock — a bug that reads as "the
    menu is laggy when someone else is scrolling".
  * `decode_menu_frame` is extracted so the global frame and the per-seat frames
    cannot drift: two implementations of "what does this controller mean in a
    menu" diverge the first time one learns a new binding, and it shows up as
    one screen where the bumpers work and another where they do not.
  * The SCREEN declares the resource, not only the host. That is what lets a
    headless test press a button — and reaching past the port into the answer is
    exactly how this screen came to be fully tested and completely inert.
- ✔ **the select screen has a VISUAL** (2026-07-31). It was passed as the
  composition's launcher route, so it rendered the launcher's experience list
  while people pressed buttons at an invisible menu — the same defect as the
  inert driver, one layer up. `select_ui` draws one panel per OFFERED seat,
  spawned on route arrival and torn down on leaving (route-driven, not
  state-driven: `SmashSelect` keeps its decision through the match, and panels
  tied to it would be drawn over the fight).
  * ⚠ the panels first drew all four seats, so P4 read "press confirm to join"
    at a chair nobody could sit in — `drive_the_select_screen` only walks the
    seats the pads offer, so it was an invitation the screen could not honour.
    Hidden rather than removed, because a select screen is exactly where a pad
    gets plugged in.
  * The test asserts what the panels SAY, not that they exist: four empty boxes
    would pass the second. `select_walkthrough` prints the whole flow through
    the same `panel_text` the UI renders, so it cannot show a screen the player
    would not see. Both probed red.

- ✔ **`Jump` was the one verb L2 offered without asking whether the body could
  do it** (2026-07-31). `can_blink`, `can_shield`, `can_dash` all gate their
  verb; Jump did not, so a body airborne with an empty budget was handed an
  option that presses to nothing. Worse than a wasted press: L3 rolls the verb,
  the shadow's airborne jump is budgeted too, so the line goes NOWHERE — and
  nowhere scores as safe, which is the most dangerous thing a rollout can report
  about a body over a pit. Found by wiring `air_jumps_left` for the recovery
  model and noticing the pattern break next to it.
  ⚠ writing the comment is what caught the second half: it claimed the shadow's
  Jump was gated on the same budget, and it was not — grounded-only, so every
  air jump was a silent no-op. Both true now.

- ✔ **THE VERSUS STAGE'S CPU ASKED FOR A BRAIN ITS COMPOSITION NEVER HAD**
  (2026-07-31), and the S2 seat refusal is what found it. `versus_roster_from`
  named `"medium_striker"` — a row in `ambition_content`'s archetype table,
  which the versus experience does not compose. Before the refusal landed the
  lookup silently fell back to `combatant`; `versus_through_the_sdk` composes
  only that one row, so the SDK path had been seating a `stand_still` body and
  calling it an opponent. The experience now registers its OWN
  `versus_duelist` archetype: a demo naming another provider's row is the same
  borrowed-authority bug one namespace over.
  ⚠ the copy mirrors `medium_striker`'s numbers (`Smash` template) rather than
  upgrading to `Fighter`, because changing what the opponent DOES while fixing
  where it comes from is two changes riding on one commit.
- ✔ **THE `Fighter` BRAIN WAS NEVER A STATUE — the TEST was measuring
  displacement across a round reset** (2026-07-31). Tracing it per tick instead
  of reasoning about it: the body walks 576 → 218 px at its opponent, jumps,
  falls into the arena, dies, and `begin_round` puts it back on its seat. Both of
  the test's samples therefore read "at the seat", and it reported **0.4 px about
  a body that had covered 350** — the same shape as asserting a pendulum never
  moved because it came back.
  * `the_cpu_opponent_is_not_a_statue` accumulates PATH LENGTH now, ignoring
    single-frame jumps over 64 px so a fighter cannot pass it by dying
    repeatedly. Probed red against a `StandStill` opponent: 0.0 px.
  * Two hypotheses were checked and DISPROVED first, by experiment rather than
    argument: viewport clipping (seats are 192 px apart, viewport half is 480)
    and faction-only hostility (reverting the team fix changed nothing here).
  * The versus CPU keeps the `Smash` row. `Fighter` moves, but what it does with
    the movement is walk into the pit — which is not a versus-specific defect, it
    is the SAME open row as the smash stage's self-KOs, and shipping it would
    make the default versus experience worse to prove a point about which brain
    is newer.
- ✔ **`exit_on_shell_request` was ordered by luck** (2026-07-31). It reads
  `ShellEvent`, the shell writes them in `AmbitionGameShellSet::Commands`, and
  it was added unordered — so whether selecting Exit produced an `AppExit` in
  the SAME frame depended on system insertion order across unrelated plugins.
  Adding one system to the host's input chain flipped it, and
  `the_full_multi_game_lifecycle_is_leak_free` went red for a reason that had
  nothing to do with what it tests. Now `.after(...Commands)`.

- ✔ **THE WEB BUILD HAD NOT COMPILED, and no per-crate check would have said
  so** (2026-07-31). `./run_tests.sh` runs two wasm jobs and both were red:
  `bevy::app::TerminalCtrlCHandlerPlugin` does not exist for that target, and
  `asset_manager::consumer_source` is `#[cfg(not(wasm32))]` at its definition
  while its only caller was not. Neither is reachable from a `cargo check -p`,
  which is why they survived — `--target wasm32-unknown-unknown` is the only
  thing that asks.
  * `disable::<T>()` on a plugin that does not exist for the target is a COMPILE
    error, not a no-op, so the cfg has to move the CALL and not the type.
  * The asset-source path now REFUSES on web rather than being skipped:
    `consumer_source` layers a game tree over the engine's from disk, and the
    served bundle is already one merged tree. A declaration nobody can honour
    must not pass silently — the same rule as the seat refusal.
- ✔ **a test that passed by default and failed under `--features input,visible`**
  (2026-07-31). `the_screen_decides` inserted `LocalDeviceOrder` directly;
  `track_local_device_order` REBUILDS it from live `Gamepad` entities every
  frame and is itself behind the `input` feature. So the hand-inserted order
  survived where the tracker was absent and was clobbered where it ran. The
  helper now spawns real pads — the fact — and seeds the resource only when
  nothing derived it. ⚠ this is why the job plan runs crates under their real
  feature sets and a bare `cargo test -p` is not the suite.

- ✔ **PERCEPTION AND DAMAGE DISAGREED ABOUT WHO THE ENEMY IS** (2026-07-31).
  `damage_lands_between` gives the TEAM relation precedence over factions when
  both bodies are seated; `hostile_to_self` read `FactionRelations` and a grudge
  and knew nothing about teams. In a free-for-all — every seat its own team,
  which is exactly what a 4-player smash authors — `faction_for` alternates
  Player/Enemy by seat index, so **seats 0 and 2 share a faction on different
  teams: they could damage each other and could not SEE each other.** A brain
  with no perceived foe stands still, and every component that would explain the
  stillness is present and correct.
  * Perception now calls the same `team_allows_damage` the damage rule calls, in
    the same precedence, with the grudge still overriding both. One authority.
  * The viewer's own team is read off its own `PerceptionPeer` row rather than a
    new query — like `phase`, so a body cannot disagree with the rest of the
    world about which team it is on, and the system stays under Bevy's
    16-parameter cap.
  ⚠ found by chasing the versus statue and being WRONG: the hypothesis was that
  a 1v1's two seats share a faction. They do not (`faction_for` alternates), so
  the versus statue is still unexplained — but reading `faction_for` to check is
  what surfaced the 4-seat case.
- ✔ **`BodyKnockedOut` was registered only in test fixtures** (2026-07-31), so an
  app with the damage pipeline and no stocks ruleset panicked "Message not
  initialized" the moment a body reached zero. `mary_o`'s power loop is that app.
  Now registered by `CombatSchedulePlugin`, beside the writers rather than beside
  the stocks RULES — a writer whose message is registered by a different plugin
  is a composition that works until somebody composes differently.

- ✔/▢ **Campaign 4 — its completion criterion is now TESTED, and the criterion
  was already met** (2026-07-31). *"A failed preparation leaves the current
  session byte-for-byte semantically unchanged except for diagnostics."*
  `process_sandbox_reset_request` already prepares a `RoomConstructionPlan`,
  declines on failure ("DECLINE, do not die — the preflight runs before the wipe
  precisely so a refusal costs nothing"), and every teardown system keys on
  `SandboxResetCommitted` rather than on the request. What was missing is that
  none of it was asserted: the criterion lived in a comment.
  `a_declined_reset_leaves_the_running_session_untouched` points the session at
  room index 999, which `prepare_from_parts` refuses with `UnknownRoom`, then
  asserts the save flag survives, the registries survive, and NO commit was
  announced. Probed red by announcing the commit before the preflight.
  * ▢ **the named `PreparedSandboxReset` value does not exist**, and it is not
    obvious it should. The campaign sketches one bundling "scopes to close /
    player transfer / resource replacements / expected identities"; today the
    plan covers the ROOM and the rest happens inline past the refusal point. The
    valuable half — the refusal boundary — is built and now measured. Bundling
    the rest is worth doing when a second operation needs the same shape, which
    is what the campaign says: *"only after both operations exist should the
    agent consider extracting a shared trait."*
  * ✔ **second operation: ALREADY BUILT, under a different name** (surveyed
    2026-07-31). `commit_room_transition_geometry` documents itself as *"Every
    fallible room/content lookup occurred when the plan was prepared. This
    function is therefore the short covered commit"* — the same prepare/commit
    shape as the reset, through the SAME `RoomConstructionPlan`. The transition
    side goes further: `world/rooms/transaction.rs` verifies the committed room
    against an opening baseline and refuses publication, recording the outcome
    in `LastConstructionVerification`.
    And its criterion is TESTED, with a poison counterpart —
    `a_room_that_fails_verification_is_not_published` beside
    `the_same_room_publishes_once_its_relation_lands`, whose docstring says why:
    *"Without this the test above would also pass if rooms never published at
    all."*
  * ⊘ **so do NOT extract the shared trait.** The campaign says to consider one
    "only if both implementations naturally expose the same useful interface" —
    and they do not merely expose the same interface, they share the same TYPE.
    `RoomConstructionPlan` IS the prepared transaction for both operations. A
    trait over one struct is a layer, not an abstraction.
  → **Campaign 4's completion criterion holds for both operations**, and the one
    thing genuinely missing when this row was opened was that the RESET's half
    was never asserted. It is now.

- ✔ **THE FIGHTER HELD JUMP FOREVER — released 2026-07-31.** `jump_held` is written at exactly two
  sites in `decision.rs` (`Jump` and `Recover`) and **never cleared** — and
  `frame` starts each tick as `state.held.clone()`, so one jump pins the button
  down for the rest of the match. Found by reading, not measuring; the same class
  as the `locomotion.x` leak fixed earlier today ("a chosen verb replaces the
  held movement, it does not add to it") — I fixed the stick and left the button.
  ⚠ **the fix is not simply to clear it every decision.** A held jump is what
  buys height, and decisions land every 5 ticks; releasing on the next decision
  makes every jump an 83 ms hop. Re-choosing `Jump` to keep holding would press
  `jump_pressed` again and spend an AIR jump. The defensible shape is the one
  already written down for lateral: the chosen verb authors the whole intent —
  `Jump`/`Recover` hold, every other verb releases — which is what a player's
  thumb does. That is what landed: `frame.jump_held = false` beside the lateral
  clear, so the button is re-stated by whichever verb runs.
  ⚠ **and `ladder_probe` did not move at all** — same first self-KO, same
  survival, every rung. So the stuck button was NOT what kills this fighter. It
  was still an input the brain never chose to keep, and the test asserts the
  RELEASE rather than the press: a test on the press was green for the whole time
  this was broken.

- ✔ **SWEPT: `debug_assert!(false, ..)` used as a REFUSAL** (2026-07-31). Found
  by fixing seating's, where the release behaviour was "return quietly and let
  the match activate around the hole" — the bug the guard was written for,
  reintroduced by the guard's own build configuration. Three in production:
  * `character_runtime/seating.rs` — now publishes `MatchSeatingRefused` in
    every build.
  * `ambition_characters/snapshot_impls.rs` — a `Select` in a resolved timeline
    encodes as tag 4, which decodes to `None`. In release that is **silent state
    loss across a rewind**, surfacing later as a checksum mismatch with no name
    attached. Now logs at error in every build; the wire format is deliberately
    unchanged, because a new tag would be a schema change made on a suspicion.
  * `ambition_demo_sanic/monitors.rs` — said "break it visibly but loudly note
    the miss in debug builds" and then noted it in debug ONLY, so in the build a
    player runs, breaking the monitor granted nothing and said nothing.
  * `ambition_persistence/save_data.rs` is left alone and that is the
    distinction: its release path is CORRECT (version bumped, loop broken), so
    the assert is a developer alarm on a handled case rather than the handling.
  ✔ **TWO MORE in seating's COMMIT pass — and they are GONE, not made louder.**
    Both were `debug_assert!(false, ..)` + `return` inside the pass whose own
    comment says *"Nothing below may return early."* Debug panicked; release did
    the one thing the pass forbids, silently.
    * **The spawn arm is now infallible.** `seat_character`'s only failure is a
      registry lookup the resolve pass already performed and then threw away.
      The plan carries the resolved `PreparedCharacterDefinition` forward and
      the commit calls a new `seat_prepared_character`, which returns `Entity`
      rather than `Option<Entity>`. There is nothing left to refuse — which is a
      better answer than refusing more loudly.
    * **The adopt arm uses `expect`.** Nothing can be carried forward there: the
      commit needs a mutable borrow the resolve pass cannot hold. So the two
      builds are made to AGREE instead, and between a loud stop and a half-built
      match the repo has already chosen once — initial session setup panics
      because "a silent partial start would be worse than a loud stop".
    **`grep 'return;'` past the commit boundary now finds nothing.** The pass's
    stated invariant is structural rather than aspirational.
  ⊘ **a CONTRACT for this was attempted and WITHDRAWN, deliberately.** Written,
    run, and deleted the same hour. Two reasons, both worth more than the
    contract:
    * **It fired on correct code.** After the fixes, all four remaining sites
      log in every build and use the debug assert as an ALARM over handled code
      — which is the legitimate shape. A pattern ban flags them; a path
      allowlist encodes today's file list and goes stale.
    * **The distinction is not expressible here.** `check_absence_contracts.py`
      greps candidate lines and confirms them per LINE, so "an assert not paired
      with release handling" — which spans lines — cannot be written. The
      machinery's `{grep, match}` pair exists for lookarounds within one line,
      not across a branch.
    → **the rule stays prose**: *does the release path do the right thing on its
    own?* If yes the assert is an alarm and is fine; if the assert IS the
    handling, it is a bug. A reviewer can answer that and a regex cannot, and
    shipping a guard that flags correct code is how guards get waived.
  * ✔ one more found while writing it: `actor/control.rs` returned an unresolved
    SCREEN-space fire direction as a world one in release, silently — wrong under
    any rotated gravity, with nothing to grep for. Now logged.

### ✔ S29. The sentinel leaves `crates/` — `examples/capability_demo` (2026-08-01)

Jon's call, and living in `crates/` is exactly what made it read as a shipped
engine feature rather than a worked example.

⚠ **its own workspace root**, like `examples/portal_tutorial` and
`fixtures/minimal_game`. A crate that exists to prove an OUTSIDE author can write
a capability has to be built the way an outsider builds it — own lock, no
feature unification from the engine's workspace.

⛔ **and moving it BROKE it, twice — both real costs an outside author pays and
both invisible while it lived inside:**

1. **`workspace = true` dependencies do not exist out there.** `ron` and `serde`
   carry versions now, like anyone else's manifest.
2. **`[patch.crates-io]` is NOT inherited.** The engine pins a forked
   `bevy_ggrs` rev; outside, `ambition_platformer2d_runtime` resolved the released crate and
   failed to compile on a missing `GgrsFrameTiming`. Any consumer that reaches
   `ambition_platformer2d_runtime` must repeat that patch. The crate had been inheriting it
   for free and nobody knew.

Both recorded in its manifest and in the recipe, because they are the first
things the next capability author hits.

⚠ **`scripts/run_tests.py` gained a job IN THE SAME COMMIT.** Leaving the
workspace drops a crate from `cargo test --workspace` SILENTLY, and those 19
tests are the only proof that a capability contributes a schema, an action,
rollback state and causal facts without editing anything central. Moving it
without the job would have deleted that coverage invisibly — the exact defect
this crate exists to catch.

The KB instrument caught the stale workspace-members evidence (57 → 56) on the
first run, unprompted.

---

### ✔ S28. The GPT 5.6 review of the pulse/causal work — all SIX addressed (2026-08-01)

The review's own priority order, followed. Every fix probed against the defect
it names.

1. ⛔ **CRITICAL — pulse ran outside the authoritative simulation schedule.**
   `PulsePlugin` registered into bare `Update`, which is what a Bevy habit
   produces and what **this crate's own recipe warns against** — the worked
   example contradicted the documentation it exists to illustrate. Fixed through
   `SimScheduleExt::sim_schedule()`, one explicit phase
   (`SandboxSet::GameplayEffects`), nothing naming GGRS.
   ⚠ **invisible because `sim_schedule()` DEFAULTS to `Update`**: all ten
   existing tests passed before and after. The new pair gives the host a
   schedule of its own and probes red on the old registration.
   ⚠ the sentinel's closure is **8** crates now, not 7 —
   `ambition_platformer2d_shared_tangle` costs exactly one and is the honest price of
   being rollback-safe rather than claiming to be. Corrected in three places
   that said 7.
2. ⛔ **the compiler validated content the runtime ignored.** Schema registered,
   packs lowered, and `init_resource::<PulseProfiles>()` used the built-in
   defaults. `PulsePlugin::from_prepared(&pack)` consumes the lowered artifact;
   a pack that prepared none is a REFUSAL, not a silent default. The acceptance
   test authors radius 500 / force 1234 / cooldown 7 and measures a body 300px
   away — outside the default 96, inside the authored 500.
3. ⛔ **the positive test asserted on a REJECTED app**, because `requires_rollback`
   shipped without a `provides` half: a rollback game mounting a capability with
   required state could not be composed at all.
   `ModuleDraft::provides_rollback::<T>(owner, name, probe)` is that half,
   applied BEFORE the requirement check. The acceptance is now a composition
   that returns `Ok`; the refusal is its own negative test.
4. ⛔ **the rollback contract named the cheap half.** `PulseBody`'s velocity IS
   the mechanic's observable effect and was undeclared; the active profile was
   mutable sim state nothing rewound. The body is declared now (and the
   composition refused until it was provided — the contract working), and
   `select(&mut self)` became `with_active(self) -> Option<Self>`, so choosing
   happens before the resource exists and no `&mut` path survives into the sim.
5. ⛔ **CPU and boss facts appeared in every participant's explanation.** A
   subject-less fact is a WORLD fact to `explain`. Probed: seat 0's explanation
   returned `[(None, Int(1)), (Some(Seat(0)), Int(2))]` — the CPU's stock loss
   beside its own. Subjects are strongest-available now and never `None`; the
   old test had asserted the defect as intended behaviour.
   ⚠ the genuinely global case (`match_decided`) is tested too, because a fix
   that filed everything under a body would break what the old behaviour got
   right.
6. ⛔ **two resimulations of one tick were one explanation.** `ExecutionKey`
   gains `attempt`, stamped by the host on the rising edge of
   `replaying_history` — one rollback batch is one attempt. An ORIGINAL
   execution is always attempt 0.

✔ **the GGRS round-trip EXISTS now** —
`examples/capability_demo/tests/rollback_round_trip.rs` (2026-08-01). It asserts
cooldown, profile and velocity after real rewinds, under a sync test where every
frame is saved, rewound and resimulated.

The old objection — *"that needs a rollback host, and building one inside the
sentinel would make it a host"* — was answered by two things that did not exist
when it was written: `Platformer2dSimHarness::app_mut()`, so a FIXTURE can
install systems onto a rollback session instead of a crate becoming a host; and
the observation that the footprint ratchet reads `cargo tree --edges normal`, so
a **dev-dependency** leaves the capability's own closure untouched. The SDK and
the runtime already sit in that crate's dev-deps under exactly this reasoning.

⛔ **the probe is the point.** Delete the one line that registers `PulseCooldown`
as rollback state and the test fails with *"after 5 resimulated tick(s) the
cooldown reads 11, not 15"* — the gate ageing twice per resimulated frame, which
is the desync the offer exists to prevent and which nothing had ever run.

⛔ **and building it found a real composition defect in the engine.**
`ExternalEffectQuarantinePlugin<M>` installed systems taking
`ResMut<Messages<M>>` and left the REGISTRATION to whoever else wanted the
message — which in a shipped app is the render/audio plugins, so it worked there
and only there. A host that composes rollback WITHOUT presentation failed
parameter validation on frame one, naming a message type it had never heard of.
The plugin now registers its own channel (`add_message` is guarded by
`contains_resource`, so it is idempotent and changes nothing for a composition
that already had it).
⚠ same family as everything else this session: a plugin asserting an authority
it does not own. The shipped composition hid it because a sibling happened to
satisfy it.

⚠ the minimal host still needs `PresentationPhase` init'd by hand — the rollback
plugin publishes its intra-tick phase for the presented-pose layer, and a
headless host has no presentation to provide it. Noted at the call rather than
papered over; `PlatformerEnginePlugins` is NOT the answer, because it drags in
`seat_match_participants`, which wants a `CharacterCatalog` a capability host has
no reason to have.

---

### ▢ S27. What the 08-01 session leaves open (written the way S26 was)

Nothing here is speculative: each is a thing that was OBSERVED and not closed.
The closed work is in its own rows above; this is only the residue.

**1. The boss-animator fold is blocked on ONE line of content, and the line is
not free.** Five pins are in place and every step is checkable. The decision —
give `apple_rain` its rows in `special_animation_keys()`, or keep the profile
identity — is in
[awaiting-maintainer-decision.md](awaiting-maintainer-decision.md) because
adding the row changes a live boss's damageable shape on the no-sample path.
⚠ **and the circularity has to go first either way**: `runtime_animation_keys`
pushes the sample's own key into the list the other checks compare against, so a
key-based rule cannot be tested at all until it does. A probe of one passed 21
tests and proved none of them.

**2. Jon's doors, narrowed to three candidates.** The rollback-host one is
eliminated with a regression test. Still open: a specific room's authored zone,
the pause/dialogue `gameplay_allowed` gate, or the prompt not showing so the
wrong key was pressed. **Needs from Jon: which room, which door, and whether the
prompt appeared.**

**3. `docs/planning` is 15k lines of DESIGN docs over budget.** Measured; the
mechanical rule reaches about 1% of it. Which of the 33 `engine/` documents have
become history is Jon's call and is written down as one.

**4. The per-seat input claim still has no PER-SEAT consumer.** The pause menu
gave the resolution its first production consumer, but that is a whole-screen
claim. The genuine per-seat case is a seat opening a surface mid-match while the
others keep playing, and the nearest candidate — dialogue — is blocked on the
world-stop product question, not on the seam.

**5. The fighter thread has no open defect and no next suspect.** Measured over
2279 decisions: the emitted frame reaches the body intact every time, and the
input/velocity disagreements are momentum. Whatever loses the fighter its stocks,
it is not the brain→body seam. The next person starts somewhere else.

**6. ⛔ EVERY `ambition_menu` SURFACE TOFUS NON-ASCII TEXT** — found by
photographing the launcher while checking an unrelated change, and narrowed to
one crate with five eliminations and a one-frame reproduction. The launcher
footer's `·` drew a hollow box; the same codepoint on a bare `Text::new`, same
route, same frame, renders correctly. Not the font asset (JetBrains Mono is
bundled and has it), not the string, not the atlas, not `TextFont`. It is
invisible until a string steps outside ASCII, which is why one separator was the
only symptom it has ever had — and it affects every menu in every game. Full
elimination list in the 24h queue's Z1 row.

**7. `InputMap<SemanticAction>` — still the migration, not the design.** An
action can be declared and queried and cannot carry a device binding. Unchanged
today, deliberately: the review's "do not expand defensively" list names it, and
no consumer is blocked on it.

⚠ **the thing this session would most want its successor to know**: SIX
suggestive numbers turned out to be measurement artifacts, four of them produced
by instruments built the same day to prevent exactly that. In every case the fix
was to check the WRITER rather than reason from the reader, and the artifact was
shaped exactly like the bug being hunted. See
[the causal instrument notes](engine/headless-verification.md) and the ⚠ rows in
S25/S26 above.

---

## Rows that are waiting on JON, not on work

Collected in [awaiting-maintainer-decision.md](awaiting-maintainer-decision.md).
Do not burn run time re-deriving them; work around them.

---

## S30 — recon for the incoming CRATE RENAME campaign (2026-08-01)

Jon has an untracked `docs/planning/engine_rename_campaign.md` naming the
renames (`ambition_platformer2d_core` → `ambition_platformer2d_core`, `ambition_platformer2d_world`
→ `ambition_platform2d_world`, `ambition_portal2d` → `ambition_portal2d`, …). It
says the renames "can mostly be accomplished with sed." Two of them cannot, and
both are cheap to know BEFORE the sed rather than after.

⛔ **1. A crate name is a ROLLBACK WIRE-FORMAT FACT today.** `descriptor()`
stores `std::any::type_name::<T>()`, `schema_dump()` writes it, and
`schema_fingerprint()` hashes the dump — so renaming a crate moves
`SnapshotSchemaFingerprint` for every component that lives in it, and two peers
with BYTE-IDENTICAL wire formats then refuse to agree.

That is the *same defect* v5 fixed one level down. `registry.rs:33` says it in
its own words: hashing the registration OWNER "made 'which module registered
this' a wire-format fact", so v5 stopped hashing it. The crate path inside
`type_name` is the same category of organisational label, and it is still
hashed. Measured impact: **94 of the 352 lines** in
`game/ambition_app/tests/rollback_schema_baseline.txt` name a crate on the
rename list (`ambition_portal2d` 31, `ambition_platformer2d_core` 27,
`ambition_platformer2d_shared_tangle` 26, `ambition_platformer2d_world` 8, plus
`ambition_platformer2d_runtime`/`ambition_platformer2d_ldtk`).

  ▢ **decide which, and it is a real fork, not a formality:**
    (a) regenerate the baseline and bump `GGRS_ROLLBACK_SCHEMA_VERSION` —
        honest, one commit, and concedes that renaming a crate is forever a
        compatibility break; or
    (b) hash the type's FINAL SEGMENT plus its module path below the crate, so
        the fingerprint stops caring where a type is packaged — which is what v5
        already decided for owners, applied consistently. ⚠ this makes two
        same-named types in different crates collide, so it needs the
        `RollbackRegistry` duplicate-name check to stay the thing that catches
        it (it already rejects duplicate `name`s).
    The baseline regenerates either way; only (b) makes the NEXT rename free.
    → **moved to [awaiting-maintainer-decision.md](awaiting-maintainer-decision.md)
      2026-08-01, because it stopped being free to defer.** The
      `ambition_geometry` carve landed; the snapshot codec is the next general
      thing stuck in the platformer core, and moving it rewrites the
      fingerprint. That row carries the recommendation — (b), because the engine
      is at the START of a carve campaign and (a) prices every future move as a
      compatibility break — plus the probe that choosing (b) obliges.

⚠ **2. `const OWNER: &str = "ambition_platformer2d_runtime"` in ten files** is not caught by
a rename of the crate DIRECTORY, but sed on the string catches it, and since v5
it is cosmetic — it is not hashed and nothing reads it. Named here only so the
inconsistency is deliberate rather than missed.

✔ **the rest really is sed-shaped**: no crate on the list appears in any `.ron`
or `.json` under `content/` or `assets/`, so no authored content names a crate.
46 hits across `scripts/` are ordinary paths.

Related: the rollback schema baseline notes and the v5 note at
`crates/ambition_platformer2d_runtime/src/rollback/registry.rs:33`.

---

## S31 — the crate rename landed (2026-08-01)

Eleven crates renamed to the `platformer2d` / `portal2d` family, on Jon's brief.
Full report and eight follow-ups in
[engine_rename_campaign.md](engine_rename_campaign.md); the rows below are the
ones that stay OPEN in this queue.

✔ **the rename itself** — 1,469 files, no aliases, no shims. Full suite 31/31.
✔ **follow-up 2, the retired-name guard** — `scripts/check_retired_crate_names.py`,
   probed against a real injected regression. It found `ambition_engine` (retired
   long ago) still live in 17 places on its first run, three of them real.
✔ **follow-up 6, `run_game.sh`** — discovers worlds under
   `game/ambition_content/assets/worlds/`, prints what it found, fails loudly.
   Cross-file LoadingZone validation had been silently degraded to nothing.

▢ **⭐ the general kernel is trapped inside a platformer-named crate — CENSUS
RUN 2026-08-01, and it changes the plan.** The highest-value item the rename
exposed. 27 workspace crates depend on `ambition_platformer2d_core`, and 19
unqualified crates thereby declare a platformer dependency they do not have.
`scripts/core_import_census.py` now measures exactly what each one takes
(`--kernel` for the general-named union, `--detail` for every symbol).

⛔ **the headline: the two most-wanted "kernel" items are not the core crate's at
all.** `Vec2` is `pub use bevy_math::Vec2` and `Aabb` is `pub type Aabb =
Aabb2d`. Four general-named crates were taking a platformer-named dependency to
borrow a maths type. That part of the carve-out is not a carve-out — it is an
import change.

✔ **and two of them are done.** `ambition_interaction` (took exactly `Aabb`) and
`ambition_dialog` (took exactly `Vec2`) now depend on `bevy_math` directly and
**do not depend on the core crate at all**. 27 → 25. Their unqualified names are
now true rather than aspirational.

▢ **what the census says is actually left**, for the 10 genuinely-general crates
(56 distinct items before the two removals):
* ✔ **`bevy_math` re-exports — EXHAUSTED, checked 2026-08-01.** The remaining
  three holders cannot drop the dependency by switching to `bevy_math`, because
  none of them takes ONLY maths: `gameplay_trace` also takes `BodyMode` and
  `LocomotionState`, `vfx` takes `CombatVolume`/`VolumeShape`, `sim_view` takes
  `World`/`RoomGeometry`. `interaction` and `dialog` were the whole win. Do not
  re-open this bullet expecting more.
* ✔ **DONE 2026-08-01 — `ambition_geometry` exists.** (Was: the carve with the
  best evidence and no fingerprint entanglement.) `ambition_vfx` takes exactly four things:
  `Vec2`, `Aabb` (maths), and `CombatVolume` + `VolumeShape` (hit-volume
  geometry). Nothing platformer-specific at all. So moving those two modules
  makes `ambition_vfx` drop the core dependency outright — 25 → 24 — and its
  unqualified name becomes true.
  ✔ **and they are already free-standing**, which is why this is the first move
  rather than the third: `combat_volume.rs` (328 lines) imports only `parry2d`
  plus `Aabb`/`AabbExt`/`Vec2`; `volume_shape.rs` (176) adds `AccelerationFrame`;
  and `reference_frame.rs` imports **only `Vec2`**. No rooms, no blocks, no
  ledges, no portals — grep says so.
  ⚠ contrast with the snapshot codec below, which is equally general and moves
  the ROLLBACK FINGERPRINT with it (S30). This one carries no such cost, so it
  can land while that fork is still undecided.
  ✔ **the home was a NEW crate**, because that is the only option that shrinks a
  closure rather than renaming one. `ambition_geometry` holds `geometry.rs`,
  `combat_volume.rs`, `volume_shape.rs`, `reference_frame.rs` and (after the
  `shaped-volumes` merge) `swing_shape.rs`. **`ambition_vfx` now declares no
  platformer dependency at all** — 27 → 24 dependents with `interaction` and
  `dialog`, re-measured with the census rather than asserted.

  ⭐ **the contract MOVED WITH THE FLOOR, which is the part worth copying.**
  `engine-core-is-the-floor` said core may depend on NO workspace crate, and the
  carve violates its letter. Waiving it would have been the easy wrong move.
  Instead `ambition_geometry` took its own `geometry-is-the-floor` contract with
  `forbidden: "*"`, and core's gained a single NAMED allowance — so the chain
  still bottoms out with no outward edge. Probed both halves: a second edge out
  of core goes red, any edge out of the new floor goes red. An `allowed` entry
  is a named crate, never a category, and each one has to itself carry a
  `forbidden: "*"` contract or the invariant has just moved somewhere nobody
  checks.

  ⚠ **the merge immediately tested it.** `shaped-volumes` added `SwingShape` to
  the CORE crate — correct when written, wrong by the time it merged, because
  `ambition_vfx` reads it and would have got its platformer dependency straight
  back. It moved to the kernel on the same self-containment test rather than on
  preference. A carve is not done when it lands; it is done when the next branch
  that crosses it lands correctly.

  ⚠ four instruments caught their own consequences here, which is the argument
  for having built them: the agent-KB check found two docs naming moved files,
  `modules_md` found two stale maps, the sub-workspace lockfile guard found all
  four lockfiles, and the footprint ratchet stopped on the new crate name (right
  to stop — the baseline moved in the same commit, with the reasoning recorded
  in it, which is what its own message demands instead of a waiver).
* **the snapshot codec** — `snapshot::{Reader, SnapshotState, SnapshotCursor,
  put_*}`, wanted by `ambition_time` and `ambition_sprite_sheet`. 347 lines,
  genuinely general, and the strongest carve-out candidate. ⚠ moving it moves the
  rollback fingerprint again — see the S30 fork below, which should be decided
  FIRST.
* **the control-frame vocabulary** — `ControlFrame`, `ControlFrameModes`,
  `InputFrameMode`, wanted by `ambition_input` and `ambition_persistence` (which
  also wants `ConfirmedFrameBoundary` / `world_state_is_confirmed`). General.
* **`AabbExt`** — the core's own extension trait on a `bevy_math` type. Small,
  travels with whatever holds the maths.
* ⚠ **the rest is platformer content reached by crates with general names**, and
  that is a different problem: `ambition_sprite_sheet` takes `BlockKind` and
  `BlinkWallTier`; `ambition_sim_view` takes `World`, `RoomGeometry`,
  `BodyMode`, blink clusters — ~20 of the 56 on its own. Those crates are not
  general services that got tangled; they are platformer code with unqualified
  names, and carving a kernel will not fix them. Renaming might.

⚠ **the census itself needed three corrections before it could be believed** —
it read free functions and macros as modules (inventing nine module names), and
it resolved the `ae` alias from Cargo.toml `rename` when the alias is declared in
RUST, so `use ambition_platformer2d_core as ae` made two crates look like they
imported NOTHING from a crate they use on every line. Spot-checked against grep
for `ambition_input` and `ambition_time` before any of it was acted on. See
`feedback_ask_the_tool_dont_model_it`.

▢ **S30 stands, sharpened by evidence.** The rename moved the rollback schema
fingerprint (94 of 352 baseline lines), so a crate name IS a wire-format fact
today. Decide (a) accept and regenerate forever, or (b) hash below the crate.

✔ **`MODULES.md` drift — CLOSED, and it now cannot recur silently.** Measured
2026-08-01: `modules_md.py` reports **55 crates up to date**, and
`ambition_causal` / `ambition_content_cli` / `ambition_content_pack` all have
maps. The reason it could drift 19 crates deep is that the script had a check
mode **nothing called** — the D-B navigability standard was maintained by
whoever remembered. `scripts/tests/test_modules_md_is_current.py` calls it now,
in the fast loop.
⚠ that was one of four checks found that day with no caller at all; see
`test_every_guard_has_a_caller.py`. A guard nobody executes is indistinguishable
from one that passes.

✔ **assert every symlink resolves — DONE**
(`scripts/tests/test_tracked_symlinks_resolve.py`, 2026-08-01). The sprites
symlink dangled through a clean `cargo check --workspace --all-targets` and
would have through the whole suite: no build step reads that directory, so the
first available symptom was a game with no sprites, at runtime, in whichever
composition loaded them first. Repository internally inconsistent, every
automated check green, only a human launching the right game can tell.
Symlinks come from `git ls-files -s` mode `120000`, so a NEW one is covered by
existing; probed by repointing the link at its pre-rename path.
⚠ `Path.exists()` follows the link (it is `stat`), which is what makes this
work — `is_symlink()` does not, and an `is_symlink() and ...` guard that reads
naturally can assert nothing.

▢ **`examples/portal_tutorial` is still `ambition_portal_tutorial`** — not on
Jon's list, so deliberately untouched. It demonstrates `ambition_portal2d`; the
name is now the odd one out.

---

## S32 — Sandbox naming retired, and couch multiplayer starts (2026-08-01)

Jon replaced `engine_rename_campaign.md` with three briefs. Two are worked here;
the third (engine restructures by difficulty) is deliberately untouched.

✔ **`Sandbox*` stops being a historical namespace.** Twenty-plus type renames in
three classes, then Jon's ownership audit corrected most of them: the asset
catalog has external consumers and both demos, developer state is read by
rendering/runtime/host/settings, and the audio plugin is shared by three games —
so they are `Platformer2d*`, not `AmbitionGame*`. Only what lives in
`game/ambition_app` is shipped-product. Two types were DELETED rather than
renamed (`SandboxQueues`, `SandboxSolidContributor` — neither had a reader), and
the reset vocabulary settled on `NewGameReset*` because that is what the
operation does.

⚠ every surviving `Sandbox*` is now a literal one — `PreloadClass::SandboxCore`,
`MovementProfile::SandboxFast`, `AbilityGrant::SandboxAll`, a content-CLI test
temp dir, and a forbidden-name ratchet that must keep naming a deleted resource
to keep working. That is the brief's own completion criterion.

▢ **couch multiplayer — the milestone that did not work is now one slice in.**
Reproduced first: two seats and one pad handed the ONLY controller to the
keyboard player and left the pad player with no device. `sources.rs` gives the
keyboard an owner it never had (`InputSourceId`, `InputAssignmentPolicy`,
`keyboard_owner_for`), and pad distribution skips the seat that already holds one.
`UnifiedPrimary` is the default and is today's behaviour byte for byte.

  ✔ [closed: S39 — `seats_offered_under` counts the keyboard] **next: the keyboard must COUNT as a source.** `LocalSeatTopology::players()`
  counts pads, so keyboard + one pad still reads as one player. Seats come from
  the lobby roster, not hardware — which is correct and must stay — so the lobby
  needs to be able to seat a keyboard claimant. ⚠ this touches GGRS session
  sizing and the frozen topology, which is why it is not in the first slice.
  Milestones 1–5 land there.
  ✔ [closed: S42 — milestones 6 and 7] **then: disconnect and reconnect** (milestones 6 and 7). A disconnect must
  not reorder participants or transfer ownership; the current pass CLEARS a
  seat's association when its pad vanishes, which is right for the device and
  says nothing yet about the participant keeping its seat and actor.

---

## S33 — the goal harness was measuring retired crate names (2026-08-01)

✔ **S1 slice H and S2 match activation were red about work that was done.** Both
facts held the whole time — `all_capabilities` is declared,
`cargo check -p ambition_platformer2d --no-default-features` exits 0, and the two
`activation_transaction` tests pass. The CHECK COMMANDS named
`crates/ambition/Cargo.toml`, `-p ambition` and `-p ambition_actors`, so cargo
failed on unknown packages and the harness turned that into "not done".

⛔ **that is the ninth measurement artifact of this run**, and the pattern has not
changed: a broken instrument is indistinguishable from a broken product, and the
only reliable defence is to verify the underlying FACT before touching the
instrument. Done here in that order.

⭐ **The blind spot generalises and is now closed.** `check_retired_crate_names.py`
swept `git ls-files`, and `.goal/` is untracked — so the one file whose entire
content is executable references to crate names was the one file it could not
see. It reads `.goal/*.json` explicitly now (`done-*.json` stay exempt as
records), and found **31 more stale commands** across four other goal
definitions the moment it could look. Each of those goals would have opened
reporting finished work as unfinished.

  ✔ [closed: the sweep ran — `.agent/index` was pruned, `.agent/reports/module-graph` regenerated, `.goal/*.json` is read explicitly] **the rule this leaves**: a guard scoped to TRACKED files has this hole by
  construction. Anything both untracked and executable — hooks, local configs,
  generated runners — has to be named explicitly or it rots silently. Worth one
  sweep for other instances; `.claude/`, `.agent/` and `scripts/lib/` are the
  obvious candidates and have not been checked.

✔ **S33 follow-through — the sweep found two more instances.**

* `.agent/index/crates/` kept nine packets for crates that no longer exist. The
  builder only ever WROTE, so a rename left confident, detailed, false
  descriptions an agent reads BEFORE opening any source. `build_catalog` prunes
  now, probed both ways. This is the regen invariant: a fresh clone would not
  produce those files, so the index depended on the machine's history.
* `.agent/reports/module-graph/` described the old crate names in 1,226 places.
  Regenerated through the tool's own `uv` script path — 56 crates, 0 stale.

▢ **`.agent/reports/jscpd/` is stale from 2026-06-09** and needs a Docker image
(`scripts/run_jspcd.sh`) to rebuild. Left alone: it is two months old on every
axis, not just crate names, so refreshing it is a decision about whether that
report is still wanted rather than a rename repair.

⚠ **the difference between those two is worth keeping.** The jscpd report carries
a `detectionDate`, so a reader can SEE it is old. The module graph carried no
stamp at all, which is why 1,226 wrong names sat there unnoticed. A generated
artifact without a generation stamp cannot be judged stale by the person relying
on it — and this repository generates a lot of them.

---

## S34 — ⛔ TWO authorities decide how many people are playing (2026-08-01)

Found while working the couch-multiplayer brief. This is the reason milestones 1
and 2 cannot be reached by adding a policy, and it is an authority conflict
rather than a missing feature.

**Rule A — the roster decides.** `seat_input_participants_for_roster`
(`crates/ambition_platformer2d_actor_monolith/src/schedule/input_systems.rs:100`)
states it outright:

> The roster is the authority on who is playing, so it is the authority on how
> many seats exist. Deriving seats from connected HARDWARE instead would mean a
> controller left plugged into a machine silently becomes a second player in
> every game on it.

**Rule B — the hardware decides.** `LocalSeatTopology::players()`
(`crates/ambition_input/src/local_seats.rs:125`) is `self.seats.len().max(1)`,
and `seats` is captured from `LocalDeviceOrder` — **the connected gamepads**. So
the count is derived from hardware, which is the exact thing rule A forbids.

And rule B is the one that reaches the session: `versus.rs:359` builds the roster
with `versus_roster_from(topology.players(), …)`, and the GGRS session sizes
itself from the same topology.

⚠ **it is wrong in both directions, which is why neither symptom alone found it:**

* a spare controller on the desk inflates the roster — precisely the failure rule
  A was written to prevent, live in the code that rule A's own layer feeds;
* a keyboard player plus one pad player DEFLATES to one player, because the
  keyboard is not a device row. That is Jon's milestones 1, 2 and 5 blocked, and
  no assignment policy can fix it from above — the count is already wrong before
  any policy is consulted.

### The resolution

`LocalSeatTopology` should freeze the **seat → source assignment for the seats
the roster declared**, not derive a seat count from devices:

* `players()` comes from the DECLARED seat count (rule A), not `seats.len()`;
* the frozen map is `seat → InputSourceId` (`sources.rs`), so a seat can be held
  by the keyboard, by a pad, or by nothing;
* `device_for_handle` keeps returning `Option<Entity>` — `None` for a keyboard
  seat, which is already the shape callers handle for an empty seat.

⚠ **this must not change solo play.** Under `UnifiedPrimary` the keyboard and
every pad collapse onto the primary participant, so one declared seat stays one
player however much hardware is attached — milestone 8. The keyboard becomes a
distinct source only when a seat CLAIMS it.

⚠ **and it touches GGRS session sizing**, which is why it is its own slice and not
a rider on the device-assignment fix already landed. `LocalSeatTopology` is
rollback-adjacent: the session freezes it once and every reconstruction — startup,
hot reload, proof-pulse restore — reads the frozen value. Changing what it freezes
changes what those three agree about.

Prerequisite already in place: `crates/ambition_input/src/sources.rs`
(`InputSourceId`, `InputAssignmentPolicy`, `keyboard_owner_for`), landed with the
pad-distribution fix and defaulting to today's behaviour.

---

## S35 — ⛔ the frozen seat topology does not exist in a shipped build (2026-08-01)

Found while looking for where to pass S34's declared seat count. There is nowhere
to pass it, because in a shipped build the thing that would receive it is never
created.

**Evidence chain, all verifiable by grep:**

1. `LocalSeatTopology` is CAPTURED in exactly one non-test place:
   `local_seat_topology()` in `game/ambition_app/src/dev/rollback_observatory.rs:295`,
   called lazily from the GGRS session builders at lines 393 and 581.
2. That module is `#[cfg(feature = "dev_tools")]` (`game/ambition_app/src/dev/mod.rs:9`).
3. `dev_tools` is in the `desktop_dev` default persona and **not** in `android`.
4. Every other use is `Option<Res<LocalSeatTopology>>` — a READ that returns early
   when it is absent (`versus.rs:352`, `assign_local_seat_devices`).

So on any build without `dev_tools`, and on desktop until somebody presses F9:

* `reconcile_roster_with_frozen_topology` returns on its first line, every frame.
  The roster and the session can disagree about seat count and nothing says so —
  which is the precise failure that function exists to report.
* `assign_local_seat_devices` always takes the LIVE-discovery branch. Its own doc
  says why that is wrong: *"a session that froze `handle 0 → keyboard, 1 → pad A,
  2 → pad B` and then let a disconnect reorder the live list would keep its GGRS
  handle COUNT while quietly changing which physical device drives each handle"*
  (GPT 5.6, 2026-07-29). The fix landed; the mechanism that makes it apply did
  not ship.

⚠ **this is the app-only-class defect one level worse.** The repo already has
`check_engine_systems_are_engine_installed.py` for "the engine owns the code, the
shipped app owns the REGISTRATION". Here the registration is owned by a DEV
FEATURE, so the capability is absent from every build a player runs, and every
test that exercises it passes because tests construct the resource by hand
(`versus.rs:1070`, `local_seats.rs` tests).

### What to do

▢ **freeze the topology where the gameplay session begins**, not where a rollback
proof begins. The doc on `LocalSeatTopology` already names the correct lifetime —
"decided ONCE per GAMEPLAY session, not per GGRS session… REMOVED when gameplay
ends" — and the observatory is simply the only caller that ever implemented it.
That is also the natural seam for S34's `capture_for_roster`: the roster is known
at session start, which is exactly when the count should be frozen.

✔ **the instrument exists — `scripts/check_capability_ships.py` (2026-08-01).**
This class WILL recur: a capability whose only installer is behind a
`#[cfg(feature)]` that shipping personas omit is invisible to both the compiler
(the reader is `Option`) and the suite (tests build the resource by hand). The
existing engine-systems check compares APP registrations against ENGINE ones; it
never asked whether the registration survives the feature set a player gets.

It flags a type that is read as `Option<Res<T>>` from shipping code and whose
every writer sits in a module gated by a non-shipping feature, walking the module
tree from each crate root so a file inherits its parent's gates — which is how
`dev/mod.rs` being `#[cfg(feature = "dev_tools")]` makes everything beneath it
dev-only, the exact shape S35 had.

**Probed by recreating S35**: gate the module holding the freeze system as
`dev_tools` and it reports, in as many words, *"LocalSeatTopology: every writer
is dev-only; 3 shipping readers take it as Option and return early forever."*

⛔ **and the first version was green through its own motivating case.** The
writer does not name its type at the call — `freeze_local_seating_for_the_decided_match`
builds a local, mutates it, and passes the binding
(`commands.insert_resource(topology)`). Matching only `insert_resource(TypeName …)`
left `LocalSeatTopology` with NO writer at all, so it fell into the
"written nowhere, not our question" branch and the check passed. It now follows
`let mut topology = …::LocalSeatTopology::default()` to the binding.
⚠ that is three instruments in one day whose first draft was wrong about the
thing it was written to measure. Probe by restoring the ORIGINAL defect, not by
inventing a new one.

⚠ `DEV_ONLY_FEATURES` is deliberately just `dev_tools`, not every feature a
shipping persona omits: a wider net fires on platform features
(`android_platform` legitimately installs what desktop must not), and a guard
that cries wolf gets waived.

---

## S36 — why couch multiplayer cannot be checked (2026-08-01, Jon's report)

Jon: *"even when we add a CPU player in smash there is only ever one player that
shows up in game, that will make it very hard to check that couch multiplayer is
working right."*

**Measured, not guessed:**

✔ **the seating is fine.** `a_two_participant_roster_actually_seats_two_bodies`
runs the real select flow, adds a CPU, starts the match and counts `MatchSeat`
entities: TWO bodies are seated. Every previous test in that file stopped at the
route and the session, so a roster of two putting one fighter on stage passed the
whole suite.

⛔ **the two fighters are built by different paths.** The census the test prints:
both carry 84 components and the SETS DIFFER — seat 0 is player-bodied
(`PlayerVisual`, `BodyPoseView`, `PresentedPose`, `Transform`, `GlobalTransform`),
seat 1 is actor-bodied (`ActorIdentity`, `Perception`, `RoomVisual`,
`RuntimeStagedActor`) with no transform and no pose view. That is the two-port
body fighter-unification exists to remove, surfacing as a user-visible symptom.

⚠ **and a headless test CANNOT settle whether seat 1 draws.** `BodyPoseView` is
the player-bodied read model; an actor-bodied fighter draws through the id-keyed
`ActorAnimIndex`, a resource rebuilt in the RENDER presentation plugin and
explicitly *"NOT the sim schedule … so a headless / RL build never pays for poses
it won't draw"*. The first draft of the test asserted seat 1 must carry a
`BodyPoseView` — a confident measurement of the wrong port, caught before it
shipped.

### ⛔ The instrument gap, which is the actual blocker

`capture_scene --route smash_gameplay` photographs the stage with **one fighter**
— correctly, because navigating straight to a route decides no roster. There is
no way to photograph a DECIDED match: `capture_scene` navigates, it cannot drive
the select screen to add a CPU and lock in.

So the only state anybody wants to look at — two fighters on a stage — is
reachable by neither the headless suite (blind to rendering by construction) nor
the capture tool (cannot reach the state). That is precisely Jon's "very hard to
check", and it is an instrument problem before it is a gameplay one.

  ✔ [closed: S36 — `capture_scene --press`] **give `capture_scene` a way to reach a decided match**: a `--smash-cpu N`
  style flag, or more generally a way to drive a route's own lobby before the
  shutter. The select screen already has headless drivers in
  `smash_in_the_host.rs` (`add_cpu`, `confirm`); the capture binary needs the
  same entry, not a new mechanism.
  ✔ [closed: S36 — photographed; both fighters draw once the roster survives] then photograph it, and the question "does the CPU fighter draw" answers
  itself in one image.

✔ **S36 instrument gap CLOSED, and the bug is now photographed.**
`capture_scene --press Down,Enter,Enter` drives a route's own lobby before the
shutter, reusing the same key taps the smash tests already use rather than adding
a second mechanism. One command reproduces Jon's report:

```bash
cargo run -p ambition_app --bin capture_scene -- \
    --route smash_select --press Down,Enter,Enter out.png 1600x900
```

The screen decides a real two-fighter match (`P1 — smash_duelist_a READY`,
`P2 — CPU · smash_duelist_b READY`), the stage loads, and **one** fighter stands
on the platform.

⚠ the first run photographed the select screen mid-"Starting…" — a confirmation
STARTS a route change, so the shutter caught the moment the presses were accepted
rather than the state they asked for. `POST_PRESS_SETTLE_FRAMES` fixed it, and
the distinction is the same one the route-camera grace draws.

  ✔ [closed: it was the roster clobber, not the draw path] **next, and now cheap**: the CPU fighter is seated and not drawn. It is
  actor-bodied, so it draws through the id-keyed `ActorAnimIndex` rebuilt in the
  render presentation plugin. Check whether that index has a row for the seated
  CPU's id — if it does not, the actor visual was never joined; if it does, the
  visual exists and is mispositioned (seat 1 carries no `Transform`). One
  instrumented capture run answers which.

⚠ **and an unrelated observation from the same photograph, worth recording**: the
select screen renders `·` (U+00B7) and `—` (U+2014) CORRECTLY — "P2 — CPU ·
smash_duelist_b". The menu-text tofu (Z1, nine dead hypotheses) is therefore NOT
repo-wide text rendering; this screen reaches glyphs `ambition_menu` cannot. That
is a live discriminator the tofu row did not have.

### ⚠ CORRECTION (same day): the photograph does NOT reproduce Jon's case

I wrote that `--press Down,Enter,Enter` reproduces "add a CPU and only one player
shows up". It does not, and a probe caught it before it became received wisdom.

Instrumenting the shutter frame in the capture binary:

```text
[stage-probe] seated: []
[stage-probe] feature visuals: 0 -> []
[stage-probe] roster=None active_match=false frames=245
```

**No `MatchSeat` entities at all, no roster, no active match** — and yet the
photograph shows a fighter on the platform. That fighter is the ordinary primary
player body, not a seated one. So the capture reaches the smash STAGE with no
match behind it; the image is a true photograph of a state, but not of the state
Jon described.

⛔ **and the divergence is the finding.** The same key sequence, through
`shell_host_app()` in `smash_in_the_host.rs`, seats TWO bodies — that is the
census test, and it passes. Through the `capture_scene` composition the roster is
gone by the time the stage is up. Two compositions of the same engine reach
different states from identical input, which is the composition-divergence class
this repo has been bitten by three times before.

  ✔ [closed: neither — the reconciler transferred ownership of the roster] **chase that first**: does the capture binary lack the plugin that publishes
  or activates the roster, or does the roster get published and then dropped on
  the route change? `active_match=false` with `roster=None` says activation never
  ran; whether that is a missing registration or a cleared resource is one more
  probe.
  ✔ [closed: superseded: the two-fighter stage is photographed] ⚠ **and it means the headless test is the ONLY thing currently proving the
  two-fighter seating**, in one composition. Until the capture path agrees, "two
  bodies are seated" is a fact about `shell_host_app`, not about the game.

The `--press` mechanism itself is sound and stays — it drove the select screen
correctly (`P1 READY`, `P2 CPU READY`, `Starting…`) and is what made this
measurable at all.

⚠ **and one candidate is already eliminated.** I assumed the capture binary
simply lacked the host composition — the plugin list visible at the top of
`capture_scene.rs` builds `AmbitionGameSimulationPlugin` and friends directly,
with no `compose_ambition_shell_host` in sight. **That list is the ROOM-mode
builder.** Route mode goes through
`build_visible_app(VisibleRenderMode::OffscreenGpu, /* shell_hosted */ true)`,
which does call `compose_ambition_shell_host` and `install_ambition_shell_visuals`.

So both compositions ARE shell-hosted, and the divergence is narrower and more
interesting than "a missing plugin": the same host, driven by the same key taps,
publishes a roster under `shell_host_app()` and not under the capture app. The
remaining differences worth probing, in order — the render mode
(`OffscreenGpu` vs none), `TimeUpdateStrategy::ManualDuration`, and whether 150
settle frames is simply too few for select → roster → activation when the tick is
manual.

⚠ that last one is the cheap check and should be first: raise
`POST_PRESS_SETTLE_FRAMES` and see whether the roster appears. A timing shortfall
and a composition defect look identical from one photograph, which is the whole
reason this row exists.

⚠ **the cheap check ran, and it did not answer the question — it found a second
limit instead.** Raising `POST_PRESS_SETTLE_FRAMES` to 600 makes the capture fail
outright:

```text
capture_scene: timed out waiting for texture readback after 691 frames
               (warmup 90 + grace 600)
```

The readback deadline is computed from the same budget the settle spends, so the
settle cannot be raised past it without the tool giving up before the shutter.
Reverted to 150.

  ✔ [closed: the press settle waits on readiness now] **so the settle has to wait on a CONDITION, not a frame count**: the route
  the presses asked for having become active, and — for a lobby that starts a
  match — the match having activated. `capture_scene` already draws exactly this
  distinction for the other end of the capture, in its own words: *"warmup is a
  duration, readiness is a fact"*. The press settle is currently a duration and
  should be a fact, and the readback deadline should extend while it waits rather
  than counting against it.

That is a small, contained change to the tool, and it is the thing standing
between Jon and a photograph of a two-fighter stage.

✔ **timing ELIMINATED** (2026-08-01). The press settle now waits on readiness
rather than a frame count — the presses end one capture and begin another, so the
route's own warmup and readback deadline apply. The photograph is unchanged: one
fighter, subject at the primary player's position, resolved after the route's own
90-tick warmup. The missing match survives a correct settle.

  ✔ [closed: explained: Versus rebuilt Smash's roster as its own] **what is left to explain the divergence**, now that timing is out: the
  capture app runs `VisibleRenderMode::OffscreenGpu` with
  `TimeUpdateStrategy::ManualDuration`, and `shell_host_app()` runs
  `MinimalPlugins` with real time. A match activation that depends on a
  `FixedUpdate`/tick cadence would behave differently under manual duration, and
  that is the first thing to check — it is one probe (does the select screen's
  confirmation reach the activation systems at all under the capture's clock).

### ⚠ the divergence is REAL and reproducible; timing and entry route are both out

Two entry paths (`--route smash_select`, and through the launcher with a `wait`
between screens), two settle strategies (fixed 150 frames, then readiness-based):
**every run reaches the stage with one fighter.** The probe at the shutter says
`roster=None active_match=false`, and no `MatchSeat` entities exist.

Eliminated, each by measurement rather than reasoning:

* ⊘ **settle timing** — readiness-based settle gives the same image, and raising
  the fixed count instead breaks the readback deadline.
* ⊘ **entry route** — going through the launcher, the way the passing test does,
  changes nothing.
* ⊘ **the clock** — route mode does NOT set `TimeUpdateStrategy::ManualDuration`;
  that is the room-mode builder. Both compositions run on real time.
* ⊘ **a missing host composition** — route mode calls
  `build_visible_app(OffscreenGpu, shell_hosted = true)`, which composes the shell
  host and its visuals.

  ✔ [closed: explained: the roster clobber, not the composition] **what is left**: `VisibleRenderMode::OffscreenGpu`, and whatever else
  `build_visible_app` installs or omits that the test's explicit
  `MinimalPlugins + PlatformerHostPlugins + compose_ambition_shell_host` list does
  not. The next probe is a diff of those two compositions' registered systems —
  `schedule-census` already prints a total (623 systems in the capture run), so
  printing the same census from `shell_host_app()` and diffing the NAMES is the
  measurement, not another guess.

⚠ **and the test is the weaker instrument here, not the stronger one.** It passes
in a composition no player runs. Whatever the diff turns up, the fix belongs on
the side that makes the SHIPPED composition seat two — not on the side that makes
the test agree with the capture.

## ✔ S36 CLOSED — Versus was rebuilding Smash's roster as its own

Root cause, from a per-frame probe through a real capture:

```text
route=smash_select    roster=2  ← the screen decides two fighters
route=smash_gameplay  roster=2
[roster-remove] on_versus=false mine=true published_by=Some("ambition_versus")
route=smash_gameplay  roster=-  ← gone, nobody seated
```

`versus_roster_from` stamps `published_by: ambition_versus`, so
`reconcile_roster_with_frozen_topology` rebuilding Smash's roster did not resize
it — it **transferred ownership**. `maintain_versus_stage` then did the right
thing with a versus-owned roster on a non-versus route and deleted it.

⛔ **the guard already existed one function over** and the reconciler never
learned it. `maintain_versus_stage`'s own comment states the rule — *"MINE, not
'a roster exists'… the resource is global and had no owner until it did."* One
`is_published_by` check in the sibling that was missing it. Photographed before
and after: one fighter, then two.

⚠ **and this makes S35 urgent rather than tidy.** The bug only bites where a
topology is FROZEN, and today the only thing that freezes one is the rollback
observatory behind `dev_tools`. `shell_host_app()` freezes none, so the
reconciler returned early and the headless test counted two seats and passed —
**about a composition no player runs**. Fixing S35 (freezing the topology in a
shipped build, where it belongs) would have SHIPPED this bug. The regression test
therefore freezes a topology deliberately; one written without it would have been
green and worthless.

  ✔ [closed: `a-second-writer-of-a-match-global-must-answer-ownership`] **the general form**: `MatchParticipantRoster` is a global
  resource with an owner field, and ownership is enforced by convention at each
  site. Two sites had the check, one did not, and nothing names the rule in one
  place. A `published_by`-aware accessor — or an absence contract that every
  `remove_resource::<MatchParticipantRoster>()` and every roster REPLACEMENT sits
  behind an ownership test — turns the third omission into a build failure
  instead of a photograph.

## S37 — a goal check that could not fail (2026-08-01)

Found while probing the new roster contract. `.goal/active.json` ran
`python3 scripts/check_absence_contracts.py >/dev/null` — **without `--check`** —
and the script returns `1 if args.check else 0`. So it printed `RED` for a
violated contract and exited 0, and *"S1 slice H: the absence contracts and the
footprint ratchet are green"* has been reported satisfied regardless of whether
they were.

Probed both ways with a real violation present: exit 0 without the flag, exit 1
with it. Repaired.

⚠ **the contracts themselves were never unguarded** — `scripts/tests` enforces
them through pytest, which is what the suite runs. Nothing rotted. What was
broken is narrower and worth naming exactly: the GOAL was reading an instrument
wired to always agree, so one of its twelve green rows carried no information.

  ✔ [closed: S37 — all twelve audited, two more repaired] **the class is worth a sweep, and it is cheap**: every `.goal` check is a
  shell command whose exit code is the whole signal. A command that cannot return
  non-zero — a script with an optional strictness flag, a `| head`, a `grep` whose
  failure is swallowed, a `; true` — is a row that is green by construction. The
  other eleven have not been audited this way. ⚠ note two already-known traps
  apply directly: `| head` closing a pipe under `pipefail` reports SIGPIPE
  (`reference_goal_check_sigpipe_footgun`), and a `pkill -f` matches its own
  shell.

✔ **S37 sweep done — all twelve audited, two more repaired.**

The `|| true` in six checks is CORRECT and not the smell it looks like: the
output is captured and a trailing `grep -E 'test result: ok\. [1-9][0-9]* passed'`
is what decides, so a compile error produces no matching line and the check
fails. The `[1-9]` also rejects `ok. 0 passed`, which is what a filter matching
nothing prints.

⛔ **but two checks ran TWO cargo commands and grepped the concatenation** (S2
match activation, S4 damage percent). A single `ok. N passed` from EITHER half
satisfied the grep, so the other half could fail to compile entirely and the row
would stay green — partial credit, in a check whose whole job is to say both
halves hold. Both now also require the absence of `test result: FAILED` and of a
leading `error[`/`error:`. Probed: an injected `FAILED` line flips the verdict.

Clean afterwards: checks 1 (inverted ledger), 2 (`cargo check`), 3 (`test -z`),
4 (`git grep -q && cargo check`), 7, 9, 11, 12 all fail properly — each ends in a
command whose exit code is the signal, with no swallowing.

⚠ **the general rule this leaves**, worth applying to any future goal: a check is
a shell exit code, so the LAST command must be the one that can say no. Capturing
output and grepping it is fine — grepping a pile assembled from several commands
is not, because the pile cannot say which half spoke.

✔ **and the sibling globals are clean — audited, not assumed.** The same clobber
shape was checked on the three other resources a second experience can write:

* `ActiveMatch` — two production removals, both in `versus.rs`, both inside
  route-gated arms (`(true, false)` clears it because a new roster is not yet a
  match; `(false, true)` is the teardown, now ownership-gated).
* `DeclaredCombatRules` — one removal, in the same ownership-gated teardown.
* `DeclaredInputSeats` — no production removals at all.

⚠ **but the asymmetry is the finding.** `MatchParticipantRoster` has a
`published_by` field, so an ownership question can be ASKED. `ActiveMatch` and
`DeclaredCombatRules` do not — they are safe today only because exactly one
experience touches them. The moment a second one does, there is no check to
write, and the failure will look like this one did: a match that quietly belongs
to nobody.

  ⚠ that is the argument for making the owner part of the resource rather than a
  convention per resource. Not urgent — one writer each — but it is the same
  design question the roster answered once and the others have not been asked.

✔ **both sides of the reconciler guard photographed.** Smash now seats two
(`Duelist B` on the stage beside the player); Versus is unchanged and correct —
two fighters, `Long Guard 60/60` / `Close Guard 52/52`, `ROUNDS blue 0 - red 0
(first to 2)`, `FIGHT`. A guard that fixed one game by breaking the other would
have looked identical in the Smash photograph alone.

## S38 — Ambition's HUD is drawn over a Smash match (2026-08-01)

Seen in the same photographs, and it is not a Smash HUD at all:

    smash_gameplay   →  HP 100/100 · MP 100 · $0        ← Ambition's adventure HUD
    versus_gameplay  →  Long Guard 60/60 · Close Guard 52/52 · ROUNDS · FIGHT

Smash is a PERCENT-and-STOCKS game — S4's own row says *"the damage meter can
EXCEED the pool (percent is not health)"* — so a health bar, a mana bar and a
money counter are three readouts that describe a different game. Versus, which is
the same fighting stack, has the right ones. The touch bezel differs too: the
Smash stage offers Blink / Fly Toggle / Ranged / Bubble Shield, which are
Ambition's protagonist verbs, not a fighter's.

⚠ **this is the app-only-presentation class again** — the HUD a route gets is
whatever the host installed, and nothing asks whether the ROUTE wanted it. The
declared-HUD seam exists for exactly this
([`project_declared_hud_seam_2026_07_21`]) and Smash is not using it, or is being
overwritten after it does.

  ✔ [closed: S38 — a MISSING declaration, not precedence] check which: does `smash_gameplay` declare a HUD that Ambition's overwrites,
  or declare none and inherit? One is a precedence bug and the other is a missing
  declaration, and the photograph cannot tell them apart.

✔ **S38 HUD half CLOSED.** Smash declares its own readouts now — `Duelist A 0% ·
3/3`, one gauge per fighter, seat-coloured — and Ambition's health/mana/money is
gone. The answer to the row's open question was **a missing declaration**, not a
precedence bug: Smash called `.with_hud(..)` nowhere at all.

⚠ **the sharpest part is what was already there.** `damage_percent()` is
deliberately unclamped with a test called
`damage_percent_is_unclamped_so_a_hud_can_print_188`, and `FighterStocks` keeps
`started_with` *"so a HUD can draw '2 of 3'"*. Two APIs were built FOR this HUD
and nothing ever consumed them — which is exactly why nothing was red. Every
piece worked; none was wired to a screen.

  ✔ [closed: S38 — it was the ability set, and the bezel was reporting it correctly] **the touch bezel is still Ambition's** — Blink / Fly Toggle / Ranged /
  Bubble Shield on a platform fighter, where Jab / Jump / Dash / Shield belong.
  Same class as the HUD (an inherited declaration nobody overrode) and probably
  the same one-line shape, but it is a different seam and is not assumed to be.
  ⚠ note the versus photograph shows a SHORTER bezel (Interact / Jab / Jump), so
  something already differs per route — find what decides it before adding a
  third mechanism.

### ⚠ CORRECTION: the bezel is a SYMPTOM, not a second declaration

The row above guessed the touch bezel was "an inherited declaration nobody
overrode, probably the same one-line shape" as the HUD. It is not, and the
mechanism says something more interesting.

Touch buttons are not declared per route at all. `touch_action_layout()` is ONE
fixed ten-button array, and visibility is decided per button by
`touch_action_available(action, &prompt)` where `prompt: Res<ControlPrompt>` —
the read-model of **what the CONTROLLED SUBJECT can do**. The versus stage shows
three buttons because its fighter can do three things. The prompt is working
exactly as designed.

⛔ **so the smash stage offering Blink / Fly Toggle / Ranged / Bubble Shield is
the prompt correctly reporting that seat 0 can do those things** — because seat 0
on that stage is Ambition's protagonist body, adopted, carrying Ambition's
ability set. The seat census in
`a_two_participant_roster_actually_seats_two_bodies` already showed it and I read
it as a rendering split: seat 0 carries `PrimaryPlayer`, `LocalPlayer`,
`PlayerEntity`; seat 1 is a constructed actor. The HUD calls it `Duelist A`
because the ROSTER says so; the BODY never became one.

  ✔ [closed: the ability levelling commit] **the real row: an adopted seat keeps the adopting body's abilities.** Player
  one fights as the exploration protagonist — blink, flight, projectiles, bubble
  shield — while player two is a duelist with a jab. That is not a couch-play
  balance question, it is two different games on one stage, and it makes "is
  couch multiplayer working" unanswerable for a second time.
  ⚠ do NOT fix this by filtering the bezel. The bezel is the only honest thing in
  the picture — it is reporting the defect.

## ✔ S38 CLOSED — and the bezel correction was itself corrected

Final state, photographed: two fighters, `Duelist A 0% · 3/3` / `Duelist B 0% ·
3/3`, and a bezel of Attack / Dash / Jump / Interact / Ranged.

⚠ **I was wrong twice about the bezel and the measurements are what settled it.**

1. First guess: "a missing declaration like the HUD." Wrong — buttons are one
   fixed layout gated by `touch_action_available(action, &ControlPrompt)`.
2. Correction: "seat 0 is Ambition's protagonist body." Also wrong — measured,
   `worn=smash_duelist_a`. It wears the right character.
3. What was actually true: it wears the right character and carried the wrong
   VERBS. seat 0 had every ability in the game (fly, blink,
   blink_through_hard_walls, glide, swim); seat 1 had run, jump, double jump and
   attack. The bezel reported seat 0 honestly the entire time.

Seating already levels this, gated on `roster.fighter_abilities`, with a comment
describing the identical failure found on the VERSUS stage in July. Versus
declares a set; Smash declared nothing, so the levelling never ran.

⚠ **and both named ability sets were wrong, each caught by re-measuring after
declaring it.** `basic()` would have removed double jump and attack. `sane_subset()`
— which reads like a fighter's kit in its opening lines — also grants fly, blink,
wall climb and pogo, so declaring it made the two seats agree that they could
both FLY. The set is spelled out now: run, jump, double jump, fast fall, dash,
attack.

  ✔ [closed: S38 — they come from the MOVESET, not abilities] `Ranged (V)` and `Interact (F)` survive on the bezel and no ability grants
  them, so they come from another source (a moveset or technique). Small, and
  now the only thing on that bezel not explained.
  ✔ [closed: `an_adopted_seat_and_a_spawned_seat_agree_on_every_roster_declared_field`, scoped to what the MATCH declares because health/box/mass are per-character] the four divergences seating has had to unify one at a time — health, box,
  mass, and now abilities — are all the same shape: *an adopted body keeps what
  the session gave it*. A fifth is likely. The comment listing them is the best
  record; a test that asserts an adopted seat and a spawned seat agree on every
  declared identity field would be better.

✔ **and the last two bezel buttons are EXPLAINED, not defective.** `Ranged (V)`
and `Interact (F)` do not come from the ability set at all — an action scheme maps
`ControlSlot::Projectile` to the `"ranged"` MOVESET verb and
`ControlSlot::Interact` likewise (`action_scheme.rs:238`,
`combat_from_moveset`). So the bezel is showing what the duelists' moveset
authors, which is a character-authoring question ("should a duelist have a
projectile?") and not a leak from Ambition.

⚠ worth stating because the same button would have read as another inherited-verb
bug: the bezel has exactly TWO inputs — abilities (movement) and moveset verbs
(combat) — and only the first was wrong.

✔ **the rule the four divergences share is now a test.**
`an_adopted_seat_and_a_spawned_seat_agree_on_every_declared_field` runs the real
select flow and asserts that every seat agrees on what the ROSTER declared,
scoped deliberately to declared fields — per-character differences are the point
of a fighting game, and "both seats are identical" would have to be waived the
first time somebody authors an asymmetric pair. Probed by deleting the ability
declaration: fails naming the seats, passes when restored.

## S39 — couch multiplayer: milestones 1–2 are expressible now (2026-08-01)

Three pieces, landed in order, each with the default preserving solo play:

1. `sources.rs` — `InputSourceId`, `InputAssignmentPolicy`, `keyboard_owner_for`.
   States who owns the keyboard, which nothing could previously say.
2. `assign_local_seat_devices` — pads go to seats that NEED one, skipping the seat
   already holding the keyboard. Fixes "the only pad goes to the person typing".
3. `seats_offered_under(devices, policy)` — the keyboard COUNTS as a source under
   `JoinToClaim`, so keyboard + one pad is two players. The smash select screen
   declares that policy while it is up.

⚠ **`UnifiedPrimary` is the default throughout** and every pre-existing test
passes untouched. Milestone 8 — a solo player driving one character with keyboard
or pad — is not something the couch work is allowed to charge for, and each of
the three pieces was written so that installing it changes nothing until a lobby
asks.

  ✔ [closed: S41 — the milestone-5 test passes] **milestone 5 is NOT proven**: no test drives a real keyboard press and a real
  pad press into two different seats in one match. That needs a fixture holding
  both source kinds, and it is the honest end of this slice — everything above is
  the lobby being ABLE to seat two people, not evidence that two people's inputs
  stay apart.
  ✔ [closed: S42 — milestones 6 and 7] **milestones 6 and 7** (disconnect keeps the seat, reconnect restores the
  same participant) are untouched. The current pass CLEARS a seat's association
  when its pad vanishes, which is right for the DEVICE and says nothing yet about
  the participant keeping its seat and its actor.
  ✔ [closed: S35 CLOSED] ⚠ and S35 still stands underneath all of it: the frozen topology that would
  make a MATCH stop re-sampling devices is created only by a `dev_tools` tool.

### ⛔ S39 addendum — the fix reintroduced the bug, and the test agreed with it

Attempting milestone 5 (a keyboard player and a pad player driving different
fighters) measured something the couch work had supposedly removed:

    slot 1 menu_select_pressed=true     slot 0 menu_select_pressed=true

One South press on one pad, arriving at BOTH seats. `assign_local_seat_devices`
gave the keyboard-owning seat `None` — and in leafwing an unset gamepad means
*whichever pad this finds first*, which the top of that same module documents as
the couch bug. The commit that fixed the couch bug reintroduced it one branch
over.

⚠ **and the test passed throughout**, because it asserted
`assigned(seat_one) == None` — written from the code rather than from the
property. `None` is the spelling that means the OPPOSITE of "this seat owns no
pad". Now `Some(Entity::PLACEHOLDER)`, which is what leafwing's own fallback
resolves to when no gamepad exists, with the property on the line.

After: `slot 1 true, slot 0 false`.

  ✔ [closed: S41 — the policy expiring at the route change was the last bug] **and milestone 5 is still not reached, one layer further along.** With the
  isolation correct, the pad's `MenuSelect` reaches slot 1's `ActionState` and the
  smash select screen still shows `seats: LockedIn Empty Empty Empty` — seat 1
  never joins. So the gap is now between a SEAT's action state and the select
  screen's per-seat menu frame (`populate_seat_menu_frames` /
  `SeatMenuFrames`), not in device ownership. That is a much smaller search than
  it was this morning, and the exploratory test that found it was removed rather
  than committed red.
  ⚠ the probe worth keeping in mind: print `menu_select_pressed` per slot at the
  moment of the press. It distinguishes "the input never arrived" from "the
  screen ignored it" in one line, and those two look identical from a photograph.

## ✔ S40 — couch milestones 1 and 2 are REACHED (2026-08-01)

A keyboard player and a gamepad player now take different seats and start a match
together. Measured, same scenario each time (keyboard confirm ×2, pad South ×2,
one pad connected):

    before   seats: LockedIn Empty        0 bodies
    after    seats: LockedIn LockedIn     2 bodies

Four things had to be built or repaired, and three of them were bugs found by
measuring the previous fix rather than by reading it:

1. `seats_offered_under(devices, policy)` — the keyboard COUNTS as a source under
   `JoinToClaim`. It was never a row in `LocalDeviceOrder`, so it could only be
   assumed, in prose, next to arithmetic that could not see it.
2. pads go to the seats that NEED one, skipping the seat already holding the
   keyboard. Before: the only pad went to the person typing.
3. ⛔ the keyboard seat must be associated with `Entity::PLACEHOLDER`, NOT `None`
   — an unset gamepad in leafwing means *whichever pad this finds first*, so the
   fix for (2) reintroduced the couch bug and its test agreed with it, because
   the test asserted `== None`, written from the code instead of the property.
4. ⛔ `drive_the_select_screen` iterated the UNIFIED count while the lobby
   declared the couch one. Two counts of the same thing: seat 1 existed, owned
   the pad, had the confirm in its `ActionState`, and was never read.

### ▢ milestone 5 is NOT reached, and here is the number

With both fighters seated, the pad's DPadRight moved BOTH — **14.19px** on the
keyboard player's fighter against **11.44px** on the pad player's.

So MENU input is seat-isolated (`MenuSelect` reaches only slot 1) and GAMEPLAY
input is not. Two different paths; one fixed. The next attempt starts here rather
than at the beginning.

  ⚠ the likely shape, stated as a hypothesis and not as a finding: seat 0 is the
  PRIMARY player, whose movement travels the global `ControlFrame`
  (`populate_control_frame_from_actions`) rather than the per-seat
  `SlotControls` path seat 1 uses. If so the isolation has to happen where the
  frame is populated, not in the input map — and the `InputMap` association fix
  is necessary but cannot be sufficient.
  ⚠ the test is written and NOT committed. A red test in the suite is worse than
  a written-down failure with its numbers.

## ✔ S41 — couch milestone 5 PASSES (2026-08-01)

`a_keyboard_player_and_a_pad_player_drive_different_fighters` runs the real
select-screen flow, seats a keyboard player and a pad player, moves one and
measures that the other does not follow. Milestones **1, 2 and 5** are reached.

⛔ **The last bug was mine and it wore a convincing disguise.** The policy was
`if on_select { JoinToClaim } else { UnifiedPrimary }`, so the assignment the
lobby made was undone the instant the stage loaded:

    during the match   slot 1 move_right=true   slot 0 move_right=true
    on the select      MenuSelect reached slot 1 alone

That reads exactly like *"menu input is isolated, gameplay input is not — two
different paths and only one is fixed"*, and the previous row said so. It was ONE
path under two policies. The wrong conclusion was specific, mechanical and
testable, and would have sent the next attempt hunting a second isolation bug in
`populate_control_frame_from_actions` that does not exist.

Jon's brief had the answer in a sentence: *"Before the match starts, freeze:
participant, session seat, control channel, input sources."* An assignment that
expires when the lobby closes is the opposite of frozen.

**Five pieces, four of them bugs found by measuring the previous fix:**

1. the keyboard COUNTS as a source (`seats_offered_under`)
2. pads go to the seats that need one, skipping the keyboard's
3. ⛔ the keyboard seat needs `Entity::PLACEHOLDER`, not `None` — unset means
   *every* pad, and its test agreed with the code because it asserted `== None`
4. ⛔ the select screen iterated the unified count while its lobby declared the
   couch one
5. ⛔ the policy reverted on the route change into the match

⚠ **the instrument that resolved four of the five** is one line per slot at the
moment of the press — `move_right=` / `menu_select_pressed=` per participant. It
distinguishes *the input never arrived* from *the surface ignored it* from *both
seats got it*, and those are indistinguishable from a photograph or a body
position.

  ✔ [closed: S42] **milestones 6 and 7 are untouched**: a disconnect must not reorder
  participants or transfer ownership, and a reconnect must restore the same
  participant. Today `assign_local_seat_devices` CLEARS a seat's association when
  its pad vanishes — right for the DEVICE, and it says nothing about the
  participant keeping its seat and its actor. That is the next slice, and it is
  where `LocalSeatTopology` being frozen (S35) starts to matter for real.
  ✔ [closed: S43 — both asserted in the milestone-5 test] milestone 3 (stable session seats) and 4 (distinct controlled actors) are
  implied by the passing test but not asserted separately.

## ✔ S42 — couch milestones 6 and 7 (2026-08-01)

⛔ **Milestone 6 was violated and a test blessed the violation.** Two seats, two
pads, unplug player ONE's:

    before   seat one → pad A     seat two → pad B
    after    seat one → pad B     seat two → pad B

`LocalDeviceOrder` forgets a pad that leaves, so every later seat shifts down one
and positional assignment redistributes the room. Invisible with a single pad,
which is how milestone 5 was reached without meeting it.

⚠ **`an_unfrozen_session_still_follows_live_discovery` asserted exactly this** —
*"with no session owning the seating, the live order is the authority"* — while
the test DIRECTLY ABOVE IT forbids the same promotion in the frozen case and
names the consequence: *"silently hand seat one's confirmed GGRS inputs to seat
two's physical controller."* Two tests, one hazard, opposite verdicts, differing
only in whether a topology happened to be frozen — and the unfrozen branch is the
one every player runs (S35).

`SeatDeviceOwnership` makes the assignment a fact that is REMEMBERED rather than
a position that is RECOMPUTED: a seat keeps its pad while that pad exists, a pad
that leaves frees exactly its own seat, and a free pad is only taken by a seat
that has none.

✔ **milestone 7 falls out of the same record.** A reconnecting pad is a new
entity — Bevy moves the generation, so remembering "seat one had 3v0" would never
match again — and what restores the participant is that the seat which lost a pad
is the one still holding none.

**Couch status: milestones 1, 2, 5, 6, 7 pass; 8 (solo unchanged) is pinned by
every pre-existing test plus an explicit one. 3 and 4 are implied by the passing
milestone-5 test and not asserted separately.**

  ⚠ ⚠ **three of the last four bugs were in code written the same day to fix the
  previous one**, and each was found by a measurement rather than by rereading:
  `None` meaning every pad, the select screen counting seats differently from its
  own lobby, the policy expiring at the route change, and now positional
  reassignment on disconnect. The pattern is worth naming — a couch input fix
  that is not measured under TWO sources is not known to work, because every one
  of these was invisible with one.
  ✔ [closed: S35 CLOSED] S35 still stands: the topology freeze that would make a MATCH stop
  re-sampling devices is created only by a `dev_tools` tool. Everything above
  makes the unfrozen path correct, which lowers the urgency and does not remove
  the hole.

## ✔ S43 — every couch milestone is now asserted by a test (2026-08-01)

| # | milestone | where |
|---|---|---|
| 1 | keyboard joins one participant | `a_keyboard_player_and_a_pad_player_drive_different_fighters` |
| 2 | a gamepad joins a second | same |
| 3 | both receive stable session seats | same (seats 0 and 1) |
| 4 | both select distinct controlled actors | same (two bodies) |
| 5 | both produce independent control frames | same (move one, the other does not follow) |
| 6 | disconnecting does not transfer ownership | `unplugging_one_pad_does_not_hand_its_seat_the_other_players_pad` |
| 7 | reconnecting restores the same participant | `a_reconnecting_pad_comes_back_to_the_seat_that_lost_one` |
| 8 | single-participant Ambition unchanged | `without_a_declared_policy_the_pad_still_goes_to_seat_one` + every pre-existing seat test |

⚠ **milestone 4 is NOT "distinct characters", and the first draft asserted that
and failed.** Both players joined with the cursor at slot 0 and picked
`smash_duelist_a`. That is a mirror match, which every platform fighter allows —
a test banning it would have been a rule nobody asked for, enforced by a test,
discovered by a player. The note stayed in the test because the wrong reading is
the tempting one.

  ⚠ what the milestone list does NOT cover, stated so it is not mistaken for
  done: online play, a rebinding UI, every device backend, cross-build protocol
  compatibility (Jon's brief excludes all four), and — from this side —
  **anything about a rollback session**. Every one of these tests runs on the
  shell host with real time. A GGRS match with two local seats re-samples nothing
  today because `LocalSeatTopology` is never frozen outside `dev_tools` (S35),
  and the couch work has made the unfrozen path correct rather than removed the
  need to freeze.

✔ **the MODULES.md check is wired** (S31's open ▢). `modules_md.py` has had a
check mode the whole time and appeared in no runner. It is a `scripts/tests` case
now — that suite already runs first and cheaply in the backbone, and its own
comment gives the reason this was needed: *"a guard nobody executes is not a
guard."* Probed with a real new module.

⚠ **that is the third instrument this run that existed and was never called**:
`modules_md.py --check`, `check_absence_contracts.py --check` (the goal ran it
without the flag, so it could not fail), and the `.agent` index's prune (which
did not exist, but its absence had the same effect — a generated artifact nobody
verified). The pattern is worth a sweep of its own: **a repository that writes
its own tooling accumulates checks faster than it accumulates callers.**

  ✔ [closed: S44 — the sweep ran; four guards had no caller] sweep for the rest: every `scripts/*.py` with a `--check`/`--verify` mode or
  a non-zero exit path, cross-referenced against `run_tests.py`, `scripts/tests/`
  and `.github/workflows/test.yml`. Anything in the first list and none of the
  other three is a guard that has never run.

## ✔ S44 — the uncalled-guard sweep (2026-08-01)

Cross-referenced every `scripts/*.py` with a non-zero exit path against
`run_tests.py`, `scripts/tests/` and `.github/workflows/test.yml`.

**Four checks existed and nothing called them:**

| guard | state |
|---|---|
| `modules_md.py` (check mode) | ✔ wired — found 19 stale maps + 3 missing |
| `check_absence_contracts.py` | ✔ the GOAL called it without `--check`, so it could not fail |
| `check_doc_links.py` | ✔ wired — I had been running it by hand all run |
| `check_roadmap_evidence.py` | ✔ wired |

⚠ **`check_doc_links.py` is the instructive one.** It was green the entire time
*because a person was running it manually after every doc edit*. That is exactly
how a check with no caller stays green until the person stops — and the greenness
is evidence about the person, not about the repository.

Deliberately NOT wired, because they are generators or one-shots rather than
guards: `audit_git_media_history.py`, `ecs_inventory.py`, `non_ecs_inventory.py`,
`regen_music_registry.py`, `render_line_profiles.py`. Named so the next sweep
does not re-derive the distinction.

  ✔ [closed: S44 — `test_every_guard_has_a_caller`] **the pattern deserves its own guard.** Everything above was found by a
  five-line shell loop; a repository that writes its own tooling accumulates
  checks faster than callers, and this will recur. A `scripts/tests` case that
  fails when a `check_*.py` exists with no caller would close it permanently —
  ⚠ and needs the generator/guard distinction above encoded, or it fires on
  `ecs_inventory.py` forever and gets waived.

✔ **and the class is closed.** `test_every_guard_has_a_caller` fails when a
`scripts/check_*.py` is named by no runner. The `check_` prefix is the rule
deliberately — the first draft used "any non-zero exit path" and instantly needed
carve-outs for the generators, an allowlist that would need maintaining and would
be waived the first time it got in the way. Probed with a fresh orphan guard.

## ✔ S45 — GPT 5.6's review of the couch slice: all five findings were right

Five findings, all confirmed against the code, all fixed. Three of the five were
**false green guardrails I wrote**, which is the part worth carrying.

1. ⛔ **reconnect swapped participants.** My test left exactly ONE seat vacant, so
   any returning pad filled the "correct" one — it proved vacancy filling and I
   wrote it up as restoration. With both seats vacant, pad B back first took seat
   0 and the two players swapped characters. Fixed with `PadIdentity` (OS name +
   USB vendor/product), which is what survives an entity dying. ⚠ two IDENTICAL
   controllers stay indistinguishable — the information does not exist — and that
   limit is in the type's own doc rather than left to be discovered.
2. ⛔ **a freeze protected the mapping from REPAIR as well as reorder.** A frozen
   seat whose pad returned kept pointing at a dead entity forever. The identity
   pass runs frozen or not now: handing a seat back its own controller is not a
   reorder. ⚠ the first attempt fell through to `None` and an existing test
   caught it — `None` means EVERY pad.
3. ⛔ **the Smash plugin stamped a global policy on every route**, including other
   games'. It writes only its own claim now and restores the default once, on the
   frame it leaves its own routes.
4. ⛔ **the keyboard-plus-pad test never pressed a key.** It drove only the pad
   and asserted the other body stayed still — which a seat wired to nothing
   passes. Both directions now.
5. ⛔ **"every declared field" compared two fields** and its rationale named four.
   ⚠ extending it to the other three would have been WRONG — health, box and mass
   come from each body's own character, not the match, so comparing them across
   seats fails on any asymmetric pair. Renamed to what it checks, and the third
   roster-declared field (`opens_suspended`) added.

⚠ **the pattern across 1, 4 and 5**: each test's NAME and its ASSERTIONS described
different things, and the name is the part people read — including me, a day
later, when deciding whether something was already covered. A test that proves
less than its name is worse than a missing test, because it answers the question
"is this checked?" with a confident yes.

  ✔ [closed: the sweep ran and came back clean] `None`-means-everything has now bitten twice in one session (leafwing's unset
  gamepad, both branches of `assign_local_seat_devices`). Worth a look at whether
  any other Option in the input path reads as "nothing" and means "anything".

✔ **the `None`-means-everything sweep came back clean, which is itself the
result.** Three sites in the input path treat an absent resource as permissive —
`smash/lib.rs:878` and two in `touch_input/bevy_plugin.rs` — and all three are
`Option<Res<SeatInputContexts>>` with the reason written next to them (*"a test
that wires no contexts is testing the screen, not the arbitration"*, *"composes
into apps that never install the participant-context resolver"*). Deliberate and
documented is a different thing from silent.

⚠ the two that bit were both `InputMap`'s gamepad association, where `None` is
leafwing's spelling for *whichever pad is found first*. That is a foreign API's
default, not a pattern in this repo — worth knowing, because the instinct after
being bitten twice is to distrust every `Option` in sight.

## ✔ S35 CLOSED — the frozen seating exists in a shipped build (2026-08-01)

`freeze_local_seating_for_the_decided_match` runs in the host plugin, so every
composition gets it. Before today the only creator was the rollback observatory
behind `#[cfg(feature = "dev_tools")]`, which `android` omits — so the mechanism
that stops the roster and the session disagreeing had never shipped, and every
test passed because tests build the resource by hand.

⚠ **the lifetime is the ROSTER's, not the session's**, which is exactly why the
first attempt failed: freezing at gameplay-session start put a topology in the
world before any roster existed, and the reconciler rebuilt a roster from a
device count nobody had declared. *"Before the match starts, freeze"* — a match
starts when a roster says who is in it.

⚠ **two earlier fixes had to land first, and both were bugs found this session.**
Freezing before the reconciler's ownership guard would have let Versus rebuild
Smash's roster on every match; freezing before the reconnection fix would have
pinned every seat to a dead entity the moment somebody unplugged. Shipping S35 in
the morning would have shipped two live defects — the delay was not caution, it
was the dependencies being real.

Probed by removing the registration. `a_decided_match_freezes_the_local_seating`
also asserts the DECLARED seat count, so a freeze that quietly fell back to
counting pads fails it too.

  ✔ [closed: `two_local_seats_drive_independently_under_a_rollback_host`] **what freezing now makes checkable, and nobody has**: a GGRS session sized
  from this topology, with two local seats, surviving a rollback. Every couch test
  runs on the shell host with real time. The pieces exist — `LocalSeatTopology`
  is what the session sizes from, and `drive_seat_frame` has existed since queue
  Y1 — and no test puts them together.
  ✔ [closed: `the-seat-topology-has-one-engine-side-creator`] ⚠ and the ratchet worth adding while it is fresh: an absence contract that
  the ONLY creator of `LocalSeatTopology` is engine-side. What made this survive
  months was that a dev-only tool could satisfy every consumer, and nothing
  distinguished "installed" from "installed where players are".

✔ **and the rollback half is checked now too.**
`two_local_seats_drive_independently_under_a_rollback_host` seats two HUMAN seats
in a sync-test session sized for two players, drives seat one through `step` and
seat two through `drive_seat`, and asserts `rollback_health` on EVERY tick.

⛔ under a sync test every frame is saved, rewound and resimulated, so it is not
"two seats moved" — it is two seats whose inputs survive being replayed. The Y1
seat-frame seam had existed for weeks and no test had ever used it alongside a
second seated body.

⚠ two things in that test are load-bearing and are stated in it rather than
assumed: `with_rollback_players(2)`, without which an authored frame is *"written
and never asked for — inert rather than wrong"* by the harness's own doc, and
`opens_suspended: false`, without which both fighters stand still and the test
passes for the wrong reason. Probed by removing the seat drive: `0.00px`.

  ✔ **disconnect/reconnect under rollback IS covered now** —
  `rollback_seat_devices.rs::unplugging_one_pad_leaves_the_other_seat_alone_and_the_reconnect_goes_back`
  runs under `with_sync_test_rollback_settings(4, 10)`, so every frame after the
  unplug AND after the reconnect is saved, rewound and resimulated, with
  `rollback_health` asserted on each. The identity rebind is therefore replayed
  across rewinds rather than merely happening once.
  ⚠ that test is also what FOUND the rebind was broken in every real session
  (S48/S49 neighbours: a frozen topology never recorded which pad a seat held),
  so it is not a formality wrapped around working code.

  ✔ **AC24 one seat further is DONE** —
  `rewinds_across_a_two_human_activation_and_reconstructs_both_seats`. The
  sibling fixture proves the rewind for two CPU participants; humans are the
  case couch multiplayer ships, and the only one that carries a `device_slot`,
  sizes the frozen topology, and holds per-seat input latches. Asserts the
  SLOTS (`[0, 1]`), not the count — a resim that rebuilt two seats both numbered
  0 keeps the count and loses the match. Probed by removing
  `rollback_resource_optional_canonical::<ActiveMatch>`, the same lever the
  sibling documents.

  ⛔ **and the first version of it asserted something the engine deliberately
  does not do.** It GREW the roster from one seat to two mid-session and expected
  the rewind to reconstruct two. `seat_match_participants` opens with
  `if active.is_some() { return; }` — seating is ONE-SHOT, and growing a live
  match is not a feature. The test failed, and the failure was the useful part:
  "re-seats across a rewind" read from the row is ambiguous, and I picked the
  reading the product has no intention of having. Written from the phrase instead
  of from the code — S50's mistake, one more time, caught by the test rather than
  by review.
  ▢ if a live match ever SHOULD grow (a drop-in second player mid-fight), that is
  a design change to seating's one-shot guard, not a missing test.

## S46 — the rollback harness has no input participants, so a device-level test there is vacuous (2026-08-01)

⛔ **Wrote a disconnect-under-rollback test, and it passed while proving nothing.**
It spawned a `Gamepad`, seated a two-human roster in a sync-test session,
despawned the pad mid-match and asserted the seats and the checksum survived. It
was green.

A probe at the moment of the unplug:

```text
[seat-probe] []
```

There are **no `InputParticipant` entities in the sim harness at all**. The pad
was never associated with anything, so the "disconnect" removed a controller
nobody was holding, and the test would have passed with the unplug deleted. It
was not committed.

⚠ **the reason is structural, not an oversight**: `seat_input_participants_for_roster`
and `assign_local_seat_devices` live in the HOST plugin's `Update` schedule, and
`Platformer2dSimHarness` is a sim-only composition. The device layer and the
rollback harness genuinely do not overlap — one is about which physical thing
authors a frame, the other about what happens to frames once authored.

⚠ **and this is why `two_local_seats_drive_independently_under_a_rollback_host` is
still sound**: it drives seat two through `drive_seat`, which writes the seat
LATCH — the rollback's own input path — and never touches an `InputMap`. It tests
the half that exists in that fixture. The disconnect test was reaching for the
half that does not.

  ✔ **BUILT 2026-08-01 — `game/ambition_app/tests/rollback_seat_devices.rs`.**
  The fixture that has BOTH. `Platformer2dSimHarness::app_mut()` is the new seam
  (systems, not just state: `world_mut()` could never have installed a schedule),
  and the test composes the host's four seating systems — primary spawn, freeze,
  seat, and the `PreUpdate` device assignment — onto a two-player sync test, in
  the host's own order. Two tests: two pads own two seats and keep them across
  every rewind; unplugging one leaves the other seat alone and the reconnect
  comes home.

  ⛔ **and it immediately found a live bug in the shipping path.**
  `assign_local_seat_devices` skipped its claim pass entirely for a FROZEN
  session — reasonable-looking, since the freeze decides the mapping. But
  claiming is also what RECORDS which physical controller a seat holds, so
  `remembered` stayed empty and the identity pass added for GPT 5.6's finding #1
  had nothing to match on. Unplug a pad mid-match, plug it back in, and its seat
  keeps pointing at the DEAD entity: not a swap, that player is deaf for the rest
  of the match.
  ⚠ **this was not an edge case, it was the only case.** Before a roster exists
  there is one participant, `players < 2` returns early, and no claim is ever
  made; then the match freezes. Ownership is empty at every real freeze, so
  identity-based reconnect had never once worked in a real session.
  → fixed by recording the identity of the pad the SESSION already chose
  (`topology.device_for_handle`), which repairs without letting the freeze be
  reordered.

  ✔ **and the claim below is now PROVEN.** `SeatDeviceOwnership` and
  `LocalDeviceOrder` are NOT rollback state: neither is registered, and the
  fixture runs a sync test — every frame saved, rewound, resimulated — with
  ownership stable across all of it. Which device authors a seat's frame is an
  input to the rollback, not a fact inside it; a disconnect takes away somebody's
  INPUT, not their character.

  ⚠ two things worth carrying forward, both found by building this:
  * seats are created in `Update` and assigned in `PreUpdate` (the host's own
    split), so on the frame a seat first exists NO assignment pass has seen it
    and every `InputMap` reads `None`. The first version of the test asserted
    there and "failed" against a topology that had already frozen correctly —
    it was measuring the gap between two schedules.
  * `cargo test -p ambition_input` runs **none** of the device-ownership tests.
    The module is `#[cfg(feature = "input")]`, so the per-crate invocation
    reports 55 passing while compiling out 29. `--workspace` unifies the feature
    on through `desktop_dev → visible → input`, so CI is fine — but 55 is the
    number a person sees while iterating, and it is silently partial.

✔ **and the sibling globals have a ratchet now.** `ActiveMatch` and
`DeclaredCombatRules` carry no `published_by` and are safe only because exactly
one writer touches each — the roster's exact position before Smash's select
screen published one from a different route.
`a-second-writer-of-a-match-global-must-answer-ownership` confines them to the
systems that own them.

⚠ it deliberately does NOT add an owner field. Adding one to two types with a
single writer each is a mechanism for a problem nobody has, and the eventual
owner's shape should be decided by the second writer's actual needs. The contract
makes that writer visible at the moment it appears, which is when the question
has to be answered rather than months later from a photograph.

✔ **the two-human re-seat across a rewind is checked.**
`two_local_seats_drive_independently_under_a_rollback_host` now also asserts, on
every tick, that the seat set is the same bodies in the same SLOTS after
resimulation — the two-human form of *"rewinding around the first active frame
reconstructs the identical roster"*. Probed by corrupting the expected set.

⚠ the slots matter, not just the count: a resim that rebuilt two seats both
numbered 0, or swapped which body wore which, keeps the count and loses the
match, and "two bodies exist" is exactly the assertion that misses it.

⚠ **and a process note that cost real work twice today**: `git checkout -- <file>`
to undo a probe DELETES the uncommitted change being probed. Commit the real
change first, or restore from a copy. Both times the injected break and the new
code went together, and the second time only the restore-check caught it.

## S47 — the tests nobody was running, and the one that lied 16% of the time

✔ **a flake, measured rather than felt.**
`ambition_causal`'s `a_fact_published_off_thread_is_counted_rather_than_vanishing`
failed **4 times in 25 runs** of `cargo test -p ambition_causal`, and never
single-threaded. The sink counters are PROCESS globals, so any other test calling
`record` on a thread with no sink bumps `LOST_OFFTHREAD` between this test's
`before` and its assertion; it then reads `before + 2` and reports a real-looking
instrumentation bug that is nothing of the sort.

`global_sink_test_lock` exists for exactly this and says so in its own docs —
*"Any test that opens a sink or reads either counter takes this first."* **Two of
seventeen did.** Now all of them do: 0/25 against a measured 4/25 baseline.

⚠ **a rule written at the lock is not a rule.** This is the same shape as the
guards nobody called and the `MODULES.md` check nobody ran: an invariant stated
where it is defined, obeyed by whoever remembered. The difference is only that
this one had a visible symptom, and even then it survived because a 16% flake is
always somebody else's problem in the moment.
⛔ deliberately NOT enforced by a test-of-tests — Jon's standing call. The fix is
that the tests take the lock, and the reason is at the lock.

✔ **`scripts/tests/test_no_test_module_is_dark.py` — the exemption lists are now
checked against the repository instead of describing it.** `run_tests.py` skips
some crates' feature jobs and denies some features from them, and each exemption
carries a prose claim about why it is safe: SKIP_FEATURE_JOB's own comment says
*"adding a `#[cfg(feature = ...)]` test to a skipped crate must remove the skip
in the same commit — a stale entry here silently un-runs tests."* True today,
unenforced, and silent when it stops being true: the test still exists, still
looks green in the file, and is never compiled. Probed on both paths.

⛔ **and the guard itself is the lesson — it took three drafts, two of which
produced confident FALSE findings.**
1. Hand-rolled the feature closure from the manifests → reported five dark
   modules, three of which cargo resolves ON. Feature unification has more edges
   than the manifest text shows.
2. Asked `cargo metadata`'s resolve graph → still wrong about
   `ambition_causal/bevy` and `ambition_platformer2d_actor_monolith/causal`,
   because it had never read `run_tests.py` and did not know a per-crate job
   turns on every non-default feature at once.
3. Asks both, and checks only what neither covers.
→ **a guard that MODELS a system instead of asking it produces findings that
look exactly like real ones.** Both wrong drafts were internally consistent and
would have been believed; what caught them was checking two findings against
`cargo metadata` by hand before acting on any of them.

▢ open, and worth doing when something touches this again: the per-crate number
is still misleading in the other direction. `cargo test -p ambition_input`
reports 55 passing while compiling out 29 device-ownership tests behind
`feature = "input"`. The suite runs them (unification), so nothing is dark — but
55 is the number a person reads while iterating on exactly those tests. A note
now sits at the module; a better answer would be for the per-crate invocation to
say what it left out.

## S48 — ▢ `locomotion.x` has TWO conventions and nothing enforces either (2026-08-01)

Found while chasing the ladder self-KO (the row above: *"the brain emits FULL
LEFT for three decisions and the body keeps accelerating right"*). ⚠ **this is
NOT confirmed as that bug's cause** — see the last paragraph — but it is a real
defect in its own right, and it was invisible because the two conventions agree
in the only configuration anyone tests.

**The contract, stated in the kernel** (`ambition_platformer2d_core`,
`LocalAxes`):

> *"Controlled-body-local axes: `+x` local side/right, `+y` toward the feet. The
> frame every unqualified movement verb is written in. **Produced exactly once
> per controller tick by resolving raw `ScreenAxes` against the body's current
> `AccelerationFrame`.**"*

**Who obeys it.** `brain/player.rs` says so at the write —
*"unqualified direction is controlled-body-local … Downstream movement code
should not re-resolve this"*. `brain/smash/emit.rs` uses
`obs.side_face_toward_target()`, a gravity-perpendicular side sign, and its
comments are explicit that this is *"correct under any gravity"*.

⛔ **Who does not.** `brain/fighter/decision.rs::apply_movement` — the FB brain,
the one the ladder runs — writes RAW WORLD X into that field, in every verb:

```rust
let toward = foe.pos.x - view.self_view.pos.x).signum();   // world
frame.locomotion.x = toward;                               // ...into a LOCAL field
// and Recover:
let home = ((bounds.min.x + bounds.max.x) * 0.5 - pos.x).signum();
frame.locomotion.x = home;
```

⛔ **and the conversion that should catch it is a REINTERPRETATION.**
`ActorControlFrame` → kernel does `LocalAxes::from_vec(self.locomotion)`, which
copies the components and renames the type. There is no resolution against the
`AccelerationFrame` the doc names, so a world-space vector becomes a
body-local one by assertion. The type says the transform happened; nothing
performed it.

⚠ **the same brain resolves gravity ELSEWHERE**, which is what makes this an
inconsistency rather than an omission: `fighter_from_self(view, gravity_down)`
builds the shadow model against the gravity frame, and `is_punishable(foe,
me.gravity_down)` reads it. The rollout reasons in the body's frame; the emit
does not.

▢ **why it has never been seen**: under screen-down gravity on a flat stage,
local side IS world `+x`, so both conventions produce the same number. The
divergence needs rotated gravity or a surface-walking frame — which the Noether
Chamber has and the ladder does not. So this is LATENT, and the ladder trace
above still needs its own answer.

✔ **step 1 DONE — the FB brain now emits in the body's frame.** Both writes go
through `view.self_view.acceleration_frame().to_local(world_delta)`, which is
the resolution `LocalAxes`' own doc names. Byte-identical under screen-down
gravity (457 existing tests unchanged), and `Recover`'s stage-centre delta is
resolved the same way rather than compared on raw `x`.
→ pinned by `every_lateral_this_brain_emits_is_in_the_bodys_own_frame`: with
gravity pointing world-left the foe sits on the body's gravity axis, so no
sideways throttle can close on it and every emitted lateral must be zero.
Probed by restoring the world-space sign — forty ticks of `1.0`.

⛔ **and the FIRST version of that fix had a bug the test caught**:
`f32::signum(0.0)` is `1.0`, not `0.0`, so a delta lying exactly along the
gravity axis came back as FULL THROTTLE sideways — the same forty ticks of `1.0`
the broken code produced, from the opposite cause. The deadzone matches
`smash/emit.rs::signum_or`. ⚠ worth remembering that a fix and the defect it
replaces can fail a test identically.

▢ **what is still open:**
2. Make the wrong convention UNREPRESENTABLE. `LocalAxes::from_vec` still
   accepts a bare `Vec2` and renames it, so the next brain trips the same way;
   the kernel already has `WorldVec2` beside `LocalAxes` (`blink_quick_dir`
   uses it), so the frame can carry the type that says which space it is in.
   ⚠ this is the honest fix — the one above corrects one caller of a seam that
   remains a lie by construction.
3. ✔ **the audit RAN and found the mirror defect, in the shipped player path.**
   `velocity_target` is documented *"exact world-space velocity command in
   px/s"*, and every other writer agrees — `limbs.rs` sends
   `(home_world - pos) * gain`, the smash shadow model assigns it straight to
   `f.vel`. `brain/player.rs` wrote the BODY-LOCAL stick into it.
   ⛔ measured: under gravity pointing world-left, a screen-RIGHT push resolves
   to the local vector `(0, -1)` (local `-y` is toward the head, and the head
   points world-right), which as a world command is `(0, -speed)` — **the
   possessed flyer flies world-UP when the player pushes right.** Probed by
   reverting; the test prints that exact vector.
   → fixed with `frame.to_world(local_axis)`, pinned by
   `a_possessed_flyer_steers_where_the_stick_points_under_any_gravity` across
   three rotated frames.
   ⚠ **same struct, adjacent field, OPPOSITE direction**: the fighter brain
   wrote world into a local field, the player brain wrote local into a world
   one. Both compile because both fields are a bare `Vec2`, which is the whole
   argument for item 2 above.
   ✔ **and the audit is COMPLETE for `velocity_target`**: all 21 non-test
   assignments checked, and `player.rs` was the ONLY wrong one.
   * `state_machine/mod.rs` — `to_orbit = desired_pos - actor_pos`, where the
     offset came through `frame_to_world`. World in, world out.
   * `boss_pattern/tick.rs` — `delta = target - ctx.actor_pos`, world.
   * `smash/mod.rs` — `aerial_steer` builds `side_axis() * a + up_axis() * b`,
     and both axes are WORLD unit vectors derived from `self.down`. Its comment
     *"frame-agnostic (side / up axes)"* reads like local language and is not:
     expressing a world vector in a gravity-derived basis is exactly right.

   ⭐ **what makes the correct sites correct is worth copying**, because it is
   not discipline — it is that the frame is visible at the point of use:
   1. build world vectors from WORLD BASIS VECTORS derived from gravity
      (`side_axis()`, `up_axis()`), rather than from raw `x`/`y`;
   2. convert explicitly and say so in the name (`frame_to_world(snapshot, v)`);
   3. name the frame in the BINDING — `to_target_raw` (world) sits three lines
      from `to_target_local` in the same function, and nobody confuses them.
   The two defective sites had none of these: a bare `local_axis`, a bare
   `.signum()` on `pos.x`.

   ▢ **so item 2 is prevention, not cleanup.** 46 assignments across the two
   fields, 44 of them already right. Typing the fields would stop the 2 rather
   than repair a mess — which is a weaker argument for the churn than it looked
   before the audit, and a stronger argument for doing it CHEAPLY: the naming
   convention above, applied at the remaining bare-`Vec2` writes, buys most of
   it for none of the blast radius.
4. Only then re-run the ladder trace. The self-KO is still unexplained: the
   ladder runs screen-down gravity, where this fix changes nothing.

## S49 — ▢ a BODY-LOCAL vector copied BETWEEN two bodies (2026-08-01)

The `locomotion` half of the S48 audit: 25 non-test assignments, and 24 are
sound. `player.rs` writes `local_axis` into a local field; `smash/emit.rs` uses
`side_face_toward_target()`, a local side sign; `state_machine` routes through
`snapshot.locomotion_for(..)`, whose parameter is *named*
`desired_local_velocity`; the fighter brain's five were fixed in S48.

⚠ **the twenty-fifth is `mount/mod.rs::steer_mount_from_rider`:**

```rust
mount_frame.locomotion = rider_frame.locomotion;         // BODY-LOCAL
mount_frame.velocity_target = rider_frame.velocity_target; // world — safe
```

`velocity_target` is world, so copying it between bodies is frame-safe.
`locomotion` is **controlled-body-local**, so copying it is only valid when the
two bodies resolve the SAME local frame.

✔ **normally they do**, and this took tracing to establish rather than assume:
`sync_riders_to_mounts` sets `rider.surface.gravity_scale = 0.0`, which zeroes
the MAGNITUDE and not the direction, so the rider keeps the room's gravity
direction — the same one the mount is in. `control_down` comes from the body's
own `resolved_frame.down()`, so both resolve equal. The copy is correct and the
reason is non-obvious enough to be worth writing down.

⛔ **the exception is named in the engine's own words.** From
`actors/update.rs`: *"a surface-walker's frame is its clung surface; everyone
else's is gravity at their position."* A surface-walking MOUNT therefore
resolves its frame from the surface it is clung to while its rider resolves from
gravity — and this line then hands the mount a vector authored in a different
frame. Its `locomotion.x` would drive the mount along the rider's side axis, not
its own.

✔ **CHECKED, and it is NOT reachable today.** Both preconditions were named and
both are now answered:
* `ControlGrant::Total` — **always**. `attach_mount_role` hard-codes
  `control_grant: ControlGrant::Total` for every archetype carrying a
  `mount_class`, so there is no configuration in which a mount does not take
  its rider's intent.
* a surface-walking mount — **none authored**. `character_archetypes.ron` has
  exactly two `mount_class` archetypes: the **shark** (`is_aerial: true`, so it
  steers by `velocity_target`, which is WORLD and frame-safe, and whose
  `locomotion` the smash brain zeroes) and the **giant** (*"no motion / no AI —
  the carried giant just stands"*).

→ so the action is the note, not the fix, and it is now AT THE LINE: why the
copy is sound (the rider keeps the room's gravity direction because
`sync_riders_to_mounts` zeroes the SCALE, not the direction), what would break
it (a crawler/adhesive mount), and what the fix would be then (convert through
each body's own basis rather than copy).

⚠ **the precondition is unenforced, and a new mount archetype is the trigger.**
Nothing fails if somebody authors `mount_class` on a surface-walker; the mount
simply drives along its rider's side axis. That is a good candidate for an
absence contract if a third mount ever appears — two is too few to be worth a
guard, and the note is where the next author will be standing.

⭐ **the general shape is worth naming**: a body-local quantity is only meaningful
with its body. Every OTHER cross-body hand-off in this file already uses world
quantities (position, the saddle offset rotated through `mount_frame.basis()`),
which is why this one line is the only place the question arises.

## S50 — ⛔ five first-draft instruments, all green through their own bug (2026-08-01)

Not five mistakes. One mistake, five times, in a single day of building guards —
and in every case the PROBE was the only thing that caught it.

1. **the tofu guard** swept for `TextFont {` literals, so it could not see the
   defect that has no literal: a `Text` with no `TextFont` at all, which is what
   the menu's row labels and tab labels had.
2. **`check_capability_ships.py`** matched `insert_resource(TypeName …)`. Its
   motivating writer builds a local and passes the binding
   (`commands.insert_resource(topology)`), so `LocalSeatTopology` came back with
   NO writer and fell into the "written nowhere, not our question" branch.
3. **the launcher-rebuild test** asserted after one `app.update()`. The command
   is consumed and the cursor moves that frame, but `render_basic_shell` has
   already computed its key — the rebuild lands on the NEXT frame, so restoring
   the bug still passed.
4. **the frozen-seat unit test** let an UNFROZEN claim pass run before freezing,
   which the shipping path never does, so `remembered` was populated either way.
5. **the rollback-baseline path guard** checked that the named crate still
   EXISTS. `ambition_platformer2d_core` was never deleted — `CenteredAabb` moved
   OUT of it into another live crate.

⚠ **the common cause is writing the guard from the MEMORY of the defect rather
than from the defect.** Each of those five is a faithful one-sentence
description of its bug and none is the bug at the level of code. "A `Text` with
no font tofus" does not tell you the failing site had no `TextFont` token to
grep for; "the topology is only written in dev code" does not tell you the write
is `insert_resource(local_binding)`.

→ **write the PROBE first.** Restore the real defect, watch the new check go red,
then believe the check. If restoring it is awkward, that awkwardness IS the
signal — there is currently no way to demonstrate the claim about to be made.

⚠ and a sixth of the same family, one level up: the geometry carve broke the
rollback schema baseline and **nothing caught it for four commits**, because
`cargo check --all-targets` compiles tests without running them and the only
authority is one integration test behind a full app build. Guard #5 above exists
for that gap and is deliberately weaker than the real test — see its docstring
for what it cannot see.

## S51 — the emit→body seam, MEASURED against the real app (2026-08-01)

The first thing the new `causal` door was pointed at. The ladder row says the
fighter *"emits FULL LEFT for three decisions and the body keeps accelerating
right"*, reaching ~750/s — dash speed, when a run caps at 270 — with `Dash`
chosen zero times.

**Recipe** (it works now; it did not this morning):

```rust
let mut sim = Platformer2dSimHarness::new_with_options(
    Platformer2dSimHarnessOptions::default().with_timestep(TimestepMode::fixed_60hz()))?;
sim.app_mut().add_plugins(ambition_platformer2d::causal::CausalPlugin);
ambition_platformer2d::causal::record_domains(sim.app_mut(), RecordingPolicy::All);
// … insert a two-CPU roster ("medium_striker"), step, then …
log.explain(tick, &SubjectKey::Sim("player_robot_v3".into()))
   .first("control_frame_received")   // locomotion_x, vel_x, dash_* on one fact
```

⚠ run it with `--features "rl_sim causal"`. Nothing enables `causal` by default,
deliberately.

### What a plain two-CPU match actually shows

240 ticks, two fighters: **72 ticks where `sign(locomotion_x) != sign(vel_x)`**
with `|vel| > 50`. Sampled:

```text
t=59..65  v3  loco=+0.65  vel=-86 -86 -86 -86 -86 -86 -86   dash_pressed=false charges=0
t=66      v3  loco=+0.65  vel=-76
t=67      v3  loco=+0.65  vel=-65
t=65..67  v2  loco=-0.65  vel=+101 +90 +79   (exact mirror)
```

▢ **most of that is ordinary turnaround and must not be read as the bug.** A body
asking to reverse has opposite input and velocity for as long as it takes to
decelerate; the `-86 → -76 → -65` tail is exactly that, and a sign-disagreement
COUNT would call normal physics a defect.

⛔ **but the FLAT stretch is not deceleration.** Seven consecutive ticks at
exactly `-86` while the brain asks `+0.65` is a body that is neither obeying nor
slowing. Candidates, none checked: hitstun / `recoil_lock_timer`, an attack
committing the body through `MovePlayback`, or a knockback impulse that owns
velocity until it expires.

⚠ **this composition does NOT reproduce the ladder symptom**, and that is the
useful half. The ladder trace showed velocity RISING in the wrong direction
(549→598→647); here it is pinned then decaying. `dash_charges` is `0` throughout,
so the 750/s dash-speed suspicion is not exercised at all by a plain sandbox
match. **The seam is not universally broken** — whatever the ladder does is in
the ladder's composition, not in the emit→body path as such.

→ next: run the same recipe against the LADDER rig rather than a sandbox match,
and compare the flat stretch's `on_ground`/`facing` against a `MovePlayback` or
hitstun window. The instrument is no longer the blocker.

⚠ the throwaway that produced this was DELETED rather than committed: it printed
and asserted nothing, and a test that cannot fail is the thing Jon has been
most explicit about not wanting. The recipe above is the durable part.
