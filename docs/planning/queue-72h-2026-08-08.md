# The 72-hour queue — continuing execution ledger

**This file is the SPINE and the ledger `scripts/goal_guard.py` reads.** It is
intentionally self-replenishing. The literal open marker `▢` appears in this
header as well as on executable rows, so the guard never interprets an empty
snapshot of the queue as permission to stop.

The operating rule is simple:

> **Finish work, then promote the next highest-value verified item and keep
> going. There is no “the queue is empty, therefore stop” state.**

Document roles:

- **this queue** owns current execution order;
- [`tracks.md`](tracks.md) is the standing reservoir from which new work is
  promoted;
- focused plans own technical design and acceptance criteria;
- [`JONS_OBSERVATIONS_BUGS_AND_ISSUES.md`](JONS_OBSERVATIONS_BUGS_AND_ISSUES.md)
  owns direct maintainer observations;
- [`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md) owns work
  that cannot advance without a product/feel decision;
- completed investigations and migration evidence belong in `docs/archive`, not
  in this live ledger.

Before starting any row, inspect HEAD and confirm the named gap still exists.
When a row closes, remove its historical case file from this ledger rather than
leaving a second copy of the implementation history here. If the history is
worth retaining, archive it. Then promote another verified item from `tracks.md`
or the maintainer-observation file and continue.

The full pre-prune queue, including the closed D0–D114 investigation history,
is preserved at
[`../archive/planning-superseded/2026-08-13/queue-72h-2026-08-08-pre-semantic-prune.md`](../archive/planning-superseded/2026-08-13/queue-72h-2026-08-08-pre-semantic-prune.md).

---

## Current execution order

- ✔ **D73 CLOSED 2026-08-14 — the enemy-archetype system is deleted and ONE
  character authority remains.** Eight phases landed; all twelve AC7 exit
  criteria verified against HEAD. Five production roads reach one constructor,
  an unresolvable identifier is refused at preparation, and
  `adopt_character_intrinsics` has zero references. ⚠ five refinements survive
  in the brief and none is a second definition system.

Its sources, for the successors that start from its vocabulary:

- [`authority-convergance-campaign-2026-08-13.md`](authority-convergance-campaign-2026-08-13.md)
- [`character-template-architecture-2026-08-10.md`](character-template-architecture-2026-08-10.md)

⛔ **the successors were ordered after AC7 and AC7 is closed**, so they may
begin — but re-read the authority-convergence plan's non-goals first: neither is
licence to reopen the character work.

- ▢ **D72 — Continue Smash as the body-generic combat acceptance customer.**

Use [`smash-body-generic-combat-2026-08-09.md`](smash-body-generic-combat-2026-08-09.md)
and [`demos/super-smash-siblings.md`](demos/super-smash-siblings.md) as the
current authorities. Re-measure the focused plan before taking a slice: much of
its original combat work has already landed, and only surviving acceptance or
body-generic gaps should generate code changes.

Keep Ambition and Smash on the same combat/body semantics unless a genuine game
rule differs. Do not recreate player-only physics or combat paths to satisfy the
demo.

- ▢ **D33 — Continue actor-monolith decomposition by coherent ownership.**

Use [`engine/actor-monolith-decomposition.md`](engine/actor-monolith-decomposition.md).
Choose the next carve from current dependency/authority measurements, not from an
old LOC target. A successful slice creates or strengthens one natural owner and
removes dependency/change amplification; it does not merely move a large file to
a new crate.

D73 has priority where the same code is being rewritten by authority
convergence. Re-measure after D73 rather than preserving a transitional carve.

- ▢ **D71/D92 — Finish the real room-transition transaction path.**

The eager/deferred split and swept-detector defects have been repaired. The
remaining work is recorded in
[`engine/room-transition-loading.md`](engine/room-transition-loading.md):

- exercise a **real movement-kernel → loading-zone → readiness/commit** path
  rather than only hand-constructing the sweep sample;
- keep rollback-host transitions on the same prepare/readiness/commit
  transaction;
- close the remaining provenance/carry/P2P trigger work only where the focused
  plan still shows a concrete gap.

Do not reopen the already-fixed detector geometry or door-SFX bugs.

- ▢ **D64 — Finish only the verified Mary-O/presentation residue.**

The multi-coin block exists and directly credits the purse, but it still lacks
the requested visible coin-pop VFX. Implement that without replacing the
existing block-state or payout authority.

Other old D64/D70 restart or 1-2 block reports are **not executable without a
reproduction**: current source/tests cover the known spent/discovered/reset
paths. Do not rewrite them speculatively. The gravity-camera idea is a product
decision in [`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md),
not part of this row.

---

## Waiting on an external fact or maintainer decision

These are real unresolved items, but they are deliberately **not** `▢` queue
work. An autonomous coding agent should not spin on them.

- **D23 — projectile collision feel:** choose authored hurt geometry vs the
  coarse body box. See [`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md).
- **D50 — dropped held-item lifetime:** choose room-scoped vs persistent-world
  semantics. See [`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md).
- **D53 — Android suspend/resume:** engineering-side work is as far as source
  evidence can take it; validate the residual behavior on a real device before
  opening another fix.
- **D54 — reported visual/VFX issue:** engineering is complete; requires the
  requested reproduction before more changes.
- **D70 — Mary-O restart observation:** current tested death/reset routes do not
  reproduce it; needs the game/room/time context recorded in
  [`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md).
- **D42 / D47 — character sizing/rig art:** current measurements put the
  remaining problem principally in authored rig/body-inset data and visual
  review, not a missing generic engine mechanism. Keep it in the sprite/authoring
  lane unless a reproduced engine defect appears.
- **D114 — fighter-vs-fighter hit emphasis:** requires the feel decision recorded
  in [`awaiting-maintainer-decision.md`](awaiting-maintainer-decision.md).

---

## Verified closures removed from the live queue in the semantic-prune pass

These are named only to prevent stale handoffs from resurrecting them as work.
Their detailed evidence is in the archived pre-prune ledger.

- **D28:** real workflow/test/compile timing infrastructure exists; any future
  runner-parallelism work is trigger-based and lives in `tracks.md`.
- **D36 (closed 2026-08-14 — and the mechanism changed that day, so this
  summary was already stale):** the geometry condition fired ~30 times per
  Android boot because it could not tell a shared RIG target (`toon` x17,
  legitimate, never resolved by name) from a CHARACTER id. `SheetRegistry` now
  records collisions and reports the count once; `ambition_app`, which owns the
  catalog, warns only when the shadowed target is a character.
- **D81 (measured 2026-08-14):** the `BrainPreset` retirement is **gated on
  character REGISTRATION**. A `BrainProfile` needs a `CharacterDefinition`, so an
  unregistered character's only way to state policy is the `default_brain` being
  retired — every row that has left is registered, and the smallest remaining
  keys have unregistered adopters. ⚠ 168 gitignored `*_actor.ron` sidecars name
  the same keys and are invisible to `grep -r`; they are inert today, so retire
  keys before the actor-contract migration consumes them.
- **D93:** Gradient Sentinel summons now resolve to authored characters and the
  goblin encounter uses the authored `npc_goblin_brute` character.
- **D95:** Sanic's badnik is now a registered authored character rather than an
  accidental placement-policy fallback.

When the executable rows above close, **do not stop**. Re-read HEAD, promote the
next highest-value verified card from [`tracks.md`](tracks.md) or a reproducible
maintainer observation, record it here, and continue.
