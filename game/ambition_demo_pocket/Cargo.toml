[package]
name = "ambition_demo_pocket"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Tiny fourth-provider acceptance fixture for Ambition's provider authoring surface."
publish = false

[lib]
crate-type = ["rlib"]

[dependencies]
ambition_platformer2d = { path = "../../crates/ambition_platformer2d", default-features = false, features = ["all_capabilities"] }
bevy = { version = "0.19.1", default-features = false }
