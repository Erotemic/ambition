# Delete the dive drill's anonymous "Target".
#
# ⭐⭐ JON, 2026-08-13: *"The dive-drill content is AI-invented placeholder
# content that Jon does not care about preserving and expects to redesign
# eventually. Remove the dive-drill and its anonymous Target if doing so
# simplifies the character/construction migration. Do not create a real
# character, fixture architecture, compatibility exception, or other permanent
# machinery merely to preserve it. … Do not let dive-drill block making ordinary
# authored EnemySpawn character-first or required-character-id work."*
#
# It was the LAST `EnemySpawn` in any shipped world naming no character — the one
# placement standing between the campaign and making `EnemySpawnSpec::character_id`
# required and deleting the `presentation_identity` display-name fallback
# (D73 checklist item 15). `worlds::tests::only_the_uncast_placements_still_ride_
# the_display_name_fallback` counted it as the whole remainder.
#
# ⚠ **the TARGET goes and the ROOM stays**, and the conditional in his sentence is
# why: removing the Target is what simplifies the migration, and removing the room
# adds nothing to that while requiring surgery on the `portal_bridge` LoadingZone
# that reaches it. The drill still teaches the dive — pickup, gap, label, far
# ledge — and remains available for the redesign he expects to do.
#
# ⚠ **the engine behaviour it exercised is NOT deleted with it.** The reachability
# test's second claim — a dive lunge damages the body it passes through — moves to
# a body the test spawns for itself via `spawn_enemy_character_at`, which is a
# better home than an authored placeholder: it no longer depends on a room's
# contents to make an engine claim.
#
#   PYTHONPATH=tools/ambition_ldtk_tools python3 -m ambition_ldtk_tools entity delete \
#       tools/ambition_ldtk_tools/specs/delete_dive_drill_anonymous_target.yaml \
#       --ldtk game/ambition_content/assets/worlds/sandbox.ldtk --in-place
level_id: dive_drill
deletes:
  - target: { iid: EnemySpawn-6126 }
