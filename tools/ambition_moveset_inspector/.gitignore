# The bundle is GENERATED by `cargo run -p ambition_app_tools --bin moveset_export`.
# It is a 2.6MB snapshot of the composed host's cast; committing it would put a
# derived artifact in the history and make every balance edit a binary diff.
data/moveset_bundle.json
data/takes/
